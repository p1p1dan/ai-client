# GUI A~E 现场验收清单（用户点击回填）

> 日期：2026-09-09 · 应用：`D:\Program Files\AiClient\AiClient.exe`（1.0.0-test.11，源码 `b6aa0844`）
> 后端：native（须按第 0 步启动）· 依据：`docs/plantree/plans/gui-sdk-experience/现场验收清单.md` · 结果：由用户点击后回填
> **说明**：Read/Edit/Write 相关检查用可丢弃的临时文件，不改用户工作文件。本文件不修改产品/测试源码。

## 第 0 步 · 启动（native 后端）

| 操作 | 通过标准 |
|---|---|
| 先退出已在运行的 AiClient（若在） | 任务管理器无 `AiClient.exe` |
| 在 PowerShell 执行：`$env:AICLIENT_RUNTIME_BACKEND='native'; $env:AICLIENT_RUNTIME_TRACE_DIR='E:\code\GitTmp\ai-client-runtime\Windows-P4-6-evidence\native-trace2'; & 'D:\Program Files\AiClient\AiClient.exe'` | 应用启动，能开一个会话 |
| （可选核验）启动后看 `native-trace2\runs.jsonl` | `"backend":"native"`、`"carrier":"bundled-node"` |

## 第 1 组 · 工作目录菜单与终端设置

| # | 操作 | 通过标准 | 结果 |
|---|---|---|---|
| 1a | 左栏某目录 hover → 点“…” | 松手后菜单保持；点设置/移除能打开对应界面；Esc 或点外部关闭，焦点回到触发器，无遮罩阻塞 | 正常 |
| 1b | 设置 → 终端 | 进入不崩溃；切换 Shell、Custom 并填参数；DOM/WebGL、回滚行数、主题/收藏能改；离开再进不崩溃。Windows 至少测 PowerShell 与 Custom | 正常，但是点击网络那一项会报错 |

## 第 2 组 · 文件打开

| # | 操作 | 通过标准 | 结果 |
|---|---|---|---|
| 2a | 关闭全部编辑器标签 → 点聊天里 Markdown 文件链接，再点 Pi read/edit/write 行里的路径 | 实际文件在编辑页打开（不是只展开文件树） | 正常，点击聊天中的Edited test123.txt可以正常在编辑栏中弹出和查看 |
| 2b | 同文件已有标签时再点 | 复用同一标签 | 正常 |
| 2c | 分别点相对路径、绝对路径、`C:\` 路径、带空格路径、带 `:行号`/`#L行号` | 都能正确打开定位 | 这个没有现成的给我测试 |
| 2d | 点一个不存在文件的路径 | 有缺失通知，非崩溃/空白 | 提示无法打开 |

## 第 3 组 · 新会话与 TEMP

| # | 操作 | 通过标准 | 结果 |
|---|---|---|---|
| 3a | 普通项目、worktree、已有 TEMP 各输入 `/new` | 新会话无旧对话内容；首次发送/工具 `pwd` 仍是原 cwd；旧会话与旧文件不变 | 正常 |
| 3b | 未绑定且从未发送的会话 `/new` | 保持未绑定；已有 scratch cwd 的会话 `/new` 沿用该 cwd | 正常 |
| 3c | 正在运行时 `/new` | 提示先停止；另一后台会话运行时不误阻止当前空闲会话 | 正常 |
| 3d | 五月命名 TEMP 里点“新建会话” | 明确沿用原目录 | 未测，五月目录删掉了 |
| 3e | 同一 TEMP 里点“新建临时工作区” | 新建目录时间与机器当前时间一致，不改五月目录 | 时间正常，但是无法删除，删除报错。且无法正常对话提示：Error:Error invoking remote method<br/>'chat:createsession': WorkerSlotError: spawn D:\Program Files\Aiclient\resources<br/>node-runtime\node.exe ENOENT |
| 3f | 新建过程中切换会话，完成后看 | 新会话不得抢选或误绑 | 正常 |

## 第 4 组 · 长输出/重试/状态

| # | 操作 | 通过标准 | 结果 |
|---|---|---|---|
| 4a | 触发长输出，使其末尾遇 529/429 可恢复错误 | 末尾及输入区可见重试次数、可用的等待信息；详情可展开 | |
| 4b | 让重试成功继续输出并完成 | 旧重试提示与红框消失，最终非失败状态 | 只发现503好像不会重试，会直接失败 |
| 4c | 向上阅读时发生重试/继续输出 | 不强制滚底 | |
| 4d | 重试耗尽、或点击停止 | 保留明确终止错误；停止后无“仍在重试” | |
| 4e | 切换会话、重载 | 各会话状态不串 | |
| 4f | 依次观察：等待模型/输出/执行工具/原生权限/扩展问答 | 分别显示真实状态，等待确认期间不伪报“正在输出” | |

## 第 5 组 · 权限与问答

| # | 操作 | 通过标准 | 结果 |
|---|---|---|---|
| 5a | 触发普通自动放行 | 不再刷多行 Allowed；授权详情可展开 | 正常，但是目前这个授权详情好丑，不好看，得看看其他方案怎么做的 |
| 5b | 人工批准后看 | 批准结果可查；待确认/拒绝/权限失败仍可见 | 正常 |
| 5c | 触发 QuestionCard：长问题/长选项/窄窗口/多问题 | 按钮不重叠，内容可滚动，底部操作可达 | 当前这个会话里没有提问工具，所以没法真正弹出 QuestionCard。我这边能用的只有读文件、写文件、编辑、bash、glob、grep 和 new_context。 |
| 5d | 触发扩展问答（多选/单选/自由输入） | 同上 | 没有提问工具 |
| 5e | 键盘 Tab/方向键/Enter/Space | 正常 | 正常 |
| 5f | QuestionCard 用 Ctrl/⌘+Enter 提交；中文输入法确认文字 | 不误提交 | 没有提问工具 |
| 5g | 提交中连点 | 防重复 | 没有提问工具 |
| 5h | 模拟 IPC 失败再重试 | 可重试 | 未测 |
| 5i | 完成后 | 只读；Esc 正确关闭/折叠 | 没有提问工具 |

## 第 6 组 · 文件 diff

| # | 操作 | 通过标准 | 结果 |
|---|---|---|---|
| 6a | 常规设置 → 聊天中的文件修改 → 开启“显示文件修改 diff”；重启 | 开关保持 | 正常 |
| 6b | Edit 运行前看参数预览 | 明确 | 正常 |
| 6c | Edit 成功看 SDK patch | 明确；展开/折叠正常 | 不知道是啥，反正没看到我想要的diff对比模块，左侧为原文件，红色表示删除，右侧修改后，绿色表示新增 |
| 6d | Edit 失败看参数预览 | 明确，不冒充已应用 | 未测 |
| 6e | 重载历史后 | 仍能看到已有 SDK patch | 历史能显示 文件xx 修改 +2 -0 ，展开有绿色新增，但是单栏 |
| 6f | Write | 仅写入内容、无 before | 正常 |
| 6g | Bash | 不显示“已捕获全部修改” | 正常 |
| 6h | 关闭设置后 | diff 隐藏，工具结果与错误仍可见 | 正常 |

## 第 7 组 · 上下文详情

| # | 操作 | 通过标准 | 结果 |
|---|---|---|---|
| 7a | 进上下文详情入口（原百分比入口） | 仍在原位置 | 正常 |
| 7b | hover / 键盘 focus | 可看详情；窄窗口不出屏 | 正常，重启后要发一句话才会显示 |
| 7c | 切换会话 | 不残留旧数据 | 正常 |
| 7d | 看已用/剩余 | 口径标清 | 正常 |
| 7e | 看最近一次已结算请求与累计 | 分开 | 正常 |
| 7f | 对照输入 token 数 | 缓存命中率与真实 input/cacheRead 一致；工具摘要只计当前轮 | 正常 |
| 7g | 未上报会话 | 不伪造 0% | 正常 |

## 第 8 组 · 输出跟随

| # | 操作 | 通过标准 | 结果 |
|---|---|---|---|
| 8a | 停在底部，连续长文本/大代码块/突然一大段输出 | 均跟随；跳底按钮不反复闪现 | 正常 |
| 8b | 输入框多行增高/缩小、调整窗口高度 | 仍贴底 | 正常，但是大量输入时增高有限，空间其实可以大一些 |
| 8c | 输出中真实上滚 | 保留阅读位置，随后增长不抢回 | 正常 |
| 8d | 主动回到底部或点跳底 | 恢复跟随 | 正常 |
| 8e | 切换会话 | 不继承另一会话的阅读偏移 | 正常 |
| 8f | 快速输出 | 观察是否闪烁/跳动/撕裂；记录硬件加速设置 | 正常 |
