# 现场测试操作单（照着做，不用懂背景）

> 2026-08-10。这份是**动手用的**：每条只写「怎么做 / 看什么 / 算不算过」。
> 背景与技术依据在 [加密机测试方案](./2026-08-06-encrypted-machine-test-plan.md) 与 [T-10 清单](./t10-packaged-gui-checklist.md)，本单不重复。
> 术语只有一个要先说：**chip = 小标签**，就是界面上那种带边框的小圆角方块，里面写一两个词。

---

## 第 0 步 · 拿包（在有网的机器上做）

**两条路，任选一条：**

**路 A（推荐，不需要 Windows 机器）**
1. 打开 GitHub 仓库 → 顶部 **Actions** → 左边选 **Build** → 右上 **Run workflow**
2. 分支选 `feat/openchamber-chat-refactor` → 点绿色 Run
3. 等它跑完（20–30 分钟）→ 点进这次运行 → 页面最下方 **Artifacts** 区
4. 下载 **`windows-unpacked`**（这是目录版，解压出来就是整个程序）

**路 B（你自己在 Windows 上出包）**
```powershell
git pull
pnpm install
cd src\agent-host
npm ci
cd ..\..
pnpm build:win
```
产物在 `dist\win-unpacked\`。

**另外还要带一个文件**：仓库里的 `scripts\make-test-claude-config.mjs`（单个文件，拷走）。

---

## 第 1 步 · 到了测试机，先看版本

**这一步双击打开就行**（只看版本，不需要凭证；从第 3 步起才必须用命令行启动）。

双击 `AiClient.exe` → 标题栏右上角菜单 → **About / 关于** → 看版本号 → 看完**关掉**。

- 是 **`0.4.0-test.2`** → 继续
- 是 `0.4.0-test.1` 或别的 → **停**，这是旧包，往下测全白测

---

## 第 2 步 · 先探网关（30 秒，不通就别浪费时间）

关掉程序，开 PowerShell：
```powershell
curl https://cch-jyw.pipidan.qzz.io/
```
- 有反应（返回 307 之类）→ 继续
- 完全不通 → **凡是要对话的项全都做不了**。但**第 4 步**（看包里的 node 有没有生效）和
  **第 8 步**（记 codex 版本）不需要网关，可以只做这两项，其余带回来再说。

---

## 第 3 步 · 灌测试凭证 + 启动（**这两步必须在同一个窗口里连着做**）

### 先说清楚为什么不能"设完变量再双击图标"

`CLAUDE_CONFIG_DIR` 这个环境变量是**跟着命令行窗口走的**：你在一个窗口里设了它，
只有**从这个窗口启动的程序**才看得见。**双击桌面图标启动的程序看不见**，
它会回落到你自己的 `C:\Users\<你>\.claude`——那就变成用你自己的账号在计费，
而且测的不是测试网关。

所以下面这一段是**一整块**，从头到尾在同一个 PowerShell 窗口里跑完。

### 你的程序在哪（两种情况）

| 你拿到的 | exe 路径 |
|---|---|
| **目录版**（CI 下载的 `windows-unpacked`，解压出来） | `<你解压的位置>\win-unpacked\AiClient.exe` |
| **安装版**（跑了 NSIS 安装包） | `%LOCALAPPDATA%\Programs\AiClient\AiClient.exe` |

下面用 `$APP` 代表这个路径，你按自己的情况填。

### 一整块，复制粘贴（PowerShell，不是 cmd）

```powershell
# ① 填两个路径：程序位置、你要用来测试的仓库/文件夹
$APP  = "D:\win-unpacked\AiClient.exe"
$WORK = "D:\你的测试仓库"

# ② 灌凭证（用程序自带的 node 跑，测试机可能没装 node）
#    末尾必须带 $WORK —— 它把这个目录标记为「已信任」，
#    不带的话 cli.js 首次用它时会卡在信任确认上
& "$(Split-Path $APP)\resources\node-runtime\node.exe" "D:\make-test-claude-config.mjs" $WORK

# ③ 设变量 + 启动，必须在同一个窗口连着做
$env:CLAUDE_CONFIG_DIR = "$env:TEMP\aiclient-gui-test-config"
& $APP
```

第 ② 步会打印出它生成到哪、用的哪个网关、信任了哪些目录——**核一眼**，
特别是 `trusted workspaces` 里有没有你的 `$WORK`。

### 怎么确认真的生效了

程序起来之后不用急着测别的，**先看第 5 步的第 3 项（发 PONG）**：
- 能正常回 PONG → 凭证注入成功，往下走
- 报认证错 / 一直转 → 变量没生效。**最常见的原因是你双击图标启动的**，
  回到上面那一整块重来。

### 每次重启程序都要重来一遍第 ③ 步

关掉程序后再想启动，**不能双击图标**，得回到那个 PowerShell 窗口再跑一次
`& $APP`（变量还在那个窗口里）。**窗口关了就得从 ③ 重来。**

### 嫌麻烦的话：做个快捷启动脚本

在桌面建一个 `跑测试版.ps1`，内容就是上面 ③ 那两行（把路径填死），
以后右键 → 用 PowerShell 运行。或者建个 `.bat`：

```bat
@echo off
set CLAUDE_CONFIG_DIR=%TEMP%\aiclient-gui-test-config
start "" "D:\win-unpacked\AiClient.exe"
```
（`.bat` 里用 `set`，PowerShell 里用 `$env:` —— 两种写法别混。）

### ⚠️ 全程别碰程序里的登录引导

程序自己那个登录/引导流程写死了用 `C:\Users\<你>\.claude`，跟这里的测试配置
**打架**。混用会造出假故障，查半天发现是配置串了。

---

## 第 4 步 · 第一取证：包里的 node 到底有没有生效（**优先做这个**）

菜单栏 **View → Developer Tools** → 切到 **Console** 标签 → 粘贴这一行按回车：
```js
await window.electronAPI.agentHost.resolveNode()
```

**把结果截图。** 看 `source` 这个字段：
- `"bundled"` → 包里带的 node 生效了 ✅
- `"path"` 或 `"nvm"` → 用的是机器上原有的 node（**这也是有效结论，记下来就行**，但后面判断要跟着变）

**为什么排第一**：后面所有加密相关的结论都建立在这一条上，它不成立后面全白测。

---

## 第 5 步 · 通用功能九项

| # | 怎么做 | 算过的标准 |
|---|---|---|
| 1 | 按第 3 步那一整块启动（**不是双击**） | 不白屏、不弹报错框 |
| 2 | 设置 → Appearance → 打开 **OpenChamber Workspace Shell** 开关 | 界面变成新版（左边栏 + 中间对话 + 右边面板） |
| 3 | 新建一个会话，发这句：`Reply with exactly: PONG. Do not use tools.` | 能看到字一个个流出来，内容里有 PONG |
| 4 | 发这句：`Create PING.txt with content pong` | 弹出一张**授权卡**问你允不允许 → 点 Allow → 磁盘上真的多了 `PING.txt`，内容是 `pong` |
| 5 | 再让它写一次文件，这次点 **Deny** | 程序不崩，模型收到拒绝后正常把话说完 |
| 6 | 发个长任务（比如 `Count from 1 to 200 slowly`），跑到一半点 **Stop** | 立刻停住，状态回到空闲，**还能接着发下一条** |
| 7 | 关掉整个程序 → 打开任务管理器 → 搜 `node.exe` | 没有残留的进程（关之前先看一眼有几个，做对比） |
| 8 | 用一个全新的、没添加过仓库的状态启动：① 左边栏空的时候有没有"添加本地仓库"的入口；② 从文件资源管理器**拖一个文件夹**到窗口里 | ① 能弹窗添加成功；② 拖进去时窗口出现高亮遮罩，松手弹出的窗口里路径已经填好了 |
| 9 | 顶部那行会话标题，跟正文放一起对比看 | 标题**明显更粗更醒目**。要是粗细看不出差别 → 这是字体问题，截图带回来 |

---

## 第 6 步 · 上次五个问题的回归（**这次的重点**）

### R1 · 带图片的消息会不会重复

1. 发一条消息，**带一张图片**
2. 等它回完
3. **关掉这个会话，再重新打开它**（或者干脆重启程序再打开）

**看什么**：刚才那条消息**只出现一次**，并且带着图片缩略图。
**不过的样子**：出现两条——一条有缩略图、一条没有。

### R2 · 会不会太早就说超时

发一条**重一点**的消息（带大图，或者让它做点费劲的思考），**掐表**。

**看什么**：3 分钟以内**不应该**弹超时提示。
**不过的样子**：没到 3 分钟就弹超时。

### R3 · 能不能移除仓库

1. 左边栏找到仓库那一行，**鼠标悬停上去**
2. 应该出现一个「文件夹带减号」的图标 → 点它
3. 弹确认框 → 确认 Remove

**看什么**：那个仓库从左边栏消失了，**磁盘上的文件没被删**。

⚠️ **重点**：请**挂两个或以上仓库**再测，然后核对**删掉的是不是你点的那个**。（这个取键逻辑刚从"猜"改成"精确匹配"，这是唯一的现场验证机会。）

### R4 · 侧栏的分支标签（**这项按新方法做，别只看"有没有"**）

先说清楚看哪：左边栏每个会话行，右边有一两个小标签（chip）。其中一个写着 `Claude Code`，另一个应该写着 git 分支名（比如 `main`、`feat/xxx`）。

**⚠️ 上次那个"修复"经复核基本等于没改，所以这次不要按"修好没有"记分。** 按下面三步走，目的是**拿回一个能定位到原因的结论**。

**第一步（先做这个，很可能一步就定案）· 截图**

把某一行会话行**放大截图**，看 `Claude Code` 那个标签和右边的时间之间：

- **能看到一个很小的、空的带边框小方块**（大概比一个字还窄，里面没字或只有一两个字）
  → **数据是好的，问题在宽度不够**。到此为止，不用再查了。
- **两者之间什么都没有**（连边框都看不到）
  → 走下面第二、三步。

（依据：这个标签有 16 像素的硬下限，被挤压时只会变成空方块，**不会整个消失**。所以"空方块"和"什么都没有"是两种完全不同的病。）

**第二步 · 关掉新界面看旧界面**

设置 → Appearance → **关掉** OpenChamber Workspace Shell → 回到旧版左边栏 → 展开那个仓库。

旧界面会把 git 的错误**直接显示成文字**。**看到错误文字，抄下来就是答案**，不用做第三步了。

**第三步 · 开发者工具跑一行**

View → Developer Tools → Console，粘贴这一整段按回车：

```js
(async()=>{const k=p=>p.replace(/\\/g,'/').replace(/[\\/]+$/,'').toLowerCase();
for(const r of JSON.parse(localStorage.getItem('aiclient-repositories')||'[]')){
try{const w=await window.electronAPI.worktree.list(r.path);
console.log('REPO',JSON.stringify(r.path),'key',k(r.path),w.map(x=>({p:x.path,key:k(x.path),b:x.branch,main:x.isMainWorktree})));}
catch(e){console.log('REPO',JSON.stringify(r.path),'THREW',e.message);}}})()
```

**把整段输出截图带回来。** 五种结果，对号入座：

| 输出里出现 | 说明 |
|---|---|
| `THREW` 且带 `ENOENT` / `spawn git` / `不是内部或外部命令` | **程序找不到 git**（它找 git 的路径和你在命令行里的不是同一个） |
| `THREW` 且带 `dubious ownership` / `not a git repository` | **git 拒绝这个仓库**（属主不对，拷贝来的仓库常见） |
| 有正常输出，但 `key` 那两个值**不一样** | **路径对不上**（映射的网络盘 / 快捷方式 / 或者你注册的是仓库的子目录） |
| 有正常输出，`key` 一样，但 `b` 是 `null` | **当前不在任何分支上**（切到某个 tag 上了） |
| 有正常输出，`key` 一样，`b` 有值 | **数据完全正常** → 回到第一步那个"宽度"的结论 |

> ⚠️ **别用程序内置的终端敲 `where git` 来下结论** —— 内置终端用的路径和程序真正跑 git 用的路径**不是一回事**，在终端里能找到不代表程序能找到。

---

## 第 7 步 · 加密相关六项（这台机器专属）

| # | 怎么做 | 看什么 |
|---|---|---|
| ① | 在文件树里打开一个**已知是加密的**文件 | 是明文还是一堆以 `%TSD-Header-` 开头的乱码 |
| ② | 在真实的加密工作区里跟它对话，让它用 Read 工具读一个加密文件 | 读出明文就算过 |
| ③ | 授权卡点 Allow 让它写一个文件，然后自己打开那个文件看 | 记录是明文还是密文 |
| ④ | 打开一个有**加密历史记录**的会话 | 应该明确报"历史无法读取"，而不是静默显示空白 |
| ⑤ | 第 4 步里 `source` 是 `bundled` 的话，直接做②——能读出明文就证明包里的 node 被白名单认了 | —— |
| ⑥ | 如果①②失败：先分清是"找不到程序"（ENOENT）还是"白名单不认" | **这两个别记混，结论完全不同** |

**万一②③全失败**：用逃生口，**不用改代码**。回到第 3 步那个 PowerShell 窗口，
在启动程序**之前**多设一个变量：

```powershell
$env:AICLIENT_NODE24_PATH = "<这台机器上已被白名单的 node.exe 完整路径>"
$env:CLAUDE_CONFIG_DIR = "$env:TEMP\aiclient-gui-test-config"
& $APP
```

**出发前先把那个路径查清楚**，否则一失败整轮作废。

---

## 第 8 步 · 顺手记三条（30 秒，对后面有用）

```powershell
where codex
codex --version
node --version
```
把输出抄下来。后面要让程序去起 Codex，得知道这台机器上有什么。

---

## 带回来什么

1. 第 4 步 `resolveNode()` 的截图
2. R4 第一步的**放大截图**（那个位置到底有没有小方块）
3. R4 第二/三步的输出截图（如果做到了那一步）
4. 九项和六项里**没过的那几条**，各配一张截图 + 一句话说明怎么操作的
5. 第 8 步那三行命令的输出

过了的项不用截图，说一声"过了"就行。
