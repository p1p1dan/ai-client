# 任务：对「0820 使用反馈批（FB 批）施工规格 rev.1」做独立对抗评审（只读）

你是一名**独立评审员**。这是一次**双盲评审**：另有一位评审员在评同一份规格，你**看不到也不需要猜**他的结论。
你的产出会与他的合取后决定这份规格能否开工。

## 硬性纪律

1. **只读**。禁止修改、创建、删除任何代码或文档（写你自己的评审报告文件除外）。禁止 `git` 写操作。
2. **每条主张必须有 `file:line` 证据**，并摘录关键原文。**不得臆造符号名、行号或文件**——臆造即本次评审作废。
3. 区分你自己的**【实测】**（你亲自读了当前 HEAD 的文件）与**【推测】**（推断）。规格里标【实测】的，你要复核它现在是否还成立。
4. 只提**真问题**。不要为凑数写风格建议；不要复述规格已经写对的东西（除非放进末尾「已复核正确清单」）。

## 评审对象

- **主档**：`docs/plans/2026-08-19-fb-batch-spec.md`（rev.1，1210 行）
- 上游分诊：`docs/plans/2026-08-19-usage-feedback-0820-triage.md`
- FB7 取证：`docs/plans/2026-08-19-fb7-merge-forensics.md`
- 地基批（F456，已 as-built 合入）：`docs/plans/2026-08-18-f456-readability-composer-spec.md`
- 决策台账：`docs/plans/openchamber-chat-refactor-ledger.md`（D53 / D55）
- 工程规范：`docs/agent-project-engineering.md`；设计规范：`docs/design-system.md`；`CLAUDE.md`

规格的锚点基线 HEAD 是 `99dfd78`；当前 HEAD 更新（阶段 4 打包链已落）。
**已知事实**：`src/renderer/**` 自 `99dfd78` 起零提交，故渲染端锚点原则上仍有效——但 `electron-builder.yml`、
`scripts/**`、`src/main/**`、`src/agent-host/**` 在阶段 4 打包链批次里**大改过**，凡涉及这些路径的锚点**必须重新实读**。

## 不可复议（提出即无效，请勿浪费篇幅）

- 规格「结论先行 → 本规格内已决、不再复议」表的六条（①~⑥）。
- 红线 R1~R7。
- 用户拍板 **D53 ①②③**。
- §12.1 六条已由用户拍板收口（见文末「拍板收口（2026-08-19，D55）」）：
  Q4 = 底部状态行与 TurnFooter **合并成一行**；Q7 = Denied 警示色**追认**；Q1 = 段间距**同档同值**；
  Q3 = copy 按钮**常显低对比、禁 hover-only**；Q5 = PendingTurnHead 相邻观感**转 GUI 点验项**；
  Q6 = itemId **本片不做、走安全回落**。
- §12.3 的 Q10（台账措辞勘误，已入 D55 ④）与 Q11（子档已删）均已了结。
- 本批范围就是 FB1/FB2/FB3/FB4/FB6/FB7/FB8/FB9 八件，不评「该不该做」，只评「这么做行不行、写清楚没有」。

## 必须逐项给出结论的检查面

- **A. 锚点有效性**：抽验规格 §0.4 锚点总表与全文【实测】引用；指名任何失效/漂移/指错的锚点（尤其 `electron-builder.yml` 与打包相关的）。
- **B. §12.2 五条评审确认项**（Q2 hwm 落点 / Q8 小时档 / Q9 行内 `$…$` / Q12 折叠取 E1 / Q13 触发器取 G1）——**逐条**给「同意 / 反对 + 依据」。这五条本就是等你拍的。
- **C. §9 断言与变异计划**：变异臂是否真能被断言咬红？有没有「咬不到的空转臂」或「按规格照做即落成某条变异」的自相矛盾？「零跳过纪律」是否现实？
- **D. §11 切片**：依赖序 `① ∥ ⑤ → ② → ③ → ④` 是否成立；§11.1 混面分析是否漏了文件级/同区冲突；§11.3 每片收口三件套是否可验。
- **E. §13 空壳自查三类**是否漏项（同名空壳 / 生产者缺席 / 硬编码信念）。
- **F. FB9 两个地雷**（`electron-builder.yml` 的 `!node_modules/katex/**` 裁剪、`globals.css` 无 bundled webfont 红线与 KaTeX woff2 正面冲突）的处置是否真的闭环——**按当前 HEAD 的 electron-builder.yml 与打包链复核**，阶段 4 之后这里变过。
- **G. FB6 零回归网**：§5.6「先补钉子」的方案是否真能钉住「head 是回合首子元素」这条事实。
- **H. 全局**：有无 blocker 级的自相矛盾、施工入口互斥、红线误伤、或安全/隐私边界（R2 `turnCopy.ts`）被绕过。

## 输出

写入文件：`{OUTFILE}`

结构：

```
# {TRACK} —— 0820 FB 批规格 rev.1 双盲评审

## 判语
（可开工 / 修订后开工 / 须重写，一句话理由）

## 发现
1. **严重度：blocker|major|minor｜规格节号：§x.y｜主张摘要：一句话**
   **证据：** file:line + 原文摘录
   **建议修法：** 具体到改哪一行写成什么

## §12.2 五条逐条结论
（Q2 / Q8 / Q9 / Q12 / Q13，各一段）

## 已复核正确清单
（你亲自核过、确认成立的关键点，列表）
```

严重度定义：**blocker** = 照此施工必然出错或必然违反红线/安全边界；**major** = 会导致返工或验收不成立；**minor** = 需订正但不阻塞开工。
