# Claude 主线台账（🤖）

> 归属：OpenChamber 气泡对话重构 — Claude 主线（复杂/架构攸关任务）
> 任务定义：[`2026-07-23-openchamber-chat-refactor-execution-plan.md`](./2026-07-23-openchamber-chat-refactor-execution-plan.md) §2
> 总台账：[`openchamber-chat-refactor-ledger.md`](./openchamber-chat-refactor-ledger.md)
> 维护人：Claude（每完成一个 C-xx 任务或重要中间结论加行；里程碑级结果同步总台账）
>
> ℹ️ **2026-07-24 起本文件为过程档案（append-only）**；当前任务状态看
> [`docs/plantree/plans/openchamber-chat-refactor/`](../plantree/plans/openchamber-chat-refactor/README.md)。C-xx 全部完成（C-11 机动 / C-12 Phase 5 除外），后续 T-xx 过程记录亦并入本文件（双轨合一）。

## 任务状态

| ID | 任务 | 状态 | 备注 |
|---|---|---|---|
| C-01 | agent-host 构建产物与打包配置 | ✅ | 2026-07-23 完成；打包整链验证归 C-02 |
| C-02 | 打包态自动化验证 | ✅ | 2026-07-24 完成；M1 自动化半边齐，GUI 点验 → T-10 → CP2 |
| C-03 | Question 桥 spike | ✅ | 2026-07-24 完成；官方机制 = permission 流 + updatedInput.answers |
| C-04 | Question 桥实现 | ✅ | 2026-07-24 完成；官方 canUseTool 机制全链，网关 smoke 三场景绿；T-05 Question 卡解锁 |
| C-05 | Thinking 支持度探测 | ✅ | 2026-07-24 探测+收尾全完成（CP3 拍板默认开，`8449e88`）；T-04 解锁 |
| C-06 | Resume 历史重放（协议+Host+Store） | ✅ | 2026-07-24 完成；CP4 定稿 + 全链实现 + 网关端到端 smoke 过，T-03 解锁 |
| C-07 | Session Index（Main + IPC） | ✅ | 2026-07-24 完成，T-02 已解锁 |
| C-08 | Store 结构优化 + 批处理 | ✅ | 2026-07-24 完成（a 批处理 `138ccb3` + b 分桶 `922d689`）；行为基线 344 例全绿，GUI 观感随 T-xx 联调复验 |
| C-09 | 测试基建 + lint 恢复绿 | ✅ | 2026-07-24 完成；lint 0 诊断 + 70 新单测（全套 308 绿）；C-08 行为基线锁定 |
| C-10 | Effort/Plan/Build 探测 | ✅ | 2026-07-24 完成；仅 xhigh 有实证、plan 非硬只读；UI 承接待 CP3 |
| C-11 | stream-json fallback | ⬜ 机动 | 阻塞时提级 |
| C-12 | 旧路径收缩 + 压测 | ⬜ Phase 5 | |
| C-13 | 附件协议探测与桥接 | ✅ | 2026-07-24 全部完成；attachments[] 协议 + Host 消息流包装，网关双场景绿；T-18 解锁 |
| C-14 | Host 挂起看门狗 | ✅ | 2026-07-24 完成；生产性事件判定（api_retry 风暴照杀），非法 model 网关实证 51s 显性 failed |
| C-15 | 随包 Node 运行时 | ✅ | 2026-07-24 完成；bundled 源 + verify:packaged 25 项 PASS（PONG 跑在随包 node 上）；加密机实证归 T-11⑥；产物 141MB（+21MB）待用户过目 |

图例：✅ 完成 · 🟡 进行中 · ⬜ 未开始 · ❌ 阻塞

## 过程记录（按时间）

| 日期 | 节点 | 结果 | 证据 / 提交 |
|---|---|---|---|
| 2026-07-23 | 执行计划定稿（CP1），双轨分账结构落库 | ✅ | 本文件 + 执行计划 + 团队台账骨架 |
| 2026-07-23 | C-01 事实摸底：node_modules 424MB 构成与剪枝空间 | ✅ | SDK 平台包 252MB（claude.exe，sdk.mjs 无引用，我方恒传 pathToClaudeCodeExecutable）；Cometix 平台包 26MB（install.cjs 装机时已把 cli.js+vendor 拷入主包，运行时冗余）；node-pty 64MB（prebuilds 58MB 含 4 平台，加载序 build/Release→prebuilds/{platform}-{arch}，根应用先例只留 lib+prebuilds/win32-x64）；@img/sharp-win32-x64 19MB（cometix 可选二进制）。剪后估 ~112MB。SDK 0.3.218 主包无 vendor/cli/wasm（执行计划该备注过时）；`files: out/**/*` 会把产物双打进 asar，产物目录需避开 out/ |
| 2026-07-23 | C-01 评审结论采纳（deep-reasoner GO-WITH-CHANGES） | ✅ | 采纳全部：剪 SDK/Cometix 平台包与 node-pty 子集；保留 `@img/sharp-win32-x64` 兜底（cli.js 加载 sharp 有 try/catch 容错，19MB 保险）；产物用「拷贝开发 node_modules + preflight 断言」而非全新安装（install.cjs 在 `--omit=optional` 下产出无 cli 残包；公司机半离线）；选址仓库根 `out-agent-host/`（避 electron-vite out/ 清理与 `files: out/**` asar 双打）；TSD 修复在 afterPack 端到端收口（electron-builder 复写 extraResources 会再加密，只修中间产物不够）。一处按实调整：评审建议 preflight 校验 cli.js sha256，但 PINNED.md 记录的是 npm tarball hash（无法与拷贝后的 cli.js 直接比对），改用版本 pin + cli.js 存在 + >5MB 断言，防「--omit=optional 残包」目标等效 |
| 2026-07-23 | C-01 完成：构建脚本 + 打包配置 + 产物验收 | ✅ | `scripts/build-agent-host.mjs`：preflight（pin 断言 2.1.212/0.3.218、cli.js>5MB、vendor、node-pty prebuild）→ esbuild bundle（index.ts→out-agent-host/index.js，external SDK+Cometix）→ 剪枝拷贝 424MB→87MB → Windows TSD `.tmp.bin` 修复 → 产物自检；`electron-builder.yml` extraResources `out-agent-host→agent-host`（明文不入 asar）；`afterPack.mjs` 兼扫 `resources/agent-host`。验收：① 产物 initialize→host.ready（cometixVersion=2.1.212，cliPath 解析自产物内 node_modules）；② 产物 PONG smoke ok:true + 产物 permission smoke ok:true + dev 入口回归 ok:true；③ `pnpm build:agent-host` 独立通过（约 4.5s），`dist:prereq` 已挂钩，整链打包验证→C-02。typecheck 绿、改动文件 biome 绿；`pnpm test` 仅 2 个存量失败文件（stash 对照证实与本次无关，归 C-09）。独立验证代理复核 PASS（含 stdin 畸形输入/孤儿会话对抗探测、重建幂等、afterPack 假 pack 功能测试）。hash：`f21fec7` |
| 2026-07-23 | 测试凭证统一约定落库（用户拍板） | ✅ | 执行计划 §4 新增统一约定：测试一律走网关 `cch-jyw.pipidan.qzz.io`（凭证落文档）；Host 侧脚本经 `spikes/testCredentials.ts` 临时 `CLAUDE_CONFIG_DIR` 自动注入，含 onboarding 种子（实测定位：无 `.claude.json` 种子时 cli.js 首跑 onboarding 挂起）；逃生口 `AICLIENT_SMOKE_USE_LOCAL_SETTINGS=1`、`AICLIENT_TEST_AUTH_TOKEN`/`AICLIENT_TEST_BASE_URL` 覆盖。与团队台账「token 不入库」表述的分歧已呈用户裁定。hash：`0c868c0` |
| 2026-07-24 | C-02 断言与辅助工具落地 | ✅ | `scripts/verify-packaged-app.mjs`（app 壳/agent-host 结构与剪枝断言、TSD header 哨兵、Node24 寻径复刻、复用 PONG spike 对打包产物冒烟+失败重试一次）——先以「真产物+假 app 壳」自测全绿；`scripts/make-test-claude-config.mjs`（GUI 点验走网关凭证，`pnpm prepare:test-config`，利用 AgentHostProcess `{...process.env}` 透传 + claudeSettings CLAUDE_CONFIG_DIR，兼答复团队台账 T-17 行需求——无需改 Host 代码）；T-10 清单 `t10-packaged-gui-checklist.md`；**CI build.yml 两个打包作业补 agent-host 构建+结构断言步骤（原配置缺失，下次打 tag 必炸）**；build-agent-host.mjs 兼容 Linux node-pty 无 prebuild（npm 包仅带 darwin/win32 prebuilds，Linux 装机编译进 build/Release；仅在无对应 prebuild 时收纳 build/Release，防 Windows 产物误载本机编译版） |
| 2026-07-24 | vflow 移除 Phase A（用户拍板：vflow 不再需要） | ✅ | 背景：本机 dist:prereq 被 prepare:vflow 卡死（GitHub Packages 401；包不在公共 registry；本机无 sibling 仓库）。用户拍板整体移除。委派 fast-worker 执行：package.json 摘 prepare/assert:vflow、`dist:prereq` 简化为 `build && build:agent-host`；electron-builder.yml 摘 vflow/vflow-pkg 两条 extraResources；CI 摘两作业 vflow 步骤；删 prepare/assert 脚本 + 2 个测试文件 + fixtures。副产物：**`pnpm test` 首次全绿（21 文件/111 用例）**——原 2 个存量红文件即 vflow 测试。遗留：`scripts/sync-vflow-resources.mjs` 孤儿脚本删除待用户确认（权限策略要求用户裁定）；运行时代码摘除立项 Phase B（已核实缺资源时优雅降级：AgentInstaller 返 null 走原错误、VflowService 存在性检查，开发机日常即此状态） |
| 2026-07-24 | Host 修复：SDK 流结束无 result 时补发终态（团队红线需求⑤） | ✅ | 团队（Cursor）定位：`claudeRuntime.ts` 流结束无 result 事件时仅静默改 registry 状态、不发事件 → UI 永驻 running。修复：normalizer 增本轮 `sawResult` 标记 + `finishTurn()`（result 已发终态→no-op；有 assistant 输出→补 message/session.completed + status idle；全程无 assistant 输出→session.failed + status failed，把网关挂起显性化）；runtime 该分支集成并清理孤儿 permission。关键陷阱：正常完成后 `session.status` 仍是 'running'（无回写），该分支每轮必走——靠 sawResult 防重复终态。单元 spike `spikes/phase2-stream-end-unit.ts` 三场景绿。协议影响：无新事件类型、无 bump；行为变化已记总台账并通知团队 |
| 2026-07-24 | C-02 断言脚本首战立功：打包产物 node_modules 丢失 | ✅ | 首次真打包后 `verify:packaged` 抓到 `resources/agent-host` 仅 0.0MB（index.js+package.json，node_modules 整树缺失）。根因：electron-builder 对每个 extraResources 拷贝强制注入 `!**/node_modules/**`（app-builder-lib fileMatcher.js:126-138），仅当 filter 显式含 `node_modules/` 模式时该排除被插至其前、由后者重新包含。第一版修复（filter 重新包含）引出第二个问题：87MB/万级文件的 extraResources 并行拷贝与 rcedit 改写 AiClient.exe 版本资源发生竞态（"Unable to commit changes"，加 filter 后 0/3 复现，唯一成功恰是无 node_modules 的那次；手动对静止文件跑 rcedit 正常）。**最终方案：撤销 extraResources 条目，改在 afterPack.mjs 钩子串行拷贝 out-agent-host → resources/agent-host**（打包主流程后执行、无并发争抢，同钩子顺路做 TSD 修复；缺产物时 fail loudly 提示先跑 build:agent-host）。C-01 阶段「假 app 壳」自测测不出此层，正是 C-02 打包态验证的价值所在 |
| 2026-07-24 | **C-02 验收：完整打包链 + 打包态全量断言 PASS** | ✅ | `pnpm dist:prereq`（build + build:agent-host）→ `electron-builder --win portable` → afterPack 串行拷贝 87.0MB 产物 + TSD 修复 2386 文件 → `dist/AiClient-0.3.4-portable.exe`（约 120MB）。`pnpm verify:packaged` 全绿 22 项：app 壳 / agent-host 结构与剪枝 / TSD header 哨兵 / Node24 寻径（nvm v24.18.0）/ **打包产物直跑网关 PONG 冒烟**（host.ready cometixVersion=2.1.212；产物含当日 stream-end 修复，兼作该修复集成回归）。全量回归：typecheck 绿、`pnpm test` 111/111 绿。GUI 手工点验清单移交 T-10（`t10-packaged-gui-checklist.md`）。hash：`dbb20be`（build 链）/ `6a633d6`（Host 修复） |
| 2026-07-24 | vflow 移除 Phase B：运行时代码整体摘除（D16 收口） | ✅ | 用户复确认「彻底摘除」后委派 fast-worker：删 VflowService+测试、shared/types/vflow.ts、VFLOW_PROJECT_INITIALIZED 通道、preload 桥、App toast 监听、onboarding vflow 步骤与文案、设置条目、CliDetector 条目、AgentInstaller 安装/离线兜底分支（净 -89 行）及 4 个专属用例、孤儿脚本 sync-vflow-resources.mjs；类型联合收缩（AgentCliType/InstallAgentId/InstallStepId）。验证（先定标准后动手）：src+scripts+配置 grep vflow 清零（抽查复核 exit 1）、typecheck 绿、测试 20 文件/103 用例全绿（-8 例均为 vflow 专属）。累计净删约 -1360 行。hash：`eac23f7` |
| 2026-07-24 | 用户反馈映射落库（F1-F5）+ 新任务 C-13/T-18 | ✅ | 执行计划新增 §7 映射表：F3/F4/F5 验证重构方向（气泡化/真实文本域/四区并排）、F1 → T-05 验收增强（工具卡路径可点击跳转）+T-13 联动、F2 → 新增 C-13（附件协议 spike+桥接，主线）与 T-18（Composer 粘贴 UI，团队，依赖 C-13）。两条子台账任务表已同步 |
| 2026-07-24 | C-07 完成：SessionIndexService + 3 条 chat IPC（解锁 T-02） | ✅ | Explore 地形扫描定关键设计：① runtime 事件不带 workspacePath/model → 索引在 `CHAT_CREATE/RESUME_SESSION` IPC 入口捕获，事件仅富化 runtimeIdentity；② 持久化镜像 RemoteConnectionManager 成熟模式（原子写 tmp+rename、懒加载在途去重、串行 flush 队列），存 `userData/session-index.json`；损坏 JSON warn 后空索引启动。fast-worker 按逐文件规格实现，8 项单测（round-trip/损坏容错/updatedAt 降序/事件富化/tmp 无残留）全绿；全套 21 文件/111 用例、typecheck 抽查复核通过。store 侧 hydrate（initRuntime 接 listSessions）按计划归 T-02 联调。hash：`f6807c9` |
| 2026-07-24 | C-06 协议草案 + fresh-fable 对抗评审吸收（后经 CP4 定稿） | ✅ | 草案 `2026-07-24-c06-session-history-protocol-draft.md`：`session.history` 批量事件（resumed→history→idle，读失败非致命）+ `session.listHistory`/`session.historyListed`（Main `requestAndWait` promise 化）+ `session.updated`（补身份缺口：SDK session_id 中途发现只写 Host registry 无事件回传，应用内会话索引 runtimeIdentity 恒空，重启 resume 不成立）+ `host.ready.capabilities.history`；Host `historyReader`（resume 按 uuid 文件名定位绕开 munge 有损性、宽容解析、流式双侧裁剪）。协议纯增量不 bump 版本。对抗评审裁定 GO-WITH-CHANGES，12 findings 全采纳，要害：F-1 BLOCKER（running 会话 resume 重入孤儿化运行中 turn + 整段替换吞流式消息 → session_busy 拒绝 + registry merge + store 按 `h:` 前缀替换）、F-2（SDK forkSession 默认 false，分叉叙事降级为防御性覆盖+回归钉子）、F-3（实测最大会话 3.97MB/1293 行，补输入侧 32MB tail + user text 64KB）、F-4（Host 非白名单读到密文会静默空历史 → TSD magic 探测 + `encrypted_unreadable`，入 T-11⑤）、F-5（Host 命令循环串行 → 历史读取异步化；requestAndWait 补进程 exit 即 reject）。评审附真实数据盘点：12 JSONL/1669 行、配对模拟 0 孤儿 0 悬空、munge 3/3 吻合、4 种未记行型补入分类表 |

| 2026-07-24 | **C-06 完成：Resume 历史重放全链落地（解锁 T-03）** | ✅ | 定稿协议全量实现：shared 三文件（sessionHistory.ts 新建 + runtimeEvents 三事件/capabilities + agentHost 新命令）；Host historyReader（922 行：uuid 文件名定位、宽容解析 controlLines/badLines 分账、合并配对、输入 32MB tail + 流式裁剪、TSD magic → encrypted_unreadable）+ runtime 改造（session_busy 拒绝、registry merge、resumed→history→idle 异步时序、session.updated）；Main requestAndWait（exit 即 reject）+ chat:listHistory IPC/preload + 索引富化；store h: 前缀幂等替换 + historyErrors + 权限卡排除。**实现要点**：tsconfig 排除 agent-host → typecheck 不覆盖 Host 侧，端到端 smoke 是 Host 集成的真实门禁。验证：新增 49 单测（A28/B9/C12）→ 全套 27 文件/181 用例绿、typecheck 干净、改动文件 biome 绿；**验收 smoke `spikes/c06-resume-history-smoke.ts` 真实网关 ok:true**——capabilities.history / session.updated 身份发现 / resumed→history→idle 时序 / 历史含码字 + h: 前缀 / running 会话 resume 拒绝 session_busy / resume 后追问召回码字（ORANGE-42 式）逐项断言通过。工作树中团队 ChatComposer 在途改动已避让未混入。hash：`db41f63` |

| 2026-07-24 | C-05 收尾：thinking 默认开 + capabilities + store thinking 块（CP3 拍板执行） | ✅ | claudeRuntime enabled(budget 4096)；host.ready.capabilities `{history, thinking}`；store ChatBlockType 增 thinking + thinking.started/delta 处理 + 历史 thinking 块入渲染数据（T-04 数据面就位）。验证：28 文件/184 用例绿、typecheck 干净；Host smoke 事件链完整（**thinking 事件网关侧非确定**——两跑均未引出，与 C-05 spike 观测一致，管线以单测+spike SDK 层验证为准，GUI 实证归 T-04 验收）。**附**：首跑 90s 停滞复现「答案已到流不收尾」——C-14 看门狗立项依据+1。hash：`8449e88` |
| 2026-07-24 | 四路支持度探测（C-03/C-05/C-10/C-13 并行，用户要求提并发） | ✅ | **C-03**：AskUserQuestion 走 canUseTool 权限流是**官方机制**（`AskUserQuestionInput.answers` 字段注释即 "User answers collected by the permission component"），三分支实测通过；最大 footgun：bare allow（无 updatedInput）被 cli.js 静默作废重问（仅源码可见）；取消建议映射 allow+空 answers（不产生 denial 记录）；自由文本走 `updatedInput.response`；超时可下沉 SDK `askUserQuestionTimeout`。**C-05**：thinking enabled 单轮+多轮 resume（签名 thinking 历史回放）8 turn 零 400——`claudeRuntime.ts` disable 防御已过时；normalizer 现链路够用（assistant content 路径），stream_event 分支为死代码（未开 includePartialMessages）；注意 disabled 非强保证（对照组一次跑出 thinking 块）+ 延迟上浮（最慢 75s）。**C-10**：effort 仅 xhigh 有可测差异（cache miss + inference_geo=global + 绕过 thinking:disabled），其余档位与非法值签名相同（不校验）；permissionMode 全枚举 default/acceptEdits/auto/bypassPermissions/manual/dontAsk/plan（CLI 层校验）；**plan 非硬只读**——模型照样调 Write，只读约束须 canUseTool 侧识别 plan 自行拒绝；acceptEdits/auto/dontAsk 旁路 canUseTool；bypassPermissions 安全阀非硬约束；**非法 model 不快速失败、会话挂死**（→ 拟立项 C-14 看门狗）。**C-13**：SDK 消息流 prompt 携带 base64 image/document 块全部实测可行（152KB 大图过网关），推荐 `attachments[]={kind:'image'\|'text', mediaType, data, name?}`，path 不进协议；Host 改造是加法（string prompt 包成单元素消息流）；大图延迟显著（152KB→79s），T-18 需发送中状态。四 spike 落库 `9bda9e5`。C-03+C-05+C-10 → CP3 汇报 |

| 2026-07-24 | **C-04 完成：Question 桥全链落地（解锁 T-05 Question 卡部分）** | ✅ | 协议重塑（question 事件/命令为从未发射的 stub，纯重塑无 bump）：`question.requested` 载 `questions[]`（question/header/options{label,description,preview}/multiSelect）；`question.resolved` 载 `outcome: answered\|cancelled\|rejected` + answers/response；`question.respond` 命令 `answers?: Record<question全文, 答案>`/`response?`/`cancel?` 三选一——**bare allow 被 cli.js 静默作废重问（footgun ①），index.ts 与桥双层校验拒空**。Host `questionBridge.ts` 镜像 permissionBridge（单次 settle、abort→deny interrupt、rejectSession/rejectAll 挂 send 三分支+stop/close/dispose）；canUseTool 改 dispatcher（AskUserQuestion→question 桥，其余 permission 桥）。Main/preload respondQuestion 链路本已存在仅重塑 payload；store question block + pendingQuestion + respondQuestion action。**deep-reasoner 对抗评审 GO（0 BLOCKER/MAJOR），逐条核 cli.js 2.1.212 源码**：footgun ① 确切路径（`requiresUserInteraction` + 空对象 updatedInput→undefined 转换）；cancel→allow+空 answers 即原生 Skip 分支（"The user did not answer"）；multiSelect 输入侧接受数组或 ", " 拼接（renderer 拼时用逗号+空格）；**response 注入依赖「SDK 不对 updatedInput 二次校验」的未文档化不变量**（cli.js 已 pin；smoke 的 response 场景即该脆弱性回归钉子）；answers+response 并存时模型只见 response → 协议注释标互斥，T-05 UI 二选一。验收：unit smoke 9 组断言绿；**网关 smoke 三场景全绿**——answers→`CHOSEN: Blue`；response 自由文本→`CHOSEN: Neither — I actually prefer green.`（footgun ③ 实证）；cancel→outcome cancelled 无重问 + `NO-ANSWER`（footgun ② 实证）。三绿：typecheck / 31 文件 211 用例（+8 question 单测）/ biome 改动 12 文件。遗留（评审 #6，未来增强）：per-answer annotations（notes/preview 选择）暂不透传。hash：`c9522d2` |
| 2026-07-24 | **C-14 完成：Host 挂起看门狗（无模型进展超时 → 显性 failed）** | ✅ | send 循环 stall 看门狗：默认 120s（`AICLIENT_HOST_STALL_TIMEOUT_MS` 可配，0 禁用）无**生产性事件** → abort + `session.failed` 带明确错误 + status failed（不是 stopped）。**关键设计修正（网关实测逼出）**：首版「无任何流事件即重置」被非法 model 击穿——cli.js 进入无限 api_retry 循环时 `system` 事件持续流出把看门狗喂活（105s 无终态复现）；改为只有 assistant/user/result/tool_progress/stream_event 重置计时器，system 控制面事件不算进展。**三类豁免不误杀**：permission/question 挂起（用户思考无上限，靠 hasPending 判定续命）、本地工具执行中（normalizer 新增 openTools 跟踪，tool.started 未 completed 即豁免）、慢 thinking 轮（thinking delta 是生产性事件天然重置）。normalizer 增 `hasOpenTools()` + `emitFailed()`（关流式块后发 failed 终态）；`ClaudeRuntimeOptions.queryFn` 测试注入缝。验证：unit spike 5 场景（挂死 303ms 击杀 / 事件流动不杀 / permission 挂起 900ms 豁免 / open tool 900ms 豁免 / **api_retry 风暴 312ms 照杀**）；网关 smoke：非法 model `not-a-real-model-c14` 51s 显性 failed（watchdog 错误文案）+ PONG 对照 13s 完成。**附带观测**：首跑 control 撞 `API Error: 400 thinking 格式无效`（网关中文报错）——C-05「thinking 400 已消失」结论的反例，属网关瞬态（重跑即过、同日 C-04 三场景同配置全过），T-04/GUI 若复现按 session.failed 显示即可，无需回滚 thinking 默认开。三绿：typecheck / 211 用例 / biome 改动 4 文件。hash：`f87c1cc` |
| 2026-07-24 | **C-13 完成：附件桥接全链落地（解锁 T-18）** | ✅ | 协议纯增量（无 bump）：`session.send.payload.attachments?: SessionAttachment[]`——`{kind:'image'\|'text', mediaType, data, name?}`，image 走 base64、text 走原文，**path 不进协议**（Renderer 本地读取编码，spike 结论沿用）。Host：有附件时 prompt 从 string 换成单元素 `SDKUserMessage` 消息流（`{type:'user', message:{role:'user', content:[text?, image/document…]}, parent_tool_use_id:null}`，image→`source:{type:'base64'}`、text→document `source:{type:'text'}`+title=name，形态均出自 c13-attachment-probe 实测）；纯文本发送路径完全不变。index.ts 附件逐项容错校验（invalid_payload 带下标）；text 允许空（attachment-only）。Main sendMessage 走 `SessionSendCommand['payload']` 类型自动跟随；ipc/preload 内联类型补齐；store `sendMessage(text, attachments?)` + 导出 `ChatSendAttachment`（T-18 直接消费）。验收：**网关 smoke 双场景绿**——8x8 红 PNG→assistant `Red`（24.5s）；text/plain 文档→召回码字 `kumquat`（8.0s）。回归：232 用例绿、biome 改动 7 文件绿；typecheck 唯一红点在团队在途 T-04 文件 `thinkingCard.ts:35`（未提交的团队工作，与本次无关——git status 证实归属，我方文件零错误）。**T-18 注意**：大图延迟显著（C-13 spike 152KB→79s），Composer 需发送中状态；C-14 看门狗对上传期静默有 120s 容忍。hash：`d339f70` |
| 2026-07-24 | **C-15 完成：随包 Node 运行时（D17 落地，开发机侧全验）** | ✅ | pin `24.18.0` win-x64（官方 zip SHA256 `0ae684…e821` 校验；`scripts/node-runtime-pin.mjs` 单一事实源）。`fetch:node-runtime`：nodejs.org→npmmirror 兜底、幂等 skip、`--force` 重取；**实施坑**：Git Bash 的 GNU tar 既不认 zip 又把 `D:\` 误判为远程主机，显式用 `System32\tar.exe`（bsdtar）。afterPack 串行拷贝 `out-node-runtime→resources/node-runtime`（沿 C-02 rcedit 竞态先例；仅 win32 目标，Linux 作业不受影响）。Resolver 增 `bundled` 源：**explicit/AICLIENT_NODE24_PATH 逃生口之下、nvm/fnm/volta/PATH 之上**（随包损坏时用户仍可覆盖；开发态 bundledPath=undefined 行为不变）；`source` 无穷举消费方，加值零连带（Explore 证实）。CI build-windows 加 actions/cache + fetch 步骤。验收：整链 `electron-builder --win portable` + **`verify:packaged` 25 项 PASS——PONG smoke 显式跑在 `resources/node-runtime/node.exe` 上**（无用户 Node 机器可跑通对话的等效证明）；resolver 单测 +2（5/5）。**体积交用户过目**：node.exe 88.2MB 原始 → portable 产物 120MB→141MB（+21MB 压缩后）。加密机「任意路径 node.exe 读 TSD」白名单实证仍归 T-11⑥（开发机不标注通过）。三绿：typecheck / 32 文件 238 用例 / biome 改动 9 文件。hash：`adc3127` |
| 2026-07-24 | 用户关键信息两条：白名单口径 + 备用网关（风险 #1 降级） | ✅ | ① **TSD 白名单按进程名**——「只要是 node 就是白名单」（待 T-11⑥ 实证）：C-06 加密机风险从「架构级未知」降为「待实证」；据此并依用户意向立项 **C-15 随包 Node**（D17，解 T-09 自装痛点；ARD 查证：D1「独立」原义为独立进程且曾以「白名单未经证明」否决随包路线——该前提已被新口径推翻，Electron 内嵌路线仍不可行因进程名不匹配）。② 备用网关 `api.vllmproxy.com` PONG 实测通过，凭证入执行计划 §4（延迟同级；双端点同时刻齐挂一轮——波动确系环境性，C-14 依据+1）。另：T-19 让号 T-20（`9d37df0`），T-19 留给消息队列提案（内容待用户提供） |
| 2026-07-24 | **C-09 完成：测试基建 + lint 恢复绿（C-08 行为基线锁定）** | ✅ | **lint 侧**：517 错误中 494 为 CRLF 格式噪音——根因 `core.autocrlf=true` + 无 `.gitattributes` + biome 要 LF（index 全 LF，归一**零内容变更**，团队 6 在途文件 hash 前后比对零触碰）；biome.json 补排除 `out-agent-host`/`out-node-runtime`（构建产物在被 lint）；新增 `.gitattributes` 锁代码文件 `eol=lf`（否则下次 checkout 即回退红）；手修三处：AISettings biome-ignore 错位归位、SessionManagerView 骨架屏 index key 豁免 + 移除 button 上降级交互性的 `role=listitem`。历史已知手修点：`electron.vite.config.ts:42` 随 unsafe fix 修掉，`afterPack.mjs:46` 问题已不存在。**坑**：加 .gitattributes 后未随提交的文件出「幻影 M」（stat 缓存过期、diff 为空），`git add --renormalize` 清除。hash `ce5a577`+`1505031`。**测试侧**：4 新文件 70 例——`chatSessionsCore` 28（applyRuntimeEvent 纯 reducer：delta 追加/未知 messageId/permission 幂等与**跨会话定位 decoy**/stop 冻结/乱序容忍/recent 上限 8）；`eventNormalizer` 21（单信封/工具去重/openTools/finishTurn 三态/emitStopped/emitFailed/畸形输入不崩/system 非 init 零事件=看门狗契约）；`permissionBridge` 14（单次 settle/预中止/abort 豁免 running/rejectSession 会话隔离/description 三级兜底含 displayName 末级）；`protocolErrors` 7（**真实 Host 子进程** NDJSON 协议错误路径，hermetic 不加载 SDK 不触网——parse_error/invalid_command/protocol_mismatch 含 `[]` 边界/not_implemented 回显 requestId/invalid_payload/空行忽略/shutdown 干净退出）。编写全程零行为偏差报告（预期=实际全吻合，行为基线可信）。三绿：typecheck / **36 文件 308 例** / lint 0 诊断。hash `49a6031` |
| 2026-07-24 | **C-08a 完成：Runtime Event 批处理（分段执行，形状零变更）** | ✅ | 纯函数 `applyRuntimeEvents` 折叠既有 reducer（批内后序见前序效果、合并 patch），initRuntime 队列 + **16ms 窗口冲刷** + 256 上限立即冲刷（防隐藏窗口定时器节流积压）+ 清理先排干再退订（晚到事件不丢）。**流式每 delta 一次 set→全组件重渲染的抖动源消除**；reducer 契约与 state 形状不变——C-09 基线测试原样有效、消费方零改动。**分段依据**：`state.messages` 仅 3 个消费文件（MessageTimeline/ChatComposer/LeftNav），但 MessageTimeline 恰在同事在途（T-04 收尾中，用户告知其仍可能改动）→ **分桶 + 选择器迁移拆为 C-08b**，等其落地后做，避免共树对撞。新测 `chatSessionsBatch.test.ts` 8 例（折叠语义 5 + 假定时器调度 3）。三绿：typecheck / **37 文件 316 例** / lint（我方提交时点绿；随后红点来自同事在途新文件 fileMention.* CRLF，不属我方）。hash `138ccb3`。**注**：同事 T-04 随即落地 `22ef2ff`——C-08b 只剩 T-07（fileMention 在途）解除后可做 |
| 2026-07-24 | **C-08b 完成：messages 分桶 + 消费方迁移（C-08 全量收口）** | ✅ | 同事 T-07 落地（`1ff7fc1`）+ 用户确认全权接手后动工。store：`messages` 扁平数组 → `Record<sessionId, ChatMessage[]>`，全部事件分支桶内操作（delta/thinking/tool 查找、permission/question 反向定位、history h:* 幂等替换只触本会话桶；history 插入简化为「桶内历史在前」，跨会话隔离天然成立）。消费方 3 处：MessageTimeline **只订阅本会话桶**（其他会话流式不再牵连重渲染——C-08 消费端收益主体）、ChatComposer lastUserPrompt/hasAssistant、LeftNav hasTimeline。5 个 store 测试文件形状适配（fast-worker 执行，59 例数量不变、断言语义不变，两处升级整桶相等性）。每 delta 成本 O(全部消息)→O(本会话桶)。**行为差异说明**：无 bucket 时 permission/question.resolved 不再返回 messages 键（旧版返回等值映射拷贝）——语义等价，测试未受影响。三绿：typecheck / **38 文件 344 例** / lint 0 诊断。hash `922d689`。C-08 验收「长流式无卡顿」机制面完成，GUI 观感并入 T-xx 联调 |

| 2026-07-26 | **T-02/T-07 GUI 联调不通过 → 两枚真 bug 修复落地** | ✅ | 用户首轮人工联调（隔离配置 `CLAUDE_CONFIG_DIR`，dev 态 `SKIP_ONBOARDING_GATE=true` 直进壳）。**① 会话标题全空（T-02 不通过）**：症状=左栏每行只剩状态徽标。根因两处叠加——写入侧 `chat.createSession` IPC payload **无 title 字段**（`ipc/chat.ts:47`），`SessionIndexService.ts:56/76` 永落 `title: ''`，仅显式 rename 才写真值；读取侧 `sessionIndexMerge.ts:69` 注释「持久 title 权威」**无条件覆盖**，把 UI 种子 `'New chat'`/`'Live Agent Host'`（`useSyncChatWorkspaceTree.ts:158/42`）抹空。渲染层无罪（`LeftNav.tsx:455` 完全符合 `min-w-0 flex-1 truncate` 规范）。**A/B 实证**：往索引播 3 条带真标题的条目 → 只有它们显示标题，空标题条目仍只剩徽标，实锤空值路径。附带症状：`LeftNav.tsx:112` 按 `title.includes(query)` 过滤，标题全空时**输入任何搜索词全部会话消失**。修法=读取侧三分支加守卫（持久值仅非空时权威，两端皆空回退 `Session <短码>`），持久 `''` 保留「从未重命名」语义、不动 Service 契约（故 `SessionIndexService.test.ts:34` 断言 `title:''` 仍有效）。另注：现成 `resumeIntent.ts:84` `resumeDisplayTitle()` 是死代码，仅测试文件 import。**② `@` 引用 Windows 反斜杠（T-07 不通过，P0）**：`path.relative` 在 win32 返回反斜杠，而 `relativePath` 全链按 POSIX 串消费 → `@src/` **零命中**（子序列断裂）、`ChatComposer.tsx:370` 与旧路径 `EnhancedInput.tsx:668` 的 `lastIndexOf('/')` 恒 −1（整条路径当文件名显示、灰色目录后缀永不出现）、`fileMention.ts:77` 插入反斜杠 mention。抽 `toPosixRelative()` 一处归一化三症齐消。**未修留档**：`searchContent` 同 `relative()` 反斜杠问题（`SearchService.ts:246/295`），消费链未验证，不做静默半修。三绿：typecheck 干净 / lint 583 文件 0 诊断 / **39 文件 354 例**（+`SearchService.test.ts` 5 例 + merge 空标题回归 5 例）。hash `db5116a` |
| 2026-07-26 | **thinking 空白根因更正：非网关剥离，是 `display` 默认 omitted** | 📌 | T-04「压根看不到 thinking 卡」排查。逐环验证代码链**全部正确**：`historyReader.ts:770` 认 thinking → `chatSessions.ts:211` 映射 → `thinkingCard.ts` 出 view-model → `MessageTimeline.tsx:182` 渲染；能力闸 `agent-host/index.ts:136` 硬编码 `thinking: true`。实测 `readSessionHistory` 对含 31 个 thinking 块的真实 JSONL 吐出 `text=15 tool_call=40 tool_result=40`、**thinking=0**——因为三条会话共 153 个块 `thinking` 字段**全是空串**（signature 763~2627 字符俱在），`historyReader.ts:773` 的 `if (!t) continue` 正确跳过空块。**首轮误判为「中转网关剥离明文」，查官方文档后更正**：`display: "omitted"` 是 Opus 4.8/4.7、Sonnet 5、Fable 5 的**默认值**，行为即「返回 thinking 块但文本为空」；代码从未设过 `display`。官方口径：`display` 只控可见性，thinking 照常发生、照常计费、原始思维链任何模型都不暴露；多轮只需把块**原样回传**（含空块），API 拒绝的是被修改的块而非空块。故对推理质量/计费/多轮**均无影响**，只影响可观测性。**连带挖出真缺陷（记为 #8，待办）**：`claudeRuntime.ts:393` 发 `thinking: { type: 'enabled', budgetTokens: 4096 }`——该形态在 Opus 4.8/4.7、Sonnet 5、Fable 5 上**已移除、发送即 400**（仅 Opus 4.6/Sonnet 4.6 尚可用且已废弃），正确写法 `{type:'adaptive'}` + `output_config.effort`。这大概率就是 C-14 行记录的「API Error: 400 thinking 格式无效」瞬态根因（会话 model=opus），亦即 open-questions #5；与 T-20 Effort 选择器同一处改动，合并处理 |

| 2026-07-26 | **T-02 / T-07 GUI 复验通过 → 转 Done** | ✅ | 用户当日复验 `db5116a` 两项：① 左栏会话行标题正常显示（我方播的 3 条历史 fixture 显中文标题，空标题条目回退 `Session <短码>`）；② `@src/` 有结果、目录后缀正常渲染。**T-02、T-07 转 Done**（roadmap 已验收段）。遗留另计：T-02 归档无 un-archive 入口 → open-questions #6；T-07 目录不可选 / 隐藏文件被吞 / 10 条静默截断 → roadmap「T-07 补强」；`searchContent` 同款反斜杠 → open-questions #7。**Phase 3 剩余**：T-03（历史读失败 UI 缺）、T-04（阻塞在 #8）、T-06（唯一完全未测）、T-05（未开发）。下一个开发任务 = **#8**（一处改动解四件事：400 瞬态 / thinking 可见 / T-04 解锁 / T-20 协议底座） |

| 2026-07-27 | **#8 thinking 形态修正落地：`display` 是真凶，`budgetTokens` 400 假说被推翻** | ✅ | 先定验证后改代码（规范第 12 条）：新建 `spikes/c16-thinking-shape-probe.ts` 五场景实测网关（`claude-opus-4-8[1m]`，cometix 2.1.212 / SDK 0.3.218），退出码 0。**① 真凶确认（T-04 解锁）**：`{type:'adaptive'}` 单发 → thinking 块 1 个、**文本长度 0**；加 `display:'summarized'` → **文本 408 字符**，且 summarized 侧另有 8~9 个 `system/thinking_tokens` 事件（裸 adaptive 一个都没有）。**② 上一行「budgetTokens 发送即 400」假说被实测推翻**：场景 A 原样发 `{type:'enabled', budgetTokens:4096}` 返回 `ok=true`、无 400、无 api_retry。故它**不是** C-14「400 thinking 格式无效」瞬态的根因，**open-questions #5 不能结项，保持 open**。仍迁移该形态的理由改为「上游文档已标 removed（Opus 4.8/4.7、Sonnet 5、Fable 5），属潜在 400，网关哪天收紧即炸」——而非「今天就坏」。**③ 更正 C-10 行的 `output_config.effort` 假设**：SDK 0.3.218 `sdk.d.ts` 中 `effort` 是 **query() 顶层 option**（`Options.effort?: EffortLevel`，`low\|medium\|high\|xhigh\|max`），非 `output_config.effort`；场景 D 顶层 `effort:'high'` 实测干净。**改动**：`claudeRuntime.ts` 抽 `THINKING_CONFIG = {type:'adaptive', display:'summarized'}`（含证据注释）；协议 `agentHost.ts` 加 `SessionEffortLevel` + `session.create.effort`（会话默认）/`session.send.effort`（单轮覆盖），**纯可选加法故不 bump 协议版本**；`sessionRegistry` 加 `effort` 字段沿用 CP4 F-1 合并语义；边界加 `normalizeEffort()` 白名单守卫——Main 侧 payload 是不可信 JSON，非法档位丢弃而非透传给 API（否则 API 直接拒）。**验证双层**：新增 `__tests__/claudeRuntimeOptions.test.ts` 10 例钉死送进 query() 的 options（typecheck 排除 agent-host，且两个缺陷都是静默的——形态错是运行时 400、缺 display 是空文本无报错——单测是唯一护栏）；新增 `spikes/c16-thinking-host-smoke.ts` 起真 Host 走 NDJSON 全链，实测 `thinking.started` 触发 + `thinking.delta` **361 字符非空**（正是 T-04 缺的可观测对象），effort 与非法 effort 两会话均 completed 无 host.error。三绿：typecheck 干净 / lint 586 文件 0 诊断 / **40 文件 364 例**（基线 39/354 +10）。hash `bfd4f6b` |

| 2026-07-27 | **T-07 补强四项 + open-q#7 全部落地** | ✅ | 先定验证后改代码。**① 目录可选**：`rg --files` 只列文件，`@src/renderer` 压根选不到（但它是合法引用，CC 读整棵子树）。抽 `collectDirectoryEntries()` 从文件列表反推各级祖先目录并去重——**派生而非 stat**，保持纯函数、零额外 syscall、可单测；`FileSearchResult` 加可选 `isDirectory`，popup 用黄色 Folder 图标 + 尾随 `/` 区分同名文件。实测本仓 **+144 条目录**可选。**② 隐藏文件**：加 `--hidden`；关键点是 `EXCLUDE_GLOBS` 的 `!.git/**` 必须仍然生效（否则 .git 对象库整个灌进来），已加集成断言钉死。实测本仓 **90 个隐藏条目**从「全被吞」变为可见。**③ 报告截断**：查 `chat` 实测命中 **319** 条却只显示 10 条且无任何提示。**形状选择上推翻了自己的第一版方案**——最初想把 `total` 作为非枚举属性挂在数组上以求零破坏，但**只有自有可枚举属性能过 IPC 结构化克隆**，那样 Renderer 侧会静默丢失 `total`；改为显式 `FileSearchPage {items,total,truncated}`，让 tsc 在每个消费方逼出适配（并加了一例 `structuredClone` 往返断言钉死这条教训）。**④ 同分 tie-break**：`compareFileResults()` 同分按路径深度浅优先、再 localeCompare，给出全序（原本落到 rg 输出顺序，同一查询两次结果可能不同、无法断言）。**open-q#7 一并修**：`searchContent` 两处裸 `relative()` 改 `toPosixRelative()`。该项原记「消费链未验证故不做静默半修」，本轮**已确证真消费方**——`SearchResultList.tsx:185` 用 `split('/').pop()`、`:137` 用 `replace(name,'')`，win32 反斜杠下整条路径会被当文件名显示，故升级为可修并修掉。**消费方适配 4 处**（tsc 逼出）：ChatComposer（并接上 `10/319` 截断提示）、EnhancedInput、useGlobalSearch、useXterm。**远程分支不动远端**：`RemoteHelperSource` 是独立部署到远端的注入脚本，存在版本偏斜风险，故在 `ipc/search.ts` 内包一层适配（`total` 退化为页长＝「未知截断」，诚实值），远端零改动。**验证两层**：`SearchService.test.ts` 22 例纯函数 + 新增 `SearchServiceRipgrep.test.ts` 8 例**真跑 ripgrep 打本仓**（纯函数测不到 rg 真实行为——`--hidden` 是否真生效、`!.git/**` 是否还拦得住、目录条目能否走通全链，这三条正是本轮改动的核心断言）+ fileMention 加 2 例锁目录插入（不追加斜杠）。三绿：typecheck 干净 / lint 587 文件 0 诊断 / **41 文件 391 例**（#8 后基线 40/364，+27）。hash `0f886a8` |

| 2026-07-27 | **T-20 Effort 选择器全链落地** | ✅ | #8 只把 `effort` 通到 **Host 协议**，本轮补齐 Renderer→Main→Host 剩余两段（`ipc/chat.ts` 与 `preload` 的 createSession/send payload 声明原本是**手写内联类型**，不加字段就静默丢弃）。**Renderer 三件**：`efforts.ts` 档位目录（low/medium/high/xhigh/max，附各档 hint；**不按模型过滤**——SDK 对不支持的档位是静默降级而非报错，`xhigh` 在非 Opus 4.7+ 上退回 `high`，Host 侧另有 `normalizeEffort` 兜底）、`sessionEffortStore.ts` + `useSessionEffort.ts` 持久化、`EffortSelect.tsx` 选择器（挂在 ModelSelect 旁，完全对称 T-08 模式）。**关键语义**：「Default」是**独立选项而非 high 的同义词**——它令 payload **完全不含** `effort` 键，从而保持模型默认、与 T-20 前行为逐位一致；选 `high` 则是显式钉住。`toWireEffort()` 返回 `undefined` 而非 `null`，正是为了让展开语法丢键，并有单测钉死。**顺带修掉一处可测性债**：原打算给 `useSessionEffort` 直接写测试，实测发现**仓库 vitest 环境是 `node`、无 React 渲染器**，`useCallback` 在测试里根本调不动（这正是 T-08 的 `useSessionModel` 至今零覆盖的原因）。故把存储逻辑下沉为纯函数 `sessionEffortStore.ts`，hook 退化为薄封装——逻辑可测，且给 T-08 留了同样可循的路子。**验证三层**：`efforts.test.ts` 11 例（目录顺序 / 哨兵不混入真档位 / `toWireEffort` 丢键语义）+ `sessionEffortStore.test.ts` 10 例（按会话隔离、Default 哨兵持久化、blob 损坏与 storage 不可用**均降级为未设而非抛**——Composer 每次 Send 都读它，抛异常等于发不出消息）+ `chatEffortPayload.test.ts` 5 例（`structuredClone` 往返钉死 `effort` 过 IPC 桥不丢、Default 时键不存在；**因为 `ipcRenderer.invoke` 返回 `Promise<any>`、handler 又是手写内联类型，tsc 对这条链完全抓不到丢字段**——与 T-07③ 非枚举属性过不了克隆是同一类陷阱）。Host 侧 `c16-thinking-host-smoke.ts` 复跑仍 exit 0（thinking 307 字符、effort 与非法 effort 两会话均 completed 无 host.error）。三绿：typecheck 干净 / lint 594 文件 0 诊断 / **44 文件 417 例**（T-07 后基线 41/391，+26）。hash `4c3f67e` |
| 2026-07-27 | **本机基线更正：Linux 检出上「44/417 全绿」不成立（环境性，非回归）** | ℹ️ | 本轮在 Linux 检出上作业，实测发现两处与既有台账口径不符，**均为环境差异、不涉及代码**：**①** `pnpm install` 的 `@vscode/ripgrep` postinstall 被 GitHub API **403** 挡掉（下载 4 次重试全失败），导致 T-07 那 8 例真跑 ripgrep 的集成测试有 6 例红——从本机已有的 rg 二进制（`@cometix/claude-code` 自带，ripgrep 15.1.0）拷进 `node_modules/@vscode/ripgrep/bin/rg` 后恢复全绿；**只动 node_modules，仓库零改动**。**②** 剩余 3 例失败是**真正的 Windows-only 断言**，在 Linux 上不可能绿：`ShellDetector.test.ts` 2 例（断言 `shell: 'powershell.exe'` / `execArgs: ['-NoLogo','-ExecutionPolicy','Bypass','-Command']`，Linux 上解析出 `/bin/sh` + `['-c']`）、`CliDetector.test.ts` 1 例（cmd fallback）。**故本机「三绿」口径 = typecheck 干净 / lint 0 诊断 / test 只剩这 3 个 Windows-only 失败且总例数只增不减**。Windows 机器上仍应是全绿，两边判定标准不同这件事需要交接时讲清楚。 |
| 2026-07-27 | **T-03 收尾：历史读取失败的 UI 展示（T-03 结项）** | ✅ | 协议 §7 早已把 error 记进 per-session 的 `historyErrors` 并写明「具体 UI 归 T-03」，但 Renderer 一直没消费——读失败与真没历史在界面上都是一句 `No messages yet`，**用户分不清是空会话还是读挂了**。**先定验证后写代码**（规范第 12 条）：设计与验证方案两路并行只读产出，实现按方案先写测试。**新建纯函数模块 `historyError.ts`**（`.ts` 是硬约束——仓库 vitest 是 `node` 环境且 include 只收 `.ts`，`.tsx` 里的逻辑根本测不到，沿用 T-20 的下沉套路）：`parseHistoryError` 解析 store 的扁平 `` `${code}: ${message}` `` 串（只按**首个**冒号切，避免 `ENOENT: no such file` 这类含冒号 message 被截断；无分隔符的裸 code 不得 slice 掉首字符；未知码降级 `unknown` 且**保留整串**不丢信息）；三种契约码给不同 severity/标题/说明——`jsonl_not_found` 走 **warning 黄**（最高频最良性，红色廉价化会淹没真正要警觉的那条）、`encrypted_unreadable` 走 error 红且文案**明写「这不代表该会话没有历史——记录仍在磁盘上」**（协议 §5.5 的 TSD 探测存在的全部意义就是不许静默当空历史）、`read_failed` 走 error 红。`deriveHistoryNotice` 判 error/empty/none 三态，`selectHistoryError` 只取当前会话那一键。**MessageTimeline**：ScrollArea 内**首条**渲染 `<Alert>`（`ui/alert.tsx` 的 variant 是 `error`/`warning`，**没有 `destructive`**——那个只存在于 button.tsx 与 badge.tsx，照「借鉴 destructive 错误条」的字面做会编译不过），有错误时抑制空态文案。**刻意不做顶部横幅**：该形态已被 `HostStatusBanner` 占用来表示「Host 死了」，而协议 §120 明确历史读失败**非致命、会话仍可继续对话**——提示随对话滚走正是这一点的视觉对应物；同时不写 `lastError`、不禁用 Composer、不改 status badge。**Retry 只给 `read_failed`**（另两种码重试是确定性同结果，「点了永远失败」的按钮比没按钮更糟），复用现成 `useResumeSession`，**零新增 IPC**；重读成功后红线 store 自行 `delete historyErrors[sessionId]`，通知自动卸载。**三路对抗评审（契约保真 / 规范合规 / 正确性）抓到 1 个 major 并修**：Retry 吞掉 `resume()` 的 `false` 返回值，且会话忙时是死按钮。修法比评审建议更收口——评审建议复用 `isTurnActive`，但它**只覆盖 running/starting，漏了 `waiting_permission` / `waiting_question` / `stopping`**，照做会留下三个仍然 no-op 的可点状态；改为把 `resumeIntent.ts` 里内联的忙状态集合提取为导出的 `isSessionBusy()`，**UI 的 disabled 判定与 Host 的 `session_busy` 拒绝条件从此是同一份定义**（纯提取重构，`shouldResumeSession` 外部行为与 `reason` 串逐字不变）。另采纳 5 条 minor/nit：`key={sessionId}` 防局部态跨会话串台、`jsonl_not_found` 文案改过去式（原文案断言「下面没有历史消息可显示」，用户发完新消息后与屏幕自相矛盾）、删对比度不足的 `opacity-70`、删零消费者的 `data-history-error`、selector 收窄为订阅单键（后台会话重放历史不再触发本 timeline 重渲染）。**测试 38 例**，其中 3 例是**契约往返**：import 红线文件导出的纯 reducer `applyRuntimeEvent`，构造真实 `session.history` 事件（含 error），断言 `parseHistoryError` 能还原 code 与原始 message，并断言干净重读会 delete 该键——**主线若改编码格式本轨立刻变红，而不是「全绿地」退化成 unknown**。三绿：typecheck 干净 / lint **596 文件 0 诊断** / **45 文件 455 例**（T-20 后基线 44/417，+38；失败仍只有那 3 个 Windows-only）。hash `7a5c2cd` |
| 2026-07-27 | **给主线的需求（T-03 评审衍生，本轨不实现）** | ⬜ | 协议 §3.1 的 `truncated` / `omittedCount` **全链路无展示**：`historyReader.ts:604-607/816-817` 在超过 `HISTORY_MAX_MESSAGES(1000)` 或输出预算时会丢弃老消息并回填这两个字段，`runtimeEvents.ts:264-266` 也把它们写进了 payload 契约，验收标准 §9.2 要求「超限流式裁剪有标记」——但 `chatSessions.ts` 的 `session.history` 分支（:310-347）根本没读，UI 自然无从展示。**后果**：resume 一个 3000 条消息的真实会话只会灌入最后 1000 条，界面上没有任何「已省略 2000 条」的标记，用户会认为这就是全部历史——**与 T-03 刚消灭的「分不清空/失败」属于同一类误导**。**请求**：store 的 `session.history` 分支保留这两个字段（建议 `historyTruncation: Record<sessionId, {truncated, omittedCount}>`，与 `historyErrors` 同样在成功重读时覆盖/清除）。字段到位后 Renderer 侧成本很低——在历史消息之前补一行 muted 提示即可。 |

| 2026-07-27 | **T-18 上限依据重测：C-13 的「152 KB → 79s」不可复现，`0.36 s/KB` 模型作废** | ✅ | 动工前先验证依据（规范第 12 条 + 第 13 条量系统成本）。**起因**：设计调研按 C-13 三个数据点拟合出 `T(s) ≈ 24.5 + 0.36·KB`，反推出「单附件 150 KB / 总计 200 KB」并据此论证「必须做图片降采样否则截图全灭」。该模型只有 3 个点且全部来自同一天。**重测**（同一共享测试网关、真 CSPRNG 噪声 PNG 保证不可压缩、探针在仓库外的 scratchpad 不入库）：`150 KB→11.2s` / `500 KB→9.5s` / `1 MB→11.2s、11.9s、16.1s`（3/3 通过）/ `2 MB→10.6s` / `3 MB→13.0s`。**结论：延迟与载荷大小基本无关**——3 MB 比 150 KB 还快。同日复跑 C-13 自带 smoke 作交叉验证：8×8 PNG **8.4s**、text/plain **9.0s**，而 C-13 当时记的是 24.5s / 8.0s，**图片路径快了约 3 倍**。故 79s 是当日网关负载快照而非尺寸函数，`0.36 s/KB` 不得再作设计依据；120s 看门狗在这些尺寸下余量 10 倍以上，先前「265 KB 是理论击穿点」的推导同样作废。**探针自身的两个 bug 已修并记录**（诚实起见）：① 首版用 JS 浮点 LCG 造噪声，`seed * 1103515245` 超 2^53 精度后序列退化、被 deflate 压掉约 6 倍，三档标称 150 KB/500 KB/1 MB 实际只有 24.8/79.8/94.3 KB → 改 `randomFillSync`；② 首版把 Host `beginTurn()` 回显用户 prompt 的那条 delta 当成了模型首 token（测出 2~100 ms 的假值）→ 改为跳过与 prompt 全等的 delta。**顺带收获一个 open-questions #5 样本**：1 MB 那轮返回 `API Error: 400 thinking 块格式无效`，同进程紧接着的 2 MB 成功、复测 3/3 全过——**确认是瞬态、与尺寸无关**，为该长期未结项的报错再添一份「非确定性」证据。 |
| 2026-07-27 | **T-18 Composer 粘贴图片/文件落地（T-18 结项）** | ✅ | C-13 已把协议与 Host 侧做完，本轮只余 Renderer。**上限改按官方硬限制**（已查 platform.claude.com vision 文档核实：单图 **10 MB base64**、**8000×8000 px**、单请求 **32 MB**、格式**仅 jpeg/png/gif/webp**）：定为单图 5 MB 原始字节（base64 约 6.7 MB，距上限 33% 余量）、文本 512 KB（卡的是 **token 成本**不是体积，512 KB ≈ 12~13 万 token；更大的应走 `@` 引用让模型自己读）、单次总计 10 MB、最多 5 个。阈值全部集中在 `attachmentLimits.ts` 一处，与超时公式共用同一批常量不会漂移。**不做本地降采样**（推翻设计稿要求）：5 MB 下真实截图直接通过，且**服务端本来就会把超 2576 px 长边的图自动降采样**，本地再做一遍不提升保真度，反而引入一块 vitest node 环境测不到的 canvas 代码、且是对用户数据的有损改写——降级为 followUp（token 成本优化）。**45s 超时修复**（本任务发现的功能性 bug，派活单未提）：`runSend` 原本硬编码 `waitUntil(..., 45000)`，超时会写一条「No assistant/tool progress after send」并弹 Retry；改为 `sendTimeoutMs(bytes) = bytes<=0 ? 45_000 : min(45_000 + ceil(bytes/1MB)*30_000, 115_000)`。理由不是补偿上传耗时（已证明与尺寸无关），而是**给网关坏日子留保险**——同量级波动约 8 倍。`sendTimeoutMs(0)` 恒等 45_000 保证纯文本路径逐位不变；上界 115_000 压在 `claudeRuntime.ts` 的 `DEFAULT_STALL_TIMEOUT_MS = 120_000` 之下，**让 Host 看门狗永远先说话**（它的文案精确，Composer 的是泛化兜底），单测 T-06 把这条不变式钉死。原 `:541` 的 `'up to 45s'` 硬编码文案删除，改由纯函数按本次实际预算渲染，文案与行为不可能再脱节。**粘贴拦截规则**收窄为「有文件条目 **且** `text/plain` 为空」才 `preventDefault`——纯文本 / 富文本 / Explorer 复制带路径文本一律走原生插入，文字一个字不丢；paste 处理器**绝不写 `value`、绝不动 `setSelectionRange`**（IME 铁律：合成态下写 value 会打碎拼音缓冲区），已写进注释。`items` 优先、`files` 仅在取不到 File 时兜底，**绝不合并**（Chromium 下两者是同一批 File，合并必重复计入）。**8000 px 闸门用解图片文件头的纯函数**而非 `createImageBitmap`——把一块原本零回归保护的 IO 换成 12 条断言，且四种白名单格式解析覆盖完备无残留 gap。**零字节三重防线**（Host 侧 `index.ts:302-321` 要求 `data.length > 0`，任一项非法整条 send 被丢弃）：入列拒收 + 编码后兜底 + `toWireAttachments` 再过滤，且全空时返回 `undefined` 而非 `[]`，保证纯文本 payload 里**根本没有 `attachments` 键**。**三路对抗评审去重后 9 个独立问题全修**，最重的 1 blocker + 2 major 是同一个：`canRetry` 只要有附件草稿即为真 → **健康会话里粘一张图就冒出 Retry 按钮，点下去会带空文本重发并抹掉用户已输入的句子**；修法是加 `lastTurnFailed` 前置判据，并让 `retryDrafts` 直接取实时 chip 而非快照（否则失败后用户删掉的 chip 会被 Retry 复活）。**顺带修一处既有泄漏**：会话切换时清空草稿与 `retryable`——旧的 `retryablePrompt` 同样没有会话作用域，会话 A 的失败会让 Retry 出现在会话 B 上并用 A 的文本重发到 B。**防重复提交三处**：`inFlightRef` 同步闩（渲染闭包里的 `canSend` 加异步 `setSending` 挡不住同 tick 双击）、Enter 分支补 `composingRef` 守卫（中文输入法回车确认候选词曾会直接发送）、读取中 Send 禁用。**新增 106 例**。三绿：typecheck 干净 / lint **605 文件 0 诊断** / **49 文件 561 例**（T-03 后基线 45/455）。hash `703f981`。**未做**：GUI 点验（本环境不能起 Electron）——尤其纯文本粘贴回归、真剪贴板取图/取文件、jpeg/gif/webp 至今未过网关实测，须由能开 GUI 的一方补做并入账。 |
| 2026-07-27 | **给主线的需求（T-18 衍生，本轨不实现）** | ⬜ | **① 用户气泡回显附件元信息（中优先）**：`eventNormalizer.ts:132-154` 的 `beginTurn(userText)` 只 emit 一条 text delta，附件信息完全不进事件流——**用户发完图后时间线上只看到自己的文字，没有任何证据表明图真的发出去了**。请求让 `send()` 把附件元信息透给 `beginTurn`，emit 时附带 `attachments: Array<{kind, name?, mediaType, byteLength}>`（**只要元信息，不要 data**）。Renderer 侧无法自救（`chatSessions.ts` 与 `eventNormalizer.ts` 均在禁动清单）。当前只能靠「失败时保留 chip」作为临时替代证据。**② 看门狗对上传期的处理（中优先）**：`claudeRuntime.ts:83` 的 `DEFAULT_STALL_TIMEOUT_MS = 120_000`，而 `PRODUCTIVE_EVENT_TYPES` 在上传阶段一个都不触发——整个上传窗口被当成 stall 计时。本轮实测下余量充足，但这是**任何未来提高上限的硬天花板**。请求二选一：(a) `send()` 增可选 `stallTimeoutMs`；(b) 看门狗在首次 outbound flush 完成后才 arm（更根本，把「上传≠挂起」的误判语义修掉）。**③ 协议加 `document`(base64) 类型（低优先/可选）**：`SessionAttachment.kind` 只有 `image|text`，T-18 v1 显式拒收 PDF 并给文案（不阻塞）；SDK/API 本身支持 `{type:'document', source:{type:'base64', media_type:'application/pdf'}}`，Host 侧是纯加法。**④ 双发送路径漂移（提示，非请求）**：`chatSessions.ts` 的 `sendMessage(text, attachments?)` 已完整支持附件但**无人调用、无测试覆盖**，ChatComposer 走的是自己的 `runSend`（含 closeSession→createSession→send 编排 + 超时 + Retry 兜底）。记一笔避免以后误以为 store 路径被验证过。 |
| 2026-07-28 | **Linux 开发环境从零搭建：四处环境坑与修法（环境性，非代码回归）** | ℹ️ | 本机(Ubuntu 26.04)首次跑通 GUI 联调，动工前环境即不完整：**①** Electron 二进制缺失——`node node_modules/electron/install.js` 直连被 DNS 拦，需 `ELECTRON_GET_USE_PROXY=true GLOBAL_AGENT_HTTPS_PROXY=<proxy>`；**②** sqlite3/node-pty/@parcel/watcher 是 Node ABI 非 Electron ABI，主进程启动即崩「Could not locate the bindings file」——`npx electron-builder install-app-deps` 重建，但须覆盖 `~/.npmrc` 里失效的 `127.0.0.1:7890` 代理（`npm_config_https_proxy` 指到可用代理）；**③** `pnpm install` 会同时冲掉 ②的产物与 `@vscode/ripgrep/bin/rg`（postinstall 被 GitHub 403 挡）——**每次 install 后须重跑 ② + 从 `src/agent-host/node_modules/@cometix/claude-code/vendor/ripgrep/x64-linux/rg` 拷回 rg**，否则 app 起不来且 T-07 集成 6 例红；**④** Node 自身 `fetch`/`got` 不读代理 env，任何要下载的脚本（remote-runtime bundle 等）需 `NODE_USE_ENV_PROXY=1`。本机三绿口径沿用 07-27 行：3 例 Windows-only 失败为基线。联调命令已在 baseline 门禁文档改为平台中立（`pnpm prepare:test-config` 按机生成，不再硬编码作者机路径）。 |
| 2026-07-28 | **「cli.js exists but failed to launch」根因链闭合：demo 工作区硬编码开发机路径 + OpenChamber 壳无添加仓库 UI** | ✅ | 用户 GUI 首测即复现该错。**根因链**（多代理调查 + 对抗复核 CONFIRMED）：`SKIP_ONBOARDING_GATE=true` 硬强制 OpenChamber 壳且旧壳不可达 → 添加仓库的唯一 UI（AddRepositoryDialog 的全部入口）都在旧壳分支，LeftNav 的 Workspace 按钮是 disabled → 新机器上 `aiclient-repositories` 永远无人写入（leveldb 转储证实无此键）→ T-01 同步桥空树 early-return → `DEMO_WORKSPACES` 写死的 `D:/Code/projects/ai-client` 一路兜底成会话 cwd → SDK spawn ENOENT。**实证**：真 Host NDJSON 探针，`/home/dan/projects/ai-client` 全链 completed（约 9s），`D:/...` 则 session.failed 且错误串逐字一致；**顺带发现 Host 在 `session.create` 不校验 workspacePath 存在性（坏路径也 created+idle，到 send 才炸）→ 已列入给主线的需求**。**修复**：demo 路径改空串哨兵 + Composer/resumeIntent 拒空路径（`eea2f25` 尾段 + `0bd70d5`）；本机数据修复=清污染索引（6 条 D:/ 条目，备份 `.bak-d-path-pollution`）+ `--open-path` 注册真实仓库。**全仓机器绑定审计 34 项全部经对抗复核确认**：最重四项=本条 demo 路径、`fetch-node-runtime.mjs` 无平台守卫（非 Windows 机 `dist:prereq` 必失败）、`AgentInstaller` 无守卫 spawn powershell.exe、plantree/台账多处硬编码作者机 `C:\Users\13927` 路径与两枚明文网关 token（**token 在库里属既定测试凭证约定，暂不动，记录在案**）。**enso 溯源**：EnsoAI 是本仓原名（上游作者 j3n5en），品牌改名分三层提交完成；`dev.js` 的 `enso-remote-runtime-` 是最后一处行为性残留（缓存永不命中 → Linux 每次 dev 重下 48MB），已修（`576f3bd`，含 argv 透传使 `pnpm dev -- --open-path=<仓库>` 可用）；其余 enso 残留（package.json author"baseOnEnso"/README Telegram 徽章/userscript 类名等）核实全部为死标识符或文档，**无一影响运行时**；remote-runtime 归档名三端（生产脚本/消费方/CI+GitHub release 实物）核实一致为 `aiclient-`。 |
| 2026-07-28 | **多轮上下文丢失确认为 bug 并修复：每次发送 close→create wipe 掉 resume 身份** | ✅ | 用户 GUI 实测报「每条消息都是独立新对话、newapi 每条新建缓存」。**机制**（对抗复核 CONFIRMED）：`runSend` 每次发送 close→create；Host 的续接机制本身健全——`claudeRuntime.ts` 只要注册表条目带 `runtimeIdentity` 就传 `resume`——但 close 删掉唯一持有者，create 重建为空，故每轮都是全新一-shot 对话。**实证**：真 Host 探针，保活序列第二轮召回「47」，GUI 镜像序列（close→同 id 重建→send）每轮新 runtimeIdentity 且模型自述无记忆。**溯源**：close→create 是 `a01712a`（07-24）绕「Session already exists」的临时手段，台账当日即标 🟡 未闭环，此后五次 Composer 重构无人触碰；**所有 GUI 验收记录全是单轮操作，「第二条消息记得第一条」从未进过任何 happy path——规范第 3 条的教训**。**修复**（`eea2f25`）：纯函数 `decideSendPreamble` 三分支（hostBound→直发 / 有身份→resumeSession 再发 / 全新→原 create 流），direct 对 `session_not_found` 单次回退重建，失败路径统一 unbindHost；`hostBoundSessionIds` 不再在发送起点无条件清空。sendPreamble 5 例单测。**已知 edge（记录不修，待拍板）**：曾绑定过的会话若 Host 中途重启后走 resume 分支，`session.history` 重放会把 h:* 历史整段排在存活的 live 消息之前——同一段对话视觉上双份。 |
| 2026-07-28 | **Agent Host stderr 可观测性 + open-path 首启竞态修复** | ✅ | **①**（`da9a5da`）Host 子进程 stderr 与 error 事件此前均无监听：stderr 整体丢弃（本日 D:/ 故障在 main.log 零线索，只能写复现脚本反推），error 无监听会直接抛、可把主进程带崩。新增 `hostStderr.ts` 纯函数（分块拼行/截断/防无界，11 例），逐行 info + 环形缓冲 50 行，**进程 error 或非零退出时按 error 级 dump**——首版只打 info 被默认 error-only 日志级别整体过滤，等于白修，验证层级教训记档；`getBundledNodeRuntimePath` 限定 win32（随包 Node 链整体 win-x64 专属，其他平台原先永远收到不存在的 node.exe 候选）。**②**（`9331d51`）首启 `--open-path` 必丢事件：App 经 `lazy()` 动态导入，`did-finish-load` 先于 IPC 监听器注册数秒，推送落空（主进程内那条「已处理竞态」注释只治了主进程侧排序）；改拉取握手 `app:takePendingOpenPath`，渲染端挂好监听器后主动拉。**连带揪出两个隐蔽 bug**：`whenReady` 启动体未被 `gotTheLock` 拦——被拒的二次实例仍跑完整启动体，**实测会删掉运行中实例的 `/tmp/aiclient-input`（T-18 粘贴暂存）并争抢 leveldb 锁**；`useOpenPathListener` 的 `enabled` 写死 true——水合前收到事件会在空 `repositories` 闭包上 `saveRepositories([newRepo])` **覆盖已有仓库列表**，现以 `hydrated` 门挡住。诊断过程另录得「二次实例转发失败」为**误报**：Chromium localStorage 有约 5s 提交节流，leveldb 检查过早。 |
| 2026-07-28 | **T-04 网关侧结论：sonnet 空 thinking 文本；默认模型今日确定性 400（open-q #5 升级）** | ⬜ | 用户 GUI 实测无 thinking 卡。真 Host 探针（各 2/2 复现）：**①** GUI 默认 `sonnet`（cometix 解析为 claude-sonnet-5）在本网关走完全程但 **thinking 块文本为空**（不理会 `display:'summarized'`，返回 omitted 形态；CLI 历史 JSONL 里可见带签名的空 thinking 块=上游确实思考了）；Host 的 normalizer 按设计吞空 thinking（`emitThinkingDelta` 空文本 early-return + thinking.started 惰性触发），渲染端因此零事件——**渲染链逐门核查全开，无 bug**。**②** #8 当日验证过的网关默认模型（opus-4-8[1m]）**今日确定性 400「thinking 块格式无效」**（2/2，独立 session）——open-questions #5 那个「瞬态」在该路径上已不瞬态。**结论：T-04 卡在网关对 thinking 的处理（跨模型不一致且随时间漂移），非 app 缺陷**；#8 烟测矩阵只验了网关默认模型、没验 GUI 默认 sonnet，属验证矩阵缺口。**待用户**：查 newapi 渠道 thinking/透传配置；是否给「有签名但空文本的 thinking 块」渲染无文本指示（产品决策，入 open-questions）。 |
| 2026-07-28 | **给主线的需求（今日衍生，本轨不实现）** | ⬜ | **①** `session.create` 校验 workspacePath 存在性（目前坏路径 created+idle，到 send 才以泛化错误炸，UI 无从给出准确文案）；**②** resume 重放与存活 live 时间线的去重/编时序合并（见上文多轮修复行的已知 edge）；**③** thinking 空块指示（依赖用户拍板后再议归属）。 |
| 2026-07-29 | **「不出窗口」故障闭环：兜底 show 复核通过、**原诊断根因判错并更正**、连带两处补修** | ✅ | 用户交来同事的修复请复核并拉起。**① 修复有效**：`MainWindow.ts` 给 `win.show()` 加 `did-finish-load` + 5s `setTimeout` 两条兜底（三路共用幂等 `showWindow()`，`hasShown` 去重 + `isDestroyed()` 守卫 + `closed` 清 timer）。插桩实测 `[diag] show() called via did-finish-load` —— **`ready-to-show` 在本机从不触发**，本机实际生效的就是这条兜底。**② 但 `docs/design/BUG-2026-07-29-no-window.md` 原第二节结论是错的**，已重写：原报告用 `ls -l /proc/$P/fd \| grep wayland` 判定「窗口从未被创建、卡在 `openLocalWindow()`（`index.ts:735`）之前」——**方法不成立**，Unix socket 在 `/proc/<pid>/fd/` 的 symlink 目标恒为 `socket:[<inode>]` 不含路径，grep 不到是必然。按 inode 反查 `ss -x` 实得 `2 × /run/user/1000/wayland-0`，**窗口一直是建好的**；更省事的判据是 `pgrep -af "type=renderer"` 有输出即已构造 BrowserWindow。真根因=无 3D（`VMware: No 3D enabled` + `cc/raster/one_copy_raster_buffer_provider.cc:299 Creation of StagingBuffer's SharedImage failed`）导致首帧不提交，`ready-to-show` 不来。最小 Electron 复现脚本同报 GPU 错但窗口正常显示，**GPU 不阻止建窗**，只断 `ready-to-show`。原报告点名的 `autoStartHapi()` 亦排除（`hapiSettings.enabled` 已为 false）。**③ 连带修既有 bug（非本轮引入）**：`MainWindow.ts:120` 的 `replacementState` 三元在 `replaceWindow` 为 null 时走 else 分支得到 `{isMaximized: undefined}` —— **真值对象**，`?? loadWindowState()` 永不回落 ⇒ `width/height/x/y` 全 `undefined`，**窗口状态从来没被恢复过**（diag 里的 `state={}` 即此）。改为先判 `replaceWindow && !isDestroyed()`。**④ 兜底日志改 `console.error`**：原用 `console.warn`，被 `initLogger(loggingEnabled=false)`（默认）静音，唯一能证明「兜底生效」的日志看不见——插桩一律 `console.error` 这条已写进报告第五节。**⑤ 顺带修 `McpSection.tsx:136`** 折叠按钮内嵌 `<Button>`（`<button>` 套 `<button>`，React hydration error，GUI 日志 `02:26:05` 实录），改为兄弟节点 + `min-w-0 flex-1 truncate` / `shrink-0`；全仓扫描无第二例。`d68d3c6` · 门禁：typecheck 干净 / lint **615 文件 0 诊断** / vitest **54 文件 618 例**（3 失败=既有 Windows-only 基线：`CliDetector` 1 例 + `ShellDetector` 2 例，与 07-28 口径一致，例数未增=本轮未补测试） |
| 2026-07-29 | **dev 态凭证隔离：`dev.env` 启动期注入，禁止回落开发者本机登录** | ✅ | **暴露过程**：复核完窗口后用裸 `node scripts/dev.js` 拉起，用户追问「不会是直接用我本地登录的凭证吧」。**确实是**——`claudeSettings.ts:29` 为 `process.env.CLAUDE_CONFIG_DIR ?? ~/.claude`，未设该变量即回落；而 `~/.claude/settings.json` **没有 `env` 段**，于是走 `~/.claude/.credentials.json` 的 OAuth 登录直连 api.anthropic.com，**计到开发者本人账号**。偏离执行计划 §4「测试凭证统一约定」（应经 `CLAUDE_CONFIG_DIR` 指向 seed 目录）。**未产生实际请求**，三条证据：应用日志 `aiclient-2026-07-29.log` 全 46 行只有窗口/渲染项、无 session 或 agent host 活动；`/tmp/aiclient-gui-test-config` 从未被 seed 过；同期唯一被写的 `.jsonl` 是 `version 2.1.212` + `skill_listing`/`queue-operation` 的 Claude Code CLI 会话，非 app 的 Host。**修法（用户口径：不改程序代码，只要启动期注入 + 一个可随时改的文件）**：改动**仅在 `scripts/dev.js`**，读仓库根 `dev.env`（gitignore，模板 `dev.env.example`），启动时三步——**剥离**继承自 shell 的全部 `ANTHROPIC_*` 外加 `CLAUDE_CODE_OAUTH_TOKEN`/`CLAUDE_CONFIG_DIR`/bedrock/vertex 七项；**注入**仅 `dev.env` 声明的变量（`ANTHROPIC_AUTH_TOKEN` 存在时删 `ANTHROPIC_API_KEY`，避免陈旧 key 在部分 CLI 路径胜出）；**隔离** `CLAUDE_CONFIG_DIR` 指向生成的 `node_modules/.cache/aiclient-dev-credentials/`（seed `settings.json` + `.claude.json` 预信任，`dev.env` 显式指定则尊重）。**缺文件直接 `exit(1)` 拒绝启动**并给出 `cp dev.env.example dev.env` 修法，逃生开关 `--allow-local-credentials`（dev.js 自己消费、不透传给 Electron）。**投毒实测**：`ANTHROPIC_API_KEY=leaked-from-shell ANTHROPIC_BASE_URL=https://should-be-stripped.example node scripts/dev.js` → 输出 `stripped from shell: ANTHROPIC_API_KEY, ANTHROPIC_BASE_URL`，主进程 `/proc/<pid>/environ` 只剩 `dev.env` 的值；`dev.js → electron-vite → Electron → AgentHostProcess({...process.env})` 全链验证。**副作用（正向）**：应用自动写的三个 hook（`aiclient-hook.cjs`）现在落进隔离目录，不再污染真实 `~/.claude/settings.json`。`b18ccac` · 门禁同上行。**已知缺口见 open-q #14**（打包版 / preview 不经 dev.js；`dev.env` 解析无自动化断言） |

| 2026-07-29 | **对话缓存全量重写排查闭环：主因网关无会话亲和（app 无可修）+ 次因 resume 丢 model/effort（已修）** | ✅ | 用户面板实测:同会话两条消息全是 1h 缓存写(42,435 → 42,466)、读恒为 0,每条多花 ≈¥0.25 且随上下文线性恶化。**排查路径 = 先对照实验二分、再拦截收尾**:① 绕开 app 直连网关的对照实验(三轮 10 请求):字节级相同重发 **4/4 命中**、前缀增长(多轮形态)**2/4 命中 2/4 全量重写**——同样的客户端字节命中与否不确定,唯一自洽解释是 **ccmax 分组多上游账号轮询、缓存按账号隔离**(new-api 已知问题,issue #5992「API Key 级会话亲和」);直连官方 API 不可能出现此行为。**T-04 的 thinking 空文本时有时无/某日确定性 400(open-q #5/#8)与此同根因家族**。② 拦截取证(capture-proxy 挂 `AICLIENT_TEST_BASE_URL`,cli.js 每 turn 重 spawn 故必须拦这一跳):**显式传模型时(=GUI 形态)两轮请求 tools/system/messages[0]/beta 头逐字节相同**——客户端前缀完全可缓存,读 0 纯属网关路由;**不传模型时** fresh 轮带 `context-1m` beta + Bash 工具描述落款 `(1M context)`,resume 轮双双消失,tools 前缀字节变化 = 100% 全量重写。③ 由此坐实 Host `session.resume` 丢 `model`/`effort` 的真 bug(Explore 委派发现):Host 重启后续聊**静默把用户选的 sonnet 换成 cli 默认 opus 系**并触发上述翻转。**修复 `3622c19`**:Renderer→IPC→preload→协议→Host 逐层补可选字段(纯加法不 bump 协议版本,沿 T-20 先例),`claudeRuntimeOptions.test.ts` +3 例钉死 resume→query() 的 model/effort/resume 下发。三绿:typecheck 干净 / lint 615 文件 0 诊断 / vitest **54 文件 621 例**(+3,同 3 例 Windows-only 基线)。**产物**:故障档案 [`docs/design/BUG-2026-07-29-prompt-cache-rewrite.md`](../design/BUG-2026-07-29-prompt-cache-rewrite.md)、探针入库 `spikes/cache-affinity-probe.mjs` + `spikes/capture-proxy.mjs`、open-questions **#15**(网关亲和,待用户找运营方)。**待用户**:向网关运营方要「渠道亲和 + 渠道内 Key 亲和」或单上游令牌;Host 重启后 resume 保模型一项待 GUI 点验。 |

| 2026-07-29 | **侧栏两层化 + Composer 目标栏三下拉:双独立方案合并落库 + T-24 账实不符发现** | ⬜ | 用户拍板方向(openchamber 式侧栏「文件夹→对话→subagent」+ Cursor 式输入框顶部三下拉,截图证据:文件夹下拉含 Use Existing…/New Folder、分支下拉、运行位置 This PC/Cloud/Remote)。按工作模式对高风险架构题**并行双盲设计**:deep-reasoner(Opus)与 Codex 各自独立读库取证(各自核实 openchamber 冻结版 `a3519141`)后产出,主线合并。**全部核心裁定独立收敛**:① 侧栏三层降两层,Workspace 从树层级降为会话「运行目标属性」(openchamber 同款且是刻意取舍,`sidebar/DOCUMENTATION.md:6,13`);② 分支下拉 = worktree 选择器 + 新建 worktree,**禁止 in-place `git checkout`**(git 禁同分支双 checkout / 运行中 agent cwd 下换地板 / 与产品定位冲突,三条理由双方独立给出);③ New Worktree 归分支下拉(openchamber 同款);④ subagent 后置——架构性理由:openchamber 第三层是后端送的(带 `parentID` 的普通 Session),我们 Host 协议无子会话实体,要先造数据;解冻双路径已探明(Path B 读磁盘 `subagents/**` 先行,join key `toolUseId ↔ toolCallId` 已就绪;⚠️ 工具名是 `Agent` 不是 `Task`)。**重大发现(过程缺口)**:**T-24 实体已随 `b38017b`(名义 T-21)落库**——LeftNav 三处入口/拖放 ref 绑新壳/`addRepositoryEntry.ts`+`fileDragDrop.ts` 及测试俱全,提交信息零次提及,plantree 四处仍标「阻断级未开工」,违反工程规范第 15 条;**裁定不重定义 T-24**(剩余=GUI 实测+补登,文件夹下拉归拟立项的 T-27),执行计划 §3 T-24 行已加更正段。呈现层四点分歧(worktree 分组默认/运行位置形态/Recent 段去留/观感基线缺口 A07)升 open-q **#16** 待拍板;subagent 数据源升 **#17**。产物:[`docs/plans/2026-07-29-sidebar-composer-target-bar-design.md`](./2026-07-29-sidebar-composer-target-bar-design.md)(拟 D21/D22 决策文本 + T-26/T-27 任务草案 + ARD §4:88-95 改写稿 + 实施切分 S0~S3 与流程断言)。**待用户**:#16 四点拍板→D21/D22 进总台账、T-26/T-27 进任务表。 |

| 2026-07-29 | **D21~D23 拍板落库:侧栏 flat 两层化 / Composer 目标栏 / 中列 Cursor 化 + A07 基线与 T-26~T-28 立项** | ✅ | 用户对 open-q #16 四点逐条裁定(当日收口):**A** = `flat` 平铺 + 分支 chip(`by-worktree` 分组带被否),视觉参照用户提供的 Cursor 侧栏截图;**B** = 运行位置只读指示器(「以后开远程功能再加上」下拉);**C** = Recent 段保留(用户保留否决权);**D 超出原二选一**——用户明确「中部对话栏请对齐 Cursor,不要做成现在这种底部的聊天软件风格」,并补两张中列状态截图 → 演化为 **D23**(中列两态:空态 Composer 垂直居中+目标栏在上/会话态时间线+Composer 沉底+目标行在下;工具行/问答卡仍按 T-05 openchamber 口径;Tab 栏 A07 出带/不带两版验收时定)。**落库**:总台账追加 **D21/D22/D23**;执行计划 §3 追加 **T-26**(侧栏两层化 ≈1d)/ **A07**(中列 Cursor 观感基线 0.5~1d,用户验收后 T-27/T-28 方可施工——延续 Phase 0A「先基线后施工」教训)/ **T-27**(目标栏 ≈1.5d)/ **T-28**(中列状态化布局,待估)+ 2026-07-29 顺序修订段(现行全序 T-24 收尾 → T-26 → T-22 → T-27 → T-28 → T-05 → …);六张 Cursor 参照截图固化入库 `docs/design/refs/cursor-20260729/`(共享文件夹属易失位置);plantree 三文件与设计文档 §9 拍板结果同步。**遗留**:ARD §4:88-95 数据层级文字块的连带改写(D21 落地时随 T-26 一并做,设计文档 §6 有改写稿)。 |

| 2026-07-29 | **A07 中列 Cursor 观感基线产出(v3 用户正式定稿)** | ✅ | 产物 [`docs/design/a07-cursor-composer-alignment.html`](../design/a07-cursor-composer-alignment.html)(2236 行单文件自含,亮暗一键切换)。四屏:① 空态(Composer 卡居中偏上+目标栏在上,三下拉可点开)② 会话态(时间线+follow-up 沉底+目标行在下,工具行按 T-05 口径可展开)③ 会话态+Tab 栏变体(待拍板,制作方倾向**不带**——与 D21 侧栏会话入口职责重叠,且 T-22 surface 化后语义会变)④ 三下拉解剖(含「Cursor 有而我们不做」清单)。**工艺纪律**:token 块与 `globals.css` 机器校验逐字一致(合理去除 panel-opacity 包装);产品 mock 零裸 hex 全走 `var(--…)`(290 处),散落 hex 仅基线文档自身注释用色;新增视觉值仅两处且在 §01 派生表列明出处(下拉宽 280=Sidebar 默认宽、覆盖层阴影)。三处关键取舍:卡片浮起用 `--input` 强边替代 Cursor 阴影(合 design-system「浮起优先 border+bg-card」)、分组标题不缩字号靠 `--muted-foreground`+字距拉层级(等宽 UI 下缩字号破坏纵向节拍)、follow-up 圆角收 `--radius-md` 12 / 发送键收 `--h-row` 28。主线抽检:结构四屏齐全、token 抽样一致、hex 归属核实。**待用户**:浏览器打开验收,重点看屏③ Tab 栏(带/不带)与屏④ 运行位置列(只读 chip,与 Cursor 偏离最大);§06 另有发送键改图标键(aria-label 承担文字)等逐条确认项。验收通过后 T-27/T-28 方可施工。**v2 追记(同日,D24 后)**:屏②③工具行改判为 Cursor 动词灰阶单行;新增屏⑤工具行五态、屏⑥问答卡四态(Questions 折叠条/A-D 字母行/Answers 冻结/Skipped);§01 补 `--tool-arg` 二级灰与 `--h-chip` 派生;Continue 按钮抽检确认走 `var(--primary)` 非 Cursor 琥珀 hex;新散落 hex 复核全部为基线文档自身 `--page-*` 注释用色(v1 抽检只截了前 8 个,本次全量核)。**v2 新增唯一待拍板:屏⑤ E 组——工具行展开是否保留输出体**(Cursor 不显示工具输出正文,我们 T-05 原口径有滚动窗输出体)。**v3 追记(同日,用户五条裁定 + 两条追加全部落定)**:§08 五件事改写为已裁定——⑤ 形态照 Cursor+保留可展开输出体(E 组,默认收起、失败行与显式点开才展示)/ ① Tab 栏先不带(屏③标「变体存档·不采用」)/ ② 会话态目标行不显文件夹 / ③ 发送键圆形图标+aria-label / ④ 空态不放最近会话入口;**工具行交互口径**(用户实测 Cursor 补充,截图 `工具行-悬浮命中文件列表.png` 入库):Read 行文件名可点击打开文件、Grep/Search 行悬浮出命中文件浮层(可点击,复用 popover 族;用户许可降级=先用点击展开明细),屏⑤新增 F 组样张;**D21-A 微调**:所有会话行显分支 chip(含 main/master,chip 封顶 112px),屏①②样张同步。制作方自跑 lint 0 诊断;主线复检通过。**已知代价(制作方标出)**:chip 常驻后长分支名(如 feat/openchamber-chat-refactor)在侧栏会截断——屏①有真实样张,验收时留意。**2026-07-29 用户回复「没问题,正式定稿」——A07 定稿生效**,T-27/T-28 观感前置解除;T-05 验收标准已按定稿重写(执行计划 §3 T-05 行,①~⑧ 现行权威)。 |

| 2026-07-29 | **T-26 侧栏两层化落地(D21/D21-A),`dd23b01`** | ✅ | 按「纯函数断言先行 → LeftNav 改造 → 三绿」推进,用户发令「按计划开干」。**代码落库 `dd23b01`(9 文件 +839/-272);ARD §4 数据层级文字块已按 D21 连带改写(原三层结构原文存档于同节)**。**S1(主线亲做)**:新建 `sidebarTree.ts` 纯函数(照 `addRepositoryEntry.ts` 范式)——`buildSidebarFolders`(文件夹→会话两层,**按 workspace.projectId 归组防陈旧 session.projectId 造幻影文件夹**)、`chipForWorkspace`(全量分支 chip 含 main/master;temp/remote 显 kind;**分支未知不猜名不显 chip**)、`deriveRecentRows`(busy 或 48h,7 条+Show more)、`formatRelativeAge`(Cursor 式紧凑档 now/m/h/d/w/mo/y)、`resolveNewSessionWorkspaceId`(main 优先且跳过空路径 seed);**数据缺口补齐**:`ChatWorkspace.branch?`(红线 store 纯可选加法,沿 T-20 先例)+ `deriveChatWorkspaceTree` 从 `git worktree list` 带出 main 工作区实际分支名。13 例新单测(跨 worktree 归并/孤儿不崩不造文件夹/搜索只过滤标题/chip 规则/Recent 边界/相对时间档)。**S2(fast-worker)**:LeftNav 拆 `WorkspaceBranch` 树层、移除 `selectedWorkspaceId`(全仓零残留,grep 证)、会话行改 A07 权威解剖 `[运行点6px] 标题 chip(封顶112px=max-w-28) 时间⇄悬停操作`、失败态保留 destructive Badge、Repositories 段头筛选(toggle 搜索框)/添加图标、Recent 可折叠(持久化 `SIDEBAR_RECENT_COLLAPSED`)、空文件夹 `+ 新建对话` 行、文件夹行黄色 Folder 图标(`--folder` token)。**S3 对抗复核(deep-reasoner;Codex 容量满载改派,见委派记录)**:1 blocker + 3 should-fix + 4 nit + 4 info,九项当场修——**blocker:sync 桥变更签名缺 `branch`,冷启动(无 linked worktree 仓库)前后两棵树签名逐字节相同 → effect 早退 → chip 整个 app 生命周期不出现**(修:提取 `workspaceTreeSignature` 纯函数入签名 + 回归测试钉死);重命名草稿 mount 期陈旧会静默回滚(改进入编辑时播种);有会话仓库无法新开会话(文件夹头悬停 `+`,**兄弟按钮避嵌套 button**——d68d3c6 McpSection 同坑);60s 时钟 tick(空闲 app 时间列冻结/48h 窗不过期);键盘可达性 `group-focus-within`(display:none 掉出 tab 序);段头/占位文案全走 `t()`(撞出词典既有 `Recent`/`Repositories` 键,撤重复);搜索空墙加「无匹配会话」+ 搜索时隐藏零命中文件夹;`aria-pressed`;显式 `undefined` 键清理(条件展开)。**复核确认干净面**:48h 含边/时钟偏移/cap 算术/key 无碰撞/resume 语义不变/无外部依赖残留。**缓办(记档)**:`recentSessionIds`+`touchLiveUpdatedAt` 成死代码(最后读者已移除,动红线 store 归后续清理任务);Recent 双谓词分歧实测不可达(sidebarTree 注释已记);活跃≠busy 的 48h 口径按设计。三绿:typecheck 干净 / lint **620 文件 0 诊断** / vitest **55 文件 635 例**(+1 文件 +14 例,同 3 例 Windows-only 基线)。**范围外(明示未做)**:A07 样张的侧栏顶部 New Agent/Search 行与底部账户区(执行计划 T-26 未列,归 T-28/后续);subagent 层(open-q #17)。**待用户 GUI 点验**:① 会话行 chip 显实际分支名(含 main);② 空文件夹可新建对话;③ Recent 折叠重启保持;④ 文件夹头悬停 `+`。 |
| 2026-07-29 | **T-22 壳结构改造落地(D19),`95a5c04`** | ✅ | 按「三路并读(Workflow) → deep-reasoner 规格 → 四批施工(断言先行) → Codex 对抗复核 → 修复」推进。**代码落库 `95a5c04`(23 文件 +2368/-456)**。四区壳改**三列 + 44px 导轨 + surface 模型**:Sidebar 280 默认可拖 280–500(宽度所有权上移 Shell 包裹层,LeftNav 仅改一行 `w-full`,T-26 形态经复核无回退);中列阅读栏 `min(100%,48rem)` 居中/宽模式 64rem(`max-w-3xl/5xl` 标准类零自造值;MainHeader LayoutGrid 死按钮接线为宽模式开关——顺带消化一枚 A06 违规;`chat/ReadingColumn.tsx` 为 chat 侧唯一读布局 store 的接缝,chat 不反向 import workspace-shell,T-28 现成改造点);ContextPanel 380–1400 按 surface 记忆宽度(manual 优先,注册表 fraction × ResizeObserver 测量宽,未测量兜底 600),**常驻挂载**(关=宽0+`inert`,内容跨开合保挂载为 T-15 终端铺路),可**提升覆盖 Main 全视图**(expanded 右锚 absolute,px 宽度 250ms 动画)并还原,Escape 面板作用域关闭;Rail 44px(`RAIL_WIDTH` 单一权威)图标单选互斥、再点收起,**git 圆点接真实数据**(`useGitChangeCount`,isActive=false 无轮询,失败静默 0 不造假)。**surface 注册表 11 种**(rail 可见 context/git/editor/terminal 四枚;chat has-content 隐藏;其余 6 种 registeredOnly 留注册位),未接入 surface 走 `SurfacePlaceholder` 诚实空态(明写 T-1x 接线;T-12~15 扩展点=`surfaceViews.tsx` 查表各加一行互不冲突);**删除 BottomDock.tsx / RightDock.tsx**(验收⑤ `rg src/ scripts/` 零命中)。纯函数层 `shellLayoutModel.ts`(尺寸常量/夹取/宽度解析/`reduceShellSurface` 状态机/持久化消毒)+ `surfaceRegistry.ts`(注册表/可见性/圆点判据),**+79 例新单测**;store `stores/shellLayout.ts` 薄壳全委派纯函数(persist localStorage `aiclient-shell-layout`,merge/migrate 双路径同一消毒,不可能行为分叉)。**对抗复核(Codex 恢复同行位)**:1 blocker **驳回**(「docs 里仍有 BottomDock 字样」——验收⑤口径=代码零引用,台账/ARD 系 append-only 历史叙述不改,module-map 现状描述随本轮更新);7 major **采纳 4**(拖拽卸载不复位父级 data-resizing 致过渡永久禁用/pointer 会话未绑 pointerId 不拒右键多触点/动画期抓把手基线跳宽→改取 DOM 实宽/关闭动画期内层列瞬缩 380→保最后打开宽)**驳回 3**(MainHeader 死按钮+72% 假环+LeftNav Menu/Help=存量违规已归 T-23/T-27,Codex 亦确认非本轮引入);minor 采纳 1(RAIL_WIDTH 无消费点→Rail 改 inline style);另补 `clampContextPanelWidth` 非有限数防护(回落 600,+2 例)。复核确认干净面:reducer/persist 双路径一致、resolve 各分支、RO StrictMode、LeftNav 对照 dd23b01 无回退、ReadingColumn 不破坏滚动/mention 定位、useGitChangeCount 无 interval 轮询。**设计期主线驳回 1 处**:PanelRight 无历史时 no-op 违 A06→改「恢复上次 ?? 首个 always surface」(reducer 有对应用例)。**账实出入更正**:design-system.md 头注「Typography 调用点迁移归 T-22」与执行计划任务定义不符,已就地更正(T-22 仅尺寸段落+新壳按四档写,全仓调用点迁移未立项待认领)。三绿:typecheck 干净 / lint **631 文件 0 诊断** / vitest **57 文件 714 例**(+2 文件 +79 例,同 3 例 Windows-only 基线)。**范围外(明示未做)**:Rail 拖拽排序(store `setRailOrder` 已预留)/把手双击复位/多标签驻留(ARD §4 MVP 简化)/T-12~15 surface 真实内容。**待用户 GUI 点验**:① 三列拖拽边界(Sidebar 280–500 / Panel 380–1400 / Rail 恒 44);② Rail 四枚图标单选切换、再点同枚收起、PanelRight 关→开恢复上次 surface;③ 提升覆盖 Main 并还原(不遮 Sidebar/Rail);④ LayoutGrid 切 48/64rem 且时间线与 Composer 同栏宽;⑤ 脏仓库 git 图标亮 6px 圆点、`git stash` 后熄灭;⑥ 重启后布局记忆(宽度/折叠/surface/宽模式)。 |
| 2026-07-29 | **T-27 Composer 目标栏落地(D22),`e8fb36a`** | ✅ | 同 T-22 流程(两路并读 → deep-reasoner 规格 515 行 → 三批施工断言先行 → Codex 对抗复核 → 修复批)。**代码落库 `e8fb36a`(27 文件 +2790/-74)**。Composer 卡上方一行三控件(A07 屏①档位:h-6 触发/弹层 280/行 h-7):**文件夹下拉**(搜索 + Recents 前 5(session-index 派生)+ 本机/远程分组 + footer 四动作——Use Existing/Clone/Add Remote 走共享 AddRepositoryDialog 对应 tab(新增 `initialMode` prop)、New Folder 建临时工作区(复用 tempWorkspace IPC));**分支下拉 = worktree 选择器**(main 置顶 + Recent + 每行路径二级文本;搜索无匹配 → 「新建 worktree「query」…」动作行 → 受控 CreateWorktreeDialog(新增 `initialBranchName` prop)预填;**禁 in-place checkout**(守卫测试钉死)、`getBranches` 仅对话框打开时调(enabled 门,守卫做过**真实红绿验证**));**运行位置只读指示器**(缺数据整块隐藏,含 remote 无连接名分支)。**三档规则纯函数** `composerTarget.ts`(空且未曾绑定→retarget / 有消息或曾绑定→fork / 运行中→blocked)+ store 动作 `chatSessionActions.ts`(retargetChatSession 最小写入;createChatSessionOnWorkspace 自 workspace-shell 迁入解 import 方向)+ `forkDraftCarry.ts`(fork 携带附件草稿);ChatComposer 发送门全链统一 `resolveActiveTarget.cwd`;pending target 携带**源会话** id、由 workspaces 变化驱动应用(防 `rebindSessionsToTree` 顶掉,blocked 保留待 5s 兜底);**`ChatWorkspace.gitEnabled?` 纯可选加法**门控分支下拉(签名同步含回归测试——T-26 同坑防复发);删 MainHeader Folder / Host: Local 两死按钮(能力迁入目标栏,T-23 对应子项转「删除+复核」)。**+70 例新单测(总 784)**,断言四层:纯函数表驱动 / store 动作 / 组合断言(retarget·fork 后 `resolveSendCwd===新 path` 且 preamble 必 create)/ 源码扫描守卫(chat 树禁 checkout/禁 import workspace-shell/getBranches 唯一)。**对抗复核(Codex):5 blocker + 3 major 全部采纳、零驳回**——关键四条:① runtimeIdentity 未入 fork 判据(恢复会话 messageCount=0 被误 retarget → 首发 resume 把旧对话恢复进新 cwd,破坏验收②)→ everHostBound 合成;② pending 只存 path 丢源会话(rebind 后对错误会话应用三档)→ 携 sourceSessionId 现读 store 出 plan;③ 发送门仍用原始 path(三空格路径可发送)→ 统一 cwd;④ 分支下拉缺 isGitRepo 前置(非 git 文件夹显假下拉)→ gitEnabled 门。**设计期误判更正 2 处入档**:规格 §2.1「remote 找不到连接名回落 Remote」句被验收⑤(缺数据隐藏)作废;A07 偏离两处按规登记——下拉勾选**左置**(Base UI ItemIndicator 原生位,组件优先纪律)、不显 `detached HEAD @ sha`(需红线 store 加 head 字段+签名同步,成本大于收益)。批 3 亮点:抓出 LeftNav 四处 `onClick={onAddRepository}` 直绑会把 MouseEvent 当 mode 传入的真实坑(App 侧 typeof 防御)。三绿:typecheck 干净 / lint **643 文件 0 诊断** / vitest **61 文件 784 例**(+4 文件 +70 例,同 3 例 Windows-only 基线)。**范围外(明示未做)**:中列两态布局(T-28);物化已有分支为 worktree(记 ideas,现走「新建同名分支」、已有分支会如实报 already exists);Recents 依赖 LeftNav 挂载的 useSessionIndex 水合(代码注释已点名)。**待用户 GUI 点验**:① 有目标时 Composer 卡上方出现三控件、无目标时整行隐藏;② 文件夹下拉切目标 → statusHint 的 cwd 跟着变;③ 有消息会话切目标 → 提示将新建对话(fork)且输入框文字与附件都保留;④ 运行中(发送/等待)三控件禁用带说明;⑤ 分支下拉仅 git 仓库出现(普通文件夹不出现),每行右侧显 worktree 路径;⑥ New worktree 建完**自动切换**到新 worktree(重点盯);⑦ Use Existing/Clone/Add Remote 各打开对话框对应 tab、New Folder 建临时工作区并自动切换;⑧ MainHeader 的 Folder / Host: Local 按钮已消失;⑨ 本机仓库运行位置显 This PC、悬停有只读说明。 |
| 2026-07-29 | **T-28 中列状态化布局落地(D23),`4c1e4d7`** | ✅ | 同流程推进(两路并读 → deep-reasoner 规格 → 三批施工断言先行 → Codex 对抗复核 → 主线亲修)。**代码落库 `4c1e4d7`(8 文件 +1215/-319)**。中列两态:**空态** = Composer 卡垂直居中偏上(`pb-[9%]`,A07 §01 派生档、全仓唯一允许任意值)+ 目标栏三槽在卡上方 8px,卡下留白无最近会话入口(§08④);**会话态** = 时间线在上(**h-9「Session status」头行删除**,A07 屏②查证无头行,状态信息 Composer statusLine 与时间线 failed 块仍达)+ **40px 单行 follow-up** 沉底 + 目标行(分支+运行位置,无文件夹,§08②)移卡下 8px;两态同轴 `min(100%,48rem)`、首发同帧翻态。**两态判定纯函数** `middleColumnLayout.ts`(七规则短路 + 13 项导出,全部 class 经纯模块、组件零字面量,**+49 例**):关键两防——恢复态会话靠 `runtimeIdentity` 直接判 session(桶长为 0 时不闪居中卡帧);`sendAttempted` 粘滞闩(store 无乐观用户消息,回车即翻态不等首条回显)。**ChatComposer 单一稳定 JSX 槽位只换 class**(全仓 `<ChatComposer` 恰 1 处,防两态分支卸载在飞的 runSend 致二次发送——设计期 R4 红线,复核确认守住)。28px 圆形发送键 `ComposerRoundButton`(send=primary / stop=destructive 原位换色不换形 / retry=outline 仅失败现身,Send 文字改 aria-label);外层 `border-t bg-background/80` 删除;mention 弹窗空态向下开(解 240px 向上裁剪,零新视觉值);textarea `field-sizing-content` 原生长高、会话态封顶 56px 内滚。**对抗复核(Codex):1 blocker + 1 minor,全采纳主线亲修**——blocker:follow-up 静息高实为 **42px 非 40px**(设计自证 6+28+6=40 漏算 2px 边框;测试只断言 class 存在没算合成高,故 49 例全绿也没接住)→ 改 `min-h-10 + py-1` 标准档精确 40px + 测试改断高度契约;minor:`sendAttempts` 闩只增不减 → 随 sessions 变化裁剪。复核确认干净面:七规则顺序/闩会话隔离(fork 新会话正确回空态)/单实例/`[&_textarea]:` 穿透(读 ui/textarea.tsx 证)/runSend·附件·mention·IME 守卫零漂移/T-27 能力全保留(pending 流不受 bar 移位影响)/48·64rem 与 D19 骨架不回退/A06(`+` 零出现、blocked、statusLine 静息隐藏后信息可达)。**A07 偏离入档 2 条**:①「+」附件选择钮不实现(本仓无附件选择能力,T-18 仅粘贴,落死按钮违 A06——A07 自身「麦克风/Connect Your Repos 不搬」同理);② R1 残留:无 runtimeIdentity 的老 session-index 条目恢复瞬间理论上闪一帧居中卡(规则 2/5 同 tick 兜底,最多一帧,接受;`restoredFromIndex` 显式标记记 ideas)。三绿:typecheck 干净 / lint **646 文件 0 诊断** / vitest **62 文件 833 例**(+1 文件 +49 例,同 3 例 Windows-only 基线)。**范围外(明示未做)**:屏③ 会话 Tab 栏(A07 裁定不采用、变体存档);工具行/问答卡形态(T-05 屏⑤⑥);消息气泡(A01);waiting_question 专属状态文字(头行删除连带,R7 接受,需要时另立小任务)。**待用户 GUI 点验**:① 首开(空 Live 会话)Composer 卡居中偏上、无时间线、卡下留白;② 回车发首条 → 同帧沉底为 40px follow-up、横向左边界不动、焦点与草稿不丢;③ 关掉重开(恢复历史会话)不闪居中卡帧;④ 会话态目标行在卡下且只剩分支+运行位置;⑤ 圆形发送键、运行中原位变红色停止圆、失败时左侧出现重试圆;⑥ 空态 @ 文件弹窗向下开不被裁;⑦ follow-up 打多行字自动长高到 56px 后内滚;⑧「Session status」头行与输入区上方横线消失。 |
| 2026-07-30 | **T-05 工具行/问答卡重做落地(D24),`340a59a`** | ✅ | 同流程(两路并读 → deep-reasoner 规格 980 行 → 四批施工断言先行 → Codex 对抗复核 → 修复批)。**代码落库 `340a59a`(29 文件 +3812/-173)**。**工具行 Cursor 形态**(A07 屏⑤):动词开头无图标无边框三级灰单行(`--foreground`＞动词 `--muted-foreground`＞参数 **`--tool-arg` 新 token**(color-mix 62%),另落 `--text-code/--text-markdown` 两档进 @theme);**已完成 explore 前缀**聚合「Explored N files, M searches」可展开(running 调用独立行不入聚合、任一子调用失败聚合行 destructive 且默认展开——两条均为复核修正);明细缩进 0 无竖线同节拍;`Thought briefly / for Ns` 折叠行(thinking 计时事件折叠注册表,5s 阈值,无时长不硬造);`Worked for Ns` 回合行置顶吃既有 latencyMs(meta 行 omitLatency 去时长);输出体默认收起、失败自动展示,滚动窗**输入 240px(复核补齐)/bash 46vh/通用 60vh** 三档。**assistant 消息去气泡去 role label**(A07 :1725/:858 证据;user/system/error 逐字不动——复核确认零可达回归)。**交互(F 组)**:Read 行文件名静默可点(常态零色差悬停 --primary+下划线;`stores/fileOpenIntent.ts` 记意图 + `openSurface('editor')` 诚实空态,T-13 消费即通;不复用 legacy navigation);Grep/Glob 悬浮 **560×288 命中浮层**(`toolHits.ts` best-effort 解析——复核修正无尾换行完整命中误删与根级文件误判两处;解析不可信**逐行自动降级**点击展开,A07 :2460 降级条款的最小损失面用法);Read 行同时可展开时嵌套 button 用 Base UI `nativeButton={false}` 正解(本仓嵌套 button 第三坑,首次提前避开)。**问答卡四态**(A07 屏⑥):36px 折叠条挂起时入 **ScrollArea 外 `PendingQuestionDock`** 不被滚走(多题 1 of N 分页;挂起必发生在 send 后/waiting_question,两路都保证 session 态,门无漏洞)→ 20px 字母 chip 展开卡一张装全题(Other 恒末位;Skip 弱 + Continue `--primary` 实心禁 `--accent-primary` 做底)→ 回答后**就地冻结 Answers**(D20;复用 store 冻结 8 例既有测试,零重复实现)→ Questions skipped 逐题斜体;挂起时 composerPlaceholder 变 `Add more optional details…`(分支排 busy 前——waiting_question 属 busy 的次序坑,设计期抓出);**Permission 复用同外形**(Allow/Deny 行为逐字保留)。**红线 store 纯加法**:`respondQuestion/respondPermission` 返 `Promise<boolean>`——复核 blocker:提交失败(断线/Host 重启)卡片原永久锁死不可重试,现 false 复位解锁。**纯函数层六模块**(`toolCard`(三层:配对按 toolCallId 不假定相邻/断组/聚合)/`toolHits`/`turnTiming`/`questionCard`/`fileOpenIntent`/`useTurnTiming`),**+133 例(总 966)**。**对抗复核(Codex):4 blocker + 2 major + 2 minor 全采纳零驳回**——四 blocker:running 入聚合吞当前动作(测试还把错误行为锁成预期——断言盲区第二课)/聚合失败不传播/输入体 240px 档整档缺失/提交失败永久锁死;复核亦点名 toolCard↔turnTiming 双向 import 属风险偏好项未列 finding(函数体内引用,后续可抽单向模块)。**登记项四条**:① 空 thinking 块由「可展开占位壳」改「不可展开裸行」——**批准的行为变更**(更诚实的 Cursor 形态,非声称不变;复核纠正表述);② assistant 去气泡属屏②(T-28 归口)的**范围外溢非偏离**(不做工具行无法成立);③ R-12:聚合只作用 explore 类 ≥2(A07 样张反推判据,GUI 验收时留意);④ Markdown 无解析器(text-markdown 仅字号档)系**既有能力缺口**非本轮回归,记 ideas。**R-5 升 open-questions #18**:挂起时 placeholder 邀请补充但 textarea 仍禁用(解禁属 Composer 行为改动),待用户拍板。三绿:typecheck 干净 / lint **662 文件 0 诊断** / vitest **68 文件 966 例**(+6 文件 +133 例,同 3 例 Windows-only 基线)。**范围外(明示未做)**:open-q #8 空 thinking 指示待拍板项未单方面裁定;A07 命中浮层的文件类型图标(规格表无图标列,按更晚裁定从简)。**待用户 GUI 点验**:① 工具行动词灰阶无边框无图标、与正文同字号;② 连续读/搜完成聚合可展开、进行中动作独立行;③ 失败行整行红自动展开输出;④ 展开体输入段(240px)在输出段上;⑤ Thought 行展开见思考正文;⑥ Worked for Ns 在回合顶部;⑦ Read 行文件名悬停变色、点击打开右栏 editor(T-13 前为诚实空态);⑧ Grep 行悬浮命中列表、项可点击;⑨ 提问时 Composer 上方吸附 Questions 条不被滚走→字母行选择→Continue 冻结 Answers→Skip 显 skipped;⑩ Permission 新外形 Allow/Deny 可用、失败后可重试;⑪ 挂起时 placeholder 变化(输入框仍禁用=#18 待拍板)。 |
| 2026-07-30 | **首轮 GUI 点验反馈闭环:授权并发根治 + 三快赢 + 两拍板落库,`4019fed`** | ✅ | 用户完成观感对齐五任务首轮真机点验,回报 1 功能 bug + 2 拍板 + 整体观感判定。**代码落库 `4019fed`(21 文件 +902/-97,+28 例总 995)**。**① 授权并发卡死根治**:实测两张授权卡并发→点一张另一张永久 waiting、工具饿死(AbortError 现场被用户截图抓到)。设计期挖出**更重隐患:门是 messageId 级,两卡同挂一条消息时先点下方卡会静默错投授权给上方工具**。修法:`pendingPermissions` 队列化(有序数组)+ 门下沉 permissionId 级(`canRespondToPermission`)+ `respondPermission(permissionId, allow)` 必填签名故意编译断裂逐点改 + 三终态清 Host 崩溃幽灵卡 + 桥端 settle 补 `!hasPending` 一行(桥本身已是 Map 多路安全,协议零改动)+ 杂散 resolved 精确出队。+23 例。「File does not exist」判定为模型猜了已移除的 vflow/ 路径,非 app bug。**② 三快赢**:默认主题改亮(用户拍板「默认米色不要暗黑」;'system' 持久化值一次性迁移,显式选择不动)/Composer 输入框双框与暗色 `dark:bg-input/32` 泄漏(Textarea `unstyled` 根治)/流式 stick-to-bottom 跟滚(`shouldStickToBottom` 纯函数,底部阈值跟随、上滚不抢、ResizeObserver 盖流式增长)。**③ 拍板落库**:**#18 = A**(解禁打字+后发消息排队,等当前回合完再处理)→ **T-19 复活**;**#15** 记「初步观察到缓存命中,持续观察,暂缓联系运营方」。**④ T-19 研究完成**(openchamber 冻结版实证):输入框运行中从不禁用;回车默认入队(Ctrl+Enter 立发;steer 需服务端原语我们没有,走纯排队);队列纯客户端 store(每会话 FIFO 上限 20)+ **idle 触发自动放行**(FIFO 一次一条);队列消息显示为 Composer 上方 chip(可编辑/删/拖序)不进时间线;**挂起问答/授权时发送 = 自动取消挂起卡 + 入队**(直发会被运行中回合静默吞——openchamber 注释明写的坑)。四件全客户端可实现、协议零改动,我们 `respondQuestion(cancel)` 已具备前提。→ T-19 设计队列。**⑤ 观感判定与素材入库**:用户判定「布局大致可以,细节一团乱麻」,两张对照截图入库 `docs/design/refs/feedback-20260730/`;初判五条打磨底稿——满屏红(错误输出正文整块红权重过猛)/USER 气泡带标签生硬/排版层级缺失(Worked for·答案·时刻三行无层级,meta 疑丢 model 名)/顶栏杂乱(72% 假环 T-23 在队)/侧栏截断观感——归观感打磨专项待列清单过目。**⑥ 外来文件**:`docs/design/a08-final-context-panel-baseline.html` 系用户在 Cursor(Kimi K3 Max)自制的右栏/导轨定稿(参照截图里的「Session refactor options」会话,+139/-148),**是否登记为 A08 基线待用户裁定**,未入库;登记时须账实核对(其提到的 RightDock.tsx 已随 T-22 删除)。三绿:typecheck 干净 / lint **665 文件 0 错误**(外来 a08 html 29 警告不入库不计)/ vitest **69 文件 995 例**(+1 文件 +28 例,同 3 例 Windows-only 基线)。**待用户**:A08 登记裁定;第二轮 GUI 点验(授权并发修复项:并发两授权各自可点各投各的、默认亮色、输入框无双框、流式跟滚)。 |
| 2026-07-30 | **两裁定落地:授权串行呈现 + A08 临时基线入库,`beb8ccc`** | ✅ | 用户两句话裁定当场闭环。**① 授权改串行**(「就不应该并发授权,应该一个一个弹」):`canRespondToPermission` 收紧为**会话队首匹配**——并发请求仍全部入队,但逐张弹出可答,答完队首下一张自动解锁;错投风险结构性消除(队列基建 `4019fed` 正好是串行呈现的底座,改动仅门函数一处 +2 例总 997)。上一行点验清单中「并发两授权各自可点各投各的」一条**作废**,改为「逐张弹出、答完出下一张」。**② A08 入库**:`docs/design/a08-final-context-panel-baseline.html` 确认为用户 2026-07-30 早在 Cursor 自制的**右栏/导轨临时基线**,已入库登记为 **A08(临时)**;正式化与账实核对(其引用的 RightDock.tsx 已随 T-22 删除等)留待 T-12~15 设计消费时进行。 |
| 2026-07-30 | **观感审计交付 + 三拍板落库(D25/D26,T-29/T-30 立项)** | ✅ | 观感打磨审计交付 [`docs/design/polish-audit-20260730.md`](../design/polish-audit-20260730.md)(390 行 34 条,逐项独立可拍板:🟢纯打磨 20/🟠需基线修订 10/🔵T-23 在队 3/✅合规记录 1)。**审计推翻编排者三条初判中的两条半**(逐像素取色证输出体正文本就合规、满屏红实为行头红×聚合传染×自动展开三重叠加;USER 角标删除属回归基线不需拍板——两基线 markup 本就无 role 标签),坐实一条(meta 丢 model 名,根因 `ModelSelect.tsx:56` 默认值从不落盘)。**用户三拍板**:① **D18 字体条款复议通过 → D25 分域字体**(「我希望能做到 Cursor 那种协调的美感」;UI 比例 + 等宽专供代码/路径/分支,Flexoki 色与卡片形态不动;硬证据:等宽回退链字重档缺失致层级三件套塌维/letter-spacing 全仓仅 1 用/侧栏字符量 -23%;open-q #10 以此结项);② **四条 A07 修订全准 → D26**(失败行分级红/自动展开簇规则/侧栏 chip 放宽/user 气泡满宽连带 A01);③ **Markdown 方案一准 → T-29 立项**(react-markdown+shiki 依赖已在树内,实为补交付——ARD §9 早标 ✅;chat 专属映射防攻击面/一期不渲染远程图/流式期纯文本完成后切)。**T-30 立项**(审计批次 1+2:快赢 12 项零基线修订 + 分域字体落地与四条修订实施,红字符 -86% 目标)。执行计划 §3 追加 T-29/T-30 两行;open-q **#19 新立**(model/effort 对 direct 发送不生效,T-19 设计发现的既存问题)。 |
| 2026-07-30 | **T-19 消息队列落地(运行中解禁输入+FIFO 排队),`1b350ff`** | ✅ | 按「openchamber 队列语义研究 → deep-reasoner 设计规格 → 三批施工断言先行 → 对抗复核 → 修复批」推进。**代码落库 `1b350ff`(14 文件 +2453/-83)**。**研究先行(Explore 委派)**:复刻 openchamber 冻结版四件套语义——输入框运行中从不禁用/回车默认入队/纯客户端 FIFO(每会话上限 20)/idle 触发自动放行;挂起问答/授权时发送=自动取消挂起卡+入队(直发会被运行中回合静默吞,openchamber 注释明写的坑);四件全客户端可实现,协议零改动。**设计(deep-reasoner)**:8 决策+14 行边界表规格(scratchpad `t19-design.md`,487 行)。**三批施工**:批1 纯函数层 `messageQueue.ts`/`queueRelease.ts` + `stores/messageQueue.ts` 薄壳(+85 例);批2 `ChatComposer` 运行中解禁输入 + `runSend` 改**提交即消费**(草稿与附件在提交点消费,全部失败出口不吞字) + idle 触发自动放行(+5 例);批3 `QueuedMessageStrip` 队列条(支持删除与拖序回填=交换语义) + Stop→队列暂停/Resume 与新回合恢复 + 挂起问答/授权提示行(+4 例)。**对抗复核位变更**:Codex 通道连败两次(均「Selected model is at capacity」,task-ms7bkp1z-krm12j / task-ms7bnwv5-zi6tca),按编排模型降级纪律改派 deep-reasoner 执行,产出 1 blocker/6 major/17 minor。**复核结论执行(修复批 +6 例)**:按设计 R5 预案回退批3第4项「失败载荷归队」至批2组件局部 retryable 形态(一次消 B1/M1/M2/M4);M3 死键修复(`hasFailed` 改 `canRetry`);M5 放行 effect 自持闩(StrictMode 双挂载安全);M6 `isStoppable` 改委托 `queueRelease.isRunningStatus` 消灭手抄副本 + 补九态×五布尔一致性属性测试;minor 修 9 条、随批3回退连带消失 2 条、留档 5 条(m1 `pruneSessions` 防抖、m7 动态字符串 i18n 模板下沉、m15/16/17 落 T-30 文件)。**已知代价(设计决策,非缺陷)**:后台会话不自动放行(需切回前台才放行);应用重启队列丢失(纯内存态,无持久化)。三绿:typecheck 干净 / lint **672 文件 0 错误**(外来 a08 html 29 警告不入库不计)/ vitest **71 文件 1097 例**(测试 997→1097,同 3 例 Windows-only 基线)。**待用户 GUI 点验**:① 运行中输入框可打字可粘贴图,回车不发送而入队显示 Composer 上方队列条;② 队列条可删除、可拖序(交换语义);③ idle 时队首自动放行、多条 FIFO 依次消费;④ 挂起问答/授权时发送=自动取消挂起卡+入队,不直发不误代答;⑤ Stop 后队列暂停,Resume/新回合自动恢复放行。 |
| 2026-07-30 | **T-30 批1 观感快赢落地,`3dcd2dc`** | ✅ | 依据 [`polish-audit-20260730.md`](../design/polish-audit-20260730.md) 逐项落地。**代码落库 `3dcd2dc`(6 文件 +95/-40)**。**12 项快赢中 10 项落地**:P-08 删 USER 角标(回归两基线本无 role 标签的现状);P-09 user 气泡回归 A07 基线;P-11 NoticeMessage 形态修订;P-06 失败框收敛;P-17 间距调整;P-05 工具行 destructive 降为 70% color-mix;`--tool-arg` 62%→78%;P-14 meta 行模型名 `getSessionModel ?? defaultModelId` 兜底(修复 `ModelSelect.tsx` 默认值不落盘致 meta 丢 model 名的真 bug);QuestionCard/LeftNav 字距梯度 `tracking` 接通。**P-19/P-22 让给 T-23**(与存量违规清理交叠,口径不变,不重复登记)。**批次 2 未开工**(D25 分域字体 + D26 四条 A07 修订施工),施工依据交付情况见下一行。三绿:typecheck 干净 / lint 672 文件 0 错误 / vitest 71 文件 1097 例(与 T-19 同批验证,批1 本身无新增测试)。 |
| 2026-07-30 | **T-30 批2 施工依据:D25 分域字体规格交付 + 编排者三条临时裁定(待确认)** | ⬜ | deep-reasoner 产出 637 行规格(scratchpad `d25-font-design.md`):系统字体栈零随包/Latin 先 CJK 后(禁 `system-ui`);「内容 vs 控件」判据区分何处用等宽何处用比例;`ToolRowView` 需加 `argKind:'ident'\|'prose'` 字段区分参数渲染;`ToolRows.tsx` 两处 `<pre>` 缺 `font-mono` 属回归必补(翻栈即坏);字重仅 400/600 全平台真档(Win10 Segoe UI 无 500,故弃用);字距四档 + CJK 禁负值;~14 处补 `tabular-nums`;阅读栏 48rem→45rem(DPR 标定实测:Cursor 参照截图 45rem 下为 48 CJK 字/行);A07/phase0a 渲染层冻结段 + v4 追记;与 open-q #12(终端/Monaco 主题跟随边界)正交(`ghosttyTheme.ts` 零 font 引用,不受影响)。量级估 ≈3.25~3.75d。**编排者三条临时裁定(待用户 GUI 点验确认/否决,非正式拍板)**:① 规格 §2.3 三处细化点采方案 A(侧栏分支 chip sans13/分支下拉 sans14/运行位置 sans14,对齐 Cursor 参照,字面口径 mono 仅作可选回退);② 阅读栏采 45rem container token 方案;③ 新建 `a09-font-domain-baseline.html` 承载三测点 A/B 凭证(尚未建,拟 T-30 批2 开工前产出)。**待用户**:三条临时裁定确认或否决后 T-30 批2 方可开工。 |

| 2026-07-30 | **第二轮 GUI 点验反馈闭环：四项功能根治 + 网络可见性五项 + 视觉六项，`b159e4a`** | ✅ | 用户完成第二轮 GUI 真机点验（2026-07-30 晚），回报九条反馈（涵盖授权卡渲染、消息队列、模型直通、附件回显、网络重试可见性与视觉细节）。**诊断**：7 路并行工作流（3 opus + 4 sonnet）全部行级定位九条反馈根因；另委派 deep-reasoner 实跑 22 次 A/B 侦查，证伪「新仓库信任拦路」假设，坐实根因为**网络传输层瞬断触发 CLI `api_retry` 静默重试环**（报告要点：`error_status` null、内建 10 次指数退避、归一化层原样丢弃）。**代码落库 `b159e4a`（51 文件 +4721/-184，测试 1148→1240）**。**四个功能性根因与修复**：① **授权卡不渲染**（用户第 5 条，截图「卡住了.png」）——`4019fed` 引入的幂等守卫谓词过宽（`permissionId` 与 `tool_call` 块同 id 必然误判已存在）→ 收紧为仅比对 `permission_request` 块（**红线文件批准改动 1**）；拒答补偿事件回显用户选择 + 状态收敛；授权块首次裁定胜出；双桥互查挂起态。② **排队消息消失 + 新会话无回复**（第 1 条）——受理证据误用 created/resumed、七个失败出口硬编码 committed、`host.error` 不隔离会话、Host 忙位在流收尾才清 → 改 origin 语义（release 归还队首并暂停 / direct-retry 武装 Retry）、受理证据改为用户回显、result 事件即清忙位 + 2s 限时排空保 transcript、`session_busy` 有界可取消重试。③ **选 sonnet 实调 opus4.8**（第 2 条）——`session.send` 协议无 `model` 字段 + 侧栏无模型恢复 + 显示层兜底遮蔽 → 全链补可选 `model` 字段（**协议可选加法，版本仍为 1**）、事件源真模型进 meta 行（`message.started` 可选 `model`）、模型解析统一 helper、`getStatus` 补带 `settings`。④ **附件发送后不显示**（第 4 条）——Host 回显 `beginTurn` 只带文本 → `MessageStartedEvent` 补可选附件元数据（**协议可选加法**）+ 时间线渲染附件 chip。**网络可见性五项（a1~a5，a5 后被复核推翻撤销）**：`api_retry` 透传（`SessionStatusEvent` 补可选 `retry` 字段，**红线文件批准改动 2**）+ 状态行「Network retry N/M」；CLI stderr 接通；超时文案纠偏；TTFT 32s 看门狗（证据门 + 挂起豁免，健康慢轮不误杀）；**45s 放弃自动 Stop 已实装后被 Opus 复核判为误杀健康轮而删除**。**视觉六项**：combobox `min-w-0`（修下拉横向滚动）；textarea resize 穿透禁用 + `leading-6` 居中；Model/Effort 触发器补 `min-h`/`min-w`；卡片 padding 对称；时间线顶部 `scrollFade`（V-b 复核改判为贴底滚动固有行为，非 bug）。**复核链（对抗强度记录）**：Codex（2B/3M/2m，`task-ms7qipkp-ujt3xy`，此前容量满载连败两次后恢复）+ Opus deep-reasoner（2B/8M/12m）双轨独立同题（互不见对方方案）→ 合并 14 条裁定 → 迭代 1 修复后四路复验 fail（1B/5M/8m）→ 15 条裁定迭代 2 → 两路复验 fail（2M/7m）→ 8 条裁定迭代 3 → 三路全 pass → 5 条 minor 抛光。红线文件本轮批准四处微改（幂等谓词收紧、首次裁定胜出、no-match 恒等早退、可选字段加法）；`ChatMessage.model` 曾加后经复核以零消费删除。**残留与开口**：runSend 的 `.tsx` 接线不变量（origin 传递 / finalizeOutcome 汇集 / 暂停唯一权威）为 inspection-verified、vitest node 环境结构限制下不可单测，已注释入码；TTFT 证据门取舍（init 后永久静默且无 `api_retry` 的黑洞留给 120s stall，与健康长轮不可区分，有意为之，已注释入码）；网络环境复测需用户配合（VPN/代理确认 + curl 采样 + ai-client 仓复测偶发——「新仓库特有」判定已撤销），入 open-q **#22**；授权 FIX 3（`PendingPermissionDock` UX）仍留档未做，入 open-q **#23**；**用户第 7 条「整体字体布局观感」= T-30 批2**（D25 规格已备）后续开工，**D25 §2.3 三处细化点用户本轮点验未异议 → 裁定 A 生效**（批2 施工依据自此确认）。三绿（实测复核）：typecheck 干净 / lint **676 文件 0 错误**（a08 html 29 警告 + 3 infos 不入库不计，同既往口径）/ vitest **74 文件 1240 例**（较上次登记基线 71 文件 1097 例 +3 文件 +143 例；commit message 记「测试 1148→1240」，与登记基线的差值未溯源，以本次实测为准）/ 同 3 例 Windows-only 失败（`ShellDetector.test.ts` 2 例 + `CliDetector.test.ts` 1 例）。**待用户**：第三轮 GUI 真机复验本轮九条修复；网络环境信息（open-q #22）。 |
| 2026-07-30 | **第三轮 GUI 点验结果 + 快赢批落地 `514560c`** | ✅ | 用户第三轮真机点验回报：第 1 条排队消息正常 / 第 2 条网络问题未再遇 / 第 3 条模型调用正常 / 第 4 条附件显示正常 / 第 5 条授权卡正常（第二轮九条修复经实测确认闭环）；第 6 条失败路径仍未测（顺延）；遗留三条新反馈——第 7 条 Model/Effort 文字与 placeholder 居中残留、第 8 条下拉混排要分域、第 10 条会话标题不许占位。第 9 条 composer 形态与 Cursor 差异（「过于圆润 AI 化」）另立设计（见下行 C）。**用户两拍板**：图片传输确认为模型能力问题、观察项关闭；标题方案采**首句凝练**（不用小模型总结）。**快赢批代码落库 `514560c`（21 文件 +1120/-67，测试 1240→**1290**，+50）**：①下拉分域——`buildFolderMenu` 按 `projectId` 合并 Recents 多分支条目、活动项目行恒指 `activeWorkspaceId` 且豁免 `recentsLimit`（防静默切分支）、`FolderMenuEntry` 删 secondary、分支下拉宽度放宽（`max-w-40`→60/`w-70`→80/`max-w-32`→48）；②首句凝练标题——新模块 `sessionIndex/sessionTitle.ts`（句读边界 ASCII `.!?` 须后随空白才切以保 `src/foo.ts`、`v2.1`、`example.com` 类文本，截断按 code point 防代理对撕裂，60 字封顶）、占位判定 `isPlaceholderTitle` 与 `fallbackSessionTitle` 格式器同源（识别 `Session ` + 1~6 非空白，结构性覆盖格式器全输出域）、`resumeDisplayTitle` 统一复用、direct/retry/release 三出口 `runSend` 前活捕获零 user 消息旗标（仅会话首条 committed 定名，结构性排除恢复会话被 follow-up 定名）、复用既有 `renameSession` IPC（`useSessionIndex` 抽 `renameSessionIndexEntry`）、回调二次占位复查+模块级在途去重防竞态；③居中残留——`ModelSelect` `min-w-26`→22（真因是共享 `EffortSelect` 的 `min-w` 配额 + `justify-between` 死空间，非垂直偏移：像素级测量证文字与边框中心完全重合）、composer 空值 textarea 补 `rows={1}`（`field-sizing-content` 空值回退 HTML 默认 2 行撑破单行 24px 契约）；④六张参照图入库 `docs/design/refs/feedback-20260731-round3/`。**复核链**：Codex 对抗复核（`task-ms8bl0jk-fykf44`）NEEDS-FIX 六条（活动项目行错指分支/自动标题竞态/首条消息门缺失/识别器与格式器漂移/测试缺口/句读误切）→ fast-worker 修复迭代 R1~R6（+19 例）→ Codex 续会话定向复验（1a/2c/6 FIXED；2a/3/5 再判）→ 编排者仲裁：条目 3 采纳亲修（识别器改结构化匹配 + 3 例新测试 + 1 旧负例按新契约修正）；条目 2a/5 判**已接受残余**（Main 侧改名无 CAS——自动标题 IPC 在途毫秒窗内完成的手动改名可能被磁盘后写覆盖，注释已入码，见 open-q **#24**；同形手打占位标题误认由首条消息门压制血面）。三绿实测：typecheck 干净 / lint 0 错误（`docs/design/*.html` 29 warnings+3 infos 既存豁免）/ vitest 75 文件 **1290** 例（3 例 Windows-only 豁免不变）；红线 `chatSessions.ts`/`runtimeEvents.ts` 零改动。**待用户**：第四轮点验（快赢三项）+ 失败路径（第 6 条）补测。 |
| 2026-07-30 | **scratchpad 清空事故与 D25 规格抢救落库** | ✅ | 会话重启致 scratchpad（tmpfs）清空，D25 字体规格（637 行）、形态设计半成品、两个在途后台代理全部丢失（工作树零残留）。编排者从会话 transcript 按行号合并两段读取记录（1-500 + 501-637）**零缺口复原 D25 全文**，持久化 [`docs/plans/2026-07-30-d25-font-domain-design.md`](2026-07-30-d25-font-domain-design.md)；三条裁定凭上下文重写入 [`docs/plans/2026-07-30-d25-orchestrator-rulings.md`](2026-07-30-d25-orchestrator-rulings.md)（含已生效状态与施工注意更新）。**流程改进**：设计规格自此一律直接落库 `docs/plans`，scratchpad 只放一次性中间产物。此前台账 2026-07-30「T-30 批2 施工依据」行所引 scratchpad 路径以本行落库路径为准。 |
| 2026-07-30 | **T-30 批2 形态规格交付：`docs/plans/2026-07-31-t30b2-composer-form-design.md`（647 行，deep-reasoner 重派产出）** | ⬜ | 「过于圆润 AI 化」归因量化：非卡本身圆角，而是卡内四件满圆控件 + 静息边框重 Cursor 2.5 倍（ΔL 0.137 vs 0.055）+ 聚焦转品牌橙 + 两件高饱和圆钮叠加。十三处裁定中八处系回归 A07 既有裁定（Model/Effort 无框文字 chip、目标行满圆钳形修正、8px 净空等），三处修 A07 自身测量/算术错误（follow-up 卡高 40→42 漏算 2px 边框、圆钮「目视 36px」实测 24、目标行圆角 8px 档）。麦克风/action pills/卡填充极性三处按 A06 诚实性记「有意不对齐」。编排者复核：八处可证伪主张（A07 四锚点行原文、`select.tsx` 满圆钳形成因、`globals.css` 三 token oklch 逐位）抽查全属实，规格采信为批2 施工依据。**待用户三拍板**：①模型档位是否合并一体式 `Sonnet High ⌄`（推荐合并，+0.5d；否则两 ghost chip +0.25d，两选均解主因）；②follow-up 卡是否改满圆 pill（**唯一实测与用户口头判断相反处**：Cursor 实测就是 pill，圆弧四点拟合残差≤1px，按「尽量一比一」推荐改）；③圆钮 28→24px + 发送键 `--primary`→近黑（推荐都改）。合并 D25 后批2 总量级 **≈5.3~5.8d**（采纳①）/ 4.8~5.3d（不采纳）；施工序 S0 断言合批→S1 D25 token→S2 形态主体（必须早于 D25 域映射）→S3~S4 D25 原语/域映射→S5 复核→S6 A07 v4 追记一次成文→S7 a09 截图。 |
| 2026-07-31 | **第四轮 GUI 点验结果 + 重试双发根治落地 `cb2d8d7`（14 文件 +1178/−119，测试 1290→1345）** | ✅ | 用户第四轮点验：第 2 首句标题 ✅ / 第 3 下拉分域 ✅；第 1 条新缺陷两个——点 Retry 消息双发、报错占用输入框无法输入直发；第 4/5 条 Model/Effort 形态口径（无框悬停显壳/合并一体式=原拍板①落定）；第 6 条回复解剖口径（状态读条进回复区+九段结构+尾部状态条）；第 7 条滚动置顶用户气泡。**三项拍板全落定**（①合并一体式②follow-up 卡满圆 pill③圆钮 24px+发送键近黑）→ open-q #25 结项。参照图六张入库 `docs/design/refs/feedback-20260731-round4/`（含 bug 现场两张：错误正文灌入输入框、时间线五连重复气泡+`hostAfter.state=ready` 指纹）。 **双轨诊断**（互不见对方）：Opus [`2026-07-31-retry-doublesend-diagnosis-opus.md`](2026-07-31-retry-doublesend-diagnosis-opus.md) + Codex [`2026-07-31-retry-doublesend-diagnosis-codex.md`](2026-07-31-retry-doublesend-diagnosis-codex.md) 同判核心根因：committed（sawUserEcho 受理证据）仍武装全量重发 Retry——同一份证据队列层用来禁止重排、Retry 层却允许重发；渲染侧超时有意不 Stop 原回合（护健康慢轮）与武装 Retry 并存=双发。缺陷 B 同判非 disabled 谓词：完整错误串进 composer 同行 statusLine 的 flex 收缩数学（basis:auto 数千 px vs textarea base=0）压零宽。分歧仅幂等权威层：Codex 主张 Host 受理边界 clientTurnId、Opus 主张渲染发送层 sawUserEcho 权威——**编排者采 Opus 层（红线零改动），Codex 方案入 open-q #26 纵深项**。 **修复内容**（`cb2d8d7`）：committed→草稿回填（composer 空才回填+如实文案）不再武装；lastUserPrompt 兜底删除；busy 循环睡前睡后双门+fatalHostErrorCode 重置移至双门后；host.error 严格 requestId 匹配+未知窗口有界候选集（MAX 4 FIFO）暂存、每 IPC 发起前置空 requestId 落地即重估；retry 不 clearPause；retry+skipped 结果级清快照不丢载荷；草稿 ref 全入口同步化（6 处 updateValue+getLiveDraftCount）；错误正文唯一归属横幅（含 hasStatusError+hasLargeHint 组合态）；状态槽 flex-1 basis-0 max-w-48+textarea flex-[2] min-w-32（flex-[2] 系结构性豁免，仓内 flex-[1.5] 先例，注释入码）；empty→session 焦点恢复。 **复核链**：Codex 复核 NEEDS-FIX 五条（睡后窗口/retry+skipped 丢载荷/草稿 ref 迟一拍/组合态错误回流+basis-0 零宽回归/测试缺口）→修复迭代 F1~F6（+21 例）→Codex 续会话复验 F1/F3/F4/F5/F6 FIXED、F2 留阶段切换新窗口→F2b 收口（+9 例）→编排者 diff 亲验闭合。工人 API 断线一次 SendMessage 续跑成功。 **有意不做与残余**：A5（retry-resume 历史重放）按 Opus 诊断建议留待现场取证后单独立项；Host 侧 clientTurnId 幂等入 #26；组件级事故测试（chat.send 计数/竞态注入）超 node-only vitest 能力入 #27。三绿：tsc 干净/biome 0 errors（docs html 29w+3i 既存豁免）/vitest 75 文件 1345 例（3 例 Windows-only 豁免不变）。**待用户**：第五轮点验（双发与输入框解禁）+失败路径其余场景。 |
| 2026-08-03 | **T-30 批2 落地 `9e2736b`——D25 分域字体全量 + Composer 形态对齐 Cursor 三拍板（70 文件 +4202/−603，测试 1345→1415）** | ✅ | 按合并施工序 §8.2（S0 断言先行→S1 token→S2 形态→S3 原语/argKind→S4 域映射→S5 仓外复核→S6 文档一次成文→S7 凭证页）单批完成，开工前先以 6 路并行侦查点验规格锚点漂移（三处关键漂移修正施工任务书）。 **字体面（D25）**：globals.css 分域两栈（Latin 先 CJK 后/禁 system-ui/零随包）+ --text-meta/-ui/-title + 45rem 阅读栏 token + font-synthesis-weight:none + data-font-domain A/B flag；ToolRowView.argKind ident|prose 多态（Bash description 散文）+ toolRowArgClass 下沉；ui/ident.tsx 原语；chat+workspace-shell 四类字号清零、mono 白名单补齐、tabular-nums 全量（StatusLine 8 槽重灾区）、字重 600 归位、大标题 7 处 text-title+负字距（<18px 覆写点 tracking-normal 防线 10 处）；chat 外复核补 mono 9 处（diff 行号/堆栈/路径）；**cn() extendTailwindMerge 注册六字号 token**——根治未注册 text-<name> 被 tailwind-merge 判色组静默吞类（施工中两名实施者各中招一次的真缺陷）。 **形态面（拍板①②③）**：合并一体式 ComposerModelTrigger（Base UI Menu 双 RadioGroup 勾在右/无框悬停显壳 px-2/焦点态成对显壳/Default 后缀省略/Cursor 弹层五构件按规模能力判据一件不搬），删 ModelSelect/EffortSelect（min-w 反面教材注释转写）；follow-up 卡 42px（修 A07 E1 算术）+ **静息半高 rounded-[21px]**（as-built 修正：textarea content-sizing 长高路径使 rounded-full 钳 h/2 大弧，21px 静息与 pill 逐像素等价、长高退化圆角矩形，hasExtras 仍降 rounded-md）；边框 border-border/聚焦 border-input 零色度；圆钮 24px（size-6 sm:size-6 双段压 Button icon-sm 响应式泄漏）+ send/enqueue 近黑 bg-foreground；⊕「Add file context (@)」接真实文件搜索（**结清 T-28 A07 偏离①**）；卡上方净空归一 8px（host pt-0，三上游各持有）；empty 态 statusLine 改按需显隐（folder 触发器 title 补 cwd 通路）。 **断言**：F-A 系列 18 条（含算术×字符串交叉：min-h 数值×4===composerFollowHeightBreakdown().total===42、rounded-[N]×2===total、全 token matchAll 含 variant 前缀）+ A1（font-mono 文件白名单 35 文件，preflight code/kbd/samp/pre 第二 mono 来源承重注记）/A5（字面量级+文件级合取双检，豁免清单带 reason 守卫）/A6（token 化剥 variant 前缀）+ argKind/合并菜单纯层断言。 **复核链**：Opus+Codex 双轨独立对抗复核（互不见）——Codex 4 断言真值 major（F-A2 漏响应式 padding/F-A4 只取首 token/A5 单字面量/A6 漏 variant 前缀，全部「反例仍绿」型）+4 minor；Opus 另抓 2 major（pill 守卫看错信号/合并菜单假勾——目录外模型菜单勾 options[0] 而线上发存储值，且测试只断言「恰一勾」锁死盲区）；四大攻击面（运行时/冻结区/删除面/诚实性）双轨独立判净；**Grep/Glob argKind 双轨相反裁定，编排者采 Opus**（arg 是 pattern in repo 混合串，整体 ident 会 mono 掉散文；复合渲染入 ideas）；修复批全部反例证红→修复证绿自证，minor 批 8 项同批扫尾（DiffReviewModal mono 去 font-medium/layout 计数器 tabular-nums 爆炸半径补扫/SessionBar 供应商名 CSS 兜底等）。 **文档**：A07 顶部时效标注 + v4 追记一~十一节一次成文（SequenceMatcher 证纯插入，.fx 渲染层零字节改动，含追加后锚点位移表）；phase0a 五锚点；design-system 分域全面改写（Radius 钳制/悬停显壳/数字对齐/平台字重矩阵/tailwind-merge 注册强制注记）；**a09-font-domain-baseline.html 新建**（三测点 mono/split 实渲对拍 + Cursor 参照 + 形态对照诚实占位 + 8 项指标表带状态列）。 **三绿**：tsc 干净/biome 686 文件 0 错误（29w+3i 均既有 a08 豁免）/vitest 81 文件 1415 例（3 例 Windows-only 豁免不变）。**待用户**：第五轮 GUI 点验（清单 0-nonies，含 a09 改后截图回填与 Win10 字重/CJK 18px 负字距观感）。follow-up 六条入 plantree ideas。 |
| 2026-08-03 | **第五轮 GUI 点验结果 + 四项修复落地 `6ece6cb`（47 文件 +5811/−235，测试 1475→1537）** | ✅ | 用户第五轮点验：整体观感「好很多」；五条反馈——①New 只建 ai-client ②新会话归档/关闭无效 ③follow-up placeholder 偏上（截图证）④⊕ 改附件菜单（Cursor 参照图入库 refs/feedback-20260803-round5/）⑤斜杠指令（用户明示低优先级→ideas）。四路并行诊断落库 [`2026-08-03-round5-feedback-diagnosis.md`](2026-08-03-round5-feedback-diagnosis.md)。 **修复**：D1 LeftNav 焦点夹 + resolveNewSessionTarget 三级回退（存活/归属校验，焦点夹不可用→禁用非改道）；D2 归档三级阶梯（补登记重试→live-only 兜底）+ 成功路径移行交接 + close=detach+本运行移除语义定版 + **撤销 eager 登记**（防 New 永久留痕）+ tree-sync 补种收紧 + sendMessage 跨 await 幽灵会话双守卫（**红线 chatSessions.ts 加性微改批准**：ensureHost/createSession 后重读存活判据，已删行收尸 closeSession）+ dismissedSessions 零依赖叶模块防环；D3 items-start + 状态行 h-6 定高盒；D4 ⊕ Attach files 全链——dialog:openFiles 多选 + file:readAttachment **fd 绑定读取**（grant 存 {dev,ino,size,mtimeMs} 快照、句柄 fstat 全等、身份先于大小、读后再验、一次性 take + 30s TTL + 导航/销毁双清、目录保留 grant 换诚实报错）+ ingestPickedPaths 复用 T-18 管线零分叉（count 预算读盘前置/per-file 隔离/notice 入 finally）+ @ 注入退役（F-A12 退役注记，手打 @ 不变）。 **复核链**：Opus+Codex 双轨独立（互不见）——Codex 1 blocker（stat→read TOCTOU）+4 major；Opus 把 blocker 升级为无竞态确定性绕过（file:copy 无根校验覆盖已授权路径）并另抓 4 major（归档成功不移行/eager 登记永久留痕/多选读盘不受 count 约束/白名单零测试）；合并 1 blocker+8 major+若干 minor 全闭环，二轮双线修复（安全线/会话线）。**不修入档**：FILE_COPY/RENAME/MOVE 无根校验既有暴露面、session-${Date.now()} 毫秒碰撞（入 ideas）；composer 英文文案惯例注记。F-A20~23 编号已在代码定义、规格表补行见追补文档注记。 **三绿**：tsc 干净/biome 700 文件 0 错误（29w+3i 既有 a08 豁免）/vitest 88 文件 1537 例（3 例 Windows-only 不变）。**待用户**：第六轮点验（清单 0-decies）。 |
| 2026-07-31 | **第四轮设计交付：形态追补 + 回复解剖两规格落库** | ⬜ | [`2026-07-31-t30b2-composer-form-addendum-round4.md`](2026-07-31-t30b2-composer-form-addendum-round4.md)：三拍板改记生效；Cursor 弹层五件一件不搬（目录恒 3 项无需搜索/无 Auto 路由/effort 会话级/无 Edit/子面板只剩 Effort 降为主列表分组）；触发器 `Sonnet High ⌄` 填充式悬停壳（禁 hover:border 防抖行）+focus-visible 成对（a11y 硬要求）；「字小或框大」取框大（字号降 13 破 D25 同档域映射）；右段 200px→约 116px；断言 14→18 条；**T-30 批2 量级 5.6~6.1d**（+0.3d）。 [`2026-07-31-reply-anatomy-design.md`](2026-07-31-reply-anatomy-design.md)：第 6/7 条同根=时间线缺「回合」层，主体为纯函数可推导 ChatTurn；状态读条从 Composer 卡内迁回合区（描述在飞回合的归回合/描述草稿的归 Composer）；九段映射 4 已合规/3 需改/2 需建；A07 :2396-2400 需修订（Worked for Ns 展开的是过程段非统计）；未解决 permission 强制展开禁折（安全红线）；与 D26 簇规则两级独立；置顶气泡就地 CSS sticky（锚定=DOM 顺序，零滚动监听）；scrollFade 改按边（V-b 前提消失非推翻）；**T-31 立项建议 ≈3.6d，排 T-30 批2 之后、T-29 之前**。规格内 δ（尾部不复现耗时，唯一与用户字面口径不一致处，已向用户说明）/ι（fork 图标按 A06 不做）等六项取舍已按推荐成文。 编排者抽查三处可证伪主张（模型目录 3 项/状态行在 Composer 卡内/scrollFade 24px mask）全属实，两规格采信。 |

## 委派记录

| 日期 | 委派对象 | 任务 | 结论采纳情况 |
|---|---|---|---|
| 2026-07-23 | deep-reasoner（opus） | C-01 依赖剪枝与产物布局策略对抗评审（Q1-Q7：SDK 平台 exe 252MB 可剪性、Cometix 平台包可剪性、node-pty 剪枝、sharp 去留、拷贝 vs 全新安装、产物目录选址、TSD 环境风险） | ✅ 已采纳（GO-WITH-CHANGES，逐条结论见过程记录） |
| 2026-07-24 | Explore | C-06 地形扫描（Scanner munge 规则、真实 JSONL 行格式、resume 链路、协议 envelope、store reducer、ARD D11） | ✅ 事实报告全量用于草案；发现身份富化缺口（§1.6） |
| 2026-07-24 | fresh-fable（独立无偏见） | C-06 协议草案对抗评审（源码 12 文件 + 真实 JSONL 12 文件/1669 行独立核实 + 解析模拟） | ✅ GO-WITH-CHANGES，12 findings（1 BLOCKER/4 MAJOR/6 MINOR/2 NIT）全采纳，Q1-Q6 逐条裁定（草案 §10） |
| 2026-07-24 | fast-worker ×3 并行（用户要求提并发） | C-06 实现分片：A historyReader+28 测；B Main 侧 requestAndWait/IPC/preload/索引+9 测；C store 灌入分支+12 测（Host runtime 并发敏感部分 Claude 亲做） | ✅ 三片零文件冲突合入；A 报出 tsconfig 排除 agent-host 的坑（typecheck 不覆盖 Host，靠 smoke 补位）；B 顺手修 2 处存量 import 序；偏差均已明报采纳 |
| 2026-07-24 | 探测代理 ×4 并行（claude ×2 + fast-worker ×2） | C-03 Question 形态 / C-05 Thinking 支持度 / C-10 options 逐项 / C-13 附件通道——全部 spikes/ 新文件零冲突、只探测不改运行时、不提交 | ✅ 四份结论全采纳（过程记录同日行）；C-10 顺带挖出运行时挂起缺口（→ C-14 拟立项）与 T-08/T-09 已完结、Effort/Plan 无下游工单的台账缺口 |
| 2026-07-24 | Explore + fast-worker | C-15 分工：Explore 摸随包 Node 地形（resolver 五源实现、afterPack/verify 结构、CI 作业、`source` 消费方、remote-runtime 下载先例）；fast-worker 写 fetch 脚本 + package.json/.gitignore/CI 接线（resolver/afterPack/verify 断言 Claude 亲做） | ✅ fast-worker 实测抓到并修复 Git Bash tar 坑；Explore 证实 `NodeRuntimeSource` 加 'bundled' 无穷举消费方连带 |
| 2026-07-24 | Explore + deep-reasoner + fast-worker ×2 | C-04 分工：Explore 摸 permission 链路地形（发现 Main/preload respondQuestion 已存在只差 payload 重塑）；deep-reasoner 对抗评审协议设计（读 cli.js 源码逐条裁定，GO）；fast-worker A=Main/preload payload 重塑、B=store reducer+8 单测（Host 并发敏感部分 Claude 亲做，沿 C-06 先例） | ✅ 评审 7 findings 全吸收（2 MINOR 落协议注释与 T-05 交接、其余 NIT/PASS）；分片 B 报出 chatSessionsHistory.test.ts baseState 缺新必填字段的连带（已修） |
| 2026-07-24 | Workflow（fast-worker ×4 写 + 对抗评审 ×4）+ fast-worker ×1 补缺 | C-09 分工：用例清单 Claude 定（55+ 例逐条规格）；4 写手并行各产一文件、写完即评审（流水线无 barrier）；评审三维=覆盖缺口/空断言/错误断言；lint 侧共树敏感操作 Claude 亲做 | ✅ 4/4 评审 PASS；4 个 minor 缺口（跨会话 decoy/String 强转钉子/displayName 末级兜底/合成消息块断言）补齐后全绿；写手零行为偏差上报 |
| 2026-07-24 | fast-worker ×1 | C-08a 批处理测试 chatSessionsBatch.test.ts（折叠语义 5 + 假定时器调度 3；实现 Claude 亲做） | ✅ 8/8 绿；报出并修一处测试基建 flake（等价性对比撞 Date.now 毫秒 tick → vi.setSystemTime 钉死），未动产品代码 |
| 2026-07-27 | Workflow（设计 ×2 并行 → 实现 ×1 → 对抗评审 ×3 并行 → 修复 ×1） | T-03 收尾分工：设计阶段两路只读并行（UI 规格 / 验证方案），实现按合并稿先写测试再写代码，评审三视角（协议契约保真 / design-system 与 CLAUDE.md 合规 / 正确性怀疑论），修复阶段逐条判断可否成立后再改；三门禁与 pathspec 提交由 orchestrator 亲做 | ✅ 1 major（Retry 吞 `resume()` 失败返回值）被两位评审独立命中，与 orchestrator 自读代码的判断一致；修复未盲从建议——评审建议的 `isTurnActive` 漏 3 个忙状态，改为提取 `isSessionBusy` 与 Host 拒绝条件同源。5 条 minor/nit 全采纳，1 条 nit 判为主线红线故转为需求（`truncated`/`omittedCount` 展示） |
| 2026-07-27 | Workflow ×2（只读设计调研 ×3 并行 → 实现 ×1 → 对抗评审 ×3 并行 → 修复 ×1） | T-18 分工：先跑一轮**纯只读**设计调研（剪贴板机制/编码 · UI 规格 · 验证方案），产出合并稿落 scratchpad 不入库；orchestrator 逐条审后写「更正节」覆盖其上限推导（因当日重测推翻了 C-13 的延迟模型），再启实现工作流；评审三视角（回归安全 / 规范与所有权 / 正确性怀疑论）；网关探针、三门禁、pathspec 提交由 orchestrator 亲做 | ✅ 设计调研质量高（54 条断言用例 + 9 个纯函数签名），且**主动指出自己方案的死穴**（「150 KB 硬挡在真实截图面前几乎全灭」）——正是这句促成 orchestrator 去重测依据。评审 15 条去重后 9 个独立问题全修，最重的 blocker（粘图即冒出 Retry 幽灵按钮、点下去抹掉用户已输入文本）被三路各报一次。修复阶段有三处未盲从建议并给出理由，其中「用解文件头的纯函数替代 createImageBitmap」比评审给的降级方案更优（把零回归保护的 IO 换成 12 条断言） |
| 2026-07-28 | Workflow（4 调查 + 2 对抗复核）+ resume 重跑 | D:/ 根因链 / 全仓机器绑定审计 / enso 溯源 / 真 Host 好坏路径对照——repro 首跑遇 API 断连，resume 缓存命中后单独重跑 | ✅ flow 与 audit 双双 CONFIRMED；复核抓出 3 处修正（`pnpm dev -- --open-path` 彼时不可用因 dev.js 丢 argv→促成 576f3bd；resume 链也带 workspacePath→促成 resumeIntent 守卫；currentWorktree 全仓无 setter=恒 null） |
| 2026-07-28 | deep-reasoner | open-path 注册失败双因诊断（只诊断不改码） | ✅ 全采纳：失败 2=检测过早（localStorage ~5s 节流）非真失败；失败 1=lazy App vs did-finish-load 竞态；连带揪出 gotTheLock 缺门与 enabled=true 数据覆盖隐患；方案（拉取握手）即 9331d51。其探针曾误触二次实例删 tmp 目录的 bug（正是其报告的缺陷本身），已披露 |
| 2026-07-28 | Workflow（2 调查 + 1 对抗复核） | 多轮上下文 / thinking 缺卡双诊断（真 Host 记忆探针 + 双模型 thinking 探针） | ✅ continuity CONFIRMED（机制/溯源/修法全采纳→eea2f25）；thinking 结论=网关侧，渲染链无 bug，入 open-questions 待用户 |
| 2026-07-28 | fast-worker ×2（open-path 实现中途 API 断连，SendMessage 续跑完成） | open-path 拉取握手六文件落地；多轮上下文修复（sendPreamble + runSend 三分支重接） | ✅ 两份均按规格零偏差落地且自跑门禁；continuity 份用 git stash A/B 自证 6 例 ripgrep 红为存量环境问题（rg 又被 install 冲掉，非其引入）；resume 重放重复 edge 按指示只报告不擅修 |
| 2026-07-29 | Explore | 缓存排查地形:API 调用链(SDK query→spawn cli.js)、凭证流动、现成探针、system prompt 构造点、eea2f25 机制、model/effort 下发链 | ✅ 全采纳;其发现的「session.resume 丢 model/effort」经拦截实证坐实为真 bug → 3622c19;「拦 cli.js 那一跳」的定位直接决定了取证路径 |
| 2026-07-29 | deep-reasoner(Opus)+ Codex 并行双盲(高风险架构题,互不见对方方案) | 侧栏信息架构 + Composer 目标栏三下拉独立设计 | ✅ 核心裁定全部独立收敛(两层化/禁 checkout/New Worktree 归分支下拉/subagent 后置),分歧仅呈现层四点、全部显式升 open-q #16 未静默取舍;Opus 独立揪出 T-24 账实不符(主线复核确认),Codex 补上 New Folder=Temp Workspace 的现成 IPC 路径(化解 Opus 认定的无 IPC 缺口) |
| 2026-07-29 | fast-worker ×1 | T-26 S2:LeftNav 两层化改造(全规格化 brief:行解剖/删改清单/持久化键/i18n 词条,纯函数与断言由主线先行) | ✅ 零偏差落地且自跑三道门禁;唯一自主动作为 lint --write 限于 LeftNav.tsx(仅 import 序与折行,已明报) |
| 2026-07-29 | Codex(codex-rescue)→ 改派 deep-reasoner | T-26 diff 同行/对抗复核(非高风险决策,单路复核即可) | ⚠️ Codex 连续两次「model at capacity」未产出,按编排模型降级改派 deep-reasoner;其复核产出 1 blocker(签名缺 branch → 冷启动 chip 全灭)+ 3 should-fix + 4 nit,九项当场修、三项缓办记档,并反向确认八项边界干净;blocker 与主线自查预判同点但后果判得更重(主线只看到「外部切分支后滞留」,复核用真实 derive 双跑证明冷启动即死)。Codex 容量恢复后于后续任务恢复同行位 |
| 2026-07-29 | Workflow(3 读手并行) | T-22 前置理解:openchamber 参考实现(ContextPanel/useUIStore/Sidebar 拖拽全状态模型)/本仓现状(挂载链/删除影响面/测试范式)/文档基线(phase0a/D19~D23 边界/design-system 尺寸段) | ✅ 三份事实图谱全采纳(file:line 逐条可核),为设计与四批施工的唯一事实底座;本仓图谱正确预判删除影响面收敛于 workspace-shell 目录内 |
| 2026-07-29 | deep-reasoner | T-22 实现架构(11 项决策 + 809 行文件级规格 + 4 批次划分) | ✅ 决策 10 项采纳、1 项被主线驳回修正(PanelRight no-op 违 A06);其推翻主线倾向三处均有据(git 圆点接真数据/面板常驻挂载/阅读栏用 max-w-3xl/5xl 标准类);⚠️ 首轮声称规格已落盘实未执行 Write,被主线核查抓回补写——「声称落盘必核验」入交接惯例 |
| 2026-07-29 | fast-worker ×5(B1~B4 + 修复批) | 四批施工(纯函数+77 例 → store+骨架换血 → 拖拽+测量+提升 → 圆点+阅读栏+文档)+ 对抗复核采纳项修复 | ✅ 前四批零偏差且自跑三道门禁,规格空白点均按合理默认补齐并明报(B2 抓出 tooltip delay 位置与 i18n 重复键;修复批抓出主线修复规约里 Infinity→600 的笔误);修复批中途撞会话限额终止(完成 4/6),余 2 项(内层列保宽/RAIL_WIDTH 权威化)由主线亲手补完 |
| 2026-07-29 | Codex(codex-rescue,容量已恢复回归同行位) | T-22 diff 对抗复核(只读) | ✅ 产出 1 blocker + 7 major + 1 minor + 大面积「已查无问题」清单(reducer/persist/RO/回归面逐项点名);裁定采纳 5 驳回 4——blocker 系验收口径判错(docs append-only 历史叙述不在「全仓零引用」口径内),3 major 为已归 T-23/T-27 的存量违规(其自身亦标注非本轮引入)。附运维事实:rescue 通道单发即忘,编排者取结果须直接调 companion 的 status/result(rescue 子代理无权限跟进) |
| 2026-07-29 | Workflow(2 读手并行) | T-27 前置理解:设计依据(设计文档 §4 逐字/D22 台账/A07 屏①②④逐 token 解剖)与本仓现状(ChatComposer 插入点/store 模型无 retarget/三对话框契约/UI 原语盘点/测试范式) | ✅ 两份图谱全采纳;code 图谱「无现成 retarget 动作」「AddRepositoryDialog 深层打开需接线」两个预判直接塑形了设计决策 3/4 |
| 2026-07-29 | deep-reasoner | T-27 实现架构(11 项决策 + 515 行规格 + 4 批次) | ✅ 决策全部采纳;两个下拉零新 IPC、pending 由 workspaces 驱动防 R2、中立 store 文件解 import 方向三处为其独立贡献;规格本次**实际落盘**(上轮「声称落盘必核验」教训生效);⚠️ 其 §2.1「Remote 回落」句后被 Codex 复核证伪(blocker 5),验收⑤权威覆盖设计规格——规格不是免检金标准 |
| 2026-07-29 | fast-worker ×4(B1~B3 + 修复批) | 三批施工(纯函数 58 例 → 目标栏数据行 → 动作行+对话框接线+死按钮删除)+ 复核八项修复 | ✅ 四批全零偏差且自跑门禁;批 3 自主抓出 LeftNav MouseEvent-as-mode 真实坑并防御;修复批对 getBranches 守卫做了拆真实断言的红绿验证(临时注释 enabled 行确认变红再恢复);批 2 正确自判 createTempTarget 属批 3 边界未越权 |
| 2026-07-29 | Codex(codex-rescue,同行位) | T-27 diff 对抗复核(只读;其沙箱 EROFS 无法跑 vitest,主线独立复跑补位) | ✅ **5 blocker + 3 major 全采纳、零驳回**(对照 T-22 的采纳 5 驳回 4——本轮命中率 100%,runtimeIdentity/源会话竞态/gitEnabled 三条均为主线与设计双双漏掉的真洞);「已查无问题」清单同样有价值(retarget 写入面/Combobox 受控/AddRepositoryDialog stale-tab 均点名核过) |
| 2026-07-29 | Workflow(2 读手并行) | T-28 前置理解:A07 屏①②③逐行解剖(45% 高度/40px follow-up/头行仅存在于不采用的屏③)与 chat 组件现状(恢复态 messageCount=0 陷阱/mention 弹窗 240px 裁剪面/HostStatusBanner 常规流) | ✅ 两份图谱全采纳;「store 无乐观用户消息」与「恢复态桶长为 0」两个预判直接催生 sendAttempted 闩与 runtimeIdentity 信号双保险 |
| 2026-07-29 | deep-reasoner | T-28 实现架构(10 项决策 + 规格,含两棵目标 DOM 树) | ✅ 决策全采纳;三处独立深挖有价值(同槽位防卸载 R4/空态 mention 翻向下开/textarea field-sizing-content 免自造长高);⚠️ 40px 卡高自证漏算 2px 边框被 Codex 抓出(blocker)——「设计自证的算术也要复核」补充进 T-27 轮「规格不是免检金标准」的同一课 |
| 2026-07-29 | fast-worker ×3(B1~B3) | 三批施工(纯模块 49 例 → 骨架两态 → 卡形态/圆键/目标行) | ✅ 三批零偏差且自跑门禁;B2 对 R4 红线的落实干净(宿主 div 只换 class);B3 的 flex-col 包装层与 t() 收进 ComposerRoundButton 两处默认补齐均合理并明报 |
| 2026-07-29 | Codex(codex-rescue,同行位) | T-28 diff 对抗复核(只读;沙箱 EROFS 无法跑 vitest 已声明,主线独立复跑补位) | ✅ 1 blocker + 1 minor 全采纳(主线亲修):42px 卡高用静态 class 合成算术抓出——纯函数测试只断言 class 存在接不住合成高度类验收,此类断言盲区值得记取;minor(闩无界增长)与主线自查预判同点。干净面清单逐项点名(含 fork 新会话回空态的正确性确认) |
| 2026-07-30 | Workflow(2 读手并行;跑在默认 fable,先于当日「子代理按性质指派模型」规则,此后批次已按绑定执行) | T-05 前置理解:A07 屏⑤⑥逐行解剖(A~F 组/四态链/降级条款原文)与时间线现状(question 渲染 null/无计时数据/Grep 非结构化/聚合可行性=一 turn 一 assistant 消息按 toolCallId 配对) | ✅ 两份图谱全采纳;「配对不可假定相邻(并行 tool_use 乱序)」与「editor 通路缺失」两个预判直接塑形决策 2/6 |
| 2026-07-30 | deep-reasoner(会话限额中断一次,SendMessage 恢复后完成) | T-05 实现架构(九项拍板 + 980 行规格) | ✅ 九项全采纳;气泡拆解给足 A07 行号证据、PendingQuestionDock 落 ScrollArea 外、placeholder 次序坑(waiting_question 属 busy)三处为其独立贡献;⚠️ 规格漏输入体 240px 档与聚合失败传播(被 Codex 双双抓出)——规格盲区连续第三轮由复核补位 |
| 2026-07-30 | fast-worker ×5(B1~B4 + 修复批) | 四批施工(六纯模块+120 例 → 工具行渲染+去气泡 → 问答/Permission/Dock → 浮层+文件打开)+ 复核八项修复 | ✅ 五批全零偏差且自跑门禁;B1 正确按规格权威批次表全量施工并明报与派单差异;B4 提前避开嵌套 button 第三坑(nativeButton={false});修复批含红线 store 纯加法(respond 返 Promise)与四处测试 stub 联动 |
| 2026-07-30 | Codex(codex-rescue,同行位) | T-05 diff 对抗复核(只读;沙箱 EROFS vitest 未执行已声明,主线独立复跑补位) | ✅ 4 blocker+2 major+2 minor 全采纳:running 入聚合(A07 A 组逐字比对抓出,且指出测试把错误行为锁成预期——继 T-28 卡高后**断言盲区第二例**)/聚合失败不传播/240px 档缺失/提交失败永久锁死(并纠正「Permission 行为逐字保留」的不实声称);另主动澄清 Markdown 非本轮回归、双向 import 未达 finding 线——区分回归与既有缺口的判别力是同行位价值的一部分 |
| 2026-07-30 | deep-reasoner(API 中断一次 SendMessage 恢复) | 授权并发队列化设计(store 形态/签名/桥核对/边界表) | ✅ 全采纳;**独立挖出比报告更重的错投隐患**(messageId 级门+单槽=先点下方卡放行上方工具),并否决两个诱人的简化(blocks 派生态会误判重放块/UI 串行化降级真实并行);桥端「已多路安全仅一行显示级修正」的核对结论准确 |
| 2026-07-30 | fast-worker ×2(快赢三修因 API 中断恢复一次;队列化施工) | 快赢三修(主题默认+迁移/unstyled/跟滚)与授权队列化施工(9+1 文件,23 例) | ✅ 两批零偏差;快赢批对迁移机制(该 store 无版本号,走 merge 期消毒惯例)与 empty 态 textarea 纵向 padding 的既有不对称做了正确的克制判断;施工批抓出规格 §5.1.A 遗漏的 historyError.test.ts 第 7 个 fixture 文件 |
| 2026-07-30 | Explore(sonnet,按新模型指派纪律) | openchamber 消息排队语义研究(T-19 输入) | ✅ 全采纳;五问全答且附「我们复刻需要什么」分层(客户端四件 vs steer 需服务端原语)——「挂起时发送=取消卡+入队否则被运行中回合吞」这一参考实现注释里的坑直接决定 T-19 的④号件设计 |
| 2026-07-30 | deep-reasoner ×2 并行(T-19 设计;观感审计) | T-19 消息队列实现设计(8 决策+边界表 14 行);观感打磨审计(34 条清单+D18 复议论证+Markdown 立项案) | ✅ 双双全采纳。T-19 设计核心洞察「队列前提=草稿从完成时清空改为提交时消费」并顺带揪出既存的超时丢字 bug;审计用逐像素取色**推翻委托方(编排者)三条初判中的两条半**——对抗上游预设而非顺着写,是委派审计的价值本体;D18 复议论证给出可量化硬证据(字重档/字距使用量/字符量)而非审美断言 |
| 2026-07-30 | fast-worker ×3(B1~B3) | T-19 三批施工(纯函数层 messageQueue/queueRelease/stores 薄壳 +85 例 → 解禁输入+提交即消费+idle 放行 +5 例 → 队列 strip 删除/拖序回填+暂停 Resume+授权提示行 +4 例) | ✅ 三批零偏差且自跑门禁;批3 的 strip 拖序回填按交换语义实现,授权提示行按挂起分支正确接线 |
| 2026-07-30 | Codex(codex-rescue)×2 连败 → 改派 deep-reasoner | T-19 diff 对抗复核 | ⚠️ 两次均「Selected model is at capacity」未产出(task-ms7bkp1z-krm12j / task-ms7bnwv5-zi6tca),按编排模型降级纪律改派 deep-reasoner;其复核产出 1 blocker + 6 major + 17 minor,详见主线台账 T-19 行「复核结论执行」段 |
| 2026-07-30 | fast-worker(修复批,+6 例) | T-19 复核结论修复:R5 预案回退批3第4项「失败载荷归队」至批2组件局部 retryable + M3 死键 + M5 放行闩 + M6 isStoppable 委托消灭手抄副本 + minor 9 条 | ✅ 全部按复核结论执行,五条 minor 留档(m1/m7/m15/16/17)不与本轮混做 |
| 2026-07-30 | deep-reasoner | T-30 批2 字体规格设计(637 行规格,`d25-font-design.md`) | ✅ 全采纳;「内容 vs 控件」判据与两处 `<pre>` 缺 `font-mono` 回归点均为独立发现;编排者在此基础上再加三条临时裁定(§2.3 细化点取舍/45rem/a09 基线待建),尚待用户 GUI 点验确认 |
| 2026-07-30 | Workflow(7 路并行,3 opus+4 sonnet,按模型指派纪律) | 第二轮点验九条反馈根因定位(逐条行级) | ✅ 全部命中;授权卡幂等谓词过宽/受理证据与失败出口误判/session.send 缺 model 字段/beginTurn 附件回显缺失四项功能性根因与六项视觉问题逐条给出 file:line,为修复批提供权威定位 |
| 2026-07-30 | deep-reasoner(22 次实跑 A/B 侦查) | 网络重试可见性根因排查(排队消息消失/新会话无回复的深层诱因) | ✅ 证伪「新仓库信任拦路」假设,坐实根因为网络传输层瞬断触发 CLI api_retry 静默重试环(error_status null/内建 10 次指数退避/归一化层原样丢弃),报告直接决定 a1~a5 网络可见性五项设计 |
| 2026-07-30 | Codex + Opus deep-reasoner 双轨独立对抗复核(互不见对方方案) | b159e4a diff 全量复核 | ✅ Codex 2 blocker+3 major+2 minor(task-ms7qipkp-ujt3xy,此前容量满载连败两次后恢复)+ Opus 2 blocker+8 major+12 minor,合并去重 14 条裁定 |
| 2026-07-30 | fast-worker(修复批1)+ 四路复验 | 14 条裁定修复 + 复验 | ⚠️ 四路复验未过:1 blocker+5 major+8 minor,产出 15 条裁定迭代 2 |
| 2026-07-30 | fast-worker(修复批2)+ 两路复验 | 15 条裁定修复 + 复验 | ⚠️ 两路复验未过:2 major+7 minor,产出 8 条裁定迭代 3 |
| 2026-07-30 | fast-worker(修复批3)+ 三路复验 | 8 条裁定修复 + 复验 | ✅ 三路全 pass;另 5 条 minor 抛光收尾,红线文件本轮批准四处微改(谓词收紧/首次裁定胜出/no-match 恒等早退/可选字段加法),ChatMessage.model 曾加后经复核以零消费删除 |
| 2026-07-30 | fast-worker（快赢批施工，首派随会话重启丢失后重派） | T-30 批2 快赢批施工：下拉分域/首句凝练标题/居中残留修正（三项交付） | ✅ 三项零偏差落地；居中项以像素级测量替代目测定因（`EffortSelect` `min-w` 配额 + `justify-between` 死空间，非垂直偏移） |
| 2026-07-30 | deep-reasoner（composer 形态对齐设计，重派） | T-30 批2 Composer 形态规格（647 行，`2026-07-31-t30b2-composer-form-design.md`） | ✅ 全采纳；A07 自身三处测量/算术错误（follow-up 卡高漏算边框/圆钮目视值/目标行圆角档）为独立发现，八处裁定系回归 A07 既有裁定 |
| 2026-07-30 | Codex（codex-rescue）→ 续会话定向复验 | 快赢批 diff 对抗复核（`task-ms8bl0jk-fykf44`） | ⚠️ NEEDS-FIX 六条（活动项目行错指分支/自动标题竞态/首条消息门缺失/识别器与格式器漂移/测试缺口/句读误切）→ 定向复验 1a/2c/6 FIXED、2a/3/5 再判，详见主线台账本日「第三轮 GUI 点验结果 + 快赢批落地」行 |
| 2026-07-30 | fast-worker（修复迭代 R1~R6） | 六条 NEEDS-FIX 修复迭代 | ✅ +19 例，详见主线台账本日「第三轮 GUI 点验结果 + 快赢批落地」行 |
| 2026-07-30 | 编排者（仲裁补修 + transcript 抢救） | 条目 3 识别器契约漂移仲裁亲修 + scratchpad 清空后 D25 transcript 抢救复原 | ✅ 识别器改结构化匹配 + 3 例新测试 + 1 旧负例按新契约修正；D25 637 行规格按 transcript 行号分段合并零缺口复原 |
| 2026-07-31 | deep-reasoner | 双份设计（form addendum + reply anatomy） | ✅ 全采纳；施工中途编排者实时同步用户拍板②③，规格按三条全采纳成文 |
| 2026-07-31 | Opus + Codex 双轨独立诊断（互不见对方方案） | 重试双发根因诊断 | ✅ 核心根因同判，幂等权威层分歧由编排者仲裁（采 Opus 渲染层，Codex 方案入 open-q #26） |
| 2026-07-31 | fast-worker（修复施工，API 断线续跑 1 次） | cb2d8d7 修复施工：九项裁定 + F1~F6 + F2b 三轮迭代 | ✅ 三轮迭代全部落地，详见主线台账本日「第四轮 GUI 点验结果 + 重试双发根治落地」行 |
| 2026-07-31 | Codex（codex-rescue）复核×1 + 续会话复验×2 | cb2d8d7 diff 对抗复核 | ✅ NEEDS-FIX 五条→复验后一条（F2）→F2b 收口后零条收敛；F2b 收口由编排者 diff 亲验闭合 |
| 2026-08-02 | Workflow（6 读手并行，sonnet） | T-30 批2 开工前锚点侦查（三规格 file:line 与类串 vs 现 HEAD 全量点验） | ✅ 全量命中；三处关键漂移（ModelSelect min-w-22 已先行/分支 addon 已放宽 max-w-48/user 气泡已 text-markdown）直接修正施工任务书；执行计划 T-30 行陈旧亦为其发现 |
| 2026-08-02 | deep-reasoner ×2（S2 形态主体/S6 三文档）+ fast-worker ×4（S3 argKind/S4 域映射/S5 仓外复核/S7 a09） | S1~S7 全序施工（S1 token 层编排者亲改） | ✅ S2 九项偏离全采纳且自抓 sm:size-7 响应式泄漏与 tailwind-merge 吞类两真缺陷；S6 以 SequenceMatcher 自证 A07 纯插入并**戳穿 S4 虚报**（EmptyTitle 实未迁，编排者亲修 + layout/ 8 覆写点防线）；S5 判据克制（列表行主文本 sans 按 S14 先例，存疑记录不改） |
| 2026-08-02 | Codex（codex-rescue）×3 | T-30 批2 diff 对抗复核 | ⚠️ 前两次未产出（task-msc025pm 跨进程重启失联/task-mscss2rd 拿到已清空的 scratchpad 任务书路径，主动 cancel），第三次经 companion 直发成功（task-mscsu25q，9m27s）：4 断言真值 major + 4 minor + 1 note，运行时/冻结区/删除面/诚实性四攻击面判净——「测试绿但接不住反例」类缺陷的系统性猎捕是其本轮独有贡献 |
| 2026-08-03 | Opus deep-reasoner（复核轨重放，首派随进程重启被杀） | T-30 批2 diff 对抗复核（与 Codex 互不见） | ✅ 2 major 均为 Codex 未见：pill 守卫看错信号（textarea field-sizing-content 长高不经 hasExtras）/合并菜单假勾（含测试锁盲区）；Grep/Glob argKind 与 Codex 相反裁定获编排者采纳（混合串禁整体 ident）；layout/ tabular-nums 爆炸半径边界与 preflight 第二 mono 来源为其独立贡献 |
| 2026-08-03 | deep-reasoner（修复批 A+B）+ fast-worker（修复批 C） | 双轨复核 findings 修复（2+4 major/8 minor/注释批） | ✅ 全部反例证红→修复证绿自证；pill 改 rounded-[21px] 并加 ×2===total 交叉断言；菜单未知 selection 前插真实行（null 回退保留有注）；A5 文件级合取检查新增带 reason 守卫的独立豁免清单（SessionManagerView 合法合取经 JSX 层级人工判定入档） |
| 2026-08-03 | Workflow（4 路并行，3 sonnet 定位 + 1 opus 工单） | 第五轮反馈四路诊断 | ✅ 全部命中 file:line 根因；D4 工单含通路排除表与 2.5d 估算，直接充当施工规格 |
| 2026-08-03 | fast-worker（D1+D3）+ deep-reasoner ×2（D2/D4） | 第五轮修复批一轮 | ✅ 三线零冲突并行（同文件分区编辑纪律）；D2 语义决策全部入注释；D4 六项偏离全采纳（take 语义白名单/presentational 菜单/toArrayBuffer 子视图安全等） |
| 2026-08-03 | Opus + Codex 双轨独立对抗复核（Codex companion 直发 task-mscwpplj） | 修复批一轮 diff 复核 | ✅ 互补性强：Codex 系统性猎捕断言真值与 TOCTOU；Opus 独有 file:copy 确定性绕过、归档成功不移行、eager 登记留痕、读盘预算缺口；四大正面攻击面双轨判净 |
| 2026-08-03 | deep-reasoner ×2（安全线/会话线） | 修复批二轮（1 blocker+8 major 闭环） | ✅ 安全线 fd 绑定 + 身份先于大小裁定 + 读循环抽纯函数使读盘次数可断言；会话线 dismissedSessions 叶模块防环 + A6 反例对照测试（不过滤即复活）自证断言真咬 |
| 2026-08-03 | Opus + Codex 双轨独立诊断（互不见对方结论） | 第六轮 Bug B（失败重发双显）根因诊断 | ✅ 双轨独立同判「一次真实发送 + 重放/live 未对账的显示层复制」（第四轮缓期 A5 兑现）；铁证同源（中断标记字面量全仓零命中→必来自重放）；修复算法分歧（Codex 水位 vs Opus 覆盖走查）编排者裁定采 Opus——后被复核证伪为「合取而非二选一」，D3 误判更正入档 |
| 2026-08-03 | fast-worker（Bug A 施工 + Bug A 侧测试）+ 编排者（红线/叶模块/store 测试亲改） | 第六轮修复批施工 | ✅ Bug A derive 短路零偏差；Bug B 测试套件首派撞会话限额身亡后编排者亲写；红线 chatSessions.ts 挂接与 historyReplayMerge 叶模块均编排者亲改 |
| 2026-08-03 | Opus + Codex 双轨独立对抗复核 → Codex 终验 | 第六轮修复批两轮复核（fd55a26） | ✅ 首轮双轨同一 blocker 独立命中（v1 走查无时间边界可吃新消息，Opus 四探针实测/Codex 竞态推演）+ 互补 major（B2 游标单调性/B4 子目录同因饿死/M3 双身份 first-wins/折叠丢附件与卡片）；终验轮再抓锚消失回退丢失 + sendAndWait 绝对计数谓词两真伤；全部处置，三重护栏 + 谓词基线收敛，测试 1537→1593 |
