# Plantree — 规划树入口（本仓唯一规划入口）

> 建立：2026-07-24（双轨合一后规整）。规划工作流遵循 plan-tree 规范：
> **当前活动状态**只看本树；历史证据与已拍板决策看台账档案（链接见下）。

> **可视化入口**：[`dashboard.html`](./dashboard-2026-08-06.html) — Phase、关键线路、任务看板、人工点验、阻塞与未决问题的单页聚合视图。Dashboard 是导航快照，状态权威仍是本树 Markdown。

## 阅读顺序（resume 时）

1. 本文件（注册表）
2. [`baseline/`](../baseline/README.md)（项目全局事实：模块图 / 运行时流 / 存储 / 门禁 / 风险）
3. 目标计划的 `README.md` → `implementation-status.md` → `roadmap.md`
4. 需要时再进 `open-questions.md` 与台账档案

## 活动计划注册表

| Plan | Status | Current Phase | Last Landed | Next Target |
|---|---|---|---|---|
| [openchamber-chat-refactor](../plans/openchamber-chat-refactor/README.md) | In Progress | **Phase 0 正式 Go + 0A 收口 → 意外发现小批已落地（`5dab201`）→ multi-agent 切片 5 主攻** | **2026-08-15 意外发现小批四件全修 `5dab201`**（button 嵌套 render prop / runtimeEvent 引用计数总线 / 六处延迟卸载 / dark: `@custom-variant`；vitest 3357 + 变异 7/7 + CDP 复验四查全过）；同日早前 **加密机现场 T-11 六项全过 → CP5，Phase 0 转正式 Go**（含 ⑥ TSD 白名单实证，open-q #7 关闭）；同日拍板批：开发线主攻 multi-agent 切片 5 · 三条意外发现即修小批 · **D36** Linux 包捆随包 node（转施工票）；此前同日 test.4 用户真机初验通过（安装版正常；portable 慢启动归档为已知限制）+ CI 出包跑通（run 31861599547，`0d4011c` 根治 Windows cpSync filter 失灵）；此前 2026-08-14 单日四批：流式 D32/D33（vitest 3268）→ D34 UI 反馈八提交（3329）→ D35 diff 四调（3339）。逐批明细见 [implementation-status](../plans/openchamber-chat-refactor/implementation-status.md) / [roadmap Done](../plans/openchamber-chat-refactor/roadmap.md) / [主线台账](../../plans/ledger-claude-mainline.md)；2026-08-05 前活动状态原文见 [history 归档](../plans/openchamber-chat-refactor/history) | ① **multi-agent S3 施工线已全落（切片 6 于 2026-08-15 收口）→ 下一件用户登录管理已立项 D47**（见该 plan 行）；② 用户线余量按用户节奏：CP2 = T-10 深度回归（现场操作单七项/全量 25 项/流式观感/D34-D35 逐项/包装器）+ GUI 点测批（T-03/06/07/18/20）+ T-21 目视；③ 拍板议程已全部出清（2026-08-15 四轮：D36~D44 + D31 建议序，见总台账检查点）；并行残留：T-04 网关阻塞 · open-q #15/#22/#23；④ **0820 使用反馈批已立项（2026-08-19，D53/D55）**：FB1~FB11 [分诊档](../../plans/2026-08-19-usage-feedback-0820-triage.md)（bash 污染归用户机 .bashrc 编码，本仓不改码）+ [规格 rev.1](../../plans/2026-08-19-fb-batch-spec.md)（五片 ①∥⑤→②→③→④，29 发变异）**双轨双盲评审已重跑收口（2026-08-23）**：两轨判语均「修订后开工」，合取 **3 blocker · 10 major · 7 minor = 20 条修订项**（[仲裁档](../../plans/2026-08-19-fb-batch-review-arbitration.md)，A 轨 2B8M6m / B 轨 1B7M2m，互证七组）；两条 blocker 为 A 轨独有：**§6.3 permission join 缺 `resolved` 守卫会抹掉全仓唯一 Allow/Deny 入口致回合死锁** · **§4.5 把 F456/D50 保留给用户的 D3-b 降级否决权写成施工方点验分支**；第三条由 C-1 互证升格（G1 整行触发器 × D55 ① 合并 ⇒ `<button>` 套 `<button>`）。§12.2 五条已合取收口（Q2 落点推翻改抽 `TurnTextItem` · Q8/Q9 同意 · Q12 取 E1 + 三附加条件 · Q13 改判 G1′）。**rev.2 已定稿（2026-08-23）**：20 条逐条落实，新增断言 14 条 + 变异 6 发（29→35），修订记录见规格 §14。**剩余出口两条**：① ~~E-1 取证~~ ✅ **已完成（2026-08-23）并推翻 rev.1 因果**：katex JS 被 Vite 完整打进产物、零 `node_modules` 运行时引用；`!node_modules/katex/**` 与渲染无关，`electron-builder.yml` 一字不动；`[FB9-5]` 改为产物级断言「产物中无 KaTeX 字体」、M-29 退役由 M-33 顶替（规格 §8.5-a）。**片④ 已解除阻塞**；② **Q14 待拍板** —— 用户裁定「先按 C1 施工，等 GUI 出图后再定」（判据 G-6，属 F456/D50 保留给用户的 D3-b 否决权）。⇒ **片① 已落地（2026-08-23，`d704f7dd`）**：FB8（`formatThoughtRow` 复用 `formatWorkedForDuration`，1702s→`28m 22s`）+ FB1-a（`splitClosedPrefix`/`advanceClosedPrefix` 两纯函数，未接线）；scoped vitest 128 例 / 2 文件全绿 · typecheck 0 · lint 0；变异 M-01/02/03/05/24/25 咬红复绿。as-built 五条偏差见规格 §15，其中最重一条：**`[FB8-2]` 的纯行为断言实测空转（M-24 存活）**，补源码半边后才咬红 —— 两轨评审均未抓到（评的是规格文字）。**片⑤ 亦已落地（2026-08-23，同笔 `d704f7dd`）**：FB2 代码块 copy（wrapper 挂载 + 复用 `turnCopyButtonClass()` + `/60` 弱化档）+ FB3 气泡展开开关（`userBubbleTextClass(expanded)` + `useState`）；vitest 164 例 / 3 文件全绿 · typecheck 0 · lint 0；变异 M-06~M-10 咬红复绿；同批更新两条既有承重断言（`userBubbleTextClass(expanded)` · `[D3-8]` 三子元素）。as-built 三条偏差见规格 §15，其中**负向源码断言必须剥注释**（头注写了禁用词导致自伤假红）对 `[FB9-7]`/`[FB9-3]` 直接有效。**片⑤ GUI 点验已做（2026-08-23，G-3 全过 / G-4 功能过）**，并当场修掉两处对比度不达标：copy 按钮 `/60`→`/70`（亮色 2.76<3 不达标），`Show more` 文本改回不带档位（13px 文本要 4.5:1，`/60` 只有 2.62）。**Q15 已拍板（2026-08-23 = A 维持恒显）**：展开控件在提问未被截断时仍常显（实测 `clientH===scrollH===146`），用户看图后裁定接受该观感，`UserBubble` 内**明令不得**引入几何读取。G-5 未测（构造不出贴底跟随态），留待片③ GUI 批次。**G-9 deny 实测已完成（2026-08-23，真机 CDP）并推翻规格推测**：被拒工具**有**完整 tool_call block（可展开、含输入与 `User denied permission` 结果），决议行独立同构，两行均已 `text-destructive`，回合头计入 `1 edit` ⇒ §6.5 布局设计成立、**Denied 同样走合并**（非「几乎不出现」）。触发规格 §6.7 所留的「追加一轮形态设计」⇒ **规格升 rev.2-b**：新增 §6.5-a（发现仓内早有**双臂派生**范式 `toolCard.ts:818-826`，G-8 档位收窄为「动词档继承行色 / 参数档双臂三元式」二选一，**禁单臂硬编码颜色**；徽记不负责制造红色，**join 不得改写 `view.failed`**）· `[FB7-1]`/`[FB7-2]` 夹具双臂化（Allowed + Denied，Denied 臂另钉三种载体）· `[FB7-7]` 口径订正（D24 约束实质是「无图标/无边框/无背景」而非「必须是灰的」）· 新增 M-36/M-37（零跳过 34→**36 发**）。同轮取得 **blocker A-1 的真机铁证**（未决态下工具行与 Allow/Deny 卡紧贴）+ 登记一张另立票（**被拒操作的动词与计数是完成式** —— `Edited` + `1 edit`，FB7 合并后会读成「`Edited …` · `Denied`」自相矛盾）。**⇒ 片② 已落地（2026-08-24）**：`toolCard.ts` 新增 turn 级 join 层（`joinResolvedPermissions` / `countPermissionRecords` / 两个徽记类装配）+ `ToolRows.tsx` 尾部徽记 + `chatTurn.ts` 接线；chat+stores **87 文件 / 2138 例全绿** · typecheck 0 · biome 161 文件 0；**变异 8 发（M-19~M-23 · M-30 · M-36 · M-37）8/8 咬红零存活**，md5 对账还原。as-built 七条偏差见规格 §15，其中最重两条：① 规格判「必红退役」的核心承重断言 `toolCard.test.ts:166-174` **实际未红且保留** —— join 落 turn 层、`groupTimeline` 仍在 message 层 flush，该命题在它那层依然成立；③ **join 必须额外缝合被吸收 permission 两侧的工具组**（规格只写「不再 flush」，但 flush 在下一层，turn 层只能事后缝合，且须严格限定否则误并两条 message 的工具组）。另新增 `[FB7-10]`（带决议的 run 不参与聚合）与导出 `countPermissionRecords()`（守恒律的唯一计数口径）。**G-8 已做（2026-08-25，真机 CDP 三次授权往返 + 亮暗双主题）**：合并生效、**档位裁定「动词档继承行色」确认通过**（徽记两臂同为 `shrink-0` 零颜色类，实测解析色与动词逐位相同 —— 亮 Allowed `oklch(0.4531 0.005 91.5)` / Denied `oklch(0.5042 0.1648 27.84)`，暗 `oklch(0.6956 …)` / `oklch(0.597 …)`），**让位行为通过**（超长路径下参数 `scrollWidth>clientWidth` 截断、徽记 62px 完整、动词 44px 完整）。未覆盖：`auto:` 臂与 `Denied, turn stopped` 最坏宽度（真机构造不出）。⚠️ **新发现另立票：授权记录不入 rollout ⇒ 恢复会话后审计面归零** —— 同一条 deny 回合 20h 后回放，工具行在、`permission_request` 块整个不在；**非 FB7 引入**（合并前同样丢失整条决议行），但意味着「谁批准了什么」重开会话即查不到，而 `defaultTurnProcessOpen`/`hasUnresolvedPermission` 都建立在它可见之上。**G-5 仍未测**，留待片③ GUI 批次。**片③ 已落地（2026-08-25，`871e8040`+`a13046ec`）**：FB4 正文恒在折叠壳外按块序渲染（修掉「以 error notice 结尾 ⇒ 整段正文被折进 process」）+ FB6 head 移底与 footer 合成单条 meta 行；FB6 走完 §5.6-a 三步证据链（此前**全仓没有任何断言钉住 `ChatTurn` 的子元素顺序**）；变异 12 臂全红。⚠️ GUI 抓到两个断言抓不到的缺陷：① meta 行折叠钮**曾是死控件**（四条 aria 断言全绿在一个按了没反应的按钮上）；② 规格裁定的 **E1 方案根本不工作**（读 Base UI 源码坐实：`setMounted(false)` 的两条路本片都不满足），改用规格已列的 E2。**继而 D56 用户拍板（2026-08-25）**：Q14 = C1 可接受（判据订正为「条理/逻辑/可读性」而非数边框盒）· **回合级折叠整体退役**（meta 行只留信息）· **工具行默认折叠**（推翻 sign-off ②）—— 授权红线由「条件」升为「结构」，落地 `4a1c8402`。**片④ 亦已落地（2026-08-25，`f57387fd`+`65ee39be`+`e0a3c136`）**：FB1-b 渐进 markdown 接线（开工前微基准实测：不分段 6379ms vs 分段 165ms，**38.7×**；扫描器单次最坏 1.71ms）+ FB9 块级 LaTeX（`output:'mathml'`，**零字体资产**，安全走渲染级七臂攻击）。G-10 实测发现分数线坏 —— 根因是 **Chromium 的 `math` 泛型族在 Linux 上经 fontconfig 无 `math` 别名**，补 `--font-math` 系统回落链修复（不破字体红线）；**D57 用户拍板接受 MathML 排版**，不转 KaTeX HTML 输出，矩阵括号不拉伸另立票。**⇒ 0820 批五片全部走完**，全量 245 文件 / 4962 例全绿。⚠️ **本批最该读的收口是规格新增的 §16 空转臂总表**：施工期实测出**六发**「断言全绿、变异存活」的同型空转臂，三条可复用判据（断言必须与变异同层 · 能写白名单就别写黑名单 · 属性齐全≠活着）+ 一条连带纪律（负向源码断言必须剥注释，本批撞了三次）。**未做的点验**：G-5 贴底跟随态 · G-7 band 下第一元素间距 · G-14 PendingTurnHead 相邻 · G-2 帧率观感 · G-11 打包产物公式（本机跑不动全量构建，`assert-no-webfonts` 门已在 `dist:prereq` 里）；暗色主题图未出。**D48 草稿期权限档位票已落地（2026-08-25，`9d8cb186`+`60a6aeac`）**：零回合就能定这次聊天开在哪一档（三层优先级 回声 > 草稿意图 > 模板；危险档二次确认不因「还没会话」豁免；D11 红线被既有断言拦下一次，改由 Composer 传 prop）。变异 8/8 咬红，全量 4978 例绿。⚠️ **未走双轨双盲**（用户定的优先级是功能优先）、**GUI 实测未做**。顺带修一处一致性缺陷：Codex 草稿的「另一根轴没选过」补全，我自造了第二个答案（半档位等 partner），改为复用仓内既有的 `MOST_RESTRICTIVE_*` —— 与设置层同口径；根因是「模板层与实时层逐条同口径」只写在头注、零断言，已补三条钉子。**下一件待定**（见 [inbox](../ideas/inbox.md) 与 2026-08-25 盘点）；G-4 另需点验「短提问下展开控件常显是否可接受」；D26/T-31 三处设计改判随批记档（[FB7 取证](../../plans/2026-08-19-fb7-merge-forensics.md)） |
| [multi-agent](../plans/multi-agent/README.md) | In Progress | **阶段 3 Codex CLI 选择功能 D48 四切片全落（2026-08-17 收官）→ 线协议完整性小批已落（2026-08-18 `55b45b1`）→ 阶段 4「2b 打包链」施工收口（2026-08-21：P1~P4 全落 + C7/C8 达成 + 复审 13 条全清 + 收口验证 run 32452661779 七作业全绿）→ **codex pin 升级票收口（2026-08-26：0.145.0 → 0.149.1，D54 ② / 规格 §11-Q7 关闭）+ D58 Codex 默认打开、`AICLIENT_AGENT_CODEX` flag 退役** → 下一阶段 = pi 第三后端评估 spike（候选）**（D48 单日半立项到收官：八项拍板 + 调查七篇 + 双轨双盲规格 rev.3 + S1~S4 四批施工，vitest 3973→4567，变异 80+ 臂零存活） | **2026-08-15 S3 切片 6 收口（规格 `e281435` + 施工 `81a130b`，off/on 双轮 vitest 3482 全绿）**——#7 HostAgentRegistry 全链 + #8 idle sweep 与回收后续聊（`session_revive_failed`）+ U8 三态截图判不挤 + G13 真机恢复 PASS；双轨双盲评审（Opus 4b+10M+5m / Codex 2b+4M+2m 合取）在写码前拦下 revive 失败码撞渲染端兜底、零回合无 rollout、registry 收敛断链三类返工，详见 [切片 6 spec](../../plans/2026-08-15-s3-slice6-closure-spec.md) §9/§11。同日早前 **S3 切片 5 历史收口（5a `1aa68f2` + 5b `61bcd0d` + 收尾 `4d61730`/`ae3398e`，vitest 3418）**——U2-a 真实回合四推翻（id 判死 / 重投影丢 reasoning·exec / resume 从 config 重派生权限→H9 双层重申真机双臂实证）；规格 [切片 5 spec](../../plans/2026-08-15-s3-slice5-history-spec.md) rev.2 双轨双盲合取 + as-built；P1 扇出砍 / P2 文案改（用户拍板）。此前 **2026-08-10 S3 切片 4 权限投影 `7f357c2`**（规格/夹具先行 `35b4594`/`1ae2abc`；四门全绿 **vitest 152 文件 3160 例 0 红**）——取证重生成契约在写规格前推翻 S2 三处、抓出 method-contract 快照一多一少两处错；双轨评审推翻 rev.1 十三处（含 `grantRoot` 会话级写权 blocker——被 rev.1 自己的夹具归约藏掉）；施工 16 处变异验证；复核 major（decision 发射链零覆盖）以三个接线 pin 收口；as-built 偏差见规格 §7。此前：**S3 切片 3 提问桥 `4b468f4`**（`codexQuestionBridge.ts` + `pending.forget()` 前置修复 + 渲染端 id 键与 `isSecret` 掩码；四门全绿 **vitest 146 文件 2914 例 0 红**）。动工前双轨对抗评审**在写代码之前推翻规格三处**，并抓出「设计档数字 ≠ 仓里证据」（夹具只有 2 条入向报文 / 5 颗问题，非 S2 写的 4 条 / 10 颗）。此前：切片 2c `8b0277f` · 2a `84ae4e1` · 0/1 `0314216`。逐批明细见 [roadmap](../plans/multi-agent/roadmap.md)；长叙事原文见 [history 归档](../plans/multi-agent/history) | **用户登录管理 D47 全链收官（2026-08-16 真机双步过：flag-on 冷启动零重登收编证据，分发纪律已解除）→ 阶段 3 Codex CLI 选择功能 **D48 已立项（2026-08-16）**：范围 = 全套 codeg 形态（入口 + 模型/思考档按 agent 适配 + 权限管理面）· 绑定 = 零回合可选物化后锁定**（阶段 2，**D47**；[规格 rev.2](../../plans/2026-08-15-login-management-design-spec.md) = 四路调查 + Opus/Codex 双盲双轨合取 + U1~U4 拍板收口；幂等假定经 onboard 服务源码 CONFIRMED，S7 删除）→ Codex CLI 选择功能（阶段 3，stage-3 picker 是 `capabilities.agents` 首个 UI 消费者）→ 2b 打包链（阶段 4，体积已拍板）。切片 5 遗留 L1~L7 中 G13 真机半边已随切片 6 了账；切片 6 新增 L8~L11 见[规格 §7](../../plans/2026-08-15-s3-slice6-closure-spec.md)。未决：#4 git 参照点 · #5 模型目录半边 · **L1 提问坞单槽（agent 无关，另立）**（ACP 两裁定已升格 **D45/D46**，2026-08-15 拍板）；**2026-08-18 三项拍板（dsh/pi 调研收口）**：① **线协议完整性小批已落地（同日 `55b45b1`）**——U+2028 三处 readline LF-only 对称修 / `contextWindowExceeded` 分类+定向文案 / 未知 status 告警 / phase 透传建模；commentary O1 取证结论 = Codex TUI 同路径渲染、维持现有行为；scoped vitest 1103 全绿（F8 维持另立不并批） ② pi 第三后端评估 spike 立为**阶段 5 候选**（打包链后实跑验证） ③ F7 规格须引用 pi 会话树设计——详见 [调研档](../../plans/2026-08-18-deepseek-harness-study.md)；**2026-08-19 阶段 4 立项收口（D52）**：三路摸底 + REQ 清单落 [立项档](../../plans/2026-08-19-stage4-packaging-kickoff.md)，拍板 = D36+D41 并入本批 · mac 不纳入（win+linux）；**规格 rev.2 定稿 + P1 施工中（2026-08-19 WIP `5d69e1b`）**：双轨双盲 29 条合取（`6383bf6`，A 轨 4B9M + B 轨 6B5M，唯一分歧 code-mode-host 按合取保守保留）+ §11 十项收口（**D54**）——[规格](../../plans/2026-08-19-stage4-packaging-spec.md) **P1 已收口（`271ebfb`，2026-08-20）**：codex pin + lock 重生成（双跑过）+ 两纯模块 + **CLI 外壳接线**（外壳 311→181 行，preflight/shouldCopy/verifyArtifact/prune 等七份重复实现删净，库 throw 经 `guard()` 映射 exit 1）+ **单测 68 例全绿**（A1/A2/A2b/A2c/A3/A4/A5/A6/A8/A9；期望值手写字面量，200MiB 门槛用稀疏文件真跑，静态断言钉住「只 import 库不 import 自执行外壳」）+ **变异 7/7 逐对咬红**（M1/M2/M3/M4/M5/M13/M14，改回即复绿）；真实 `npm ci` 的 `@openai/codex` 树承重假设逐条实测通过（hoisted 布局 / 别名版本 `0.145.0-linux-x64` / manifest `entrypoint`·`pathDir`·`resourcesDir` 三键 / 二进制 296.3MiB mode 775 / code-mode-host 44.0MiB / 外平台零命中），与 §0.3-A 一致。**A7 真产物验收已补跑通过（2026-08-20，装齐 build-essential 后）**：干净 `npm ci` 编出 `node-pty/build/Release/pty.node`，真实 `build:agent-host` 零 stub 出件——esbuild bundle 342KB · codex.js 随包 · manifest `0.145.0`/`x86_64-unknown-linux-musl`/`entrypoint=bin/codex` 全匹配 · 入口二进制 296.3MiB mode 775 · 外平台零命中（`@openai/` 下仅 codex 与 codex-linux-x64）· node-pty 原生二进制随包 · `.bin` 未混入；产物 398MB。**P1 四项至此全部完成。**A2c 的 `NODE_RUNTIME_PINS` key 集合半边按规格随 **P3-C9** 落（现仍是单平台 `NODE_RUNTIME_PIN`）。**P2 已落地（`c766ce11`，2026-08-20）**：随包 codex 路径注入（`deriveBundledCodexJsPath` 从 Host 入口推、不写第二遍 `resourcesPath`；`codexJsPath` 条件展开——与 D47 三键范式**刻意相反**，凭据键要压继承污染故恒在，路径键是逃生口故省略）+ `resolveCodexJsPathForEnv` 收拢三判据（`statSync().isFile()` 非 `existsSync`）+ AgentInstaller 五写侧入口平台闸（`installAll` 放 try 内保住 `InstallResult` 契约，探测面一律不加闸）；B1~B7 新增 29 例，变异 5/5 咬红（M6/M7/M8/M15/M17）；红线 `codexNodeEntry.ts`/`NodeRuntimeResolver.ts` 零改动经 git diff 核验（B8）。**P3 本机半边已落（2026-08-20，五笔）**：`f2d9147` pin 多平台表 + fetch 参数化（linux-x64 SHA-256 取自官方 SHASUMS256.txt 并经真下载二次校验，非编造）· `2e33aec` afterPack 双平台 + verify **独立硬断言**（改判 ④，不并进 node24||bundled 或）· `b8cfe15` **Linux 撤 bundled Node 闸（独立可 revert，存量用户运行时替换的唯一开关）** · `9218822` REQ-5 死规则清除 + CI 双平台 fetch + **D41 门禁**（两条 needs 边）· `485decc` format 清债。断言 C1/C2/C4/C5/C6/C9 共 20 例，变异 6/6 咬红（M9 · C9 表漂移 · Main↔构建侧 outName 漂移 · C5 摘边 · C4 规则形式 · C4 注释形式——后者证明只有原文扫描咬得住）。**A2c 另一半随 C9 一并了账**。**未完成（须真 CI）**：C7 win+linux 双作业绿 · C8 门禁红轮实证 · timeout-minutes 占位值待按 ceil(3N/10)×10 回填。**~~已知阻塞：3 个 noAssignInExpressions lint 债~~ 已解除**（`bb4a14df` readline 行切分改写，全仓 biome 0 红，D41 门禁 lint 步可绿；复审节点 6 已核验三处改写语义等价）。**P4 本机半边已落（2026-08-20，三笔）**：`b62dfba` 体积门禁纯模块（分项余量 h1/h2，上下限双边，四点真值表 18 例；win32 预算显式 null 报 PENDING 不计通过）· `ecfdfab` 打包后结构断言（§3.6 全表搬运共用同批符号 + D1 魔数 + D2 TSD 扩扫拆掉恒真 exists 包裹 + D3 exec 位 + D7 top-10 breakdown + D9 isFile）· `a377451` 零额度两级 smoke（S1 版本全等硬门禁；**S2 linux-x64 已转硬门禁**——本机拿到真实 initialize 回包，codexHome/platformOs/干净退出三条全过）。真产物负控七臂全部咬红并指名：hoisted/nested 外平台 · codex.js 同名目录 · 截断二进制（四条同时红，含总体积下限 91.4MiB）· 80MiB 超标（breakdown 点名元凶）· pin 漂移 · launcher 损坏（红而非跳过）。**未完成（须真 CI）**：C7 win+linux 双作业绿 · C8 门禁红轮 · win32 的体积预算与 S2 帧形状回填 · timeout-minutes 占位回填。**本机环境已补齐**（build-essential + electron + ripgrep），四门首次全绿：typecheck 0 · lint 987 文件 0 · **vitest 4864/4864**。**P4 复审修复批已落（2026-08-20）**：外部只读深度审查 13 条经逐条复核**全部成立**，本批修六项、挂账七项。① **S2 `exitClean` 假绿**（最严重）——`close` 监听器未接 `code`/`signal`，任何退出都判「干净」，Linux 硬断言实际只证明「close 发生过」；抽 `scripts/codex-smoke-lib.mjs` 纯模块（`isCleanExit`/`describeExit`）+ 7 例负控（非零码 / 信号 / 无状态 / code0+signal 并存），真产物现打印 `clean=true (code 0)`。② **Windows Q1「先观察后门禁」原本零机制**（preflight/200MiB/manifest/S1 全硬，CI 无 continue-on-error）——新增 `--observe`，软化范围严格限于规格 §11-Q1 四项零证据项，且**开关权在 `CODEX_MEASURED_PLATFORMS` 记名单**而非 flag：linux-x64 在册故 flag 恒无效（实测打印「--observe ignored」），win32 回填后自动转硬门禁；另加 `scripts/inspect-codex-payload.mjs`（恒 exit 0）在任何门禁前打印平台包逐文件字节 + manifest 原文。③④ **packaged verifier 版本自证 + 承重文件缺口**——期望 pin 改从仓库 `src/agent-host/package.json`（或 `--codex-pin`）取，三层版本各自硬钉（主包 / 平台包 `<pin>-<key>` / manifest.version），补主包与平台包 `package.json` 两个 `require.resolve` 承重断言，`pathDir`/`resourcesDir` 由 `existsSync` 改 `isDirectory`。⑤ **REQ-7 接线**（原本 workflow 步骤本身缺席）——win/linux 各加 agent-host npm 缓存（键 = `package-lock.json` 哈希）+ npm cache 字节 + 出包前后磁盘（`Get-PSDrive C` / `df -h /`）。⑥ **预算可分解**——`verifyArtifact` 返回 `codexPayloadBytes = dirSize(@openai)`，OK 行前打印 `baseAgentHost=42,883,148B codexPayload=363,716,282B`（P 与既有 linux 预算逐字节相等，交叉验证通过），win32 首跑即可直接回填。**真产物负控五臂全咬红并复绿**：主包 package.json 缺失 · 平台包 package.json 缺失 · **整套一致漂移 0.147.0**（三条版本同时红，且在 `--skip-codex-smoke` 下成立 ⇒ 结构断言独立于 smoke）· `codex-path` 被同名普通文件冒充 · observe 在 linux 无效。单测 106→125（+19），三处变异（`isCleanExit` 恒真 / observe 忽略名单 / payload 退化成入口二进制）逐个咬红后复绿。四门：typecheck 0 · lint 991 文件 0 · **vitest 4883/4883**。**本批挂账（未做，非阻塞首跑）**：#7 appDir 次门禁 warn 缺失（规格 §6.3 允许升硬门禁但未允许省略）· #8 `b8cfe15` 已**不可**一笔干净 revert（`485decc` 改过其新增测试，反向补丁实测 `patch does not apply`；应急回退改为只恢复 `getBundledNodeRuntimePath()` 的 Windows 闸）· #9 `dist:prereq` 按宿主平台备料 vs afterPack 按目标平台取件（CI 原生 runner 不触发）· #10 fetch 离线提示不可执行（重跑先删 archive）· #11 code-mode-host 因果闭环未取证 · #12 npmmirror 验活未回填 · #13 三处文档漂移。**阶段 4 CI 半边已收口（2026-08-21）**：C7 于硬门禁状态全绿（run 32444722264）· C8 两次自然红轮实证 · win32 四项实测回填（A0 91,535,424B / P 427,157,004B / codex.exe 342.6MiB / S2 帧）· Q8 维持 `maximum`（Build Windows 单步 2m12s ≪ 40min）· REQ-7 as-built 齐备。真 CI 另抓两条只有 Windows 能暴露的缺陷：gate 的 Node 20 缺 `--experimental-strip-types`（`b5c79b6c`）、S2 的 CODEX_HOME 撞 8.3 短路径（`aaba131f`）—— 后者是 §6.2 两步走的价值实证。 **codex 升级票已收口（2026-08-26，0.145.0 → 0.149.1）**：票写 0.147.0，执行时已落后两个 minor，**用户拍板取 npm `latest`**。契约 diff **纯增量零删除**（clientRequest 126→150 · serverNotification 70→75 · serverRequest 11 与 threadItemTypes 18 连生成顺序都不变 ⇒ `CODEX_METHOD` 零改动）；四份快照重生成，turn/settings 逐字不变、approval 只多一个 legacy 变体、question 是唯一动形状的一份（`ToolRequestUserInputParams` 新增 **required** `isBlocking`，`autoResolutionMs` 被 deprecated）；blessing 在新版 PASS 且生成器未动故 blessed fixture 逐字节不变；`.jsonl` 报文夹具刻意留在 0.145.0（真实抓取不可再取）。四门全绿（typecheck 0 · biome 995 文件 0 · **vitest 247 文件 5007 例**）· 变异 7/7 咬红 · md5 对账还原。⚠️ **三条带走的教训**：① **升级票是四件套** —— 必须重设体积预算，codex 缩了约 40MB 而 §6.3 预算是双边的，旧 `codexPayload` 下 floor 365,854,456 vs 真产物 365,843,830，**差 10,626 B 就把正确产物判成「codex 没进包」**；② **win32 的 `P` 不需要 Windows runner** —— 解包上游 tarball 按 `shouldCopy` 手算，该法对 0.145.0 反算可逐字节复现 CI 实测值；③ **A/B 实测推翻一条 `[实测]` 注释** —— `thread/items/list` 带 `experimentalApi` 打过去在 0.145.0 与 0.149.1 **都不是 -32601**，注释从写下起就错（非本次引入），已订正并把分页水合登记进 inbox（与 F7 同族）。**未做**：Windows runner 真产物复验（`P` 为反算值）· GUI 真机回合点验 · 未走双轨双盲。落账见 `src/agent-host/PINNED.md` 与规格 §0.3-A-bis / §3.1 / §11-Q7。 **同日 D58（用户拍板「直接打开就行了，要什么设置按钮变量的」）：Codex 默认打开，`AICLIENT_AGENT_CODEX` flag 退役** —— 四闸降三闸，可用性只由「关于这台机器为真为假的事实」决定。退役的决定性理由是**这开关用户够不到**：flag 只经 `{...process.env}` 继承 ⇒ 只有从终端启动 Electron 才设得上，桌面图标/Dock 启动不读 `~/.bashrc`，而 `hostEnv.ts:26` 刻意不注入、设置层无对应开关 ⇒ Linux/macOS 打包用户物理上开不了。连带行为变更：`prepareHome()` 不再被短路，每次启动都建隔离 home 并投影 `~/.codex`（只读不写、幂等）。零额度实证：`host.ready` 广播 `capabilities.agents = ["claude-code","codex"]`。四门全绿（vitest 247 文件 **5003** 例）· 变异 2/2 咬红。**未做 GUI 真机跑一轮 Codex 对话。** |
| [unified-credentials](../plans/unified-credentials/README.md) | **In Progress** | **S0' / S1 / S2 / S3 / S4 全部完成；2026-08-28 S4 随 Pi Phase 5 落地（可选 vault pi arm + 兼容回退）** | **恢复工作先读 [implementation-status](../plans/unified-credentials/implementation-status.md)。** `18da2d7f` **S2** 目录改名 `.aiclient`→`.pilab/<profile>/`、凭据并入（迁移是 copy 不是 move —— 旧根没有 profile 层，move 会让后启动的构建变成「全新机器」；硬验收线「不许重新登录」真机走通）· `7785ee1c` **S0' codex 侧** 取消隔离 `CODEX_HOME`，凭据与 provider 改经 `-c`+env，用户 `~/.codex` 整棵树结构性恢复，删掉三个模块与注册表第三道闸 · `972934d5` **S0'-b** 配置加载失败报出文件/行列/修法（S0' 引入的回归，止血） · `4f1e6897` **S3+D64** 凭据模式从构建期开关变运行期状态，存进 `~/.pilab/<profile>/settings.json`，连带删掉遗留写入侧、登出清理改为无条件 · `d29fc871`+`024990f8` **settingSources** 由 `[]` 打开为 `['user','project','local']`，两份 CLAUDE.md 与用户 env/hooks/model 全部回来 · 取证档三份：[E2](../../plans/2026-08-26-s0-spikes/e2-codex-resume-and-inherited-keys.md) · [settingSources](../../plans/2026-08-27-settingsources-spike/README.md) · [废案索引](../plans/unified-credentials/topics/discarded-approaches.md)（六个废案 + 五类探针翻车，方法论比结论更容易重犯） · 四门：typecheck 0（含 agent-host）· biome 1000 文件 0 · **vitest 248 文件 5009 例** | 本 plan 已完成；剩余两条观察风险继续见 implementation-status。**⚠️ 两条未解风险**：① codex 老会话可能变成打不开 —— `approval_policy` 在 resume 时由 rollout 定死、`-c` 与 config.toml 都改不动，两条例外未复核（改过权限偏好的会话 · 没存偏好的老会话遇上常量改值，后者会让**所有**老线程一起打不开）；② SDK 文档称 `managedSettings` 会 restrictive-only 过滤而实测没有 —— 本批未用它故不阻塞，将来当安全边界用之前必须先测执行期行为。**四条已接受的代价**（拍板结果，非缺陷）：远端旧目录孤儿 · 应用内终端 codex 不走网关 · 从没登录过的老用户会被拦一次登录页 · 配置里的免问规则会让动作不弹权限卡 |
| [pi-backend-migration](../plans/pi-backend-migration/README.md) | **In Progress** | **D13 功能补齐进度：Cycle 1/2 已完成；Phase 1/2 Done，Phase 3 下一目标 T13 历史/真实 resume/分支，Phase 4 等 T16～T18，Phase 5 含 T25 已 Done** —— 同日三批：审计修复 13 项全修 + T08-c 两切片 + **T12 时间线外壳/气泡基线**；**Q9 已拍板关闭 → D11** · T08-b 真机审批链已贯通、待内联形态 · T08-c GUI 已点验但 Q10 策略命中未收口 · T09/T10 已由 D13 转 Planned（Cycle 2）〔2026-08-28 原文：Phase 2 除 T08-c 外全部 Done〕→ **T12 已 Done（用户看图确认满意 + 拍板删 meta 行）** → **T12-a 已 Done（2026-08-29，结论与原计划相反）**：评估后**不移植** pi-app 的 display-items（[D12](../plans/pi-backend-migration/decisions/012-timeline-data-model.md)）—— 真缺陷在 `piRuntime` 把一整轮 pi 运行塞进**一条**助手消息**一个**正文块，多步回合里第二段正文被无分隔符粘到第一段后面、且渲染在它其实晚于的工具行**前面**（探针实测）；下游 `groupTimeline`/`segmentTurnBody` 全程保序无辜，只是从没拿到正确顺序。不移植的依据是 pi-app **在同一处丢同样的东西**（text 块 join 成一串、thinking 另存一字段，且注释写明对齐 pi 自家 TUI），且它没有 permission/question item 与 FB7 join。实现取「只关正文流不关容器」—— 第一版关整个容器会把连续工具串切成一卡一工具。连带修同毫秒 id 碰撞（实测非理论）与重复 `message.completed`。vitest 271/5390 · 变异 4 发全咬红（**M2 首轮存活**，空转臂同型第八发）· **2026-08-30 真机后续确认正文顺序正常**。**同日 T12-b 切片 1 亦已 Done（同族缺陷）**：pi 的内置工具名是**小写**的（`read`/`edit`/`write`/`bash`/`grep`/`find`/`ls`，取自 SDK 各工具的 `name:` 字面量），而 `toolCard.ts` 四张表全是 Claude 的大写名，**一个都不重合** ⇒ 探针实测七个内置工具**全部**渲染成 `Ran`、全归 action（**工具聚合在 pi 上从没触发过**）、`grep` 显示的是路径而不是 pattern、路径按比例字体而非等宽。已补 `PI_TOOL_NAMES` 单一来源 + 四处查表（动词复用既有英文不另造方言，`ls` 归 search 不归 read）；23 例新测含防复发兜底 · 变异 6/6 咬红 · **vitest 272/5413** · 既有测试零破坏。**切片 2 同日亦 Done**：`edit`/`write` 展开后从两坨转义 JSON 换成真 diff（**自写 LCS 不装包** —— `diff` 只是传递依赖、唯一直接依赖 `@pierre/diffs` 在 `src/` 零引用；**从参数推导不从输出**，所以运行中与**被拒**的调用同样看得到「本来要改成什么」；颜色用 `--success`/`--destructive` 语义 token 而非 pi-app 的生 palette）。范围按评估**从三层改成两层**（pi-app 的声明模板层建立在它的 extension-compat 适配器层上，那是本 plan 的**非目标**）；read/grep/bash/ls 的结构化预览**刻意不做**（输出本就是纯文本）。⚠️ **被自己的测试抓到一处死分支**：兼容 Claude 字段拼法的代码被小写工具名闸挡死、永远跑不到。17 例新测 · 变异 5/5 咬红 · **vitest 274/5442** · 真机亮暗两图。**T12-c 亦已 Done（2026-08-30）—— 第三处同族静默失效**：探针查出 `piRuntime` 里 `thinking.completed` 出现 **0 次**（只发 started），而时长用 `completed − started` 算 ⇒ **pi 上每一次思考都渲染成光秃秃的 `Thought`，从没显示过时长**。根因是 pi 没有「思考结束」事件（thinking 只是 content block 停止增长），**边界得由 Host 自己定**：取「模型开始作答」与「消息先结束了」两个，**必须幂等**否则第二次会用更晚时间戳覆盖、7 秒思考被报成 60 秒。**markdown 分段：保留我们的，但这是取舍不是对方有缺陷（结论经用户追问后订正）** —— 初版判优被用户一句「差异在哪、凭什么说我们的更好」问倒，**追问是对的**：我只测了 pi-app 那个函数、没看调用方，也没验证 CommonMark 对未闭合 fence 的行为。补测：未闭合 fence **不是错误**（实测解析出的是完整合法、高亮正常、只是短一点的 code 节点）；逐行流式下 **3–9 步两边完全相同**，pi-app 只在 **10–13** 步领先，**代码块无空行时两边完全相等**。真实取舍 = 「早出但会重排」vs「晚出但稳定」，我们的代价是长代码块闭合前一直无高亮。**「保留」= 没有足够理由换**。**⇒ 同日用户拍板「按 pi-app 的来」，行为已改**：新增 `openFence`，尾巴里**尚未闭合**的代码块交给解析器渲染（真机实测 `pre` 里 42 个高亮 span），其余尾巴仍是纯文本；**但没照抄它的机制**（那会撤掉实测 39× 的分段+memo 优化）。安全性另有论证：**fence 内部是字面量**，追加不重解释已有行。⚠️ **结果反而比 pi-app 好**：他们闭合时要重切、几行从纯文本跳进块，我们**只增不改** —— 真机实测追加后块从 5 行长到 9 行而 `top` 保持 179px 不变。⚠️ 变异第一轮两发存活：一发真缺口（`openFence` 吞掉 fence 前的在途散文）已补断言，一发经核是等价变异、已删冗余判断。**刻意没取**：2 字符淡出尾巴（纯视觉、基线刚确认过）· 按 hash 四选一的随机 live 文案（稳定文案更好）。11 例新测 · 变异 3/3 咬红 · **vitest 276/5453** · **2026-08-30 真机后续确认思考时长与流式代码块正常**。**T12-d 亦已 Done（2026-08-30，含真机 GUI 十一图）—— 原定四件事核过后只有两件成立**：**① 展开记忆**（真缺口，且是 **T12-b 制造出来的新可达路径**：工具聚合在 pi 上从没触发过，修好工具名词汇表之后，探针实测「一次 read 完成 → `[block-a "Read a.ts"]`；第二次完成 → `[block-a~agg "Explored 2 files"]`」，**用户打开的那一行在顶层不存在了**，于是「我正在读一个文件的输出，agent 又读了下一个，我在看的东西就消失了」）。**只记住那一行不够** —— 它会展开在收起的容器里面，照样看不见 ⇒ 第二条规则「`detail` 里有被记住展开的孩子，本行也展开」，而行自身的显式选择永远压过它（否则聚合永远关不上）。**取种子不受控**：`defaultOpen` 只在挂载读一次，这正是保住 T-34 的原因 —— 子代理面板的 `defaultOpen: true` 会在通道不再 live 时消失，受控 `open` 会在读者读到一半时把面板拍上；而聚合吞行本来就是一次新挂载，取种子让那个场景免费成立。读取**不订阅**（订阅会让每次开合重渲染所有工具行而屏幕毫无变化）。**刻意没取** pi-app 的「自动展开运行中回合最后 N 个工具」—— 它反转 2026-08-25 记在 `ToolRows.tsx` 的用户决定。**② 底部锚点**（真缺口：两个终端早有「滚到底部」圆钮，聊天时间线一颗没有）。**两个阈值不是一个**：40px 答「还在跟随吗」（必须紧），140px 答「下面藏的值不值一颗按钮」（紧的答案会让 41px 滑动画出按钮，闪进闪出比没有更糟），中间是**真实死区**且已写成断言防「简化」成一个常量。可见性**取几何不取跟随标志**（`nextFollowState` 会在视口就在底部时报「没跟随」，绑它会在用户正看着底部时画出「跳到底部」）；位置挂在**滚动容器外面**的 wrapper（视口内的 absolute 随内容滚走，sticky 是 F10 后禁止的形状）；形状取**本仓自己的**而非 pi-app 居中药丸。**`F-B15` 不适用** —— 那条反转的理由是「动作在别处也有」，而这颗按钮是回到运行中回合的唯一入口 ⇒ 真 `<button>` + `aria-label`，禁 `group-hover`/`opacity-0`。**③ 跟随滚动不动**：我们的 `nextFollowState` 已在做，且比 pi-app 的 `isTimelineNearBottom` 多回答「内容缩短、浏览器把 `scrollTop` 夹到底部并抛 `scroll`」那一帧（F10 放大器的一半）。**④ 问卷不做**：pi-app 的问卷挂在 `resolveV2ByPluginName`（extension-compat 适配器层）上，即本 plan 非目标第 2 条；且 `piRuntime.ts` 全文 **0 次**发过 `question` 事件 ⇒ 本仓 `QuestionCard` 在 pi 上够不着，pi 的提问通路本就是 T08 的 Extension UI ⇒ **这件事已经不欠了**。真机实测：聚合吞行后 panels `[199,146]`（两层都开）对照清空记忆后 `[]`；切走 `[]` 切回 `[199,146]`；按钮 distance 0→false · 100px（死区内）→false · 2511→true(32×32) · 点击后→false；**增长路径单独隔离**：内容在静止视口下长起来 → true（全程无 scroll 事件，只有 ResizeObserver 看得见）。净增 32 例 · **变异 15 发 14 首轮咬红**。⚠️ **M7 首轮存活的形状值得记**：断言写「`sessionId={sessionId}` 至少出现 4 次」，删掉 `ChatTurn` 那一跳后**还剩 4 个**（`HistoryErrorNotice` 自己也带一个），计数照样满足而链条第一环已断 —— 改成逐跳断言后四发全红。四门：typecheck 0 · biome 1058 文件 0/0 · **vitest 277 文件 5494 例**。**2026-08-30 后续订正**：真实 pi 长回合、思考、流式、锚点与被拒工具均获用户确认；连续 read 未聚合仅留低优先级复验。记忆仍不落盘也不清理。**同日追加：悬停操作条改为预留高度 —— T12-b 一条设计决定被用户推翻**。用户实机反馈「鼠标放到某个回复上显示复制按钮时内容会上下移动」；T12-b 当时选 `grid-rows-[0fr]→[1fr]` 并写明理由（折叠态必须真零高度，否则每回合常驻 24px 空白＝把删 meta 省下的预算又花回去），**那条理由没错，只是替用户权衡了一个用户不接受的代价** —— 用户看实机后判定「在光标下长出一行把下面的字全顶下去」更难受。改为恒定预留 24px 只切透明度；`turnActionsInnerClass()` 从**禁止**任何高度类反转为**必须**带 `h-6`（与按钮 `size-6` 同档），两版对各自机制都成立故断言改写立论而非放宽。**没加 `pointer-events-none`**（进入回合即显形，「透明且可点」到不了，加了是空转）。真机对照：新机制三态 `turnTops` 完全相同、条高恒 24px；旧 grid 折叠 `[0,0,0,0]`、强制展开 `[24,24,24,24]` ⇒ 每次悬停推低 24px。变异 5 发全咬红。⚠️ 键盘 Tab 到复制按钮仍不显形（F-B15 反转留下的既有代价，未擅自扩张范围）。**T12-e 亦已 Done（2026-08-29，含真机 GUI 三图）**：四种「发不出去」原本共用一个红框且文案是 `launch with --open-path=<repo>`，全新安装必然常亮（demo 工作区的 `path` 是空串 ⇒ 有工作区但无 cwd）；已拆成**故障**（保留红框）与**没配置**（引导卡），顺序承重。⚠️ **GUI 又抓到一发断言抓不到的缺陷（空转臂同型第九发）**：卡片已经在好好说话了，**输入框 placeholder 仍写着 `Cannot send right now…`** —— 那条 if 阶梯有「没会话」「没工作区」却**没有「有工作区但没路径」**，全新安装看到的是整个函数最没信息量的兜底句，已补。12 例新测（**7 真值表 + 5 接线扫描**，因为把 composer 里的 welcome 分支整段删掉真值表照样全绿）· 变异 4/4 咬红 · **vitest 273/5425**。**⇒ T12 全系（T12 / a / b / c / d / e）到此全部 Done，且 2026-08-30 真机确认正文顺序/思考/流式/锚点/悬停/拒绝工具/长回合通过** → **下一步改为 D13 Cycle 1**：T14 队列补验 + T15 预览安全/打包补验 + T08-c/Q10 取证与 `.pilab` ask；随后 Cycle 2 内联审批/Extension UI/T25，Cycle 3 历史与 fork/rewind，Cycle 4 GUI/TUI，Cycle 5 上线实测（分支 `feat/pi-primary-backend`） | **2026-08-28 Phase 5 全落**：T19~T23 完成，本地管理端、受管同步、模型选择、隔离目录与 Q8/D10 TUI 注入均有 [evidence](../plans/pi-backend-migration/evidence/phase5-model-config.md)；**同日 D9 rev.2 拍板（用户修正）**：保留本仓前端设计风格，但布局/气泡/按钮位置可调整；pi-app 为 MIT，**能直接取用代码就取用**，不为旧布局重复实现，独有功能可后置；用户明确认为当前气泡偏丑，Phase 3 把输入/输出气泡一并重构。**权限审批必须加**：已核实采用 MIT 的 `@gotgenes/pi-permission-system`（本机已装 v27.0.1），非 TUI 经 Extension UI `select/input` 闭环，因此 Phase 2 重排并提前 T07→T11→T08→T08-a/b/c，默认策略留 Q9 拍板；仅 Claude FB7 join 不迁移。调查档案与映射表见 [topics/timeline-reference.md](../plans/pi-backend-migration/topics/timeline-reference.md)。**2026-08-28 同日 Phase 2 全落（T08-c 除外）**：五个提交 T07→T11→T08→T08-a→T08-b，[evidence](../plans/pi-backend-migration/evidence/phase2-extension-ui-permission.md)。关键更正：piRuntime 走的是**嵌入式 SDK**（`createAgentSessionRuntime`）而非 RPC 子进程，与 pix 同构，故 pix `extension-ui-bridge.ts` 直接可搬。实测三条：插件非 TUI 分支确实走 `ui.select`（`ctx.mode` 即 `bindExtensions({mode})` 传入值）；四选项实跑捕获为 `Yes / Yes, for this session / No / No, provide reason`；**无配置时插件自身兜底一律 `ask`**（`rule.ts:112`），故 Q9 未拍板不阻塞可用性，Q9 的实质从「补防护」变成「减打扰」。新增 `extensionUi.cancelled` 事件（超时计时器在 bridge 侧，不通知渲染端会留僵尸弹窗）与 `permission.activity` 事件（`policy_allow` 不弹窗，这是「被闸过」而非「压根没跑」的唯一证据）。**打包踩坑**：该插件运行入口是 TypeScript（`pi.extensions: ["./src/index.ts"]`，`dist/` 只有 `.d.ts`），通用过滤删 `.ts` 会发出没有入口的权限插件；且 `shouldCopy` 的 walker 先问包目录本身，只答文件路径会整包跳过 —— 单元断言全绿、真实构建才暴露。**同日早前 D8 四条拍板**（方案 B 隔离 agentDir · 同步失败降级本地缓存→默认 · 管理页只给通用配置 key 经登录注入 · 先读本地后切管理页），见 [topics/model-config.md](../plans/pi-backend-migration/topics/model-config.md)，新增 Phase 5 切片 T19~T23。**T06 冒烟通过**：真机发 `hi` 流式回复正常，Q6 事件映射验证通过。Phase 1 代码提交 `e1b45ed8`。D3 rev2 拍板 utilityProcess + MessagePort | **2026-08-29 审计修复批已落地**：外部审计 13 项逐条成立、逐条修复 —— P0 两项（权限插件不可用改 fail-closed，不建 session + `fatal:true` 的 host.error；pi runtime 由进程级单例改为**每 session 一套** handle/订阅/abort/投影/UI bridge，修掉 `/repo-b` 的工具跑在 `/repo-a` 里）· P1 六项（Stop 立即排空 pending 弹窗——abort 到不了卡在 `ui.select` 里的扩展；Runtime Event 改走强类型 `RuntimeEventDraft`，`isError`/`errorMessage` 两处漂移修正且**当场再抓出一处多余字段**；附件/effort 真正生效、attachment-only send 放行、permissionPreference 明确拒绝并声明 `capabilities.permissionPolicy:false`；`permission.activity` 接入 timeline；IPC 失败改为保留弹窗可重试；Extension UI 按窗口所有权路由）· P2 五项（去重规则支持 git/本地并按 pi 过滤语义判「是否真的加载」· runtimeId 改 session 级并双匹配校验 · 打包补 `node-addon-api`/`node-gyp-build` 许可证 + verifyArtifact 强制「产物里有包就必须有许可证」+ smoke 升级为「注册 tool_call handler + 用产物 WASM 真解析复合 bash 命令」并进 CI · a11y 与 Biome）。四门：typecheck 0（含 agent-host）· biome src+scripts **0/0** · **vitest 262 文件 5275 例**（原 259/5229）· `build:agent-host` 394.3MB · packaged smoke `PERMISSION GATE INTACT`。**2026-08-30 后续订正**：真机审批 allow/deny 与工具执行已贯通，权限设置面显示正常；未完成改为内联审批形态、Q10 策略命中调查、多窗口真机与完整 `pnpm build`（VM 内存不足）。证据见 [phase2-audit-fixes.md](../plans/pi-backend-migration/evidence/phase2-audit-fixes.md)。**同日 T08-c 切片 1 亦已落地**：用户四问拍板收口 Q9（**[D11](../plans/pi-backend-migration/decisions/011-default-permission-policy.md)**）—— 基线取**务实档**（read/grep/find/ls 放行 · write/edit 询问 · bash 默认询问 + 20 条只读命令白名单 · mcp 仅四个发现类 · skill 询问）· path 横切 deny 面含 `.env` 系列（`.env.example` 排在 deny 之后）、私钥、`~/.aws/credentials` 与 **`~/<APP_STATE_DIR>/*`（拦住 agent 读我们注入给它的公司凭据）** · external_directory **一律 ask 且不预置缓存白名单**（worktree 管理器的核心边界）· 项目级 `.pi/` 配置**受管模式不生效、本机模式生效**（落为 pi `projectTrusted` 按凭据模式分叉，连带让仓库自带的 `packages` 也失效——克隆仓库加扩展就是跑代码）。策略随包写进**产物内插件目录的 `config.json`**，实测确认那是插件读的**最低优先级**层，于是既不碰用户的 `~/.pi`（T08-a 红线）又让用户配置永远压得过我们；代价是该路径被插件标为 legacy，T09 落地后那条「移到…」警告会变成可见噪音（已记为 T09 前置）。⚠️ 拍板过程另澄清两条：原五问里的「`Yes for this session` 是否生成建议 pattern」**插件写死无开关**，不需要拍；「所有 surface 一律 ask」的说法还漏了一条——插件硬编码**只读工具读 Pi 基础设施目录一律 allow**。施工中被既有护栏抓到一处：`~/.pilab/*` 字面量撞 `APP_STATE_DIR` 单一来源扫描，处理是**把强制点搬到断言里**（`permissionPolicy.test.ts` 用常量拼模式）而不是单纯加白名单。四门：typecheck 0 · biome 0/0 · **vitest 264 文件 5305 例** · smoke 含 shipped-policy 与升级绊线两查。证据见 [t08c-default-permission-policy.md](../plans/pi-backend-migration/evidence/t08c-default-permission-policy.md)。**同日 T08-c 切片 2（设置面 GUI）亦已落地**：应用内第一次能读到权限闸的判断依据 —— 按插件语义**镜像**三层合并（随包默认 < 受管/用户 agentDir < 项目 `.pi/`）并对每条规则标出**由哪一层决定**；只让**受管 agentDir 那一层可编辑**，本机路线下那一层就是用户自己的 `~/.pi`，于是 `update`/`reset` **直接抛错拒写**（红线同 T08-a）而不是静默不做 —— 一个「保存成功但什么都没变」的快照会让人相信自己收紧了一条其实不存在的策略。危险选择（write/edit/bash 兜底/external_directory 兜底/mcp/skill/顶层 `*` 选“直接允许”）走二次确认且**只在放松时确认**；`yoloMode` 只显示不给开关（它不是档位而是把 `sudo`/`bash -c` 兜底整体摘掉的旁路）。镜像里两条语义带断言：**最后匹配优先**，以及 `{...base,...override}` 的浅展开导致**重述一条已存在的规则会拿到新动作但保留原位置** —— 界面用「位置未变」徽章把这条反直觉显示出来而不是消灭它。写文件时打在**原始 JSON 文档**上以保留 `forwardingTimeoutMs`/`shellTools` 等未建模的键。⚠️ **同批修掉两个静默缺陷**：① **dev 下随包默认策略根本不生效** —— 插件目录按运行中 Host 入口解析（dev 是 `src/agent-host/`），而构建只写产物；回落到插件裸默认是**安全的**（什么都问），所以症状只是弹窗变多，而「`cat .env` 被直接拒」这一半是静默缺失的，**真机验收若在 dev 下跑会据此误判策略本身**。修法保留原意图（构建仍不碰 checkout），由 `node scripts/dev.js` 自己显式写入（`ensureDevPermissionPolicy`，启动打印 `[dev] permission policy: …`）。② 设置分类的联合类型与 localStorage 校验数组是**两份手写清单**，漏改一份的症状是「新面板能打开、重启后再也打不开」；改为单一来源 `SETTINGS_CATEGORIES` + 静态扫描（每个分类必须既有导航项又有渲染分支 —— 这两半是 JSX，类型查不到）。四门：typecheck 0（含 agent-host）· biome src+scripts 0/0 · **vitest 269 文件 5397 例**（切片 1 后为 264/5305）· `build:agent-host` 394.3MB · packaged smoke `PERMISSION GATE INTACT`。**2026-08-30 后续订正**：面板已点验正常；策略文件被识别为生效，但普通 read 与 `.pilab` 的实际判定偏离 D11，转 Q10 调查。证据见 [t08c-permission-settings-panel.md](../plans/pi-backend-migration/evidence/t08c-permission-settings-panel.md)。**同日 Phase 3 起手，T12 代码完成；其后用户已看图拍板并于 2026-08-30 完成真实回合补验**：提问气泡取 pi-app 形状（右对齐 80% · 切角从右下换到**右上** · `12px 4px 12px 12px` 实测 · **不再六行截断**），模型回复**去掉每段一个的边框盒**（`turnAnswerContainerClass()` 退役），钉住提问的 **`sticky` 吸顶条整条退役**。关键认识：**这三件本来就是一条因果链，只有第一环是当初真想要的** —— 吸顶条会跟贴底滚动打架（收起→变矮→`scrollTop` 被夹回→又展开，逐帧震荡），所以 F10 加无条件截断；截断藏掉正文，所以 FB3 加恒显 `Show more`。去掉第一环，后两件失去存在理由，代码比改之前少一个 `useState`、少一个按钮、少一个函数参数。反向纪律已写成断言（任何回合级 class 函数长出 `sticky`/`fixed`/`z-*` 即红），**单独把吸顶条加回来会原样复现 F10 的震荡**。角色区分改由不对称承担：用户侧是个物件，模型侧根本不是物件（占满阅读宽度、无面、无边）。节奏算术必须同批改 —— 吸顶条同时承担一半回合间距，`space-y-2.5` 单独留下会**悄悄把回合间距减半**且全仓没有第二处会发现，故 `F-B9` 改钉绝对值 20。三份测试逐条**改写立论而非放宽**（`F-C4` 从「会关掉 sticky」改成「宽表格在叶子上滚，祖先一 clip 就静默吞右半边」；`[FB4-7]` 从「容器只挂 answer 分支」**反转成禁令**；`[D3-1]` 从读 JSX 字面量改成读挂载了哪两个 class 函数，`cn()` 换序不再假红）。四门：typecheck 0 · biome 1045 文件 0/0 · **vitest 269 文件 5395 例** · **变异 8/8 咬红零存活**（含「`space-y-2.5` 悄悄减半回合间距」「切角换回右下」「answer 分支重新套盒子」三发定向臂）（净 −2 = FB3 三例退役换 T12 两例，**FB3 的不变量没丢、换了更强形式**：从「参数不是几何量」升级为「整个 `UserBubble` 子树不出现 `getBoundingClientRect`/`IntersectionObserver`/`ResizeObserver`/`scrollHeight`/`clientHeight`/`offsetTop`/`useRef`」）。**GUI 亮暗双主题真机 CDP 四张图已出**，`getComputedStyle` 实测圆角/面/边/内边距四项（非从类名推断）。⚠️ **两条如实说明**：① 截图内容是**注入的合成 transcript**（走真渲染路径，但**流式态未覆盖**）；② **回合底部 meta 行刻意保留** —— pi-app 把 turn footer/chrome 整个删了（`turn-chrome.tsx` 现在是空模块 + 一行「删了别加回来」），本仓不跟，因为 **D55 ① 是用户 2026-08-25 明确拍过的**而 D9 rev.2 没点名它；这是本批唯一不跟 pi-app 走的结构选择，连同另外三条一起留给用户裁定。**⇒ 同日用户看图结案（T12-b）**：整体效果满意；**meta 行跟随 pi-app 删掉**；复制按钮三选一里用户选「**完全照抄 pi-app**」⇒ hover-only，**`F-B15` 红线经知情后明确反转**（代价：键盘与触屏用户从时间线上拿不到复制，已写进 `turnActionsSlotClass()` 头注）。落地：`Worked for · N tools`/模型名/相对时间全丢，复制 + `HH:MM` 进 `grid-rows-[0fr]→[1fr]` 的悬停条（`group/turn` 按回合作用域，**命名而非匿名** —— 工具行与思考链各有自己的 hover group）；连带退役 `deriveTurnHeadModel` 四个降级档 · `TurnMetaTail` · `formatMessageMetadata` · `useMinuteTick`+`footerNowMs` 全链。**但进行中的状态行保留**：它与 meta 行只是**碰巧共用一个槽**，删掉会重现 F2「秒表丢了」。**动手前先核了 pi-app 到底丢没丢**（逐项读源码）：复制与时间戳它留着只是挪进悬停条，思考时长挪去思考块，模型名挪去输入框，**只有回合总时长与工具数是真丢** —— 实证是 `timeline-turn-timing.ts` 里算这个数的两个函数还在但**全仓零调用方**。⚠️ **GUI 又抓到一发断言抓不到的缺陷（0820 批 §16 空转臂同型第七发）**：折叠态操作条实测 **28px 而非 0**，即「收起来了但照样占位置」，正好把删 meta 行省下的预算花回去；根因是 `h-7` 从 pi-app 原样抄来，而 **grid item 一旦有确定高度就压不扁**（`0fr` 隐含的 `minmax(auto,0fr)` 取它），**三个类一个不缺、每条单看都对、断言全绿**，失效在类与类的相互作用里 —— 已修（行高改由 24px 按钮决定）并补「此行不许有任何高度类」的实测型钉子。四门：typecheck 0 · biome 1045 文件 0/0 · **vitest 269 文件 5381 例** · **变异两轮 16/16 咬红**。真机 CDP：折叠两条均 `height:0`，真悬停第二个回合后**只有第二条**打开（24px）⇒ 按回合作用域成立；亮暗各一张。⚠️ **仍未覆盖**：流式态 · 悬停条的 `HH:MM`（合成 transcript 不走 event bus，拿不到 `completedAt`）· `fork`/`rewind`（pi-app 悬停条上还有这两个，本仓无对应能力，归 T13，不发明）。证据见 [t12-timeline-shell-baseline.md](../plans/pi-backend-migration/evidence/t12-timeline-shell-baseline.md) |
| [entry-and-environment](../plans/entry-and-environment/README.md) | **主线四件全部走完；2026-08-28 用户真机反馈批已收（T-CM1 + D72 落地）→ 剩 T-M1（截图批，缺网关数据）+ GUI 点验 B 态未验** | **2026-08-27 立项当日：三条待拍板全部拍完（D63/D64/D65），A1 已随 unified-credentials S3 落地，E1 的前置由 D67 解开 → 同日 E1 取证跑完并落档**（[E1 取证](../../plans/2026-08-27-e1-local-credentials/README.md)，Claude 十一臂 + Codex 十四臂，全离线，未改动 `src/`） | 立项讨论与已核实事实见 [kickoff](../../plans/2026-08-27-entry-design/kickoff.md)（含登录页视觉参照）。**已核实**：node/claude/codex 三样确实随包，用户机器上有没有与对话能不能跑无关；`ClaudeRuntimeChecker` 已按此改过一半，但 **`resolveGateDecision` 里还留着一条来自系统 `claude --version` 的分量没退役**（债，D65 已拍板清）。**D63** 探不到时放行不置灰（连带：首条消息失败必须说清缺什么 —— 已由 `972934d5` 在 codex 侧兑现一半）· **D64** 凭据模式存进 `~/.pilab` 配置文件（已落地）· **D65** 启动探测只保留 git，非 Windows 只给提示（⚠️ 范围修正：退役的是**启动门禁那条链路**，不是 `CliDetector` 本身 —— 设置页仍要用它探测第三方与自定义 agent） | ~~E1~~ **已完成（2026-08-27），但同日被 [D68](../../plans/openchamber-chat-refactor-ledger.md) 整体推翻其产品建议** —— 用户拍板**两条路线彻底分开**：走登录 = 注入公司 url+key（可用性我们承诺）；走第二个按钮 = **零注入的原始形态**，**不探测、不置灰、无说明文字**，能不能用与坏了怎么办**全部归用户**（原话「用户选了自己会去解决」「炸了就炸了」）。⇒ 作废：探测器本身 · 第二个按钮的文案口径 · 失败面两张票在这条路上的部分（**推翻 D63 的连带要求**）。E1 取证**不作废**，角色改为「**为什么不该做探测**」的实证依据（判据在两个方向上都不可靠：静态「有」不等于能用、静态「无」更不等于不能用）。**T-E1a 未删，重定范围到登录线**并降为低优先级 —— 同样两个缺陷（`api_error_status` 被 `eventNormalizer.ts` 丢掉 ⇒ 「没登录」与「钥匙被拒」同一句话；Claude 侧被拒凭据约 180 秒才终止）**在登录那条路上照样发生**，而那条路的可用性是我们承诺的。**open-q #4 已由 [D69](../../plans/openchamber-chat-refactor-ledger.md) 关闭**：仓库能供凭据（L5）这一条**知情后仍然维持「都读」**。**A3 同日已落地**：① 门禁清债 —— `resolveGateDecision` 里那条系统 `claude --version` 分量退役， `cliMissing` 只剩「我们自己随包的运行时解析不出来」；② git 检查换位置 —— 新 `GitMissingNotice`，启动查一次、 缺了挂**不拦人**的提示；③ 补上 D65 点名的非 Windows 缺口（Windows 给安装按钮走新窄 IPC，非 Windows 给下载链接）。 四门全绿（typecheck 0 · biome 1001 · **vitest 248 文件 5015 例**）· **变异 6/6 咬红零存活**。 as-built 三条：**负向断言抓到 `Root.tsx` 4 处 invalidate 一个已不存在的查询**（类型/lint/人眼都看不出）· **装 git 不能复用 `installAll([])`**（它会顺带装 Node，而 Node 是随包的 —— 该流程 A2 删 onboarding 时要一并处理）· **负向源码断言差点自伤**（第一版自写正则剥注释，仓内早有 `stripComments`，其头注列了五种静默删代码的形状 —— 0820 批 §16 纪律第四次撞上）。⚠️ **GUI 点验未做**（缺 git 的机器要现造）。 **A2 同日亦已落地（rev.2 —— rev.1 的门禁形状当日被 [D71](../../plans/openchamber-chat-refactor-ledger.md) 推翻）**：登录页定为**每次启动都出现的首屏**，点一颗按钮才进去。主按钮**只由账号决定**（登录过 = `Continue as <email>`，否则 = `Log in with work email`），次按钮恒为 `Use my own setup`；三条进门路径走同一个 `auth:enterApp(mode)`，一次调用同时记模式与落**会话闩**（刻意不持久化 —— 持久化就等于把这屏变回「只出现一次」）。新增 `WelcomeView`/`WelcomeShell`/`AiClientMark`（内联 SVG 等轴测立方体 + SMIL 流光，零外部资源、`motion-reduce` 自动静止）· `services/auth/appEntry.ts` · `OnboardingView` 889→约 400 行 · 删 `ClaudeVsCodeOnlyShell`。四门全绿（vitest 248 文件 **5029 例**）+ `electron-vite build` 通过 · **变异两轮共 15 发全咬红零存活**。⚠️ **as-built 最重三条**：① **rev.1 被用户一句话问倒，而他是对的** —— 我把「登录过就别再要求登录」实现成「跳过这一屏」，他原话是「**显示**进入公司配置」；他追问「跟选不选使用本机已有配置有啥关系」时是对的：按钮文案本来就只由账号决定，模式只影响「这屏出不出现」，而那条是我自己加的。改完之后**代码比改之前简单**（门禁少一个入参，两个新加的读法双双作废删除，**T-A2a 连带作废 —— 重启就是切换**）；② **换轴仍修掉一个真回归** —— `resolveManagedCredentialsEnabled()` 把 `local` 与「没选过」折成同一个 `false`，旧门禁会让**刚点了「使用本机已有配置」的用户被要求去注册**（D64/S3 留下、A2 才暴露）；③ **`vscode-only` shell 退役** —— 它提供的「去装 Claude CLI」修不好它被显示出来的那个问题，不只是死代码而是错的。另：文案落选 `Bring Your Own Key`（比路线语义窄，订阅登录也走这条）· 主按钮不写死域名（允许后缀有两个）· **D70** 放宽「装 git 顺带装 Node」（可接受，须静默后台）· **D71** 定下首屏时机与文案。⇒ **剩余只有一件**：⚠️ **GUI 点验** —— 登录页、流光 logo、两态、亮暗主题、长邮箱截断，一样都没在屏幕上看过。另 T-E1a（登录线失败面）低优先级挂着。**2026-08-28 补充（用户直接拍板，非本 plan 立项，见 [roadmap 同日条目](../plans/entry-and-environment/roadmap.md)）**：登录页品牌名 `AICLIENT`→**`PILAB`**、标语换新、`AiClientMark` 动效改为顺时针接力明暗（立方体形状不变，`var(--primary)` 仍在用）；四门验证过（vitest 248/5029，同 A2 落地时无回归），**GUI 点验仍未做**，欠债范围随之扩大到新动效 **2026-08-28 首次真机 GUI 点验（CDP 截图）**：登录页确实是启动首屏、结构与参照一致 · **亮暗双主题正常** · **流光真的在动**（跨帧比对三帧互不相同 —— `x1.animVal` 单次 evaluate 内不变会误判，`getAttribute` 更不行）· `use my own setup` 写入通路端到端成立。**观感三条待定**：按钮文字被 Button cva 基类**强制小写**（参照用首字母大写）· **logo 仅 72px ≈ 窗口宽 4%**（参照约 1/8）· 次按钮说明折两行。**B 态 `Continue as <email>` 仍未在屏幕上看过**。⚠️ **实机撞出 A2 开的一个洞 → T-A2b 待拍板**：`assertAgentSpawnAllowed` 问的是「是不是托管模式」，托管+未登录即拒；**这条分支在 A2 之前永远碰不到**（旧门禁不让未登录者进主界面），A2 让「人在主界面但没登录」变成正常态，于是第一次被走到 —— 症状是历史对话打不开、agent 起不来，日志 `auth_required`。产品里今天被 `credentialMode='local'` 挡住，开发模式因 `AICLIENT_MANAGED_CREDENTIALS=1` 覆盖而炸。建议闸改问「这次是怎么进来的」（复用 `hasEnteredApp()`），属安全边界改动，须用户点头。同轮把两个 dev 环境坑写进 [baseline](../baseline/test-and-release-gates.md)：需 `AICLIENT_NODE24_PATH` 指向 `out-node-runtime/node` · `AICLIENT_MANAGED_CREDENTIALS` 会盖掉登录页上的选择。 **2026-08-28 用户真机反馈批（打包版 Windows / `71f9086f`）**：两个症状同一根因 —— **T-CM1 `settings.json` 双缓存把 `credentialMode` 抹掉**（`ipc/settings.ts` 自存一份、渲染进程首读时定格永不刷新；渲染进程又是整份写回，于是任何一次设置保存都把旧值写回盘）。抹掉 ⇒ 默认回 `managed` ⇒ 选了本地环境却满屏 `auth_required`；写回旧 `local` ⇒ 登录后不注入公司凭据 ⇒ Claude Code 用用户本机 `~/.claude` ⇒ 打到 api.anthropic.com 被 Cloudflare 拦（**用户自己先判对了后一条**）。修法按用户选定口径：消除双缓存 + `credentialMode`/`onboarding` 两个主进程私有键在落盘那一刻从当前文件重取（缺失照搬缺失）。**同轮 T-A2b 结案**：用户拍板 **D72**，spawn 闸判据换成「这次是从哪颗按钮进来的」（会话闩由 `boolean` 拓宽为 `CredentialMode \| null`），**公司账号进来的运行中途登出照旧拒绝**，边界没溶解。四门全绿（typecheck 0 · biome 0 · **vitest 249 文件 5038 例**，原 248/5029）· 反向咬红两发各钉一半 |
## 遗留规划根（保留原位，不迁移不删除）

| 位置 | 角色 | 说明 |
|---|---|---|
| `docs/plans/2026-07-23-openchamber-chat-refactor-ard.md` | **架构权威** | 目标结构、决策立论；改架构先改它 |
| `docs/plans/2026-07-23-openchamber-chat-refactor-execution-plan.md` | 任务定义 | C-xx/T-xx 验收标准、协作规则、**测试凭证约定（§4）** |
| `docs/plans/openchamber-chat-refactor-ledger.md` | 决策 + 里程碑档案 | 已拍板决策 **D1~D29（含历史空号 D4/D5/D7/D13，以总台账决策表为准）**、检查点（CP-x）、Phase 总览；append-only |
| `docs/plans/ledger-claude-mainline.md` | 过程记录档案 | C-xx/T-xx 逐任务证据与提交 hash；append-only |
| `docs/plans/ledger-team-track.md` | 过程记录档案 | T-xx 早期证据；2026-07-24 起轨道移交主线 |
| `docs/plans/`（其余 2025/2026 早期文档） | 历史计划 | ai-sdk-migration / quick-terminal / status-line 等，与本计划无关 |

**权威顺序**：ARD ＞ 执行计划（任务定义）＞ 总台账（决策/检查点）＞ 本树（当前状态）。
冲突时依此序裁定；状态类信息以本树为准，本树不复制决策原文只链接。

**右栏骨架基线（D27 后现行）**：[`docs/design/a08-final-context-panel-baseline.html`](../../design/a08-final-context-panel-baseline.html) —— 内容层三项豁免维持现状（terminal 不回 BottomDock · context 只放真实字段 · git 最小集），差异清单见 [T-12~15 规格 §7](../../plans/2026-08-04-t12-15-surface-spec.md)。
**观感对齐基线**：[`docs/design/phase0a-openchamber-alignment.html`](../../design/phase0a-openchamber-alignment.html) —— A01/A05/A06 统一产物（用户已验收）。视觉 token、三列 + 导轨骨架、工具行与问答卡形态的唯一基线，业务组件不得自行发明视觉值。
**GUI 启动口径（2026-07-29 变更）**：填好仓库根 `dev.env`（模板 `dev.env.example`）后一律 `node scripts/dev.js`，**勿用 `pnpm dev`**。详见 [baseline 门禁「GUI 联调环境」](../baseline/test-and-release-gates.md)。
**故障档案**：[`BUG-2026-07-29-no-window.md`](../../design/BUG-2026-07-29-no-window.md)（「不出窗口」根因与方法论备忘）· [`BUG-2026-07-29-prompt-cache-rewrite.md`](../../design/BUG-2026-07-29-prompt-cache-rewrite.md)（缓存全量重写，open-q #15）。
**参考版本已冻结**：全部 openchamber `file:line` 证据以 commit `a3519141` 为准（见 ARD §7）。

## 惯例

- 完成一个任务：先记台账档案（证据 + hash），再刷新 plan 的 `implementation-status.md` / `roadmap.md`。
- 里程碑（M1~M5）/ 确认点（CP-x）结果仍回填总台账检查点表。
- 低承诺想法进 [`ideas/inbox.md`](../ideas/inbox.md)，成熟后 promote 到 roadmap / open-questions。
- **归档纪律（2026-08-08 起）**：结项批次的逐批长摘要与点验清单原文移入各 plan `history/`（整文件快照式），活动文件只留一行摘要 + hash + 链接；注册表行不超过一段短叙事。
