# 审计发现：high 级（10 条，全部经反驳者确认）

Role: evidence（source material，原文保留）；来源：2026-09-14 只读审计（工作流 wf_35ec4283-35d，92 个子代理）。严重级取反驳者判定。

每条发现的格式：`[编号] 严重级 类别 最终状态 | 节点 | 文件:行 | 标题`，随后是审查员描述（DESC）、引用代码（EVIDENCE）、失败场景（SCENARIO）、修法建议（FIX）、反驳者结论（REFUTER）与文档取舍核对（WAIVER）。最终状态：confirmed=反驳者确认且无取舍；confirmed-partial-waiver=确认但文档部分取舍；uncertain=无法构造触发路径也无法排除；refuted=被推翻；waived=文档明确取舍、不计入。

### [tools-01] HIGH bug confirmed | P1-4 | src/runtime/plugins/tools/index.ts:594 | walk() 对每个目录项先 realpath，悬空符号链接会让 grep/glob 整体抛错
DESC: walk() 在判断条目类型之前无条件调用 io.realpath(path)（594 行），而 HostIoPlugin.realpath 直接转发 node:fs/promises 的 realpath。对一个指向不存在目标的符号链接，realpath 抛 ENOENT；该异常从异步生成器一路冒泡出 for await，使整个 grep / glob 调用失败。代码本意是「canonical !== path 就跳过符号链接」，但跳过判断发生在 realpath 之后，永远来不及执行。符号链接循环会抛 ELOOP，同理。抛出的还是裸 fs 错误，没有 RuntimeHostError 的 code，模型拿到的是 "ENOENT: no such file or directory, realpath '/...'"。
EVIDENCE:       const path = join(directory, entry.name);
      if (!allowed(path)) continue;
      const canonical = await io.realpath(path);
      if (!containsPath(root, canonical) || canonical !== path) continue;
      if (entry.kind === 'directory') stack.push(path);
      else if (entry.kind === 'file') yield path;
SCENARIO: 工作区里存在一条悬空符号链接（例如 dist/current -> ../build/2026-09-13，目标尚未构建）。模型调用 grep {pattern:'TODO'} 或 glob {pattern:'**/*.ts'}，walk 遍历到该条目时 realpath 抛 ENOENT，整次工具调用失败，返回值不是「跳过一个链接」而是「搜索完全没结果加一条裸 ENOENT 错误」。该工作区里 grep 与 glob 此后一直不可用，直到用户手动删掉那条链接。
FIX: 先看 entry.kind：kind === 'symlink' 直接 continue；其余情况把 realpath 包在 try/catch 里，失败时 continue 而不是让异常冒泡。
REFUTER(CONFIRMED,high): 引用属实且未被断章取义。src/runtime/plugins/tools/index.ts:585-597 的顺序是：先 join 出 path → allowed(path) → 第 594 行无条件 await io.realpath(path) → 才在 596-597 判断 entry.kind。也就是说「跳过 symlink」的判断确实排在 realpath 之后，对悬空链接来不及生效。io.realpath 就是 node:fs/promises 的 realpath（src/runtime/host/io.ts:174-175 的 track 只做 absolutePath 校验和 pending 记账，不捕获任何错误），悬空链接必然抛 ENOENT。walk 是异步生成器，glob（index.ts:422）与 grep（index.ts:501）的 for await 外层都没有 try/catch，checkShellPaths/tools 注册包装层（index.ts:92-110）也不捕获，所以异常一路冒泡出 tool.execute。pi-agent-core 会把它转成 isError 的工具结果（src/runtime/node_modules/@earendil-works/pi-agent-core/dist/agent-loop.js:506-509、517-522 的 createErrorToolResult 用 error.message），因此模型拿到的正是裸 fs 文案、没有 RuntimeHostError 的 code，这点也与描述一致。我另外找不到任何上游防护：permissions.canTraverse（src/runtime/plugins/permissions/index.ts:279-292）是纯同步判断，不做 IO；tools.test.ts 里唯一的 symlink 用例（第 267-275 行）测的是写入逃逸，没有悬空链接或遍历用例。严重级别我维持 high：同一段代码里第 585 行的 io.readDirectory 也没有 try/catch，一个无读权限的子目录（EACCES）同样会整体打掉 grep/glob，所以脆弱面比报告说的更宽；代价是 grep 与 glob 这两个最常用的只读工具在该工作区持续不可用。唯一的前提条件是工作区里确实存在悬空链接/不可解析条目，这点我无法从代码证明，故不做更高定级。
WAIVER(no): 无 — P1 证据文档明确写「搜索跳过 symlink」（docs/plantree/plans/runtime-evolution/evidence/p1/README.md「接口及兼容范围」Glob/Grep 段），说明设计意图是遇到符号链接应跳过而非报错；已提取的 P1-4 waiver 只豁免「无正则语义/gitignore 引擎/spill 文件」，未提及符号链接处理会导致工具整体报错。walk() 先 realpath 后判断 canonical!==path 的顺序缺陷，属于与文档承诺相悖的代码实现缺陷，没有任何文档条目承认或推迟这个失败模式，不属于豁免范围。

### [permissions-01] HIGH security confirmed | P1-5 | src/runtime/plugins/permissions/bash-analysis.ts:243 | command 节点上的 redirect 子节点从不被遍历：前置重定向与 <<< here-string 完全逃过权限分析
DESC: walk() 的 `command` 分支只遍历两类子节点——类型为 variable_assignment 的前缀赋值，以及 field name 为 'argument' 的参数，然后在 244 行直接 return。但 tree-sitter-bash 的 command 规则是 `prec.left(seq(repeat(choice($.variable_assignment, field('redirect', $._redirect))), field('name', $.command_name), choice(repeat(choice(field('argument', $._literal), ..., field('redirect', $.herestring_redirect))), $.subshell)))`（src/runtime/node_modules/tree-sitter-bash/grammar.js:478-496），也就是说 **前置重定向**（`> out echo hi`）和 **here-string**（`cat <<< "..."`）都是 command 的具名子节点，field name 是 'redirect' 而不是 'argument'。这两类节点因此：(1) 目的路径不进 addPath，不参与 scope/path deny 判定；(2) result.unresolvedPaths 不被置真；(3) result.exploration 不被置假（246-250 行的 file_redirect 分支只在重定向是 redirected_statement 的兄弟节点时才命中）。只有尾置重定向 `printf x > .env` 走 redirected_statement，才会被 246 行接住——shellPolicy.test.ts 里测的恰好只有尾置形式。
EVIDENCE:         for (const child of argumentNodes) visitSubstitutions(child, state, depth);
        return;
      }
      if (node.type === 'file_redirect') {
        for (const child of node.childrenForFieldName('destination'))
          addPath(value(child, state), state);
SCENARIO: 档位 accept-edits、工作区 /work。模型调用 bash `> /tmp/outside/x echo hi`：analysis.paths 只有 /work/hi，request.path 是 /work，evaluate() 走到 276 行 `gear === 'accept-edits' && ['write','edit','bash'].includes(tool)` 直接 allow，命令执行并在工作区外创建 /tmp/outside/x，全程无审批，违反 D14「工作区外路径与外部目录仍询问」。同一档位下 `cat <<< "$(cat .env)"`：here-string 里的命令替换从不被 visitSubstitutions 访问，.env 既不进 paths 也不触发 unresolvedPaths，bash 被放行，.env 内容经 stdin→stdout 进入模型上下文，绕过 PATH_RULES 的 `'*.env': 'deny'`。auto 档下 `> .env echo x` 同理会覆写被 deny 的文件。
FIX: 在 command 分支里把 `node.childrenForFieldName('redirect')`（或 namedChildren 中 type 为 file_redirect / herestring_redirect 的子节点）也走一遍：file_redirect 复用 246-250 的处理，herestring_redirect 对其 _literal 做 value()+addPath 并 visitSubstitutions；两者都应把 exploration 置假。并补 shellPolicy.test.ts 用例：`> .env echo x`、`cat <<< "$(cat .env)"`。
REFUTER(CONFIRMED,high): 核对属实，且我找到的后果比原报告更严重。语法层面已确证：tree-sitter-bash 的 node-types.json 里 command 节点确实有一个 redirect 字段（types = file_redirect | herestring_redirect），与 name/argument 并列；grammar.js:478-496 也一致。而 bash-analysis.ts 的 command 分支（156-244 行）只取 childrenForFieldName('argument') 与 type === 'variable_assignment' 的前缀，然后在 244 行 return，从不遍历 namedChildren，因此 redirect 字段下的节点既不进 addPath，也不被 visitSubstitutions 访问。246 行的 file_redirect 分支只能在 redirected_statement（尾置重定向）走 279 行泛化递归时命中。

解析归属也能排除歧义：grammar.js:162-180 的 _statement / _statement_not_pipeline 没有裸重定向语句这一项，redirected_statement 的 seq(body, redirect) 需要 body 在前，且整条规则是 prec.dynamic(-1)，所以 `> out echo hi` 只能落在 command 的前缀 redirect 上；herestring 同理在 command 的参数重复段里以 field('redirect') 出现。

执行链完整：tools/index.ts:370-373 先 checkShellPaths（只展开 analysis.paths），再 target('bash', id, '.') 把 request.path 设为 cwd，最后 383-384 行原样把命令交给 `bash --noprofile --norc -c`。所以重定向目标从头到尾没有任何一层看到过。

比原报告更严重的一点：前缀重定向不会走到 249 行的 `result.exploration = false`，而 isExplorationCommand 是对 unit（'echo hi'）判断的，所以 exploration 保持 true —— plan 模式在 index.ts:237-242 只拦 `bash && !exploration`，于是 `> notes.txt echo hi` 在 plan 模式下也不会被拦；plan + accept-edits 直接在 276 行放行，写入文件。这与 tools.test.ts:187-198 钉住的 `echo hi > changed` 必拒正好是同一条命令的两种写法。
WAIVER(no): 无 — P1-5 相关的所有 waiver（策略重载、真实自定义策略现场记录、子代理策略归属 P5）均未提及重定向/here-string 未被遍历这件事。相反，docs/plantree/plans/runtime-evolution/evidence/p1/README.md「接口及兼容范围」明确声称原生 bash 用 tree-sitter AST 解析了「重定向」且「所有操作数参与 scope/path deny 判定」——这是与发现直接矛盾的正面承诺，不是被豁免的已知缺口，属于代码未达到文档所声明行为的真实缺陷。

### [session-01] HIGH bug confirmed | H/20 | /home/ai/code/ai-client/src/runtime/plugins/session/codec.ts:357 | CLI 与 GUI 交替写入时，一次基于陈旧 seq 的追加会让会话文件永久无法打开
DESC: decodeSession 把 seq 当成严格的「位置计数器」：CLI 追加的行没有 kind/seq，解码时用 result.seq++ 补位；随后遇到我们自己写的 v4 行时要求 row.seq === result.seq + 1，否则直接 invalid。H/20 之后 CLI 成为同一个文件的合法第二写入方，而 GUI worker 在终端运行期间保持存活、内存里的 document.seq 停留在终端写入之前。只要 worker 在没有先 reload 的情况下再写一行（entry / lane / fact 都会），文件就出现「seq 比位置小」的行，之后每次打开都抛 session_invalid，且现有修复逻辑只处理「最后一行未换行」的尾片，救不回来。应用侧本来靠 CHAT_SEND 的 reloadSessionFromDisk 兜底，但那一步只在 releaseTuiOwnership 返回 true（即确实杀掉了一个活着的终端）时才执行；pi 自己退出时 PTY 记录已被 onExit 删除，renderer 的 handleTuiExit 也不触发 reload，于是这条兜底整条失效。另外 navigate/rename/label/fork 这些 worker RPC 完全不经 assertHostPromptAllowed，也没有任何 reload，是第二条（未实测）触发路径。
EVIDENCE: if (row.kind === undefined && row.seq === undefined) {
      // H/20 — a row the CLI appended. It carries neither `kind` nor `seq`
      result.seq++;
      ...
    }
    if (row.seq !== result.seq + 1) invalid(`non-consecutive seq at line ${index + 1}`);
SCENARIO: 用户在 GUI 打开会话 S（worker 存活，document.seq=N）→ 切到终端模式，在 TUI 里聊两句（pi 追加 2 行）→ 在 pi 里按 Ctrl-D / 输入退出命令，PiTuiPty 的 onExit 删除 #live 记录，renderer handleTuiExit 只切回 GUI → 用户从 GUI 发一条消息：releaseSessionForHostPrompt 找不到活终端返回 false，reloadSessionFromDisk 被跳过，worker 以 seq=N+1 追加 entry 行 → 该行在文件里排在 2 条 CLI 行之后 → 下次打开该会话，decodeSession 在这一行抛 session_invalid('non-consecutive seq at line …')，这段对话在 app 里再也打不开（CLI 侧仍可读，因为它不看 seq）。
FIX: 两处都要补：(1) 解码端不要把 CLI 行计入 seq 空间，或对「seq 小于等于已见最大 seq 的自有行」按位置重排/容忍而不是整文件拒绝；(2) 交接侧把 reload 变成无条件动作——handleTuiExit 也调用 chat.reloadSession，并且 CHAT_SEND 不以「是否真杀掉终端」为条件，只要该会话曾被 TUI 持有过就 reload。同时给 navigate/rename/label/fork 这些写路径加上与 send 相同的 TUI 释放 + reload 前置。
REFUTER(CONFIRMED,high): 我逐段核对了整条路径，代码与描述一致，且我没找到任何一处能挡住它。

1) 解码端：codec.ts:339-343 对「既无 kind 又无 seq」的 CLI 行执行 result.seq++；codec.ts:357 对我们自己的 v4 行要求 row.seq === result.seq + 1，否则 invalid('non-consecutive seq at line N') → RuntimeHostError('session_invalid')。所以「CLI 行在前、我方陈旧 seq 行在后」这种排列必然抛错。
2) 写入端：store.ts:239 与 :259 的 seq 完全取自内存 document.seq，append 前不读盘、不校验文件大小（this.bytes 只是自增）。
3) 交接端：CHAT_SEND（chat.ts:488-498）只有在 releaseTuiOwnership 返回 true 时才 reloadSessionFromDisk；releaseSessionForHostPrompt（piTui.ts:157-164）的返回值来自 controller.disposeSession 的命中列表，而 PiTuiPty.ts:233-236 的 onExit 已把记录从 #live 删除，PiTuiPty.ts:291-298 于是返回空数组 → false → 不 reload。renderer 侧 usePresentationSwitch.ts:177-185 的 handleTuiExit 只切回 GUI，注释本身就写明「there is no session to suspend and reload」。
4) 无补救：全仓没有任何地方捕获 session_invalid；chat.ts 的 isUnwrittenPiSession 修复分支只针对「文件根本不存在」，对解码失败无效。CLI 侧仍能读（它不看 seq），所以现象正是「app 打不开、终端还能打开」。

两处我要纠正原描述（不影响结论）：一是并不存在 worker.rename / worker.label / worker.navigate 这三个 RPC，GUI 改名（chat.ts:627）只写索引不写 JSONL；真正未经任何 TUI 释放就写 JSONL 的是 CHAT_COMPACT_SESSION（chat.ts:668）与 CHAT_REWIND_SESSION（chat.ts:706），rewind 会经 store.navigate → mutate 写一条带 seq 的 lane 行，同样中招。二是防御性的 assertHostPromptAllowed（piTui.ts:167）在生产代码里零调用方（只有测试引用），所以「兜底」这一层实际不存在。

唯一的非确定性是「worker 是否还活着」：若 TUI 期间 worker 被容量回收，下次 send 会重新 spawn 并 resume（重新解码，seq 自洽）从而躲过。但没有任何代码在进入 TUI 时主动关闭 worker，openGui 的注释也明说 worker 一直持有开文件时读到的那份树，所以活着是常态。
WAIVER(no): 无 — H/20 相关文档（README.md H/20 行、evidence/p6/README.md「未验」节、topics/gui-tui-session-interop.md「范围外与风险」）只承认『并发写入仍靠 writerLock.ts 与 PiTuiExclusiveGuard 挡，本轮没有新证据』以及『GUI↔TUI 交替的完整一圈未验』——这两条只豁免了『缺现场验证证据』本身。发现指出的是一个可在开发环境纯逻辑复现的缺陷（reloadSessionFromDisk 只在 releaseTuiOwnership 返回 true 时触发、pi 自行退出时该分支完全走不到），任何文档都没有承认或接受这个具体的失效路径，反而 topics 文档写的是『并发仍靠锁挡』这一断言性设计描述，不构成对此缺口的取舍豁免。

### [loop-model-01] HIGH bug confirmed | P0-4 | src/runtime/plugins/model-adapter/binding.ts:110 | 目录里展开好的 provider 请求头从未发出（含 F08 的 User-Agent）
DESC: `buildProviderModels` 把 `expandHeaders` 展开后的请求头挂在 pi-ai 的 **Provider** 对象上。但 pi-ai 在组装请求时只合并两处来源：auth resolve 返回的 `auth.headers`，以及调用方传入的 `options.headers`；`ModelsImpl.applyAuth` 走 `getAuth(model, …)`，而 `getAuth` 里那句合并读的是**传入的第一个参数（此处是 model）**上的 `headers`（`models.js:282-289`，类型声明也写明 “provider auth plus static **model** headers when passed a model”）。`buildModel` 不写 `headers`，我们的 `resolve` 回调只返回 `{auth:{apiKey}}`，于是 `mergeHeaders(undefined, undefined)` → `undefined`。pi-ai 自带的 provider 工厂（如 `providers/anthropic.js:25`）正是把 header 放进 auth resolve 结果里的，说明这是受支持的那条路，而本实现两条路都没走。后果：catalog.ts 顶部整段 `$NAME` 展开的理由（F08 加的 `$AICLIENT_PI_USER_AGENT`，以及管理员在 models.json 里配的任何 `$NAME` 路由/租户头）在 native 后端上一个都没发出去，且完全无声。
EVIDENCE: binding.ts:107-110
      // Provider headers ride on the provider, not on each model: they are the
      // same for every row and duplicating them per model would make a header
      // change a per-model edit.
      ...(Object.keys(provider.headers).length > 0 ? { headers: provider.headers } : {}),
pi-ai models.js:282-289  if (!result || typeof providerOrModel === "string" || !providerOrModel.headers) return result;  … headers: mergeHeaders(result.auth.headers, providerOrModel.headers)
SCENARIO: models.json 里 provider `gateway` 配 `headers: {"User-Agent":"$AICLIENT_PI_USER_AGENT"}`，worker 环境里该变量已设。native 会话发一轮对话 → `CatalogProvider.headers` 正确展开为 `claude-cli-pilab/x.y.z`，但实际 HTTP 请求里没有 User-Agent 头（也没有任何自定义头）；网关按「未识别客户端」处理或按缺头的路由规则走，客户端侧没有任何报错或诊断。
FIX: 把展开后的头放进 auth resolve 的返回值（`resolve: async () => ({ auth: { apiKey: provider.apiKey, headers: provider.headers } })`），或在 `buildModel` 里给每个 Model 加 `headers`；并补一条断言「请求真的带上了这个头」的测试（现有 catalog.test.ts 只断言到解析层）。
REFUTER(CONFIRMED,high): 引用属实且我亲自走完了整条链路。binding.ts:110 确实只把展开后的头挂在 createProvider 的入参上；pi-ai 侧 models.js:428-460 的 createProvider 只是把 input.headers 原样存进 provider 对象，此后再没有任何请求路径读它——我在 pi-ai dist 里把所有 `.headers` 用法扫了一遍，请求头只来自两处：适配器读 `model.headers`（openai-completions.js:548、anthropic-messages.js:688/706/716、google-*.js、azure-*.js:186 等）与 `options.headers`（applyAuth 合成）。而 applyAuth(models.js:358-375) 的 headers = mergeHeaders(auth.headers, options.headers)，auth.headers 只可能来自 resolve 回调（本仓 binding.ts:118 只返回 apiKey）或 getAuth(models.js:282-289) 里合并的 **model** headers（buildModel 不写 headers）。resolveProviderAuth（auth/resolve.js 全文）也从不碰 provider.headers。所以两条支持的路都没走，头必然丢失且无任何告警。影响面比报告说的更大：configValidation.ts:304-312 的 withClientHeaders 给**每一个**托管 provider 补 `User-Agent: $AICLIENT_PI_USER_AGENT`，PiModelConfigService.ts 的 toPiUserProvider 给每一个用户自建 provider 也补同一条，因此 native 后端上所有请求都退回 pi-ai 自带 UA。测试只断言到解析层（catalog.test.ts:100），没有任何用例覆盖到线上头。判 high。
WAIVER(no): 无 — 文档里没有任何一条对 provider 级 headers（含 F08 的 $AICLIENT_PI_USER_AGENT）在 native 后端实际发不出去这件事做过取舍说明。相关决策只有 D7（凭据复用现有 vault，通过 pi-ai 的 ModelAuth 接口注入 apiKey）和 H17-P5-5-boundary（讲的是风格覆盖面、baseUrl 推导、models.json 依赖三件事），均未提及 headers 传递问题。经查 pi-ai 源码（src/runtime/node_modules/@earendil-works/pi-ai/dist/models.js 与 dist/api/openai-completions.js、anthropic-messages.js）确认实际请求只读 model.headers 与 auth.headers 合并结果，从不读 Provider.headers 字段，代码测试 catalog.test.ts 也只断言 catalog 层对象含 headers、未断言其真正随请求发出——这是一个未被任何文档承认过的代码缺陷。

### [rpc-projector-01] HIGH bug confirmed | P3-4 | src/runtime/events/projector.ts:188 | 子代理报告被投影成「用户消息」，带着原始 attemptId 和附件芯片
DESC: projector 在 `message_start` 时把**任何** role 为 user 的消息都当成「本轮用户回显」处理：铸一个 user- 开头的 messageId，盖上 `this.userTurn.attemptId` 和 `this.userTurn.attachments`，再把消息正文整块作为 message.delta 发出去。但一次 run 里 role=user 的消息不止用户那一条。agent-loop 在委派结束后会用 `agent.prompt(report)` 把子代理汇总报告重新喂给父 agent（index.ts:379），而 pi-agent-core 的 runAgentLoop 对每条 prompt 都会 emit message_start/message_end（node_modules/@earendil-works/pi-agent-core/dist/agent-loop.js:51-54）。于是那份报告会以「用户消息」的身份出现在时间线里。projector.ts:12-19 的文档注释写的是 “What the composer said about this send, echoed back on the user message.”（单数、一次），实现却对每条 user 消息都盖章。
EVIDENCE: case 'message_start':
  if (event.message.role === 'user') {
    const messageId = `user-${this.requestId}-${++this.index}`;
    this.emit({ type: 'message.started', sessionId,
      payload: { messageId, role: 'user',
        ...(this.userTurn.attemptId ? { attemptId: this.userTurn.attemptId } : {}),
SCENARIO: 会话里模型调用了 Task 委派子代理（默认开启，nativeWorkerRuntime.ts:219-227）。子代理跑完后 agent-loop/index.ts:376-379 取回合并报告（D10 上限 50k 字符）并 `agent.prompt(report)`。projector 收到 message_start(role=user) → 发出 message.started(role=user, attemptId=用户那次发送的 id, attachments=用户原来的附件) + message.delta(整份报告原文) + message.completed。渲染层 chatSessions.ts:863-889 直接把它建成一条 user 消息并追加文本块，groupMessagesIntoTurns 还会因此开出新的一轮。结果：用户在自己的对话里看到一条「自己发的」几万字子代理报告，并且原来的附件芯片被复制一份挂在上面。
FIX: 让 UserTurnEcho 只用一次：在第一条 user message_start 之后清掉 `this.userTurn`（或在 agent-loop 调用 `agent.prompt(report)` 前显式告诉 projector 这一条是内部续跑，不投影为用户消息）。委派报告的可见通道已经是 subagent.activity + 委派面板，不该再走用户气泡。
REFUTER(CONFIRMED,high): 完整触发链在代码里逐段核实，没有任何一处挡住它。(1) projector.ts:188 对**任何** role=user 的 message_start 都铸 user- 消息并盖 this.userTurn.attemptId / attachments，userTurn 是构造时传入的只读字段，全类里没有「用过一次就清空」的逻辑。(2) agent-loop/index.ts:373-381 在父 agent waitForIdle 之后进入 while 循环，取 subagents.collectFinished() 的合并报告并 `await agent.prompt(report)`。(3) 第三方库语义已在 src/runtime/node_modules/@earendil-works/pi-agent-core/dist/agent.js:268 与 dist/agent-loop.js:50-54 核实：prompt() 把入参包成 `{role:'user'}`，runAgentLoop 对 prompts 里每一条都 `emit message_start` + `emit message_end`。(4) projector 由 execute() 里 startRun() 只创建一次（index.ts:136-148），agent.subscribe 也只订阅一次（index.ts:261），第二次 prompt 走同一条投影路径。(5) 渲染层确认二次伤害：chatSessions.ts:863-880 直接把 role=user 建成 ChatMessage 并把 attachments 原样挂上；chatTurn.ts:79-82 的 groupMessagesIntoTurns 里 user 消息**无条件开新一轮**，所以这份报告会在时间线里另起一个「用户提问」轮次。(6) 还有原报告没提到的一层：agent-loop/index.ts:262 `if (event.type === 'message_end' && session) await session.appendMessage(event.message)` 会把这条 user 消息写进会话文件，所以历史回放同样会看到它——不是只污染实时时间线。(7) 委派默认开启已核实：nativeSubagentSettings.ts:50 `enabled: typeof chosen === 'boolean' ? chosen : true`，nativeWorkerRuntime.ts:217-227 只有显式 false 才不注册 Task。唯一能反驳的口子是「attemptId 被重复确认会不会二次清 pending」，查过 pendingUserMessages.ts:45-52 是按 attemptId 查找、找不到即 no-op，无额外损害——但这不影响主结论。
WAIVER(no): 无 — P5-2 契约（docs/plantree/plans/runtime-evolution/topics/p5-2-subagent-contracts.md）第67行只提到 subagent.activity/report 字段的向后兼容扩展，从未提及委派报告经 agent.prompt(report) 回灌父 agent 后会被 projector 当成『用户消息』回显并盖用户 attemptId/attachments。全库检索未见任何文档承认或接受这一具体行为，属未被记录、未被取舍的代码缺陷。

### [subagent-core-01] HIGH bug confirmed | P5-2-2 | src/runtime/plugins/subagent/index.ts:539 | Task 在 admit 之后、SubagentRun 交接之前抛错会留下永不结算的 running 记录，父 run 与 dispose 双双挂死
DESC: `Task.execute` 先同步调用 `registry.admit()` 把一条 delegation 记为 running（并把 `completion` promise 的 resolve 句柄交给 registry），然后才 `await this.config.projectInstructions?.()`，再构造 `new SubagentRun(...)`。这中间任何一次抛出/拒绝都不会被捕获：execute 直接 reject，而那条记录永远停在 running，`completion` 永远不 resolve。第 566-583 行的 `.then(onFulfilled, onRejected)` 只保护 `run()` 的拒绝，保护不到它前面的 await，也保护不到 `new SubagentRun(...)` 构造函数本身（`new Agent({...})` 在里面）。projectInstructions 是可以拒绝的：`instructionSource` 只把 ENOENT/ENOTDIR/EACCES/EPERM/EISDIR/ELOOP 吞掉，注释明写「Host/transport failures must propagate: ciphertext is never an instruction」，所以 TSD 解密失败、host IO 传输失败、EMFILE/EIO 都会原样抛出来。后果是连锁的：`collectFinished()` 用 `waitForDelegations(targets, targets.length, null, signal)` 等这条永不 resolve 的 completion（deadline 传 null，没有超时）；用户按 Stop 也救不回来，因为 `finally` 里的 `drain()` 同样 `await Promise.all(running.map(r => r.completion))`；`bootstrap.dispose()` 又 `await Promise.allSettled([..., ...active])` 等这个 run。整个会话槽位被永久卡住，只能杀进程。
EVIDENCE:         const admitted = this.registry.admit({ ... });
        if (!admitted.ok) return this.toolError(admitted.reason);
        const record = admitted.record;
        ...
        const projectInstructions = await this.config.projectInstructions?.();
        void new SubagentRun({ ... }).run().then((result) => this.settle(record, result), (error) => this.settle(record, {...}));
SCENARIO: 加密机上工作区里有一个 CLAUDE.md，某次 `Task` 调用时 TSD 解密子进程失败（或 hostIo 传输报 EIO）→ `projectInstructions()` reject → `Task.execute` 抛出 → 记录 `delegationId` 永远是 running → 父 run 在 `collectFinished` 里无限等待，用户按 Stop 后 `drain()` 继续无限等待，关闭会话时 `dispose()` 也挂住。worker 槽位永久 busy，界面表现为「一直在跑、停不掉、也关不掉」。
FIX: 把 admit 之后的全部代码包进 try/catch，catch 里调用 `this.settle(record, { status: 'failed', ... })`（或直接 `registry.settle`）再返回 toolError；同时把 `new SubagentRun(...)` 的构造也放进这个保护范围内。另外建议给 `drain()` 加一个兜底超时，使「停不掉」不会升级成「关不掉」。
REFUTER(CONFIRMED,high): 引用完全属实，且整条挂死链路我逐段跟到底了。index.ts:504-510 先 admit（记录进 map，状态 running，resolve 句柄存进 settleHandles），539 才 await projectInstructions，544-583 才把 .then(settle) 挂上去——这中间没有任何 try/catch，execute 是裸的 async 箭头函数。

拒绝确实可能发生：bootstrap.ts:385 把 projectInstructions 接到 projectInstructionsText(instructionSource(ctx.runtimeHostIo,…))，instructionSource.ts:24 只吞 ENOENT/ENOTDIR/EACCES/EPERM/EISDIR/ELOOP，其余原样抛；host/io.ts 的 readFile 会抛 RuntimeHostError('io_tsd_unavailable') / ('io_tsd_unreadable') / ('invalid_host_request')，这三个 code 都不在白名单里，且 track()（io.ts:54-61）不捕获。另外 readFile 对「路径存在但不是普通文件」抛的是 invalid_host_request（io.ts:90-91），所以工作区里出现一个名叫 AGENTS.md/CLAUDE.md 的目录也会穿出来——白名单里那条 EISDIR 其实拦不住。

后果链逐环核实：pi 把 execute 的拒绝转成 tool error（agent-loop.js:470-477），父 loop 照常往下跑；记录永远停在 running，completion 永不 resolve（settle 只从那两个 .then 里调，根本没挂上）。collectFinished（index.ts:308-313）用 waitForDelegations(targets, targets.length, null, signal)，registry.ts:321-324 在 deadline===null 时压根不建 timer；用户 Stop 能让这一步早退，但 agent-loop/index.ts:395 的 finally 里 drain() 不带任何 signal（index.ts:287-292 的 Promise.all(completion)），永远卡住；bootstrap.ts:462 的 dispose 又 await 这个 run。三道门全堵死，只能杀进程。

打折的地方只有触发概率：父 loop 每轮开头的 PromptPlugin.compose()（agent-loop/index.ts:127）读的是同一份链且目录集是子代理那份的超集（子代理不传 targetPath，只读 root 一层），所以「开跑时就坏」的文件会先让 run 在 127 行失败，走不到 Task。真正的窗口是「run 开始之后才坏」——而这恰恰是这个回调被设计成回调的理由（index.ts:106-110 注释自己写「chain … can change between turns」）：中途加密、文件被换成目录、EMFILE/EIO 之类的瞬时 IO 故障。加上 emitActivity（532 行，events.emit 是同步遍历 listener 且不包 try）和 new SubagentRun 构造也在同一无保护窗口内，我按 high 收。
WAIVER(no): 无 — 契约 docs/plantree/plans/runtime-evolution/topics/p5-2-subagent-contracts.md §3 只承诺“满额明确工具错误”“任一真实宿主容量不足按资源边界明确拒绝启动”，讨论的是 admit 前的准入拒绝，没有涉及 admit 成功之后、SubagentRun 真正接管之前那段 await（projectInstructions）抛错的情形。P5-2-0 基线（topics/p5-2-0-baseline.md）与第 8 批证据（evidence/p5-2/README.md）记录的“两个真缺陷”分别是 Stop 后 run() 提前 resolve、子任务先于父 idle 完成时报告丢失，均已用 drain()/deliveredAt 修复，但都不是这一条：本条是 admit()→await→new SubagentRun() 之间抛错导致 completion 永不 resolve、registry 记录永远 running。经读 src/runtime/plugins/subagent/index.ts 第 534-539 行确认 `const projectInstructions = await this.config.projectInstructions?.();` 确实夹在 registry.admit() 之后、SubagentRun 构造之前，且外层没有 try/catch 兜底，问题真实存在，未被任何文档接受或推迟。

### [subagent-core-02] HIGH contract-gap confirmed | P5-2-4 | src/runtime/plugins/agent-loop/index.ts:379 | 自动交回的子代理报告被当成真实用户消息：界面上出现用户气泡，并作为 user 消息落盘成为「最新用户任务」
DESC: 自动交回路径把 `collectFinished()` 产出的文本直接喂给 `agent.prompt(report)`。pi 的 `runAgentLoop` 对每条 prompt 消息都会 `emit({type:'message_start'})` 与 `message_end`（pi-agent-core dist/agent-loop.js:51-54），而本仓的两个订阅者都不区分它：`RuntimeEventProjector.observe` 在 `message_start && role==='user'` 分支无条件发 `message.started{role:'user'}` + `message.delta`（含全文，最多 50k）+ `message.completed`，渲染层 `chatSessions.ts` 的 `case 'message.started'` 直接 upsert 一条 ChatMessage 进时间线；同一个 subscribe 回调里的 `session.appendMessage(event.message)` 又把它作为 role=user 的 message 条目写进 JSONL。契约 §3「内部报告无用户气泡，不能取代『最新真实用户任务』，不能被压缩误认成用户新需求」与任务矩阵 P5-2-4 行「内部报告不变成用户任务」都被违反。全仓没有任何过滤：grep `Integrate their reports` 只命中常量定义处。注意这与 P5-2-6 已修的那个漏（`aiclient.subagent` 以 custom.entry 上线）不是同一条路径——那条修的是 custom 条目，这条是 user 消息。
EVIDENCE:           const report = await subagents.collectFinished(request.signal);
          if (report === undefined) break;
          trace.note('note', { event: 'delegation_resume', report_bytes: report.length });
          await agent.prompt(report);
          await agent.waitForIdle();
SCENARIO: 模型派一个 explorer 后自己空闲；runtime 自动交回报告 → 用户在聊天里看到一条自己从没打过的「用户」气泡，内容是「The following subagents have finished. Integrate their reports…」加上整份报告；重开会话时这条 user 消息仍在历史里，并且是最后一条用户消息，任何按「最新用户任务」取值的逻辑（重试/续聊意图/压缩判定）都会把它当成用户的新需求。
FIX: 给交回的 prompt 打上内部标记（例如沿用 INTERNAL_CUSTOM_ENTRIES 的思路，让 projector 在这一次 prompt 期间抑制 user 投影），或让 projector 通过 `userTurn` 上下文识别「这一条不是用户发起的」；落盘可以保留（模型上下文需要它），但不得进入时间线投影。
REFUTER(CONFIRMED,high): 三条链路我都跑到了终点，而且契约原文比报告引的还要直白。

上游：pi 的 runAgentLoop 对每条 prompt 无条件 emit message_start + message_end（src/runtime/node_modules/@earendil-works/pi-agent-core/dist/agent-loop.js:51-54），agent.prompt 把字符串规范化成 role='user' 的消息（agent.js:226-232）。

投影：agent-loop/index.ts:261 挂的 subscribe 在整个 run 期间有效，projected.finish 要到 436 行才调，而自动交回循环在 373-382 行，处在 finally(390) 之前——所以交回时投影器活着。projector.ts:187-209 的 message_start 分支对 role==='user' 无条件发 message.started + message.delta + message.completed，没有任何「这不是用户发的」的判别位。渲染层 chatSessions.ts:863-879 直接 upsert 成 ChatMessage。

落盘：agent-loop/index.ts:262 `if (event.type === 'message_end' && session) await session.appendMessage(event.message)`，交回这条同样以 role=user 写进 JSONL。

下游后果不是推测，至少三个真实消费者：chatTurn.ts:75-90 按 user 消息切回合，交回报告会自成一个带用户气泡的新回合；compaction.ts:139-144 的 latestUser 取 messagesToSummarize 里最后一条 user，active_turn 模式下把它当作「checkpoint 之后唯一的新指令」保留——正是契约说的「被压缩误认成用户新需求」；store.ts:351-363 的 rewind 会把这条当用户消息、把报告全文塞回编辑器。

额外恶化项（报告没提）：projector.ts:196-199 会把 userTurn.attemptId 和 attachments 原样带上，所以交回气泡会顶着真实用户那一轮的 attemptId 和附件，chatSessions.ts:1527-1536 还会拿它再 acknowledgeAttempt 一次。

契约违反是白纸黑字的：topics/p5-2-subagent-contracts.md:42「内部报告无用户气泡，不能取代『最新真实用户任务』，不能被压缩误认成用户新需求」，topics/p5-2-subagent-tasks.md:18 P5-2-4 行「内部报告不变成用户任务」。全仓无任何过滤，也没有任何测试断言过这条。
WAIVER(no): 无 — 契约明文要求相反的行为：docs/plantree/plans/runtime-evolution/topics/p5-2-subagent-contracts.md §3 写明“内部报告无用户气泡，不能取代‘最新真实用户任务’，不能被压缩误认成用户新需求”。经读 src/runtime/plugins/agent-loop/index.ts 第 379 行 `await agent.prompt(report)` 与真实用户 prompt（第 358 行）走的是同一调用，projector.ts 第 187-206 行对 `message_start && role==='user'` 无差别处理，确认自动交回的报告会被当成真实用户消息投影为气泡并写入 JSONL。这与契约要求直接矛盾，属于未声明、未接受的实现缺陷，不是文档取舍。

### [import-catalog-01] HIGH bug confirmed | P5-4 | /home/ai/code/ai-client/src/main/services/legacyImport/LegacyImportManifest.ts:106 | 清单校验仍按 pi 的文件名规则，native 导入记录重启后被静默丢弃
DESC: manifest 落盘记录在重新载入时要求 targetSessionFile 的 basename 以 `_<targetPiSessionId>.jsonl` 结尾——这是 pi SessionManager 的命名（`${fileTimestamp}_${sessionId}.jsonl`，见 pi-coding-agent/dist/core/session-manager.js:667）。P5-4 的 NativeLegacyImportWriter 把文件命名为 `${targetPiSessionId}.jsonl`（无时间戳前缀、无下划线），因此 basename 永远不满足这个后缀，parseRecord 返回 null。该分支连日志都没有：ensureLoaded 只在「解析成功但 integrity 校验失败」时 warn，解析失败是完全静默的。后果有三条：(1) 跨重启的去重失效——同一段对话再导入一次会再建一条会话，UI 的 importedSnapshots 也归零；(2) 崩溃在 updateImporting 之后的 importing 记录在重启后不会被 reconcileInternal 看到，落在 <agentDir>/sessions/ 或暂存目录里的文件与可能残留的索引行永远没人清，直接违背 IM03「中断的导入要能被清理」；(3) cleanupPending 的 failed 记录同样消失，待清理状态被遗忘。测试之所以全绿，是因为 LegacyImportService.test.ts 的假 createImport 把文件命名成 `probe_${targetPiSessionId}.jsonl`，恰好满足这条 pi 时代的后缀。
EVIDENCE:     (value.targetSessionFile !== undefined &&
      (typeof value.targetSessionFile !== 'string' ||
        !path.isAbsolute(value.targetSessionFile) ||
        !path.basename(value.targetSessionFile).endsWith(`_${value.targetPiSessionId}.jsonl`)))
// nativeImport.ts:251 →  return join(dir, `${targetPiSessionId}.jsonl`);
SCENARIO: 导入一条 Claude 对话成功（manifest 记 complete，targetSessionFile=<agentDir>/sessions/import-claude-code-<uuid>.jsonl）→ 重启应用 → ensureLoaded 解析该记录时后缀检查失败、记录被丢弃且无任何日志 → 在导入面板对同一条对话再点一次导入，得到的是 'imported' 而不是 'already-imported'，侧边栏出现两条一模一样的会话；若上次是导入中途被杀进程，则那半截文件和索引行永远不会被 reconcile 清理。
FIX: 把后缀断言改成同时接受两种命名（`<id>.jsonl` 与 `*_<id>.jsonl`），或者直接改成 `path.basename(file) === `${id}.jsonl` || basename.endsWith(`_${id}.jsonl`)`；并给 parseRecord 失败补一条 warn，让这类静默丢弃在日志里可见。同时把 LegacyImportService.test.ts 里假 worker 的文件名改成 native 的真实命名，这条回归才钉得住。
REFUTER(CONFIRMED,high): 逐条核实后推不翻，且触发路径是必然而非偶发。(1) 命名：src/runtime/worker/nativeImport.ts:251 的 fileFor 返回 join(dir, `${targetPiSessionId}.jsonl`)，Main 侧 LegacyImportService.ts:260 生成的 id 形如 `import-claude-code-<uuid>`，文件名长度 = len(id)+6，而 LegacyImportManifest.ts:106 要求 basename.endsWith(`_${id}.jsonl`)（长度至少 len(id)+7），数学上永远为 false。(2) 路径没有被任何中间层改写：nativeImport 的 finalSessionFile 经 worker.import 结果原样回到 PiImportProcess.ts:112，isWorkerImportResult(workerRpc.ts:1059) 只校验非空字符串，LegacyImportService.ts:309/332 直接把它写进 manifest 的 targetSessionFile。(3) 静默：ensureLoaded(LegacyImportManifest.ts:241-266) 只有在 parseRecord 成功但 verifyRecord 失败时才 console.warn，parseRecord 返回 null 的分支无任何日志。(4) 后果链成立：list() 为空 → listSessions 的 importedSnapshots 归零、importReadConversation 开头的 already-imported 短路失效（重复导入）、reconcileInternal 看不到 importing/failed 记录（IM03 的中断清理失效）；而下一次 reserve+flush 会整份重写 manifest，旧记录永久消失。(5) 测试确实钉不住：LegacyImportService.test.ts:98/141 的假 worker 用 `probe_${targetPiSessionId}.jsonl`，恰好满足这条 pi 时代后缀。唯一能替它辩护的是 worker.ts 里若没有 agentDir 就抛 WORKER_AGENT_DIR_UNSET，但那只是让导入根本不发生，不改变命名规则。
WAIVER(no): 无 — 唯一相关的豁免是 evidence/p5-4-p5-5/README.md 待现场表 IM-b『中断一次导入（杀进程）后重试，reconcile 真的清干净——待现场』，但该豁免只免『没有真机/真实重启场景点验』这一验证动作，不免实现本身的缺陷。经查代码，LegacyImportManifest.parseRecord（LegacyImportManifest.ts:106）要求 targetSessionFile 的 basename 以 `_<targetPiSessionId>.jsonl` 结尾（pi 命名规则），而 P5-4 的 NativeLegacyImportWriter.fileFor（nativeImport.ts:251）写的是 `${targetPiSessionId}.jsonl`（无下划线前缀），二者恒不匹配；ensureLoaded（LegacyImportManifest.ts:241-264）对 parseRecord 返回 null 的记录不做任何 warn，直接静默丢弃。这意味着任何跨重启场景（不限于崩溃恢复，包括普通重启后的去重判断）该记录都会消失，是实现本身的逻辑错误，不是『尚未在真机验证』的范畴，因此不属于文档已豁免的缺口。

### [cutover-01] HIGH bug confirmed | P6-5 | scripts/verify-packaged-app.mjs:203 | 打包验证仍跑已被删除的 legacy lane，发版门禁必红
DESC: P6-5 删掉了旧引擎与后端开关，但打包验证脚本仍然串行跑 legacy 与 native 两条 lane。legacy lane 会给 worker 设 AICLIENT_RUNTIME_BACKEND=legacy（现在完全无效，worker 照样起 native），随后按 legacy 的断言检查 bootstrap 返回的 extensions 列表；native 的 bootstrap 结果里根本没有 extensions 字段（nativeWorkerRuntime.ts:304-326 只返回 permissionGate: 'bundled'），checkBundledExtensionsLoaded 会为两个内置插件各报一条 problem 并抛错。同一条 lane 还按 legacy 形状给 bash 工具传 timeout: 10 而不是 native 的 timeoutMs。pnpm verify:packaged 与 .github/workflows/build.yml 的三处调用（274 / 418 / 535 行）都会因此失败。
EVIDENCE:   if (!args.skipSmoke && failures.length === 0) {
    for (const backend of ['legacy', 'native'])
      reports.push(runWorkerSmoke(workerPath, backend, failures));
  }
SCENARIO: 在任意平台执行 `pnpm verify:packaged`（或 CI 的 Windows/Linux 打包作业）→ legacy lane 起 native worker → bootstrap.result.extensions 为 undefined → checkBundledExtensionsLoaded 返回「bundled extension @juicesharp/rpiv-ask-user-question did not load」等 2 条 → 脚本抛错 → verify-packaged-app 退出码 1，整条发版门禁红，而实际产物并没有问题。
FIX: 把 lane 列表收为 ['native']，删掉 packaged-worker-smoke.cjs 里的 --backend 解析与 legacy 分支（第 11-12、98、245-251、334 行），并把 report.backend 断言改成从 trace 的 stamp.backend 读。
REFUTER(CONFIRMED,high): 三段链路逐段核实，无法推翻。(1) /home/ai/code/ai-client/scripts/verify-packaged-app.mjs:202-204 确实仍写 `for (const backend of ['legacy', 'native'])`，且 `args.skipSmoke` 默认 false，CI 三处调用都没传 --skip-smoke。(2) 后端开关确实已彻底失效：src/runtime/flags.ts 的 readRuntimeFlags 直接返回 `backend: 'native'` 常量，src/agent-host/worker.ts 无条件 `import('../runtime/worker/nativeWorkerRuntime.ts')`；全仓 grep 'AICLIENT_RUNTIME_BACKEND' 只剩文档、探针脚本和 packaged-worker-smoke.cjs 自己，生产代码零消费。(3) native 的 bootstrap 结果确实不带 extensions：nativeWorkerRuntime.ts:305-327 组装的 result 里只有 permissionGate:'bundled'，src/shared/types/workerRpc.ts:323 里 `extensions?` 是可选字段，piWorkerRpcServer.ts 也不补写。(4) packaged-worker-smoke.cjs:244-252 的 `if (backend === 'legacy')` 分支把 undefined 交给 checkBundledExtensionsLoaded，后者在 bundled-extension-check.cjs 里对非数组取 `[]`，两个包各产出一条 problem 后 throw → 子进程非零退出 → runWorkerSmoke 推 failure → 主流程 `process.exit(1)`。循环不短路，native lane 即使通过也救不回来。CI build.yml:274/418/535 三个步骤都是普通 run，没有 continue-on-error，所以三平台发版门禁必红。唯一能站得住的削弱点是「该脚本不在本次审查列出的文件清单里」，但这不影响事实成立。
WAIVER(no): docs/plantree/plans/runtime-evolution/evidence/p6/README.md P6-3 表第4行「GUI 功能无回归」：『开发机已点验；打包产物的回归仍并入最后一次上机』 — 这条豁免只是把「打包产物回归测试」的执行时点推迟到最后一次上机，并未豁免 verify-packaged-app.mjs 脚本本身的正确性。经核实，脚本 203 行仍对 ['legacy','native'] 两个 backend 跑 runWorkerSmoke；packaged-worker-smoke.cjs 148 行仍把 AICLIENT_RUNTIME_BACKEND 设为该值传给子进程，但 worker.ts 已确认不再读取该变量（P6-5 已删除 readRuntimeBackend 判读与 runtimeBackend.ts），子进程实际总是起 native。smoke 脚本 245 行仍在 backend==='legacy' 分支调用 checkBundledExtensionsLoaded(bootstrap.result.extensions,...)，而 native 的 bootstrap 结果没有 extensions 字段（nativeWorkerRuntime.ts 只返回 permissionGate:'bundled'），bundled-extension-check.cjs 会把 undefined 当空数组、对每个内置包报「did not load」并 throw，导致 legacy lane 必然失败。这是脚本本身在 P6-5 后已经失效（引用了已删除的后端语义），不是「回归验证时机被推迟」的问题；docs 中搜索不到任何提及 verify-packaged-app.mjs/verify:packaged 需要同步改造的记录，P6-2 清单里「打包与脚本：按口径 A 不动」说的是 cli.js 仍要随包，不涉及这条 smoke 脚本的 legacy lane。因此该缺口未被文档豁免。
