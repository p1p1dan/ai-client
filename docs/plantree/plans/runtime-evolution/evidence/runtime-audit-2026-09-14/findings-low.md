# 审计发现：low 级与文档已取舍项（一行一条）

Role: evidence（source material）；来源：2026-09-14 只读审计。每行：`[编号] LOW 类别 最终状态 | 节点 | 文件:行 | 标题 :: 描述前 260 字`。行首 `WAIVED` 的三条为文档明确取舍、不计入缺陷。完整描述、反驳与取舍理由见 `findings.json`。

- [core-host-01] LOW contract-gap confirmed | P0-3 | src/runtime/contracts.ts:101 | DEFERRED_SERVICES 仍把已实现的 subagent 服务列为「未实现」，且 A3 门禁抓不到 :: contracts.ts 的 DEFERRED_SERVICES 至今只有一条 `runtimeSubagent`（单数），理由写的是「工具还不存在所以子代理不可能存在，D10 的 SubagentRunner 接缝故意不开」。但第 8 批 P5-2 已经落地，真实注册的服务名是 `runtimeSubagents`（复数，见 src/runtime/plugins/subagent/index.ts:72），bootstrap.ts:410-412 会把它加进必需服务清单。文件头（第 11-21 行）声称这张表的
- [core-host-02] LOW contract-gap confirmed | P0-6 | src/runtime/bootstrap.ts:96 | config_version 自 P3 起冻结，五批行为变更后的 trace 与 P3 期 trace 无法区分 :: RUNTIME_CONFIG_VERSION 的取值历史是 runtime_p0_v1 → runtime_p1_policy_v3 → runtime_p2_prompt_v1 → runtime_p3_complete_v1（`git log -S` 核实，最后一次变更在提交 78298463「完成 P3 会话与事件」），此后 P5-1 技能与提示词模板、P5-2 子代理与 Task 工具、P5-3 MCP 工具、P5-4/P5-5 模型目录换源、P6 切换全部沿用同一个值。  工程规范 §15 与 ARD D
- [core-host-04] LOW bug confirmed | P1-0 | src/runtime/bootstrap.ts:476 | dispose 里 trace flush 失败会吞掉 exec 清理失败，违反「清理失败必须可见」 :: dispose 先并发收集 exec.shutdown() 与在跑 run 的结果放进 outcomes，然后进入 try/finally 链做 trace flush → session close → io shutdown → ctx dispose，最后才在 476-477 行检查 outcomes 里有没有 rejected。  问题在于 flush() 抛出时控制流从 try 里直接离开函数，476-477 行永不执行。TracePlugin 的 persistenceError 是黏性的（trace.
- [core-host-06] LOW resource-leak confirmed | P1-0 | src/runtime/host/exec.ts:250 | 长驻子进程 kill 的清理宽限用尽时假报「已退出」，shutdown 因此把挂死的 MCP 服务器当成收干净了 :: spawnPersistent 的 kill() 在 cleanupTimeoutMs 到点时直接 resolveExit({exitCode:null,signal:null})，让 `exited` 兑现、销毁三个 stdio 流，然后 `await exited` 立刻返回——既不抛 exec_cleanup_failed，也不在任何地方记下「这个进程其实还活着」。  ExecPlugin.stop()（114-127 行）把 `children.map(child => child.kill())` 丢进 
- [core-host-07] LOW resource-leak confirmed | P1-0 | src/runtime/host/exec.ts:282 | 长驻子进程用的 killTree 没有 Windows 重入保护，taskkill 不被跟踪、失败不上报 :: 模块级 killTree（spawnPersistent 专用）与 runPipe 内部那个同名函数的成熟度差一截：  1. 没有 runPipe 的 `windowsKillStarted` 守卫——kill() 一次调用最多触发三次 killTree（立即、cleanupTimeoutMs/2 升级、cleanupTimeoutMs 兜底），Windows 上就是三个 taskkill.exe。 2. 这些 taskkill 子进程既不放进任何集合，也没有 'close' 监听，父进程不知道它们是否成功；run
- [core-host-15] LOW quality confirmed | P4-2 | src/runtime/flags.ts:12 | P6-5 删除后端开关后，flags.ts 留下一段无归属的 JSDoc，文件头仍在描述已不存在的开关 :: 第 10 批把 `export { RUNTIME_BACKEND_ENV }` 删掉了，但它上面那段 JSDoc 被改写后留在原地——现在第 12-19 行是一段不修饰任何声明的孤立文档注释，紧接着第 21-30 行又是 RUNTIME_AGENT_DIR_ENV 自己的 JSDoc。  同时第 1-10 行的文件头仍写着「ARD D8（the backend switch is a dev environment variable, never a settings-page control）」，描述的是一个已
- [core-host-16] LOW doc-mismatch confirmed | P4-2 | docs/plantree/plans/runtime-evolution/evidence/p4-6/README.md:49 | 仅存的加密机现场清单仍要求设置已被删除的 AICLIENT_RUNTIME_BACKEND :: P4-6 的 Windows 交接清单（这是最后一次上机唯一要照着做的那份）第一条仍写着「从设置了 AICLIENT_RUNTIME_BACKEND=native 的启动环境打开测试包；以 worker 证据确认实际后端」。P6-5 当天已把这个变量连同 src/shared/runtimeBackend.ts 一起删除，设它完全没有效果（ARD D8 落地注记、docs/pi-only-rollout-rollback.md:17 都已改写，这份清单没有）。  脚本侧还留着同一条的死代码：scripts/run-
- [core-host-17] LOW doc-mismatch confirmed | P1-0 | docs/plantree/plans/runtime-evolution/evidence/p1/README.md:33 | P1-0 的证据文档与任务树状态互相矛盾：一边写「尚未在 Windows 实测」，一边写「五态已通过」 :: 核心任务树 README.md:62 写的是「P1-0 ✅；Windows 五态命令树清理已通过，不再写『Windows 待实测』」，证据在 Windows-P4-6-evidence/tree-cleanup-five-states.md（2026-09-09 test.11 实测）。但它自己指向的 evidence/p1/README.md 至今仍停在 2026-09-08 的版本：表格里「Windows 进程树/加密机｜未执行」，「仍需完成的载体验收」第 1 条写「Windows 分支调用 taskkill 
- [core-host-18] LOW doc-mismatch confirmed | P1-8 | docs/plantree/plans/runtime-evolution/README.md:70 | P1-8「两载体六项探针通过」的现场证据落后 HEAD 94 个提交，而版本戳无法体现这一点 :: P1-8 的 ✅ 有真实现场证据（Windows-P4-6-evidence/test12-reverify.md:551 与 573，bundled-node 与 electron-utility 各六项 true），这点核实无误。但那次探针的 stamp 记的 git_commit 是 8115ebe1，到当前 HEAD 之间有 94 个提交，其中至少 10 个动过 src/runtime/host、plugins/tools 或 bootstrap.ts——包括 d4fcd600 给 exec 出口加 spa
- [core-host-20] LOW quality confirmed | P1-0 | src/runtime/host/exec.ts:355 | runPipe 每执行一条命令都重新 existsSync 探测 runner 助手路径，未像 io.ts 那样缓存 :: resolveHelper 内部对两个候选路径逐个 existsSync（helpers.ts:20-28）。io.ts 为此做了模块级缓存（io.ts:31-35 的 helperPath，注释还解释了为什么在首次使用时解析而非导入时），exec.ts 却在 runPipe 和 spawnPersistent 的每次调用里都重算一遍——也就是每条 bash 命令、每次工具调用都要做 1~2 次同步 fs 探测。  量级不大，但它发生在 spawn 前的同步路径上，且与同目录另一半的做法不一致。
- [tools-05] LOW bug confirmed | P1-2 | src/runtime/plugins/tools/index.ts:315 | edit 用默认 TextDecoder 解码，会静默删掉文件的 UTF-8 BOM，且 review diff 看不见 :: edit 在 315 行用 new TextDecoder('utf-8', { fatal: true }) 解码原文件。WHATWG 规范里 ignoreBOM 默认 false，含义是「把 BOM 剥掉」。解码得到的 content 因此不含 BOM，改完之后 330 行 Buffer.from(content) 原样写回——文件开头的 EF BB BF 被删除，而模型并没有要求这个改动。file-change.ts:27 的 readBeforeChange 用的是同一个默认解码器，所以 before 和 
- [tools-12] LOW quality confirmed | P1-2 | src/runtime/plugins/tools/read-lines.ts:46 | read/edit 遇到非 UTF-8 内容抛裸 TypeError，没有 code，也没有「这是二进制文件」的说明 :: readLines 用 fatal:true 的 TextDecoder 解码文件字节（46 行），edit 同样（index.ts:315）。文件含非法 UTF-8 序列时 decode 抛 TypeError（"The encoded data was not valid for encoding utf-8"），不是 RuntimeHostError，没有 code 字段。P1-0 契约要求「错误统一带 code 的 Error」，而模型碰到的是一条无法据以行动的底层异常。grep 对同一情况有专门处理（in
- [tools-13] LOW doc-mismatch confirmed | P1-2 | src/runtime/plugins/tools/index.ts:251 | read 的截断说明在「多行合计超预算」时也写成 single line exceeds byte budget :: read 只要 data.partialLine 为真就附上 "single line exceeds byte budget"。但 partialLine 的真正含义是「最后一行被字节预算裁断」，read-lines.ts:63-64 那条路径是在累计 used 逼近 maxBytes 时触发的，与这一行本身有多长无关。绝大多数情况是几百行普通代码把 50 KiB 预算填满了，提示却告诉模型「有一行超长」，会把模型引向错误的应对（去猜哪一行异常长），而不是正确的应对（用 nextOffset 续读）。
- [tools-14] LOW boundary confirmed | P1-4 | src/runtime/plugins/tools/index.ts:431 | glob/grep 恰好取满 limit 时无条件报 truncated，无法区分「还有更多」和「刚好这么多」 :: glob 在 push 之后判断 found.length >= limit 就置 truncated 并 break（431-434 行），grep 同样（536-539 行）。当结果数恰好等于 limit 且后面本来就没有更多候选时，仍然报 "[search truncated; narrow the search or increase limit]"。模型据此会再发一次更大 limit 的搜索，白跑一遍全树遍历。
- [tools-17] LOW boundary confirmed | P1-4 | src/runtime/plugins/tools/index.ts:529 | grep 按 \n 切行后不去掉 CRLF 的 \r，正则锚 $ 在 CRLF 文件上永不命中，匹配行文本也带回车 :: 529 行 split('\n') 之后每行仍带尾部 \r（CRLF 文件）。P5-2-3 新增的 regex 模式因此有两个后果：以 $ 结尾的表达式（如 'export default .*$' 或 'foo$'）在 CRLF 文件里永远不匹配，因为 $ 前面隔着一个 \r；命中行推进 matches 时也带着这个 \r，最终拼进模型可见文本。Windows 是本项目的一等目标平台，CRLF 工作区很常见，而失败方式是静默零命中。
- [permissions-04] LOW security confirmed | P1-5 | src/runtime/plugins/permissions/index.ts:250 | auto 档在 unresolvedPaths 之前返回 allow，无法解析的操作数不再受任何 path/deny 判定 :: evaluate() 里 249-250 行的 `if (gear === 'auto') return 'allow'` 排在 260 行的 `if (request.unresolvedPaths) return 'ask'` 之前。deny 判定（233-248 行）确实在 gear 之前，符合注释里「Every deny is decided BEFORE the gear」的说法——但 deny 只能在**能被静态解析出来的操作数**上生效。一旦 bash 分析把某个操作数标成不可解析（复杂展开 `${X
- [permissions-05] LOW security confirmed | P1-5 | src/runtime/plugins/permissions/bash-analysis.ts:158 | command_name 里的命令替换从不被 visitSubstitutions 访问，内层命令的操作数不参与判定 :: command 分支对前缀赋值（159-162 行）和参数（243 行）都调了 visitSubstitutions，唯独没对 nameNode 调。`$(...)`/反引号出现在命令名位置时，value() 在 110-112 的 default 分支返回 undefined 并置 unresolvedPaths=true，但内层命令本身从不被 walk，它的操作数因此既不进 paths，也不做 pathPolicy / scope / policy 判定。这条路径在 ask / accept-edits 档会因
- [permissions-07] LOW bug confirmed | P1-5 | src/runtime/plugins/permissions/index.ts:238 | plan 模式的工具集有两套互相矛盾的真值来源，browser_preview 被列出却必然被拒 :: ToolsPlugin 用 access 映射决定 plan 模式列不列一个工具（tools/index.ts:75-80，access !== 'write' 就保留），而 PermissionsPlugin.evaluate 用**工具名白名单** ['read','glob','grep','bash'] 决定 plan 模式放不放行。两者对不上的工具就会出现「模型看得到、调了必失败」。browser_preview 正好是这种：tools/index.ts:216-222 特意用 'read' access
- [permissions-08] LOW contract-gap confirmed | P1-6 | src/runtime/worker/permissionPrompt.ts:191 | 119 秒倒计时到底后，审计记录写成「用户拒绝」或「已取消」，永远不会是 timed_out :: PermissionAutoReason（src/shared/types/runtimeEvents.ts:340）定义了 'timed_out'，渲染侧也会把它画成『auto: timed_out』（toolCard.test.ts:950-956），但 native 这条链上没有任何地方产出它：settle 的 autoReason 参数只被 onAbort 传 'aborted'、drain 传 'session_closed'/'aborted'，respond 传 undefined。实际发生的是两条路
- [permissions-12] LOW resource-leak confirmed | P1-6 | src/runtime/plugins/agent-loop/index.ts:185 | 权限记录把 write 的完整文件内容原样写进 trace，并在 worker 内存里长期保留 :: ToolPermissionRequest.preview 用于审批卡原文展示，write 工具传的是未截断的 args.content（上限 8 MiB，src/runtime/plugins/tools/index.ts:269-272），MCP 传的是参数 JSON 的前 4000 字节。agent-loop 的 onActivity 订阅把整条 record（含 request.preview）原样交给 trace.note，trace.note 只是 steps.push(detail)，没有任何截断；一
- [permissions-11] LOW quality confirmed | P1-5 | src/agent-host/sessionTierAuthorizer.ts:132 | 第 10 批删掉 legacy 后端后，两个权限模块成为死代码，随包策略的 authorizerChain 指向不存在的链 :: 本批（工作区未提交改动）删除了 src/agent-host/piWorkerSession.ts 与 piAgentSessionBootstrap.ts，而它们分别是 createSessionTierAuthorizer（sessionTierAuthorizer.ts:132）和 createPermissionActivityObserver（permissionActivity.ts:139）的唯一生产消费者（git show HEAD:src/agent-host/piWorkerSession.ts
- [permissions-13] LOW bug confirmed | P1-5 | src/runtime/plugins/permissions/bash-analysis.ts:63 | BashAnalyzer.ready 缓存了被拒绝的 Promise，一次 WASM 读取失败永久废掉本会话的 bash :: analyze 用 `this.ready ??= this.initialize()` 记忆化。initialize 失败时 loadLanguage 的 catch 会把模块级 sharedLanguage 复位成 undefined（34-37 行），看起来是为重试留了后路——但 this.ready 此时已经是一个被拒绝的 Promise，`??=` 对非 undefined 不再赋值，后续每次 analyze 都 await 同一个 rejected promise。换句话说 sharedLanguage
- [permissions-16] LOW doc-mismatch confirmed | P1-6 | src/runtime/worker/permissionPrompt.ts:96 | write 的审批卡一律标 add，覆盖已有文件时显示为「新增」，且注释与代码不符 :: detailOf 对 write 无条件写 change: 'add'，对 edit 无条件写 'update'。渲染侧据此画徽标 A / M（src/renderer/components/chat/questionCardModel.ts:463-468、613-622）。所以「用新内容整体覆盖一个已存在的文件」这件事，在用户唯一能看到的决策界面上被标成「新增」。函数头注释写的是「`write` reports `add` rather than `update` only when the tool said
- [permissions-17] LOW quality confirmed | P1-5 | src/runtime/plugins/permissions/index.ts:418 | pathPolicy 每次调用都重新编译约十个正则，且被放在 glob/grep/shell 展开的逐条目热路径上 :: pathPolicy 对 AICLIENT_DEFAULT_PERMISSION_POLICY.permission.path 的每个条目都现场 split/escape/join 出表达式并 new RegExp（PATH_RULES 有 10 条）。它被 ToolsPlugin.checkShellPaths 的 check/expand 对每个候选路径调 2-3 次（tools/index.ts:176-192，上限 20000 次），也被 canTraverse 在 glob/grep 遍历里对每个目录条目
- [permissions-18] LOW quality confirmed | P1-5 | src/shared/runtimeShellPolicy.ts:11 | isExplorationCommand 把 -i 一律当危险开关，导致 grep -i / rg -i 在 plan 模式下不算勘察 :: 危险开关的正则里 `-i$` 是对所有命令生效的，而允许清单里只有 rg 和 grep 这两个会带 -i 的命令（sed 根本不在清单里，加这条规则想防的 `sed -i` 本来就过不了名字检查）。结果是最常用的大小写不敏感搜索 `grep -i foo file` 被判为非勘察：plan 模式下 evaluate 会因为 request.exploration 为假直接 deny（index.ts:237-242），用户在 plan 模式里连一次不区分大小写的搜索都做不了，而 `grep foo file` 可以
- [context-prompt-03] LOW contract-gap confirmed-partial-waiver | P2-2 | src/runtime/plugins/prompt/index.ts:41 | run.targetPath 在整个产品里没有任何生产者，嵌套 AGENTS.md/CLAUDE.md 发现实际上是死路径 :: 项目指令链的 root→leaf 走法完全依赖 `options.targetPath`：instructionDirectories 在 targetPath 为空时只返回 [resolvedRoot]（projectInstructions.ts:140）。targetPath 从 RuntimeRunRequest 经 agent-loop/index.ts:127 传到 PromptPlugin.compose，但全仓（src/runtime/worker、src/main、src/agent-host）没
- [context-prompt-04] LOW boundary confirmed | P2-4 | src/runtime/plugins/context/index.ts:259 | 刚 checkpoint 完又送进超大输入时，hard limit 下 compaction_empty_range 直接抛错且无法自愈 :: entriesFor 在 messages[0] 是本服务安装的摘要消息时返回 `[checkpoint, ...rest]`；当 rest 为空（会话最后一条就是 compaction，例如刚做完 /compact 或刚 resume）时，pi 的 prepareCompaction 因为「最后一条是 compaction」返回 ok(undefined)，于是走 giveUp('compaction_empty_range')。giveUp 在 reason === 'hard_limit' 时无条件抛 Run
- [context-prompt-06] LOW bug confirmed | P2-2 | src/runtime/plugins/prompt/instructionSource.ts:5 | OPTIONAL_FILE_ERRORS 只列了 errno 字符串，HostIo 自己的 invalid_host_request 会把整个 run 打挂 :: instructionSource 把「可跳过的缺失」定义成一组 errno（ENOENT/ENOTDIR/EACCES/EPERM/EISDIR/ELOOP），其余一律上抛——这条规则是为 TSD/生命周期错误设计的，是对的。但 runtimeHostIo.readFile 在读到非普通文件时抛的是自有 code：`if (!(await stat(path)).isFile()) throw new RuntimeHostError('invalid_host_request', 'read requires 
- [context-prompt-07] LOW resource-leak confirmed | P2-4 | src/runtime/worker/nativeWorkerRuntime.ts:481 | /compact 的摘要请求既没有 AbortSignal 也没有超时，而它占着串行化的 RPC 链 :: worker.compact 直接 await context.prepareTurn({ force: true })，请求里不带 signal（PrepareTurnRequest.signal 是可选的，这里没给），也不带 thinkingLevel。prepareTurn 在 summary family 下会发一次真实 provider 请求。worker 的 RPC 链是串行的（同文件 353 行与 426 行的注释都明确写了「The RPC chain is serialized」，startSend
- [context-prompt-08] LOW bug confirmed | P2-4 | src/runtime/plugins/context/index.ts:380 | entriesFor 按 checkpoint.retainedTail.length 切片，而 snapshot.messages 过滤掉了失败的 assistant 消息，导入的会话会被多切掉一条真实消息 :: beginRun 用 `snapshot.messages[0]` 作为身份锚点，entriesFor 随后按 `messages.slice(1 + checkpoint.retainedTail.length)` 跳过「摘要 + 保留尾」。这两个数据源的过滤口径不一致：JsonlSessionStore.snapshot() 对 buildSessionContext 的输出再做了一次 `.filter(isSuccessfulMessage)`（store.ts:194），会把 retainedTail 里 
- WAIVED [context-prompt-09] LOW quality waived | P2-7 | src/runtime/plugins/context/prefixStability.ts:84 | 前缀稳定性度量模块在 runtime 里没有任何调用方，只有单测在用 :: docs/plans/2026-09-08-runtime-evolution-ard.md D9 段落：「『请求前缀稳定性的离线度量』没有可搬运的参考实现，属于我们自建，列为 P2 的可选加强项而非门禁；门禁仍是上面这个 provider 上报的在线比值」；docs/plantree/plans/runtime-ev
- [context-prompt-10] LOW doc-mismatch confirmed | P2-8 | src/runtime/plugins/context/index.ts:203 | 提醒档位实际是「每个 run 一次」而非文档写的「每个会话一次」，且压缩后不重置 :: budget.ts 的 ReminderState 注释写「Which reminders have already been sent in this session」，selectReminder 的注释写「Each tier fires once per session」，参照实现 PI-Desktop 也是挂在 runtime 实例上的会话级布尔。但 beginRun 每个 run 开头都把 `this.reminders = NO_REMINDERS_CLAIMED`，而 beginRun 在每次 han
- [context-prompt-11] LOW quality confirmed | P2-3 | src/runtime/plugins/agent-loop/index.ts:326 | run 前那次 prepareTurn 丢掉了 reminder 与 skipped，trace 里看不到，提醒消息却被永久塞进 agent.state.messages :: 回合边界那条路径（227-256）对 compaction / skipped / reminder 三种结果都写了 trace note，run 前的历史压缩路径（315-330）只处理 compaction。结果有两处不一致：(1) 这里若返回的是 reminder，`agent.state.messages = prepared.messages` 会把那条 custom('context-budget') 消息永久写进 agent 状态，之后每一轮 createContextSnapshot 都带着它，但 
- [context-prompt-13] LOW boundary confirmed | P2-2 | src/runtime/plugins/prompt/projectInstructions.ts:92 | isWithinRoot 对文件系统根（/ 或 C:\）成立不了，根目录工作区 + targetPath 会整条指令链静默消失 :: isWithinRoot 用 `path === root || path.startsWith(root + sep)` 判断包含关系。当 root 本身以分隔符结尾（POSIX 的 '/'、Windows 的 'C:\\'，resolve 不会去掉它们的尾分隔符）时拼出来的前缀是 '//' 或 'C:\\\\'，任何真实子路径都匹配不上。instructionDirectories 于是对 targetPath 直接返回 undefined，loadInstructionChain 连根目录的 AGENTS.m
- [context-prompt-14] LOW doc-mismatch confirmed | P2-1 | src/runtime/plugins/tools/prompt.ts:11 | tool-guidance 用的工具名大小写与实际注册名不一致，plan 模式下还在介绍不存在的写类工具 :: 实际注册的工具名全是小写：read / write / edit / bash / glob / grep（plugins/tools/index.ts:224-448）。tool-guidance 段落里却写成 'Read uses a one-based line offset'、'When Edit is available'、'Bash requires the configured shell'，同一句里 glob/grep 又是小写。这个段落是 static 稳定档，会出现在每一次请求的缓存前缀里。另外
- [context-prompt-15] LOW boundary confirmed | P2-2 | src/runtime/plugins/prompt/instructionSource.ts:22 | 指令文件只按 UTF-8 解码，UTF-16/GBK 保存的 CLAUDE.md 会把乱码注入系统提示词 :: readText 用默认的 TextDecoder（utf-8、非 fatal）解码，只处理了「有界读取可能切断 UTF-8 字符」这一种边界（用 stream 标志把不完整尾部丢掉）。对整份文件本身不是 UTF-8 的情况没有任何识别：Windows 记事本另存为 UTF-16LE 的 CLAUDE.md 会被逐字节当作 UTF-8 解释，产生大量 U+FFFD 和 NUL 之间夹杂的字符，随后原样进入系统提示词并占满 32 KiB 预算。非 fatal 解码意味着不会报错，也不会跳过。BOM 情形倒是正确的（默
- [context-prompt-16] LOW doc-mismatch confirmed | P2-1 | src/runtime/plugins/prompt/segments.ts:116 | staticPrefixBytes 被描述成「provider 可缓存前缀的下限」，但工具定义在 system 之前且随 mode 变化 :: ComposedPrompt.staticPrefixBytes 的注释写「so it is the floor of what a provider could serve from cache」。对 anthropic-messages 而言可缓存前缀的顺序是 tools → system → messages（pi-ai 的适配器把 cache_control 同时挂在 system 块和最后一条 user 消息上，见 node_modules/@earendil-works/pi-ai/dist/api/a
- [context-prompt-17] LOW contract-gap confirmed | P2-3 | src/runtime/plugins/subagent/run.ts:202 | 子代理的 Agent 完全没有上下文预算与压缩，最多 80 轮全靠窗口自然撑 :: SubagentRun 直接 new Agent(...)，没有传 prepareNextTurnWithContext，也没有任何 contextBudget / 提醒接线；D10/D17 允许的 maxTurns 上限是 80。父 loop 那一整套「回合边界压缩 + 两档提醒 + new_context 工具」在委派任务里一条都不生效。委派任务恰恰是最容易堆上下文的场景（一个 explore 型 delegate 连续 read/grep）。这属于设计缺口而非编码错误，归属可能在 P5-2；在本区域记下来是因
- [session-08] LOW contract-gap confirmed-partial-waiver | H/20 | /home/ai/code/ai-client/src/runtime/plugins/session/store.ts:369 | GUI 侧的改名/书签对 CLI 不可见（互通在名字这一维是单向的） :: mutate 写出的 fact 行带的是 cliBookkeeping 的 type:'custom'，而 CLI 读会话名走的是 type==='session_info'（session-manager.js 的 buildSessionInfo 与 getSessionName），读标签走 type==='label'。因此 GUI 里改的名字、打的书签，在 pi 的会话列表和 TUI 里完全看不到——反方向（TUI 改名 → GUI 显示）是通的，也正是验收清单第 2 条所覆盖的方向。topic 文档把「一
- [session-09] LOW quality confirmed | P3-3 | /home/ai/code/ai-client/src/runtime/plugins/session/legacy.ts:486 | prepareSessionConfig 解析源文件首行时抛的是裸 SyntaxError，空文件会得到无 code 的报错 :: 这里直接 JSON.parse 第一条非空行，没有像 codec 的 decodeSession 那样包成带 code 的 RuntimeHostError。空文件（content 为 ''）会走到 JSON.parse('') 抛 SyntaxError: Unexpected end of JSON input，非 JSON 文件同理。0 字节会话文件是可达状态：writeFile(flag:'wx') 先创建空文件再写内容，进程在这中间被杀就会留下 0 字节的 .jsonl。Main/renderer 对 R
- [session-12] LOW quality confirmed | P3-1 | /home/ai/code/ai-client/src/runtime/plugins/session/legacy.ts:480 | 32 MiB 默认上限在四处各写一遍，改一处就会出现不一致的读/写边界 :: session 的默认字节上限 32*1024*1024 在 store.ts:101 与 legacy.ts 的 480、508、537 各硬编码一次。这四处分别控制「打开时读多少」「转换结果允许多大」「复用旧副本时读多少」「store 自己的预算」，任何一次调整只改其中一两处，就会出现「转换允许 64MiB 但打开只读 32MiB」这类自相矛盾的边界，而且不会有测试发现。
- [session-13] LOW test-gap confirmed | H/20 | /home/ai/code/ai-client/src/runtime/__tests__/sessionInterop.test.ts:185 | 压缩互通用例用的是手工构造的 retainedTail，钉不住真实压缩的锚点行为 :: 「shows the CLI the tail a compaction retained」这条用例直接构造 CompactResult，retainedTail 恰好就是分支上最后两条消息条目，于是 compactionAnchor 的「倒数第 N 条」算法必然算对。真实压缩路径（plugins/context）产出的 retainedTail 是「最近一条 user 消息」，长度 0 或 1，且末尾条目通常是 toolResult——即 session-03 描述的错误场景。这条用例的绿色会让人以为锚点逻辑已经
- [loop-model-02] LOW bug confirmed | P5-5 | src/runtime/plugins/model-adapter/catalog.ts:231 | model 行覆盖的 api 被解析并保留，但绑定时仍用 provider 的单一适配器 :: `parseModel` 支持 model 行自带 `api`（`asApi(model.api) ?? providerApi`），`buildModel` 也把它写进 `Model.api`，catalog.test.ts:130-143 还专门钉住了这个行为。但 `buildProviderModels` 传给 pi-ai 的是 `api: API_ADAPTERS[provider.api]()` —— **单个** ProviderStreams。pi-ai 的 `createProvider` 明确区分
- [loop-model-03] LOW bug confirmed-partial-waiver | P4-4 | src/runtime/plugins/agent-loop/providerErrors.ts:136 | 错误文本里出现 abort 一词即判为不可重试的 TURN_ABORTED，先于状态码判定 :: `classifyProviderFailure` 的第一条判定是 `(error instanceof Error && error.name === 'AbortError') || /\babort/i.test(rawMessage)`，命中即 `TURN_ABORTED, retriable:false`，排在网络判定与状态码判定之前。而真正的取消在这条路上根本走不到关键词分支：pi-ai 适配器在 signal 已 abort 时把事件发成 `{ type:'error', reason:'aborte
- [loop-model-07] LOW bug confirmed | P5-5 | src/runtime/plugins/model-adapter/catalog.ts:319 | auth.json 损坏或形状不对时被静默当成「没有任何密钥」 :: `readOptionalJsonFile` 对解析失败一律 `catch { return null; }`，`asRecord` 对数组/标量也返回 null。models.json 的对应路径是抛 `models_json_unparsable` 并把解析错误写进消息，auth.json 这条却什么都不留。返回 null 之后每个 provider 的 `apiKey` 都变成 `''`，接着触发 loop-model-04 那条 43 秒重试，最终报「No API key」。用户看到的现象与「密钥没同步」完
- WAIVED [loop-model-09] LOW boundary waived | P4-4 | src/runtime/plugins/agent-loop/providerRetry.ts:201 | 服务端给 Retry-After: 0 时退避为 0，四次请求会紧挨着打出去 :: docs/plantree/plans/runtime-evolution/README.md:217（『服务端 Retry-After 一律优先』）；docs/plans/2026-09-09-gui-defect-decisions.md:78（『retry-after-ms / Retry-After 秒数 / 
- [loop-model-10] LOW doc-mismatch confirmed | P4-4 | src/runtime/plugins/agent-loop/index.ts:212 | 注释声称「每次请求重读密钥，磁盘上改了会被拾起」，实际是 bootstrap 时固定的 :: `getApiKey` 返回的是 `resolved.requestKey`，而 `requestKey` 来自 `ModelAdapterPlugin.bindProvider` 在**构造时**读到的 `provider.apiKey`（catalog 只在 bootstrap 读一次）。binding.ts 顶部的措辞是准确的（「by the next bootstrap」），这里的措辞不是。同时 binding.ts:118 的 `resolve` 回调忽略了 pi-ai 传进来的 credential，直
- [loop-model-11] LOW quality confirmed | P4-4 | src/runtime/plugins/agent-loop/providerErrors.ts:66 | 结构化字段探测（status/statusCode/cause 链、providerCode）在生产路径上是死代码 :: 模块头写着「classification probes structured fields first and falls back to message keywords」。但两个生产调用方传进来的都是**字符串**：providerRetry.ts:381-383 取 `event.error.errorMessage`，subagent/run.ts:356-359 取 `message.errorMessage ?? '…'`。`asErrorLike` 对字符串返回 undefined，于是 `extra
- [loop-model-12] LOW quality confirmed | P5-5 | src/runtime/plugins/model-adapter/catalog.ts:247 | thinkingLevelMap 缺省时被写成 null，与相邻两行的 `?? undefined` 不一致 :: `asRecord` 在字段缺省时返回 `null`，紧邻的 `samplingParams` 与 `compat` 都写了 `?? undefined` 做归一，`thinkingLevelMap` 这一行没有，而是用 `as Record<string,string|null> | undefined` 把 null 断言掉。声明的类型是可选属性，实际值可以是 null——一个被类型断言遮住的口子。目前唯一消费者 `buildModel` 用真值判断，所以暂时无害。
- [loop-model-13] LOW quality confirmed | P0-5 | src/runtime/plugins/agent-loop/index.ts:222 | 64 轮上限以字面量在两处重复，改一处就会静默错配 :: `shouldStopAfterTurn` 里的 `64` 决定循环何时停，`resolveError` 前的 `turnCount >= 64` 决定这次停机是否报 `turn_limit`。两处是各自独立的字面量，没有共享常量。改其中一个，另一个就会把「正常结束」误报成 turn_limit（或反过来把截断静默成成功）。
- [loop-model-14] LOW boundary confirmed | P0-5 | src/runtime/plugins/agent-loop/index.ts:297 | 发车前的上下文预检不计图片附件的体积 :: `incomingTokens` 只估算 `prepared.text` + 系统提示词 + 工具定义，没有把 `prepared.images` 算进去。pi-agent-core 自己的 `estimateTokens` 对 image 块是按 `ESTIMATED_IMAGE_CHARS` 计的，说明图片确实占预算。于是「新输入必须自己装得下」这条硬门在纯图片（或大量图片 + 短文字）的场景下会放行本来该被挡住的请求，改由 provider 侧报上下文溢出。
- WAIVED [loop-model-15] LOW contract-gap waived | P4-4 | src/runtime/plugins/agent-loop/providerRetry.ts:22 | 父循环没有流中失败恢复，子代理有——同一层里两套能力 :: docs/plantree/plans/runtime-evolution/evidence/p4-6/f4-retry/README.md『边界：这一层管到哪为止』一节：『只接管流开始之前的失败。已经 start 的流原样透传——中途换掉一条半流完的 assistant 消息属于 mid-stream 恢复，需要 l
- [loop-model-16] LOW quality confirmed | P5-5 | src/shared/modelBaseUrl.ts:47 | derivesBaseUrl 是死代码，只有它自己的测试在用 :: 全仓检索（含 .ts/.tsx/scripts/docs）只找到定义与 `src/shared/__tests__/modelBaseUrl.test.ts` 的一次调用。D15 的推导实际由 `deriveInheritedBaseUrl` 在 `toPiModelsJson` 里完成，`stripRedundantVersion` 由 `userProviders.ts` 用；这个判定函数没有任何生产消费者。
- [worker-runtime-01] LOW bug confirmed | P4-1 | src/runtime/worker/nativeWorkerRuntime.ts:693 | discardFork 删除自有会话文件必然失败：dispose 之后 hostIo 已停用 :: discardFork 的「owned」分支先取 hostIo 引用，再 await this.dispose()（内部 handle.dispose() 会调用 runtimeIo.shutdown()，把 HostIoPlugin.disposed 置为 true），然后用这个已停用的 io 去 unlink。HostIoPlugin.track() 第一行就是 if (this.disposed) return Promise.reject(new RuntimeHostError('runtime_disp
- [worker-runtime-07] LOW resource-leak confirmed | P4-1 | src/runtime/worker/nativeWorkerRuntime.ts:784 | dispose 里 handle.dispose() 未兜底，抛错会让 worker 不再自行退出 :: NativeWorkerRuntime.dispose 最后一句 await handle?.dispose() 没有 catch；bootstrap 的 dispose 会把 exec.shutdown()/active run 的 rejection 以及 session.close()/io.shutdown()/fiber.dispose() 的异常抛出来。RPC 服务端 handleDispose 里这一句在 try 之外的位置：抛出后被 dispatch 的 catch 转成错误响应，this.opti
- [worker-runtime-09] LOW contract-gap confirmed | P4-1 | src/agent-host/worker.ts:120 | 一次性补全通道从不接收 modelCatalog，仍然依赖派生的 models.json/auth.json :: worker 入口构造 NativeUtilityRuntime 时只传 host/agentDir/env/log，没有 modelCatalog；utility.start 的载荷里也没有这个字段（src/shared/types/workerRpc.ts:552-559），Main 的 PiUtilityService 也无处传。于是 NativeUtilityRuntime.runtime() 建图时 modelCatalog 恒缺省，createRuntime 只能回落到 readPiCatalog 去读
- [worker-runtime-10] LOW quality confirmed | P4-1 | src/runtime/worker/nativeWorkerRuntime.ts:193 | recordFileChanges 直接读 process.env，与同一处其它配置的 options.env 约定不一致 :: 同一个 tools 配置块里，shellPath 走 this.options.host.childEnv、agentDir 走 this.options.env（resolveAgentDir，841-843 行），唯独 recordFileChanges 读真实的 process.env。flags.ts 文件头明确写了「所有开关都从传入的 env 读，不直接读 process.env，这样测试不必改进程状态」。结果是注入了 env 的探针/测试无法关掉会话评审记录，而真实进程里多一个不受注入控制的隐藏输入。
- [worker-runtime-11] LOW quality confirmed | P4-4 | src/runtime/worker/nativeUtility.ts:235 | utility.start 的 cwd 被协议强校验却完全没被使用 :: isWorkerUtilityStartPayload 强制 cwd 非空字符串（src/shared/types/workerRpc.ts:804），但 NativeUtilityRuntime 从头到尾只用 input.prompt / input.timeoutMs / input.operationId / input.model / input.effort，cwd 一次都没读。被删掉的 pi 版本同样没用它（工作目录由 Main 在 fork worker 时给），所以这是一个只剩校验、没有语义的契约字
- [worker-runtime-13] LOW test-gap confirmed | P4-4 | src/runtime/__tests__/nativeUtility.test.ts:154 | 一次性补全的 effort 用例缺 'off'，正好漏掉被删旧套件里唯一钉住它的断言 :: 新套件覆盖了「目录钉的 per-model 档位生效」和「显式 effort 覆盖目录档位」，但没有 'off' 的用例。被删的 piUtilityRunner.test.ts 里有 'omits the field for off rather than substituting a level'，虽然它测的是「配置值为 off」而非「显式请求 off」，但那是这一族语义唯一的守卫；随文件删除后，worker-runtime-02 描述的行为变化没有任何测试会失败。
- [rpc-projector-05] LOW bug confirmed | P4-3 | src/agent-host/piWorkerRpcServer.ts:991 | dispose 先置位 disposed 再拆 runtime，收尾阶段发的事件被 RPC 层全部吞掉 :: handleDispose 在 `await this.runtime?.dispose()` 之前就把 `this.disposed = true`，而 emitRuntimeEvent / emitUtilityEvent 的第一行都是 `if (this.disposed) return;`。下面两处代码是按「拆除期间发的事件能出去」写的，实际上一条也出不去：nativeWorkerRuntime.ts:765-774 专门把 permissions/questions/previews 的 drain 放在
- [rpc-projector-06] LOW resource-leak confirmed | P4-3 | src/agent-host/piWorkerRpcServer.ts:993 | handleDispose 没有 try/finally：runtime.dispose 抛错时 utilityRuntime / importWriter 不释放，onDisposed 不触发 :: 三个 dispose 顺序 await，中间任何一个 reject 都会让后面两个不执行、三个引用不置空，而且 `this.options.onDisposed?.()` 也到不了。worker.ts:142-152 里 onDisposed 是这个进程唯一的自我退出出口（electron 走 process.exit(0)，bundled-node 走 process.disconnect()）。PiImportWriter 的 dispose 注释（piWorkerRpcServer.ts:223-228）明说
- [rpc-projector-07] LOW contract-gap confirmed | P3-4 | src/runtime/events/projector.ts:256 | tool.updated 不再带 input，渲染层那条 reducer 分支因此永远是空转 :: 旧引擎的 tool.updated 同时带 `input: event.args` 和 `status`（`git show HEAD:src/agent-host/piWorkerSession.ts` 第 1157-1166 行）。native projector 只发 status。渲染层 chatSessions.ts:942-943 的第一行就是 `if (event.payload.input === undefined) return {};`，所以 native 后端发出的每一条 tool.upda
- [rpc-projector-08] LOW bug confirmed | P4-3 | src/agent-host/piWorkerRpcServer.ts:985 | worker.setPermissionTier 无条件回 applied:true，哪怕 runtime 根本没实现它 :: 这个 handler 用两个可选链把调用做成「尽力而为」，然后不看结果直接回 applied:true。同文件里的 handleSetPermissions（961-965）对同一类情况是抛 WORKER_UNSUPPORTED，两个 handler 对同一个问题给出相反答案。而 D14 的档位是安全相关的轴：Main 认为档位已生效、UI 也照着改显示，实际上 runtime 可能什么都没做。
- [rpc-projector-09] LOW quality confirmed | P4-3 | src/agent-host/piWorkerRpcServer.ts:670 | 旧引擎删除后留下一批不可达分支与过时注释（WORKER_*_UNAVAILABLE 等 11 处） :: PiWorkerRuntime 接口把 tree/commands/compact/rewind/reload/fork/discardFork/respondPermission/respondQuestion/respondPreview/setPermissions/setPermissionTier 全声明为可选，这是为了容纳第二个后端。第 10 批把旧引擎删了，现存唯一实现 NativeWorkerRuntime 把这些方法全部实现（nativeWorkerRuntime.ts:141/146/151/4
- [rpc-projector-10] LOW quality confirmed | P4-3 | src/agent-host/piWorkerRpcServer.ts:712 | handleCompact 对「runtime 存在但不支持 compact」也报 WORKER_NOT_BOOTSTRAPPED :: 同文件里 tree/rewind/reload/fork 都先判 `!this.runtime` 报未启动、再判方法缺失报各自的 *_UNAVAILABLE。compact 把两种情况合成一个可选链，统一报 WORKER_NOT_BOOTSTRAPPED，错误信息与真实原因不符。
- [rpc-projector-14] LOW test-gap confirmed | P4-3 | src/agent-host/__tests__/piWorkerRpcServer.test.ts:56 | 测试把两个工厂函数混进了 WorkerBootstrapResult，构造出一个真实环境不可能存在的 RPC 结果 :: 第 10 批给 bootstrapResult() 加了 `...engineFactories`，但 WorkerBootstrapResult（workerRpc.ts:282-330）根本没有 createImportWriter / createUtilityRuntime 字段。TypeScript 对展开属性不做多余属性检查，所以编译通过。后果是这个测试里 worker.bootstrap 的成功结果携带两个**函数**，而真实载体（utilityProcess MessagePort / Node I
- [rpc-projector-15] LOW quality confirmed-partial-waiver | P4-3 | src/agent-host/piWorkerRpcServer.ts:1021 | worker 侧 seq 到 Main 就被重编号，实际是死字段；类注释也与实际串行范围不符 :: emitRuntimeEvent 给每个事件打 `seq: ++this.eventSequence`，但 WorkerManager.dispatch（WorkerManager.ts:2901-2907）对每个事件都 `{...event, seq: ++this.eventSequence, timestamp: this.now()}`，worker 的编号与时间戳在 Main 出口被完全覆盖。全仓没有任何消费方读 worker 侧的 seq（guiEventContract.test.ts:167 还专门
- [rpc-projector-17] LOW contract-gap confirmed | P4-5 | src/shared/types/runtimeEvents.ts:203 | session.stderr 在全仓没有任何生产者，worker 的 stderr 永远到不了用户 :: SessionStderrEvent 有完整类型定义和渲染消费方（contextSurfaceModel.ts:428 的 foldStderrLine，进「运行事实」面板），但 grep 全仓 'session.stderr' 只命中类型定义、渲染消费方和测试，没有任何 emit。Main 确实收集了 worker 的 stderr（WorkerManager.ts:2113-2118 absorbStderr），但只写日志，崩溃时 dumpWorkerStderr 也只 console.error（2129-2
- [rpc-projector-18] LOW contract-gap confirmed | P3-4 | src/runtime/worker/permissionPrompt.ts:191 | 权限卡片超时被上报成 autoReason='aborted'，'timed_out' 没有任何产出路径 :: PermissionPrompt 的 settle 支持三种 autoReason（'session_closed' | 'aborted' | 'timed_out'，见 109 行的类型），但 'timed_out' 在这个文件里从来没有被传过。权限插件的 120 秒超时（plugins/permissions/index.ts:364-367）是通过 abort 同一个 approvalSignal 实现的，permissionPrompt 只有一个 onAbort 回调，无法区分「用户按了停止」与「等了两分
- [skills-mcp-07] LOW bug confirmed | P5-3 | src/runtime/plugins/mcp/config.ts:151 | 项目文件里的 disabled: true 关不掉用户文件里已启用的同名服务器 :: 合并规则是「后读的文件同名者胜」（靠 byName.set 覆盖），但 disabled 的判断是一条 continue：后一个文件里 disabled 的条目只是不写入，不会删掉前一个文件已经写进 byName 的启用条目。函数自己的文档说 disabled 之所以要认，就是为了「用户在别处关掉的服务器不该在这里跑起来」，实现与这句话相反。
- [skills-mcp-10] LOW bug confirmed | P5-1 | src/runtime/plugins/skills/loader.ts:200 | YAML 折叠/块标量的 description 被读成一个 '>' 字符，既不报诊断也不为空 :: 模块头部明确承诺：「需要真正 YAML 的 frontmatter 就当作没有声明 description，并据此报诊断」。但简易解析器对 `description: >` / `description: |` 取到的 value 是 '>' 或 '|'——非空，于是既不落 invalid_metadata 诊断，也不被丢弃，而是原样当成描述进系统提示词。多行引号跨行的写法同理（value 变成 '"Extract text' 这样的半截串）。
- [skills-mcp-13] LOW security confirmed | P5-3 | src/runtime/plugins/permissions/index.ts:395 | MCP 的「本次会话允许」按工具名记住，与参数无关——批准一次等于批准这个工具的所有后续调用 :: grantKey 由 [tool, path, command, paths] 组成。对本地工具这组值就是调用的实质（write 有 path、bash 有 command），但 MCP 调用的实质全在 arguments 里，而 path 恒为 cwd、command 恒为 null。于是一次 allow-session 之后，同名 MCP 工具的任何参数组合都直接 allow（evaluate 第 259 行 this.grants.has(grantKey(request))），不再出卡。而卡片上给用户看的恰
- [skills-mcp-14] LOW resource-leak confirmed | P5-3 | src/runtime/plugins/mcp/client.ts:121 | 帧长上限按 UTF-16 长度而非字节计；超限后连接作废但子进程从不被杀 :: 两点：(1) this.buffer.length 是 UTF-16 码元数，常量名与报错文案都写的是 bytes，中文场景下实际上限约为声明值的 1/3 到 1/2；(2) fail() 只把 closed 置真并 reject 掉在途请求，不杀子进程，也不把 connection.error 标上——那台失控的服务器继续跑、继续往管道里写，receive() 继续无谓地拼接和丢弃，直到会话 dispose 才被 exec.shutdown 收走。textOf（index.ts:164）用 parts.lengt
- [skills-mcp-15] LOW quality confirmed | P5-3 | src/runtime/plugins/mcp/index.ts:153 | spawn 失败时用 `client as McpClient` 伪造了一个并不存在的 client :: exec.spawn 抛错时 client 仍是 undefined，却被断言成 McpClient 放进 McpConnection。类型上 McpConnection.client 是必填非空，实际可能是 undefined。目前 close() 与工具注册都用 `if (connection.error) continue` 绕开了它，所以没有现网触发点；但 connections 是 RuntimeMcpService 的公开只读字段，任何以后读 connection.client 的消费者（管理界面、诊断
- [skills-mcp-16] LOW doc-mismatch confirmed-partial-waiver | P5-3 | src/runtime/plugins/mcp/client.ts:144 | 注释承诺对未实现的方法回「method not found」，代码其实是一声不吭地丢弃 :: 文件头写着「半吊子的 sampling/createMessage 比一句诚实的 method not found 更糟」，但 dispatch 对服务器发来的 request（有 id、有 method、没有 result/error）在 isResponse 处直接 return，既不回 JSON-RPC 错误也不记日志。由于 initialize 声明的 capabilities 是空的，守规矩的服务器不会发这类请求，所以影响有限；但真发了的服务器会一直等一个永远不来的回复。
- [skills-mcp-17] LOW doc-mismatch confirmed | P5-1 | src/runtime/plugins/skills/loader.ts:304 | loadSkills 的文档首句写「first-wins」，实现是 last-wins :: 同一段注释的首句说「resolve name collisions by first-wins」，正文紧接着说「the LAST one to claim a name wins」，代码（byName.set 循环）是后者。测试也钉的是 last-wins（skills.test.ts:181-189）。首句是错的，且这是读代码的人最可能只扫一眼的那一行。
- [skills-mcp-18] LOW bug confirmed | P5-1 | src/runtime/plugins/skills/expand.ts:61 | $ARGUMENTS / $@ 用字符串做替换值，用户参数里的 $& 和 $' 会被当成替换模式 :: 前两次替换用的是函数回调（安全），最后两次 replace(/\$ARGUMENTS/g, allArgs) 与 replace(/\$@/g, allArgs) 用的是字符串替换值，于是 allArgs 里的 $&、$'、$`、$1 会按 JS 替换模式展开。此外替换是顺序执行的：先展开的 $1 结果会参与后面的 ${@:N}/$ARGUMENTS 匹配，参数里带 $ARGUMENTS 会被二次展开。已核对上游 pi（pi-agent-core dist/harness/prompt-templates.js 的
- [skills-mcp-19] LOW quality confirmed | P5-1 | src/runtime/plugins/skills/templates.ts:138 | 模板描述回退到首行时不设长度上限（上游截到 60 字符） :: 没有 description 时用 firstLine(body) 作为描述，而 firstLine 只做空白折叠、不截断；扫描读取上限是 16 KiB，所以单行很长的模板会产生一条最长约 16 KiB 的描述，经 worker.commands → IPC → 补全菜单原样传递（sanitizeWorkerCommandRows 只限行数 WORKER_COMMAND_INVENTORY_MAX，不限字段长度）。上游 pi 在这里是 slice(0,60) + '...'。技能描述有 MAX_DESCRIPTIO
- [skills-mcp-20] LOW doc-mismatch confirmed | P5-1 | src/runtime/plugins/skills/index.ts:195 | 技能/模板目录在 worker 生命周期内只扫一次，与 WorkerManager 承诺的「不缓存，刚装的技能立刻可见」矛盾 :: SkillCatalog 在 createRuntime 时构建一次（bootstrap.ts:253），之后 SkillsPlugin 只读这份快照：segment()、expand()、commands() 全部走 this.catalog。而 Main 侧 getSlashCommands 的注释明确写着「Not cached ... 缓存会让用户刚装的技能一直藏着直到有人让它失效」（WorkerManager.ts:1141-1143）——那句话在 native 后端不成立，因为缓存在 worker 里。P
- [skills-mcp-21] LOW boundary confirmed | P5-3 | src/runtime/plugins/mcp/config.ts:191 | MAX_SERVERS 截断发生在按名字排序之后，保留哪 16 台取决于字母序 :: 先 sort 再 slice(0, MAX_SERVERS)，所以声明超过 16 台时，留下的是名字靠前的 16 台，而不是用户声明顺序靠前的、或项目覆盖过的那些。诊断只说「只启动前 16 台」，没说按什么排序，也没列出被丢掉的名字。
- [skills-mcp-22] LOW contract-gap confirmed-partial-waiver | P5-1 | src/runtime/plugins/skills/index.ts:151 | 项目 .agents/skills 只看 cwd 一层，pi 会一路找到仓库根 :: pi 的发现规则原文是「.agents/skills/ in cwd and ancestor directories (up to git repo root)」（pi-coding-agent docs/skills.md:31），本实现只加 join(cwd, '.agents', 'skills')。K1 的书面验收没写祖先目录，所以不算验收未达；但 loader.ts 头部声称目录布局是逐条照搬 pi 的，这一条没搬，monorepo 场景下行为会与 pi CLI 不同。
- [skills-mcp-24] LOW test-gap confirmed | P5-3 | src/runtime/__tests__/mcp.test.ts:119 | MCP 测试缺少分帧、并发、取消与重名这几类关键边界 :: 现有 fixture 覆盖了正常 / stdout 噪声 / 分页 / 永不应答 / 调用中崩溃五种行为，端到端很扎实。但本区域最容易出事的几条边界一条都没钉住：(1) 一条消息跨多个 stdout chunk 到达、且切在多字节字符中间（对应 skills-mcp-04）；(2) tools/list 返回重名工具或截断后重名（对应 skills-mcp-02）；(3) 服务器答完 initialize 后在 tools/list 上挂死（对应 skills-mcp-01）；(4) 调用途中 abort，断言 p
- [skills-mcp-25] LOW test-gap confirmed | P5-1 | src/runtime/__tests__/skills.test.ts:215 | 技能加载的限额、深度、符号链接与权限错误分支没有测试 :: 发现规则、frontmatter、覆盖优先级、描述截断与稳定排序都钉得很好。没有覆盖的是：(1) 符号链接条目（对应 skills-mcp-08）；(2) MAX_SKILLS 上限与 'too_many' 诊断；(3) MAX_SKILL_DEPTH 截断；(4) readText 抛 EACCES 时「静默当作不存在」这条路径；(5) skill 工具读不到文件时的兜底文案（index.ts:225-235）；(6) templateBody 对 CRLF/BOM 的处理（parseFrontmatter 测了
- [subagent-core-04] LOW bug confirmed-partial-waiver | P5-2-2 | src/runtime/plugins/subagent/registry.ts:271 | prune() 只保护 running，不保护「已结算但尚未交付」，超过 100 条后最老的未交付报告被静默淘汰 :: `prune()` 按 completedAt 从旧到新删除超出 `MAX_RETAINED_DELEGATIONS` 的已结束记录，唯一的保护条件是 `status !== 'running'`。`deliveredAt` 完全不参与判断。一条已结算但父模型还没读到报告的记录（正是 P5-2-2 专门引入 `deliveredAt` 要保住的那类）会和普通历史一样被删掉，`undelivered()` 从此看不到它，报告永久丢失且无任何日志。契约只写了「registry 保留最多 100 条已结束项，绝不淘汰运行
- [subagent-core-06] LOW contract-gap confirmed | P5-2-4 | src/runtime/plugins/subagent/index.ts:254 | native 路径的 subagent.activity 只有单事件大小限额、没有每委派事件条数上限，kind:'capped' 从不产生 :: `emitActivity` 对每个 payload 无条件 `events.emit`。records.ts 里确实做了尺寸夹紧（工具参数 240 字、文本 4000 字），但条数完全没有上限。legacy 侧定义的 `SUBAGENT_EVENTS_MAX_PER_DELEGATION = 200`（src/agent-host/subagentProjection.ts:74，注释写明「past this the carrier emits a single kind:'capped' and goes si
- [subagent-core-07] LOW quality confirmed | P5-2-2 | src/runtime/plugins/subagent/index.ts:662 | settle() 里两处 `record.status === 'running' ? result.status : record.status` 是永假三元，掩盖了真实意图 :: 进入这两行时 `this.registry.settle()` 已返回 true，意味着它刚刚把 `record.status` 从 'running' 改成了终态，所以 `record.status === 'running'` 恒为 false，三元永远取 `record.status`。写成三元会让读者以为存在「registry 还没改状态」的分支，实际不存在。同一表达式在 674-675 行又抄了一遍（传给 `activityForSettlement`）。属于可维护性问题，不影响行为。
- [subagent-core-08] LOW quality confirmed | P5-2-2 | src/runtime/plugins/subagent/run.ts:127 | SubagentRunOptions.resolveToolOutcome 从未被任何调用方提供，是带误导性文档的死选项 :: `resolveToolOutcome` 的 JSDoc 声称「The session's own afterToolCall bookkeeping, so a host failure inside a delegate reaches its tool-error channel the way it reaches the parent's」，但全仓 grep 只有两处命中：类型声明本身和 `afterToolCall` 里的可选调用，`SubagentPlugin` 构造 `SubagentRun` 时从
- [subagent-core-09] LOW quality confirmed | P5-2-2 | src/runtime/plugins/subagent/index.ts:231 | SubagentService.busy 没有任何消费者，「父 run 不得结束」实际由 collectFinished 保证 :: 接口注释写「True while any delegate runs; the parent run must not be allowed to end」，但全仓没有一处读它：agent-loop 靠 `collectFinished()` 循环把 run promise 拖住，bootstrap 的交叠 run 拒绝靠 `active.size > 0 || session?.busy`。这条 API 是死的，而它的注释又恰好描述了一条关键不变量，容易让后来者以为不变量是由它维持的。
- [subagent-core-12] LOW contract-gap confirmed | P5-2-4 | src/runtime/plugins/subagent/index.ts:805 | TaskWait 的 details.delegations 不限条数也不限单条报告长度，整份大对象随 toolResult 落进 JSONL :: 返回文本被 `formatDelegationResults` 卡在 50k，但 `details.delegations` 是 `targets.map(...)` 的完整数组，每项带未裁剪的 `record.result.report`（各自最多 12k），条数等于模型传的 id 个数（registry 最多留 100 条已结束项）。pi 把 `details` 原样放进 toolResult 消息（pi-agent-core dist/agent-loop.js:532-541），agent-loop 的 s
- [subagent-core-13] LOW contract-gap confirmed | P5-2-2 | src/runtime/plugins/subagent/index.ts:376 | provider 8 个的上限在 Task 路径上无人执行：subagentPinDiagnostics 只有测试引用 :: `resolveModel` 直接用 `resolveSubagentPin(definition.model, available)`，不过任何 pin 诊断。而 `subagentPinDiagnostics`（catalog.ts:251）的 JSDoc 明写「`Task` is where the model is told, because that is where the choice is made」，全仓 grep 它只在 subagentDefinitions.test.ts 里出现，生产代码零
- [subagent-core-16] LOW doc-mismatch confirmed-partial-waiver | P5-2-2 | docs/plantree/plans/runtime-evolution/evidence/p5-2/signoff.md:61 | SA09 签收声称「假时钟越过旧 idle/duration 不杀 Agent」有测试，仓库里没有任何 subagent 假时钟用例 :: signoff.md 第 61 行把 SA09 标为 ✅ tested，证据栏第一句就是「假时钟越过旧 idle/duration 不杀 Agent」。但 `grep -rl useFakeTimers src/runtime` 零命中（整个 src/runtime 没有任何假时钟测试），subagent 专项四个文件里也没有推进时钟的用例；subagentDelegation.test.ts 只覆盖了 maxTurns 截断，subagentDefinitions.test.ts 只断言 idleTimeoutS
- [subagent-core-17] LOW test-gap confirmed | P5-2-2 | src/runtime/__tests__/subagentDelegation.test.ts:454 | SA07/SA08 的两条要求没有测试：「报告裁剪后再读」与「取消不影响另一会话」 :: 任务矩阵 SA07 列了四条路径（子先完成、边界同时完成、TaskWait 已消费、报告裁剪后再读），signoff 只承认三条，实际测试里我只找到两条半：「delivers a report exactly once when the delegate finished before the parent idled」（子先完成）、「does not re-deliver a report TaskWait already consumed」（TaskWait 已消费）、「tells the parent not
- [subagent-data-04] LOW resource-leak confirmed-partial-waiver | P5-2-4 | src/runtime/plugins/subagent/records.ts:15 | native 路径上 subagent.activity 事件数量没有任何上限，kind:'capped' 从不发出 :: records.ts 模块注释声称事件通道是「bounded live SUMMARY」，但 native 侧只有每条事件的体积上限（240 字符/字段、4000 字符文本），没有条数上限：SubagentPlugin.emitActivity 无计数、EventsPlugin.emit 无节流，协议里定义好的 `kind:'capped'`（runtimeEvents.ts:863）全仓无人发出。旧 legacy 路径有 SUBAGENT_EVENTS_MAX_PER_DELEGATION=200（agent-h
- [subagent-data-05] LOW resource-leak confirmed | P5-2-4 | src/runtime/plugins/subagent/index.ts:348 | 子代理整份转录（含工具结果原文）无上限写入父会话文件，会吃光 32 MiB 会话预算 :: publish() 对每个 message_end 都写一条 custom 记录，message 是原样的 AgentMessage——包含 toolResult 消息，也就是 read（每次上限 50 KiB）、bash、grep 的完整输出。这些记录和父消息共用同一个 JsonlSessionStore 的字节预算（store.ts:101 默认 32 MiB，245 行超限即抛 session_size_limit）。记录写失败在 record() 里被静默吞掉（index.ts:248），但父自己的 app
- [subagent-data-06] LOW bug confirmed | P5-2-6 | src/runtime/plugins/subagent/records.ts:256 | 委派面板里 glob 行显示目录而不是模式，动词退化成 Ran（P5-2-6 的「补齐工具行参数」没覆盖 glob） :: records.ts 按本仓词汇投影 glob 的 pattern/path，没问题；问题在渲染侧：面板行走的是共用的 deriveToolRowView（subagentActivityModel.ts:751），而 toolCard.ts 的动词表与参数表只认 pi 的 find/ls（piToolNames.ts），不认本仓注册的 glob 与 browser_preview。于是 glob 落进 formatToolArgDetail 的 default 分支，探测顺序是 command → descrip
- [subagent-data-07] LOW bug confirmed | P5-2-5 | src/shared/subagentDefinition.ts:500 | 定义写回的引号转义与解析器不对称：描述会被改写，甚至能注入 frontmatter 字段 :: formatSubagentDefinition 用 writeScalar 写 description：需要加引号时把单引号按 YAML 规则翻倍（'→''）并整体单引号包裹；但解析侧的 unquote（同文件 229-237）只做「首尾同引号就切掉」，从不还原翻倍的引号，也无法处理跨行的单引号标量。needsQuotes 的条件里明确包含 /\n/，说明作者以为加引号能保住换行——实际上解析器逐行读 key: value，换行后的内容会被当成新的 frontmatter 键。src/main/ipc/piSub
- [subagent-data-08] LOW bug confirmed | P5-2-5 | src/runtime/plugins/subagent/migrate.ts:175 | 旧工具名走原型链取值：tools: [constructor] 会让迁移预览抛 TypeError :: LEGACY_TOOL_MAP 是普通对象字面量，用下标读取时继承 Object.prototype 上的键。legacy 工具名写成 constructor / toString / valueOf / hasOwnProperty 时，mapped 既不是 undefined 也不是 null，而是一个函数：先被 push 进 string[] 数组（污染 tools 列表），随后 mapped.toLowerCase() 直接抛 TypeError，整个 previewLegacyMigration 崩掉，而
- [subagent-data-09] LOW bug confirmed | P5-2-5 | src/runtime/plugins/subagent/migrate.ts:256 | 迁移预览对带引号/大小写变体的旧布尔字段静默不提示 :: splitLegacy 只把键名规范化，值没有。description/model/thinking/max_turns/tools 都过了 unquote，但 prompt_mode、inherit_context、run_in_background、enabled 这四个字段是直接与裸字面量比较的，写成 "append"、'true'、True、False 都不匹配，于是「这条行为 native 没有」的备注一条都不出现——而这些备注正是 SA19 要求「不可兼容字段可见」的全部内容。另外 256-265 行的
- [subagent-data-10] LOW contract-gap confirmed | P5-2-1 | src/runtime/plugins/subagent/catalog.ts:251 | 目录诊断没有出口：pin 诊断函数无人调用，加载诊断只剩 trace 里的一个计数 :: subagentPinDiagnostics 全仓只有测试调用；loadSubagentCatalog 返回的 diagnostics（read_failed / parse_failed / document_too_large / too_many）在 bootstrap.ts:325-330 里只被统计成 subagent_diagnostics 的条数写进 trace 版本戳，内容没有日志、没有事件、没有 IPC。用户能看到坏文档只是因为 Main 端 SubagentCatalogService 自己又实
- [subagent-data-12] LOW bug confirmed | P5-2-4 | src/runtime/plugins/subagent/records.ts:212 | subagentHistoryUsage 重复实现了 addUsage，但漏掉 reasoning 与 cacheWrite1h :: run.ts:161-176 的 addUsage 会累加可选字段 reasoning / cacheWrite1h；records.ts 的同类求和只遍历五个必填 token 字段和五个 cost 字段，而初始值是第一条记录的浅拷贝——于是多条委派时这两个可选字段既不是和也不是 0，而是「第一条委派的值」。两套求和逻辑各写一遍，本身就是漂移源；测试只断言 totalTokens 相等（faux provider 不产 reasoning），钉不住这个差别。
- [subagent-data-13] LOW quality confirmed | P5-2-6 | src/agent-host/subagentProjection.ts:36 | 第 10 批删掉 legacy 引擎后，subagentProjection.ts 成了孤儿模块 :: 这个模块唯一的生产调用方是 src/agent-host/eventNormalizer.ts（见 a81cae73 的引用图），而该文件在本批已被删除；工作区里只剩 __tests__/subagentProjection.test.ts 在导入它。它内部的词汇是 Claude/CLI 的（file_path、notebook_path、subagent_type，DELEGATION_TOOL_NAMES = {Task, Agent}），与 native runtime 的 path/pattern 与小写工
- [subagent-data-14] LOW quality confirmed | P5-2-1 | src/runtime/plugins/subagent/prompt.ts:80 | guidance 按「三选一」条件发整段搜索指引，且漏掉 P5-2-3 新增的 regex 参数 :: 检索指引的门槛是 read/grep/glob 三者任一存在，但正文把三个工具的参数一次讲全。模块注释本身强调「告诉子代理一个它没有的参数，比什么都不说更糟」，这里犯的是同一类错：只声明了 Read 的定制子代理会被告知 grep/glob 的用法。另外正文列举 grep 参数为 include/caseInsensitive/limit，没有 regex——而 P5-2-3 正是为 explorer 提示词里的正则检索补的这个参数（tools/index.ts:469），内置 explorer 文档还被测试钉着必
- [subagent-data-15] LOW boundary confirmed | P5-2-5 | src/main/services/agent-host/subagentCatalog.ts:297 | 带 BOM 的定义文档：runtime 能加载，管理页却列为坏文档 :: runtime 侧经 skillSource 用 TextDecoder 解码（默认 ignoreBOM:false，即剥掉 BOM），文档正常解析；Main 侧用 fs.readFile(path,'utf8')，Node 不剥 BOM，raw 以 ﻿ 开头，splitFrontmatter 的 startsWith('---\n') 判定失败 → 整份 frontmatter 读不到 → 报 missing `description` 进 broken 列表。两个读者对同一个文件给出相反结论。（此结论基于两个 
- [subagent-data-16] LOW bug confirmed | P5-2-7 | src/runtime/plugins/subagent/registry.ts:257 | registry 的 turn 计数永远为 0：turn_start 根本没被转发 :: noteActivity 有一条 `if (event.type === 'turn_start') record.turns += 1`，但 run.ts:343-345 的 handleEvent 在 turn_start 分支里只加自己的计数就 return，不调用 emit——publish() 因此永远收不到 turn_start，这段分支是死代码。运行期 record.turns 恒为 0，delegationSummary/formatDelegationHeartbeat 里 `if (turns 
- [subagent-data-17] LOW boundary confirmed | P5-2-6 | src/runtime/plugins/subagent/records.ts:267 | native 的截断没有代理对保护，T-34 已修过的「半个 emoji」问题回来了 :: clampActivityText（4000 字符文本 / 240 字符参数）与 subagentHistorySummaries 的 4000 字符报告裁剪都是直接 slice(0, max)，按 UTF-16 码元切。旧的 agent-host/subagentProjection.ts:105-116 专门为此做过修正（注释标 review m7：切在代理对中间会留下孤立高位代理，跨 IPC 后渲染成替换字符），native 重写时没有带过来。
- [import-catalog-04] LOW contract-gap confirmed | P5-5 | /home/ai/code/ai-client/src/agent-host/worker.ts:120 | 一次性 utility worker 仍然读派生的 models.json/auth.json，MC04「不再读」只对会话 worker 成立 :: NativeUtilityRuntime 声明了 modelCatalog 选项（nativeUtility.ts:58、149 都接好了），但 worker 入口构造它时没有传，Main 侧也只有 createPiWorkerSlot 的 worker.bootstrap 会带 modelCatalog（createPiWorkerSlot.ts:112），utility.start 这条链（PiUtilityService.ts:176）根本没有目录交付。于是应用内的「AI 功能」一次性补全仍然走 create
- [import-catalog-05] LOW race confirmed | P5-4 | /home/ai/code/ai-client/src/main/services/legacyImport/LegacyImportService.ts:303 | 两个并发导入批次会让第二个批次整条失败，并留下一条 cleanupPending 记录 :: flights 只按 dedupeKey 对「同一段对话」去重，不同对话的两次 importBatch（两个窗口、或用户连点两次）会并发进入 createImport。而 Main 侧只有一个导入槽：WorkerManager.createLegacyImport 在 importSlotActive 为真时直接抛 worker_import_busy（WorkerManager.ts:513）。LegacyImportService 不认这个可重试错误码，只把 message 拼进 error 字符串；catch
- [import-catalog-06] LOW contract-gap confirmed | P5-5 | /home/ai/code/ai-client/src/main/services/piModelConfig/configValidation.ts:147 | MC02/D15 声称的「model 也可显式覆盖 baseUrl」在 Main 侧组装路径上不可达 :: runtime 的读取侧支持每行模型自带 baseUrl（catalog.ts:236、binding.ts:83），但 Main 侧的组装链把这个字段吃掉了：PiManagedModelDefinition 没有 baseUrl 字段，validateModel 的返回对象逐字段白名单构造、不含 baseUrl（未知字段静默丢弃，不报错），toPiModelsJson 只在 provider 级别做推导；用户自建服务的模型更是只写 `{id}`（PiModelConfigService.ts:662）。切源之后 
- [import-catalog-07] LOW security confirmed | P5-4 | /home/ai/code/ai-client/src/shared/types/legacyImport.ts:310 | worker 边界只校验 targetPiSessionId 非空，而 native 写入器直接把它拼进路径 :: 同一个文件里就有 isLegacyImportPathSegment（禁 / \\ \0 与 . ..），manifest 载入时也用它校验 targetPiSessionId，但四个导入 RPC 的负载校验只用 nonEmptyString。pi 版写入器是用 readdir + 后缀匹配定位文件的，id 再脏也出不了目录；native 版改成了 join(dir, `${id}.jsonl`)，一个含 ../ 的 id 会让 create 写到、inspect 看到、reconcile 删除 sessions 
- [import-catalog-08] LOW quality confirmed | P5-4 | /home/ai/code/ai-client/src/runtime/worker/nativeImport.ts:189 | WORKER_IMPORT_CONTEXT_LEAK 这条硬校验是恒真的，抓不住它命名的那种泄漏 :: assertUsable 检查的是 store.snapshot().messages 里有没有 customType === display 的条目。而 snapshot() 调用 buildSessionContext(clean) 时不传任何 entryProjectors（store.ts:193），pi-agent-core 对 custom 条目的处理是 `options.entryProjectors?.[entry.customType]?.(...) ?? []`——没有 projector 就直
- [import-catalog-09] LOW resource-leak confirmed | P5-4 | /home/ai/code/ai-client/src/runtime/worker/nativeImport.ts:354 | reconcile 只删 .jsonl，写锁旁车文件与暂存目录永远留在盘上 :: JsonlSessionStore.open 会为暂存文件申请一个 `${file}.writer.lock` 旁车（writerLock.ts:36-38），正常路径上 close() 会删掉它。进程被杀时这个锁文件留在 <agentDir>/sessions/.aiclient-import-staging/ 下，而 inspectInterrupted 只 stat `<id>.jsonl`，reconcileInterrupted 也只 unlink 这一个路径，于是每次被中断的导入都会永久留下一个 .wr
- [import-catalog-10] LOW bug confirmed | P5-4 | /home/ai/code/ai-client/src/main/services/legacyImport/LegacyImportService.ts:468 | reconcile 遇到第一条清不掉的记录就抛出，后面的待清理记录永远轮不到 :: reconcileInternal 是一个顺序 for 循环，某条记录留下 cleanupErrors 时直接 throw，循环中断。fail-closed（挡住后续导入）是有意设计并有测试覆盖，但「后面那些本来可以清干净的记录被跳过」是副作用：它们的索引行与目标文件会一直留着，而且每次 reconcile 都会卡在同一条记录上重放，永远走不到它们。若首条记录的索引行因为某种持久原因删不掉（removeImported 一直返回 false），导入功能会被永久锁死，用户侧没有任何恢复入口，只能去改 userData
- [import-catalog-12] LOW test-gap confirmed | P5-5 | /home/ai/code/ai-client/src/main/services/piModelConfig/index.ts:147 | resolveNativeModelCatalog 没有任何单测，交付与否的判据全靠人读 :: PiModelConfigService.buildNativeModelCatalog 有三条用例（与写盘等价、用户服务不落盘、随包回落），但把它接到 Main 的那一层——凭据读取、空目录判定、失败时 warn 并回落——在整个仓库里没有任何测试引用（grep resolveNativeModelCatalog 在 *.test.ts 中零命中）。import-catalog-02 描述的那条「钥匙串锁着应当返回 undefined」的语义，正好落在这个没被测到的函数里。
- [import-catalog-13] LOW quality confirmed | P5-4 | /home/ai/code/ai-client/src/runtime/worker/nativeImport.ts:124 | 五处 `as never` 断言把会话条目的形状检查整条关掉 :: appendConversation 里对 appendEntry / appendMessage 的五次调用全部以 `as never` 收尾，等于告诉编译器「别检查这个对象」。NewSessionEntry / AgentMessage 的字段一旦变化（例如 assistant 消息新增必填字段、或 toolResult 的 content 形状调整），这里不会有编译错误，只会在运行时写出一份读回来缺字段的转录。这是本区域唯一的类型逃逸点，而它恰好位于「唯一的 JSONL writer」上。
- [cutover-04] LOW bug confirmed | P6-5 | src/main/ipc/piTui.ts:167 | 防双写的 assertHostPromptAllowed 从未被任何发送路径调用 :: piTuiSession.ts:109-119 把 assertHostPromptAllowed 写成「GUI 发送前的最后一道闸：谁跳过了 dispose 就当场抛错，而不是悄悄变成同一个文件的第二个写入者」，piTui.ts:166-169 也把它导出成 Main 级 API。但全仓没有任何调用点：chat.ts 的发送处理器只调用了尽力而为的 releaseTuiOwnership（它自己 try/catch 吞异常并返回 false），拿到 false 之后照常继续发送。这道被文档当作纵深防御的闸门实际上
- [cutover-05] LOW quality confirmed | P6-5 | src/agent-host/bundledFeaturePlugins.ts:95 | 三个 pi 扩展侧模块已成死代码，而文档把其中两个列为「仍在用」 :: P6-5 之后 bundledFeaturePlugins.ts（139 行）、extensionInventory.ts（139 行）、commandInventory.ts（140 行）都只被各自的测试 import，生产代码里一个引用都没有（Main 用的是 bundledPlugins.mjs 这张表，不是 bundledFeaturePlugins.ts）。而 topics/p6-cutover.md:74 与 README.md:175 明确把 bundledFeaturePlugins、extensi
- [cutover-06] LOW quality confirmed | P6-5 | src/agent-host/extensionUiBridge.ts:150 | extensionUi 整条链路在 native 下没有生产者，渲染层的对话框永远打不开 :: extensionUiBridge.ts（436 行）在 native 下仍被实例化，但没有任何东西会调用它的 ui.* 方法：唯一的调用点是 createRuntimeApprovalBridge 里的 ui.select，而 bootstrap.ts:239 写的是 `approve: options.permissions?.approve ?? approval?.approve`，nativeWorkerRuntime.ts:238 永远传入结构化权限卡的 approve，所以那条 ui.select 分
- [cutover-07] LOW quality confirmed | P6-5 | src/agent-host/permissionPlugin.ts:468 | permissionPlugin 的注入决策与加载校验已无调用方，模块头仍在描述一套不存在的 fail-closed 流程 :: 该文件里只有 packageConfiguredByUser / permissionPluginConfiguredByUser（Main 的插件页在用）与 lookupBundledPackage / hostDirectory（被同样已死的 bundledFeaturePlugins.ts 用）还有引用。decidePermissionPlugin、verifyPermissionExtensionLoaded、lookupBundledPermissionPlugin、resolveBundledPermi
- [cutover-08] LOW doc-mismatch confirmed-partial-waiver | P4-2 | src/runtime/README.md:7 | runtime 子包 README 仍写「后端由 AICLIENT_RUNTIME_BACKEND 选择，默认 legacy」 :: src/runtime/README.md 是 flags.ts 旁边最直接的开发者文档，第 7 行仍写「与现有 src/agent-host/ 并存（ARD §5.1），后端由 AICLIENT_RUNTIME_BACKEND 选择（D8）」，第 110 行的环境变量表仍列 `AICLIENT_RUNTIME_BACKEND | legacy（默认）/ native`，第 20 行的目录表也写 flags.ts 含「D8 后端开关」。代码里 readRuntimeFlags 已经把 backend 写死成常量 '
- [cutover-11] LOW test-gap confirmed | P6-2 | src/agent-host/__tests__/piCliIsBundledToolOnly.test.ts:58 | 口径 A 的越界 import 守卫只认整包说明符，子路径 import 可以绕过 :: importsPackageAsLibrary 的三个正则都要求包名后面紧跟引号，因此只能识别 `from '@earendil-works/pi-coding-agent'` 这一种写法。深层导入（pi-coding-agent 自己就是这么被阳性对照 fixture 加载的：`@earendil-works/pi-coding-agent/dist/core/session-manager.js`）不会被点名。这正是文档说的「保留这个包的唯一防线」，而 nativeWorkerDependencyBoundar
- [cutover-12] LOW doc-mismatch confirmed | P6-5 | src/agent-host/piSessionPreflight.ts:52 | piSessionPreflight 保留理由被夸大：native 只用了其中 3 行的路径比较 :: p6-cutover.md:78 把「不能删」写成执行时的重要发现，理由是「src/runtime/worker/nativeWorkerRuntime.ts 也在用它」，README.md:175 同样写「自有 runtime 也在用」。事实是 nativeWorkerRuntime 只 import 了 samePiSessionPath 这一个 3 行的路径比较助手；文件真正的主体 preflightPiSessionFile（106 行，含 64KB 分块扫头、JSON 校验、dev/ino 身份校验）与 
- [cutover-13] LOW test-gap confirmed | P6-1 | src/agent-host/__tests__/workerEntryWiring.test.ts:281 | 用例名声称覆盖「设了过期变量」的情况，实际两个辅助函数都把变量删掉了 :: 用例名是「routes bootstrap to the self-owned runtime, with or without a stale variable」，但它调用的 bootstrapOnce()（第 56 行）与 utilityOnce()（第 106 行）都执行 `delete env.AICLIENT_RUNTIME_BACKEND`，没有任何一条真实进程用例把它设成 legacy。P6-1 的验收标准写的是「用真实 worker 进程验证三种取值（不设/打错字/legacy）」，那个测试随 P6
- [cutover-18] LOW doc-mismatch confirmed | P6-2 | docs/plantree/plans/runtime-evolution/README.md:159 | 任务树 P5-5 行仍写派生明文文件「P6-2 删」，与 P6-2 的改判相反 :: README 的 P5-5 行写「派生明文文件降为 legacy 专用（P6-2 删）」，但 P6-2 在执行时改判为不删——p6-cutover.md:62 明确写「派生明文文件：改判，不删」，理由是随包 pi CLI 的 ModelRuntime 自己要读 <agentDir>/models.json + auth.json，停写等于让 TUI 没有模型可用。同一份 README 的两处结论互相矛盾。
- [cutover-20] LOW quality confirmed | P6-5 | src/main/services/terminal/piTuiSession.ts:41 | sessionKeysMatch 只有测试在用 :: sessionKeysMatch 是导出的公共 API 且有专门用例，但生产代码里没有调用方——PiTuiPty 与 piTui.ts 用的都是 normalizeSessionKey 或 guard.owns()。它是 P6-5 前后这批清理里漏下的一个导出。