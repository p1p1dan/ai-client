# 审计发现：待定与被推翻（一行一条）

Role: evidence（source material）；来源：2026-09-14 只读审计。无前缀的行为 uncertain（反驳者无法构造触发路径也无法排除，`REFUTER:` 后是其理由前 220 字）；行首 `REFUTED` 的为被推翻、不计入缺陷（后接推翻理由）。完整文本见 `findings.json`。

- [core-host-05] LOW boundary uncertain | P1-0 | src/runtime/host/io.ts:127 | TSD 回落给 helper 的输出预算是 stdout+stderr 合计，configured Node 的 stderr 噪声会把明文读取整段判失败 :: readFile 命中 TSD 头后调用 runtimeExec.run，maxOutputBytes 设成 `options.maxBytes + 1 + 4096`，overflow 用 'terminate'。但 P1-0 契约第 4 节「输出」明写 maxOutputBytes 是「stdout 与 stderr **合计保留**的上限」，exec.ts:455-464 的 consume || REFUTER: 代码事实对，但「纯代码层面的算错」这个定性被契约原文否掉了一半。io.ts:124-128 确实用 `maxOutputBytes: options.maxBytes + 1 + 4096` 且 overflow:'terminate'，exec.ts:455-462 的 consume 也确实用单个 `retained` 跨 stdout/stderr 累加——这两点属实。但原报告只引了契约第 4 节「合计保留」那句，漏掉了同一节紧接
- [core-host-08] LOW race uncertain | P1-0 | src/runtime/host/exec.ts:104 | spawn 与 shutdown 并发时，正在启动的长驻子进程会逃过回收 :: ExecPlugin.spawn 先 `await spawnPersistent(...)`（其内部要等 setImmediate 才 resolve），返回后才 `this.children.add(child)`。ExecPlugin.stop() 则在这段等待期间可能已经执行：它 `const children = [...this.children]; this.children.cle || REFUTER: 代码形态属实：exec.ts:81-106 的 spawn 先 `await spawnPersistent(...)`（内部要等 setImmediate 才 resolve，exec.ts:271-276），返回后才在 104 行 `this.children.add(child)`；stop()（exec.ts:114-118）先 `const children = [...this.children]; this.childre
- [core-host-09] LOW resource-leak uncertain | P1-0 | src/runtime/host/exec.ts:268 | spawnPersistent 注册的 abort 监听器从不移除 :: spawnPersistent 给调用方的 signal 挂了一个 abort 监听器，但无论子进程正常退出还是被 kill，这个监听器都不会被摘掉；闭包同时把 child 和整个 handle 留在 signal 的引用链上。对照 ExecPlugin.run（75-78 行）是显式 removeEventListener 的。  （推断成分说明：现有唯一调用方 MCP bridge 不传 si || REFUTER: 代码引用属实：src/runtime/host/exec.ts:268-269 确实是 `request.signal?.addEventListener('abort', () => void handle.kill(), { once: true })`，且 spawnPersistent 全程没有对应的 removeEventListener；对照组 ExecPlugin.run 在第 55 行挂、第 76 行在 finally 
- [core-host-10] LOW contract-gap uncertain | P1-0 | src/runtime/host/io.ts:177 | readDirectory 的路径校验与 disposed 判断是惰性的，且不进 pending，shutdown 不等它 :: readDirectory 是 async generator，函数体（含 absolutePath 与 disposed 检查）要到消费者第一次 next() 才执行。所以 `io.readDirectory('relative')` 不会立刻报错，错误推迟到 for-await 的第一轮；同理释放后调用也不是立刻拒绝。  另外它是 HostIo 里唯一不走 track() 的方法，因此在途的目 || REFUTER: 代码事实正确：src/runtime/host/io.ts:177 是 async generator，absolutePath 与 disposed 检查在函数体内（179-180 行），确实要到第一次 next() 才执行；它也确实是 HostIo 里唯一不经过 track() 的方法，因此不进 pending。但失败场景无法构造：三个生产消费点都是就地 for-await——src/runtime/plugins/tools/in
- [core-host-11] LOW contract-gap uncertain | P1-0 | src/runtime/host/io.ts:57 | HostIo 对非绝对路径同步抛出而非拒绝 Promise，与 exec 出口不一致 :: track() 不是 async 函数，`absolutePath(path)` 在里面同步抛出；readFile/writeFile/stat/realpath/mkdir/unlink 都返回 `this.track(...)`，因此传入相对路径时这些方法是**同步 throw**，而不是返回 rejected Promise。ExecPlugin.run/spawn 是 async 方法，同 || REFUTER: 机制描述属实：track 是普通方法（src/runtime/host/io.ts:53-60），disposed 走 Promise.reject 而 absolutePath(path) 在第 57 行同步抛出，所以 readFile/writeFile/stat/realpath/mkdir/unlink 对相对路径是同步 throw；ExecPlugin.run 是 async，同样校验变成拒绝（host.test.ts:374
- REFUTED [core-host-12] LOW boundary refuted | P1-0 | src/runtime/host/config.ts:49 | validateHost 用 access(X_OK) 校验 node.path，目录也会通过 :: 代码引用无误（src/runtime/host/config.ts:46-56 只做 access(X_OK)，POSIX 上目录会通过，Windows 上可执行位判断也弱），但给出的失败场景在本仓无法成立。node.path 只有两个生产来源，都不由打包脚本直接下发字符串：一是 src/runtime/host/worker.ts:32-34 的 bundledNodePath()，它自己 jo
- REFUTED [core-host-13] LOW bug refuted | P0-2 | src/runtime/bootstrap.ts:175 | 未显式传 host 时默认 childEnv 为空，子进程只剩一个 PATH :: 行为描述准确（bootstrap.ts:175 是 standaloneHost({})，childEnv 为空；commandEnvironment 在 exec.ts:306-323 之后只剩 env.PATH = dirname(node)），但这不是疏漏，而是契约明写的默认策略：p1-0-host-contracts.md:60-61「childEnv 是入口明确传入的子进程环境快照…ex
- [core-host-14] LOW bug uncertain | P0-3 | src/runtime/events/index.ts:18 | EventsPlugin.emit 无订阅者隔离，一个抛异常的订阅者会截断整条事件流并把异常打回发射方 :: emit 直接 for-of 遍历 listeners 同步调用，没有 try/catch。任何一个订阅者抛出，后面的订阅者就收不到这条事件，异常还会沿调用栈回到发射点——发射点包括 agent loop 的权限活动回调（agent-loop/index.ts:185-186）和事件投影器，也就是说它会中断正在跑的回合。  生产订阅者是 nativeWorkerRuntime.ts:281 的 ` || REFUTER: 结构事实成立：src/runtime/events/index.ts:18-22 的 emit 既无 try/catch 也无 [...listeners] 快照。但报告给出的两条具体后果都被上游挡掉或无法证实。其一，「异常打回 agent loop 的权限活动回调」是错的：该回调（agent-loop/index.ts:181-186）由 PermissionsPlugin.notify 调用，而 notify 在 src/runti
- REFUTED [core-host-19] LOW contract-gap refuted | P0-2 | src/runtime/bootstrap.ts:182 | D1 点名的 cordis 能力（响应式属性、Fork/Isolate、热插拔）一项未用，插件间无任何服务隔离 :: 代码事实大半成立，但这条发现的承重结论——「没有机械约束，任何新插件都能无声地依赖任何服务」——被 cordis 自己的实现直接推翻。  先说成立的：src/runtime/bootstrap.ts:182 确实只有一个 `new Context()`（另两处 new Context 在 spikes 与 worker/nativeImport 的独立图里）；全仓没有任何 ctx.isolate 
- [tools-11] LOW resource-leak uncertain | P1-2 | src/runtime/plugins/tools/index.ts:269 | write 把完整写入内容（上限 8 MiB）原样塞进审批请求 preview，链路上没有任何截断 :: write 把 args.content 原封不动作为 ToolPermissionRequest.preview.text（269-272 行）。worker/permissionPrompt.ts:152-153 把它放进 permission.requested 事件的 input.content，经 IPC 送到渲染层，renderer 的 derivePermissionContent（ || REFUTER: 「链路无截断」这一事实核对无误：index.ts:269-272 把 args.content 原样作为 preview.text，worker/permissionPrompt.ts:152-153 放进 permission.requested 的 input.content，renderer 的 derivePermissionContent（questionCardModel.ts:716-725）与 QuestionCard.
- REFUTED [tools-15] LOW quality refuted | P1-1 | src/runtime/plugins/tools/index.ts:83 | register 的 access 默认值靠工具名硬编码列表推断，MCP 工具因此全部被当写类工具、在 plan 模式下整批消失 :: 现象为真，归因是错的。事实部分核对无误：register 的默认 access 确实是按名字硬编码（index.ts:83-87），MCP 桥确实没传 access（plugins/mcp/index.ts:182），因此 mcp__* 被判 write，list() 在 plan 模式下过滤掉它们（index.ts:76-79）。但「plan 模式下 MCP 不可用」并不是这张名字表决定的：权限
- REFUTED [tools-16] LOW resource-leak refuted | P1-1 | src/runtime/plugins/tools/index.ts:70 | ToolsPlugin 销毁时只清 registry，access/locks 未清，BashAnalyzer 释放后 ready 仍指向已删除的 parser :: 两部分都不成立。第一，access 与 locks 不清空不构成泄漏：它们和 registry 一样是 ToolsPlugin 实例字段，实例本身在 dispose 后即可回收，清不清只影响写法整齐度，不影响生命周期。第二，失败场景被 dispose 顺序挡住了：bootstrap.ts:458-480 的 dispose 先 controller.abort()，再 await Promise.
- REFUTED [permissions-10] LOW boundary refuted | P1-5 | src/runtime/plugins/permissions/policy.ts:59 | 策略文件带 UTF-8 BOM 会让整个 runtime 拒绝启动 :: 与 Web/Node 的 TextDecoder 语义不符。ignoreBOM 的默认值是 false，含义是「不忽略 BOM 的特殊含义」，即解码时把开头的 U+FEFF 吃掉；ignoreBOM: true 才会把 BOM 当普通字符留在输出里。policy.ts:51 用的正是默认值（只传了 fatal: true），所以 JSON.parse 拿到的字符串不会以 U+FEFF 开头，带 B
- REFUTED [permissions-20] LOW security refuted | P1-5 | src/runtime/plugins/permissions/bash-analysis.ts:229 | find 不在动态 IO 命令清单里，find -exec / -delete 的真实操作对象不受 deny 规则约束 :: 给出的失败场景在代码里被明确防住了。`find . -name '*.env' -delete` 里 '*.env' 是普通词，addPath 不会跳过（只有 '-' 开头且不带 '=' 的才 return，bash-analysis.ts:117-122），于是 paths 里有 <cwd>/*.env；进入 tools/index.ts:184 的 expand 后，第 191 行对整串做 p
- [permissions-14] LOW race uncertain | P1-5 | src/runtime/plugins/permissions/index.ts:190 | scopeToolCall 仅按 toolCallId 建索引且 release 无条件删除，id 相同的并发子代理会互相串档 :: scopedGears 是一张 toolCallId → DelegateCallScope 的表，注册时直接 set（覆盖），返回的 release 直接 delete（不校验存的还是不是自己那一份）。子代理是并发跑的（subagent/index.ts:617-642 的注释明说这就是不改全局 gear 的原因），每个子代理有自己的 Agent 循环和自己的 provider 会话，两个子代理 || REFUTER: 代码事实全部属实：scopedGears 以 toolCallId 为唯一键，set 覆盖、release 无条件 delete（permissions/index.ts:190-195），并发子代理各自注册（subagent/index.ts:617-642），测试 subagentToolsPermissions.test.ts:317-332 的 'shared-id' 用例只验证释放不泄漏，没有撞号覆盖。但我追了 id 的来源，
- [permissions-15] LOW quality uncertain | P1-6 | src/runtime/plugins/permissions/bridge.ts:27 | ui.select 审批路径已成死代码，且硬编码 120 秒超时与中文选项文案 :: bootstrap.ts:239 是 `approve: options.permissions?.approve ?? approval?.approve`，而 nativeWorkerRuntime.ts:238 永远传自己的 this.permissions.approve，所以 createRuntimeApprovalBridge 返回的 approve 在生产路径上不会被调用（brid || REFUTER: 引用属实但当前无法触发。核实结果：(1) bridge.ts:24-36 确实硬编码 choices=['允许一次','本会话允许此操作','拒绝'] 与 timeout: 120_000；(2) bootstrap.ts:239 确实是 `approve: options.permissions?.approve ?? approval?.approve`；(3) nativeWorkerRuntime.ts:115/125/238 
- [context-prompt-05] LOW boundary uncertain | P2-3 | src/runtime/plugins/context/index.ts:251 | hard limit 触发的摘要请求把全量历史一次性发给 provider，没有任何尺寸兜底，单回合超调会让会话彻底卡死 :: compactNow 把 prepareCompaction 的三段消息全部合并成摘要范围（compaction.ts:134），因此 summary family 的那次 provider 请求携带的是整段会话。它的输入大小从不被检查：compact() 只用 reserveTokens 限制输出（maxTokens = 0.8 * reserveTokens）。正常情况下 tokens 恰好卡 || REFUTER: 「本仓不检查摘要请求的输入尺寸」这句字面为真（compactNow 只把 reserveTokens 传给 compact，摘要失败又在 hard_limit 下直接抛且没有退回 fresh_window 的降级），但 finding 给的那个具体失败场景被 pi 的序列化逻辑挡住了：generateSummary 先 convertToLlm 再 serializeConversation，而 serializeConversatio
- [context-prompt-12] LOW contract-gap uncertain | P2-3 | src/runtime/plugins/context/index.ts:291 | 压缩后仍超预算时无条件抛错，绕过了「未到硬上限就降级为 skipped」的既定契约 :: TurnPreparation.skipped 的文档明确写着：未越过边界时压缩失败不是错误，run 继续在未压缩上下文上跑；只有 hard limit 档才改成抛错——giveUp 正是这么实现的。但 compactNow 里这一处「checkpoint 仍然超预算」的判断直接 throw RuntimeHostError，不看 reason。model_requested / user_req || REFUTER: 报告写的那个失败场景我可以证伪，但它指出的契约不对称是真的，而且存在另一条我无法完全排除的窄路径，所以给 UNCERTAIN。 证伪的部分：/compact 走 worker/nativeWorkerRuntime.ts:483 的 retention: 'completed_turn'，而 shapeForCheckpoint（compaction.ts:144）在 completed_turn 下 candidates 为空数组 →
- [session-02] MEDIUM bug uncertain | H/20 | /home/ai/code/ai-client/src/runtime/plugins/session/codec.ts:332 | pi CLI 打开文件时补的换行，会把可修复的半行尾片变成不可修复的中段坏行 :: 尾片修复的条件是「解析失败的行是最后一行 且 全文不以 \n 结尾」。而 pi CLI 的 loadEntriesFromFile 在读完文件后，只要还有未换行的残余就执行 appendFileSync(file, "\n")（session-manager.js:319），也就是说 CLI 只要打开过一次，我们留下的半行就被补成了一条「完整的」坏行。此时 !content.endsWith('\ || REFUTER: 两个代码事实我都亲自核实了，但拼不出一条我能确认会发生的用户路径，所以给 UNCERTAIN 并把级别从 high 降到 medium。  已核实的部分：(a) pi CLI 的 loadEntriesFromFile 确实在最后一段残余非空时执行 appendFileSync(file, "\n")（node_modules/@earendil-works/pi-coding-agent/dist/core/session-manag
- [session-04] LOW race uncertain | P3-1 | /home/ai/code/ai-client/src/runtime/plugins/session/writerLock.ts:158 | 抢占陈旧锁存在 TOCTOU：clearStale 会把别的进程刚建好的新锁改名删掉，出现双写者 :: acquireWriterLock 的顺序是「readOwner → 判定陈旧 → clearStale(rename 后删除) → createOnly 建锁」。rename 只按名字操作，不校验内容，所以两个进程都判定同一把锁陈旧时，慢的一方会把快的一方已经建好的新锁 rename 走再建自己的锁，两个进程都认为自己持锁。第二个更容易触发的窗口是 writeFile(flag:'wx') 本身 || REFUTER: 竞态在代码层面确实存在，但我构造不出生产里两个进程同时抢同一把锁的具体路径，所以给 UNCERTAIN 并把级别降到 low。  确实成立的部分：acquireWriterLock（writerLock.ts:158-169）的顺序就是 readOwner → stale 判定 → clearStale → createOnly，clearStale（:127-138）只按名字 rename，不比对内容。若 B 先读到陈旧 owner，
- [session-05] LOW bug uncertain | P3-1 | /home/ai/code/ai-client/src/runtime/plugins/session/store.ts:512 | 释放写锁时不校验持有者，token 字段只写不读 :: close() 无条件 unlink 锁文件，open() 的失败分支同样无条件 unlink。锁记录里专门写了 token（acquireWriterLock 每次生成新 uuid），但全仓没有任何地方读它来确认「我要删的还是我建的那把锁」。一旦发生 session-04 描述的抢占竞态，或者外部工具/用户手工清理过锁，先退出的进程就会删掉另一个进程正在持有的锁，使第三个进程可以立刻建锁，把双写 || REFUTER: 标题那句「token 只写不读」我完全证实了，但它引出的危害要挂在 session-04 的竞态上，所以整体给 UNCERTAIN、low。  证实的部分：token 在 writerLock.ts:152 每次生成新 uuid，parseOwner（:65-71）会解析它，但全仓没有任何一处把它读回来做归属比对——我 grep 了 src 全树，只有 writerLock.ts 内部这两处提及。close()（store.ts:508
- REFUTED [session-07] LOW bug refuted | P3-5 | /home/ai/code/ai-client/src/runtime/plugins/session/store.ts:480 | acceptFork 在生产链路里从未被调用，fork 的「未提交产物」判据形同虚设 :: 事实陈述对，失败场景不成立——我按它给的路径追到底，删掉用户在用的 fork 这一步走不通。  对的部分：acceptFork（store.ts:480-482）在生产链路里确实无人调用。全仓唯一调用点是 NativeSessionIndexAdapter.ts:225，而这个文件除自身测试外没有任何引用；真实链路是 WorkerManager → worker.fork / worker.for
- [session-10] LOW boundary uncertain | P3-3 | /home/ai/code/ai-client/src/runtime/plugins/session/legacy.ts:325 | 旧文件里的 label 行不做校验就写成 fact 行，targetId 无效时整次导入失败 :: 转换时对 row.type==='label' 直接把 targetId/label 原样写进 kind:'fact' 行，不检查 targetId 是否是本次转换产生的条目 id、也不检查类型。函数结尾的 decodeSession(result) 会对 fact/label 行做严格校验（typeof targetId !== 'string' || !ids.has(targetId) →  || REFUTER: 机制部分完全属实，而且比报告写的还硬：legacy.ts:325-333 把旧 label 行的 targetId 原样写成 kind:'fact' 行不做任何检查；函数末尾 legacy.ts:355 的 decodeSession(result) 会走到 codec.ts:427-433，`typeof targetId !== 'string' || !ids.has(targetId)` 直接 invalid('invalid 
- [session-11] LOW quality uncertain | P3-1 | /home/ai/code/ai-client/src/runtime/plugins/session/store.ts:179 | open 失败路径的 unlink 未兜底，会用清理错误盖掉真正的失败原因 :: open 的 catch 里 `await io.unlink(lock)` 没有 catch。若此时 HostIo 已被 dispose（抛 runtime_disposed）、或锁文件已被别的进程抢走（ENOENT）、或 Windows 上文件被占用（EPERM），抛出的就是这个清理错误，原始失败（如 session_cwd_mismatch、io_limit、session_invalid） || REFUTER: 代码形状属实：store.ts:179-182 的 catch 里 `await io.unlink(lock)` 没有兜底，而同文件 store.ts:170-173 的临时文件清理写的是 `.catch(() => {})`，两处风格确实不一致；一旦 unlink 抛错，原始失败会被替换，而且锁文件还会残留。  但报告举的具体失败场景我基本推翻了：ENOENT 需要有人在我们持锁期间把锁删掉，而 writerLock.ts 的协议恰
- REFUTED [loop-model-05] LOW contract-gap refuted | P0-5 | src/runtime/plugins/agent-loop/index.ts:304 | 回合在 message.started 之前失败时，渲染层的乐观用户气泡永远退不掉 :: 报告漏了第三条、也是本场景真正生效的清场路径。ChatComposer.runSend 的监听器在 1666-1669 行把本会话的 `session.failed` 折进 `fatalHostError`（isSessionFailedForSend 只比对 type 与 sessionId，assistantProgress.ts:162-164）；该标志是 sendAndWait 等待谓词的
- REFUTED [worker-runtime-03] LOW bug refuted | P4-1 | src/runtime/worker/nativeWorkerRuntime.ts:608 | reload 拆除旧图的 dispose 失败未纳入 try，槽位留在「有缓存结果、无 runtime」的砖块态 :: 代码结构描述属实——nativeWorkerRuntime.ts:603-608 确实在 try 之外 await handle.dispose()，而 bootstrap.ts:456-476 的 dispose 会把 exec.shutdown()/active run 的 rejection 以及 session.close()/io.shutdown()/fiber.dispose() 的
- [worker-runtime-04] LOW boundary uncertain | P4-1 | src/runtime/worker/questionPrompt.ts:96 | 问答/权限的 pending map 以 provider 给的 toolCallId 为键，空串或重复 id 会让卡片永远答不掉 :: questionPrompt 用 request.questionId（= 工具调用 id）作键，permissionPrompt 同样用 request.toolCallId（src/runtime/worker/permissionPrompt.ts:136,199），两处都没有空值或重复校验。pi-ai 的 openai-completions 适配器在服务端不给 id 时会落成空串（src || REFUTER: 仓内每一环都核实过，只有最外层的前提没法在本仓证实。核实到的：questionPrompt 的 pending 以 request.questionId 为键且无任何空值/占用校验（questionPrompt.ts:96,100），questionId 就是工具调用 id（ask.ts:137 的 execute 第一参），pi-agent-core 把 provider 的 toolCall.id 原样传进 execute、不做归一
- REFUTED [worker-runtime-08] LOW resource-leak refuted | P4-1 | src/runtime/worker/nativeWorkerRuntime.ts:277 | openGraph 覆盖 this.handle/this.unsubscribe 前不检查既有图，bootstrap 失败重试会泄漏整张图与写锁 :: 原报告自己标了「推断性」，我这边可以把它推到明确反驳。代码描述属实：openGraph 在 nativeWorkerRuntime.ts:277-281 直接覆盖 this.handle/this.unsubscribe，bootstrap() 的 catch 也只清 this.booting（154-160 行），bootstrapOnce 在 openGraph 之后还有 requireSes
- [rpc-projector-11] LOW security uncertain | P4-3 | src/agent-host/piWorkerRpcServer.ts:500 | import 三个 RPC 的路径参数只校验「非空字符串」，直接进 join 拼文件名 :: isWorkerInspectImportedSessionPayload / isWorkerReconcileImportedSessionPayload（workerRpc.ts:1072-1098）只要求 targetPiSessionId 是非空字符串，isWorkerDiscardImportedSessionPayload 只要求 sessionFile 非空。这三个值被原样转交给写 || REFUTER: 代码描述全部属实，但触发路径今天不存在，报告自己也这么写了。核实到的事实：守卫 src/shared/types/workerRpc.ts:1072-1098 只要求 targetPiSessionId 是非空字符串，1112-1120 的 discard 只要求 sessionFile 非空；RPC 层 src/agent-host/piWorkerRpcServer.ts:482-516 原样转交；写入器 src/runtime/w
- REFUTED [rpc-projector-12] LOW boundary refuted | P3-4 | src/runtime/events/projector.ts:219 | 完成态白名单漏掉 stopReason 'deferred'，该消息不会收到 message.completed :: 引用的代码存在（src/runtime/events/projector.ts:219 白名单确为 ['stop','length','toolUse']，pi-ai 的 StopReason 确含 deferred，types.d.ts:287 与 447 的分组也如报告所述），但把它当缺陷站不住，有两条反证。第一，本仓自己就把 deferred 归在 error/aborted 一侧：src/
- [rpc-projector-13] LOW boundary uncertain | P3-4 | src/runtime/events/projector.ts:150 | 非前缀快照会被永久丢弃，且后续 delta 一起哑掉（推断） :: deltas() 用 `prose.startsWith(this.prose) && prose.length > this.prose.length` 判断增量，thinking 同理。一旦 provider 给出一个不是 this.prose 前缀的新快照（内容被改写而非追加），这一次的差值被丢弃，而且 this.prose 不更新——后续所有快照都会继续判假，包括 message_end  || REFUTER: 状态机性质属实：src/runtime/events/projector.ts:148-163 的 `prose.startsWith(this.prose) && prose.length > this.prose.length` 一旦判假就既不发 delta 也不更新 this.prose，而 this.prose 只有在 ensureAssistant(106-118) 新建消息时才复位，所以「一次非前缀修订会把这条消息后续文本全
- REFUTED [rpc-projector-16] LOW test-gap refuted | P4-5 | src/runtime/__tests__/guiEventContract.test.ts:228 | GUI 事件契约录制只覆盖 11 种事件，thinking/工具进度/问答/预览/子代理/压缩/失败停止全部没有回归防线 :: 录制面窄这一点属实，但标题里「全部没有回归防线」这句被现存测试直接推翻。我统计了 fixture src/shared/__tests__/fixtures/nativeGuiEventStream.json 的事件类型，确实只有 message.started/delta/completed、tool.started/completed、permission.requested/resolved
- REFUTED [skills-mcp-23] LOW race refuted | P5-3 | src/runtime/plugins/mcp/index.ts:135 | onStdout 闭包在 client 赋值之前生效，握手前的早期 stdout 会被丢掉 :: 闭包写法属实（src/runtime/plugins/mcp/index.ts:129-136 的 onStdout 是 client?.receive(chunk)，client 在 141 行才赋值），但「早期 stdout 会被丢掉」在当前生产路径上构造不出来，理由是事件循环的阶段顺序：spawnPersistent（src/runtime/host/exec.ts:166-167）在 Pr
- [subagent-core-10] LOW boundary uncertain | P5-2-4 | src/runtime/plugins/subagent/records.ts:322 | 活动投影的 text/thinking id 用 `${agentId}:${Date.now()}`，同一毫秒内两条 message_end 会撞 id :: 同一个委派在同一毫秒内产生两条 assistant message_end 时，两条 text 活动拿到完全相同的 id，渲染层 reducer 按 id upsert 会把后一条覆盖前一条（thinking 同理，只是多一个 `:thinking` 后缀）。正常情况下两次 provider 往返之间间隔远大于 1ms，所以这是推断性的窄窗口；但流内失败重试路径（失败消息 + 紧随其后的恢复消息） || REFUTER: id 确实是墙上时钟拼的（`records.ts:322` 就是 `const id = `${base.agentId}:${Date.now()}``），这部分属实。但报告里两处关键论述被代码推翻：  1. 后果方向反了。渲染层不是「按 id upsert 覆盖」。`src/renderer/components/chat/subagentActivityModel.ts:316-318` 在 `text`/`thinking` 分
- REFUTED [subagent-core-11] LOW quality refuted | P5-2-2 | src/runtime/plugins/subagent/index.ts:183 | 合并文本超 50k 时的省略提示不列出被省略的 delegationId，模型无法照它说的去重读 :: 这条的核心前提——「那些 delegationId 随 block 一起被丢掉了，模型手上没有它们」——与代码不符，因此可以推翻。  `Task` 是立即返回的，它返回的正文第一句就写着 id：`index.ts:585-590` 的 `text: \`Delegation ${delegationId} started: ...\``，同时 `details.delegationId` 也带着。
- [subagent-core-14] LOW race uncertain | P5-2-3 | src/runtime/plugins/subagent/index.ts:634 | 委派权限作用域以 toolCallId 为键，两个并发子代理若拿到相同 toolCallId 会串档并提前互相释放 :: `scopeDelegateTools` 对每次调用 `permissions.scopeToolCall(toolCallId, scope)`，permissions 侧是一张 `Map<string, DelegateCallScope>`（permissions/index.ts:161、190），键就是 toolCallId。toolCallId 由各自的 provider 流生成，两个 || REFUTER: 机制描述属实，但我没能构造出真实触发路径，也排除不掉，所以维持 UNCERTAIN。  属实的部分：`index.ts:634` 确实以裸 `toolCallId` 为键调 `permissions.scopeToolCall`，permissions 侧就是一张 `Map<string, DelegateCallScope>`（`permissions/index.ts:159`、190-195），`set` 直接覆盖、`releas
- REFUTED [subagent-core-15] LOW race refuted | P5-2-2 | src/runtime/plugins/subagent/index.ts:317 | collectFinished 在把文本交给调用方之前就 markDelivered，中途被取消则该批报告永久丢失 :: 这条声称的竞态窗口在 JS 单线程模型下不存在，而且就算真丢了也不是永久丢失。三层理由：  1. 窗口不存在。`collectFinished` 里 `markDelivered`（`index.ts:317`）之后到 `return` 之间全是同步代码（map、running()、字符串拼接）。回到调用方 `agent-loop/index.ts:376-379`，中间只有一个 `if` 和一次
- [subagent-data-11] LOW boundary uncertain | P5-2-6 | src/runtime/plugins/subagent/records.ts:322 | 活动 id 用毫秒时间戳生成，同毫秒的两条消息会被面板去重丢掉 :: text/thinking 负载的 id 是 `${agentId}:${Date.now()}`。渲染层 reducer（subagentActivityModel.ts:316）明确按 (kind,id) 幂等去重：已有同 id 的行直接丢弃。subagentDisplay.test.ts 自己的注释也点明「lane 的 ring 会丢重复 id，共用一个就会静默丢掉后到的那条」，但实现只靠毫 || REFUTER: 代码引用属实，但我构造不出真实触发路径。records.ts:322 确为 const id = `${base.agentId}:${Date.now()}`，thinking 用 `${id}:thinking`、text 用 `${id}`；渲染层 subagentActivityModel.ts:314-317 确实按 (kind,id) 幂等丢弃后到者；subagentDisplay.test.ts:44-45 的注释也确实点
- [import-catalog-14] LOW quality uncertain | P5-4 | /home/ai/code/ai-client/src/runtime/worker/nativeImport.ts:349 | inspect/reconcile 丢掉了 pi 版的归属校验，workspacePath 形参完全未用 :: pi 版的 inspectInterrupted 会对每个候选文件跑一次 preflight，确认文件头里的 sessionId 与 workspace 确实属于这次导入，再决定删不删。native 版只按路径名 stat，workspacePath 参数被改名成 `_workspacePath` 直接弃用，但 RPC 契约与 Main 侧仍然在传它。结果是：只要文件名对得上，reconcile  || REFUTER: 代码事实我全部核实：native 的 inspectInterrupted 形参写作 `_workspacePath` 并且函数体内只按路径名 stat（nativeImport.ts:349-360）；pi 版确实对每个候选跑 preflightPiSessionFile 并校验 header.sessionId（git show HEAD:src/agent-host/piLegacyImport.ts:310-344）；RPC 契
- REFUTED [cutover-09] LOW test-gap refuted | P6-5 | src/agent-host/__tests__/workerStripOnlyCompat.test.ts:84 | strip-only 兼容守卫只覆盖 worker 入口的静态图，整个 src/runtime 不在守卫范围内 :: 描述的「walkGraphFrom 只走静态图、src/runtime 不在这个守卫里」属实（src/agent-host/__tests__/workerStripOnlyCompat.test.ts:48 的正则要求 `import ... from '...'`，worker.ts 的四个 `await import(...)` 匹配不上）。但失败场景「全量单测全绿、只有 pnpm dev 
- REFUTED [cutover-14] LOW contract-gap refuted | P6-2 | package.json:47 | 随包 pi CLI 用的是浮动版本范围，而 H/20 互通结论只对 0.84.4 成立 :: 「随包 pi CLI 用浮动范围」与代码不符。真正随包的那一份来自 src/agent-host：scripts/build-agent-host.mjs:127/136 先跑 preflightHostDeps 再从 src/agent-host/node_modules 复制，而 scripts/agent-host-build-lib.mjs:80-83 对 REQUIRED_WORKER_
- [cutover-15] LOW doc-mismatch uncertain | P6-5 | docs/plantree/plans/runtime-evolution/topics/p6-cutover.md:87 | 文档称三个引擎工厂「缺一个就在构造时报错」，代码只在类型层面必填 :: p6-cutover.md 的「RPC server 的形状变化」写：三个工厂从可选变必填，「缺一个就该在构造时报错而不是运行时才发现」。实际 PiWorkerRpcServer 的构造函数只校验 generation，对 createRuntime / createImportWriter / createUtilityRuntime 没有任何运行期检查；缺失只会在第一次对应请求到达时表现为 ` || REFUTER: 代码侧事实成立：piWorkerRpcServer.ts:315-323 的构造函数只校验 generation，三个工厂没有任何运行期检查，缺失只会在 :478（createImportWriter）或 :622（createUtilityRuntime）第一次被调用时炸。但这构不成可触发的缺陷：生产里 `new PiWorkerRpcServer` 只有 src/agent-host/worker.ts:131 一处，其余全是 Ty
- [cutover-16] LOW quality uncertain | P6-2 | scripts/agent-host-build-lib.mjs:17 | 打包仍强制携带并校验三个再也不会被加载的 pi 扩展包 :: REQUIRED_WORKER_PACKAGES 与 verifyArtifact 仍把 @gotgenes/pi-permission-system（1.8M）、@juicesharp/rpiv-ask-user-question（408K）、@gotgenes/pi-subagents（1.3M）当作必须存在且必须可用的产物，verifyBundledPermissionPolicy 还会把序 || REFUTER: 三个包要分开看，报告把它们捆成「纯载荷」是错的。两个特性插件（@juicesharp/rpiv-ask-user-question、@gotgenes/pi-subagents）确实已无运行期消费者：唯一的注入口 resolveBundledFeaturePlugins（bundledFeaturePlugins.ts:97）在生产代码里零调用（只剩自己的单测），native 侧的 ask / 委派都有自有实现。但 @gotgenes/
- [cutover-17] LOW quality uncertain | P6-5 | src/runtime/plugins/permissions/bridge.ts:24 | 审批回退分支里硬编码中文选项且不可达 :: createRuntimeApprovalBridge 用三个硬编码的中文字符串作为 ui.select 的选项，并按字符串相等把结果映射回 allow-once / allow-session / deny。这段既没有 i18n（应用其余用户可见文案都走 t()），又在生产里不可达——bootstrap.ts:239 的 `options.permissions?.approve ?? appr || REFUTER: 代码原文属实：src/runtime/plugins/permissions/bridge.ts:24 确实用三个硬编码中文串作为 ui.select 选项，并按字符串相等映射回 allow-once / allow-session / deny。「生产不可达」也核实了：唯一的生产装配点 src/runtime/worker/nativeWorkerRuntime.ts:238 无条件传 approve: this.permission
- [cutover-19] LOW boundary uncertain | P6-3 | src/main/services/terminal/piTuiSession.ts:170 | 「是否已升级为双格式头」在 Main 与 runtime 用了两套不同判据 :: TUI 入口守卫只看 `kind==='header' && version===4 && type!=='session'`，而 runtime 侧判定同一件事的 isInteropHeader（codec.ts:79-83）还要求 timestamp 是字符串。两者对「type 有、timestamp 没有」的头给出相反答案：守卫放行，store 认为仍未升级。这是推断出来的分歧，当前没有已知 || REFUTER: 两套判据的差异属实：src/main/services/terminal/piTuiSession.ts:170 只看 kind==='header' && version===4 && type!=='session'，而 src/runtime/plugins/session/codec.ts:51 的 isInteropHeader 额外要求 typeof timestamp === 'string'（注意报告给的行号 79-83
- REFUTED [cutover-21] LOW test-gap refuted | P6-2 | src/agent-host/__tests__/nativeWorkerDependencyBoundary.test.ts:50 | 依赖边界守卫对解析不到的相对说明符静默跳过 :: 两处关键声称与代码不符。其一，「自检只断言 graph.files.size > 5」不成立：同一个用例 src/agent-host/__tests__/nativeWorkerDependencyBoundary.test.ts:85-86 还断言图里必须含 src/agent-host/piWorkerRpcServer.ts 与 src/agent-host/piWorkerErrors.