# 决策 004：项目 `.agents/skills` 照 pi 从 cwd 及祖先目录一路找到仓库根

日期：2026-09-14。状态：已采纳（用户拍板，回答 Q004）。

## 决定

项目级技能目录的发现规则与 pi 对齐：从当前工作目录开始逐级向上，直到仓库根（含），每一层的 `.agents/skills` 都算项目技能。现状只看 cwd 一层。

## 理由

- 行为对齐参考实现，用户不用记两套规则；monorepo 子包里打开也能用仓库根的技能。
- 代价只是多几次目录探测，且已有的 32 KiB 预算与去重规则照用。

## 影响

- T019 实现祖先目录扫描；同名技能按「离 cwd 越近越优先」处理，与 pi 一致；K1 验收补「祖先目录」一句。
- 审计 skills-mcp-22 从「文档改口」改为「实现」。
