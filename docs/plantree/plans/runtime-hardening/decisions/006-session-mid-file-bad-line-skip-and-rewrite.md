# 决策 006：会话文件中段坏行照 CLI 做法跳过并重写文件

日期：2026-09-14。状态：已采纳（用户拍板，回答 Q008）。

## 决定

解码会话文件遇到无法解析的中段行时，不再整体拒绝打开；跳过该行并重写文件，使两个读者（GUI 与 pi CLI）对同一文件的解释一致。

## 已知代价（用户知情）

- 会静默丢掉一行；若丢的是 `lane` 行，用户看到的对话分支会改变。
- 实现时应把被跳过的行记进诊断（trace 与会话状态提示），让「文件曾自愈」可被看见；这是缓解措施，不改变决定本身。

## 影响

- 批次 B 新开 T034 承接；T003 已修的「CLI 打开但未再写」尾片形态保持不变。
- open-questions 移除 Q008。
