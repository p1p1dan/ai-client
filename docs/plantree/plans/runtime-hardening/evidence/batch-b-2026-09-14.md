# 批次 B 落地记录（2026-09-14 起）

Role: evidence。批次 B（功能正确性，T012～T024 + T034 / T035）逐波落地记录。任务身份与状态以 [roadmap](../roadmap.md) 为准，本文只记落地方式、提交、偏离与转移项。批次 A 记录见 [batch-a-2026-09-14.md](batch-a-2026-09-14.md)。

## 方式

- 与批次 A 相同：一个编排者 + 每波两个实现代理（文件区域互不重叠），代理只跑本区域测试并对每条修复做反向验证；编排者每波跑三套 tsc + 全量 vitest + 改动文件 Biome，全绿后按任务分别提交。
- 波次排布：① T012 + T015 → ② T018 + T019 → ③ T014 + T016 → ④ T017 + T022 → ⑤ T034 + T013 → ⑥ T035 + T021 → ⑦ T020 + T024 → ⑧ T023。T014 先于 T035（`run.targetPath` 删除是 T035 的前提）；T015 先于 T034（同目录）；T022 先于 T013（共用 host/io.ts）；T023 改动面最广放最后。
- 起点提交 `f6533217`（批次 A 收尾与决策 008）。

## 提交与覆盖

| 波 | 任务 | 提交 | 覆盖的发现 | 全量测试 |
|---|---|---|---|---|
| 1 | T012 | `439ab922` | tools-02/03/04/05/06/08/09/12/13/14/17 | 398 文件 / 5739 |
| 1 | T015 | `ce2a7af8` | session-04/05/06/09/10/12 | 同上 |
| 2 | T018 | `942ee464` | skills-mcp-01/02/03/04/05/06/07/14/15/16/21/24 | 398 / 5774 |
| 2 | T019 | `39afacc8` | skills-mcp-08/09/10/17/18/19/20/22/25、决策 004 | 同上 |
| 3 | T014 | `2c0eb30c` | context-prompt-01/02/03/04/05/06/08/10/11/12/13/14/15/16/17、决策 007 | 398 / 5809 |
| 3 | T016 | `a9ea7230` | worker-runtime-01/02/05/06/07/09/12、rpc-projector-05/06/08/10、import-catalog-04、context-prompt-07 | 同上 |
| 4 | T017 | `84ac35e0` + `83a9d0f8` | rpc-projector-02/03/07/13/17/18、permissions-08 | 400 / 5852 |
| 4 | T022 | `84ac35e0` + `117d97a9` | core-host-04/06/07/08/09/10/11/14/20、T008 rmdir 残项 | 398/399 文件、5826 通过；仅 guiEventContract 两条录制待 T017 重录 |
| 5 | T013 | `19f9e888` | tools-10、core-host-05 | 400 / 5870 |
| 5 | T034 | `1f1f40d0` | session-02 中段形态、决策 006 | 同上 |
| 6 | T021 | `d3949e84` | import-catalog-02/03/06/12 | 406 / 5950 |
| 6 | T035 | `5b305fdb` | 决策 007 / 008 | 同上 |
| 7 | T024 | `8564ba41` | 批评者容量缺口、permissions-12 后半（tools-11 复核不成立） | 408 / 6024 |
| 7 | T020 | `60b250f3` | subagent-data-01～17、subagent-core-06/12/13、决策 005 | 同上 |

## 与 roadmap 字面不同的做法（已认可）

- T012：tools-04 的修复点在 `accept()` 而非审计定位的那一行（60 KiB 单行实际走的是另一条分支，两条分支共用同一语义）；grep 超预算给截断说明并在 details 标 `timedOut`，不抛错，保住已搜到的命中；read 可见预算固定减 128 字节留给续读尾巴；read 空结果分「(empty file)」与「(no lines at offset N)」两种措辞；顺带把「不含 `/` 按任意深度」写进 glob / grep 的 description。**残余风险**：JS 正则同步执行不可中断，单独一行灾难性回溯仍可能卡住 worker，需独立线程才能根治。
- T015：`close()` 用 allSettled 之外加了 `writeFailure` 取值器作为失败观测点（不进 contracts）；抢占陈旧锁的归属校验用原始字节比对而非 token（撕裂的锁解析不出 token）；输掉竞争时把误取的锁原样写回，避免删掉赢家的锁；无效 label 行丢弃而不是让整次导入失败。派单时把 session-09/10/12 三条的描述轮换了，代理按审计原文编号落到代码与用例里，工作项集合不变。
- T018：skills-mcp-04 用字节缓冲（遇 0x0A 整帧解码）而非 `StringDecoder`，一处同时修好跨块解码与按字节计帧长；工具注册失败记进新字段 `connection.toolErrors` 而非 `connection.error`（后者有 `mcp_failed` 计数与 `close()` 两个消费者，一个工具撞名不该把整台服务器标失败）；工具名点号替换而非拒绝，`config.ts` 服务器名校验保持 `[\w.-]`；额外加了图片 8 张上限、文本块永不为空（否则 projector 会把 base64 JSON 化进 transcript）、`fail()` 经 `onFailure` 回填 `connection.error`。
- T019：派单时 skills-mcp-17/18/19 的描述也轮换了，代理按审计原文实现。悬空 symlink 留 `read_failed` 诊断（pi 上游静默跳过）；块标量视为未声明并记 `invalid_metadata`；模板描述回退先折叠空白再截 60 字；`SkillsPlugin.refresh()` 按已解析根重扫但无自动调用方；项目 `.agents/skills` 从 cwd 向上找 `.git`（上限 64 层），仓库根先入栈、cwd 后入栈，靠 last-wins 实现近者优先，`.pi/skills` 不受影响。
- T014：context-prompt-04 的降级不是纯 skipped——hard limit 下先丢保留尾重装 checkpoint，省不动报 `context_too_large`（放一个必然超窗的请求出去比清晰报错更糟）；05 复核后成立（pi 的 `serializeConversation` 不截 assistant 工具调用参数，摘要输入端无检查），加 `summaryInputBudget` 最新优先截断，assistant 整条丢弃以保 toolCall 配对；12 审计给的场景不可达但 provider 返回超长摘要可稳定命中，改 `giveUp('compaction_over_budget')`；06 不把 `invalid_host_request` 整个纳入可选集合（它也覆盖调用方错误），改为先 stat 跳过非普通文件；14 不让 tool-guidance 随 mode 变（会把模式切换塞进缓存前缀）；16 改描述不改口径（唯一潜在消费者 prefixStability 无调用方）；17 只做预算守卫（delegate 无自己的会话，checkpoint 链无处落）。`instructionDirectories` 整个删掉，T035 重写。
- T016：compact 预算用两个共享常量并在测试里断言 worker < Main，不加 RPC 字段；`handle.dispose()` 失败吞掉记日志而不上抛；`handleSetPermissionTier` 直接回 `WORKER_UNSUPPORTED`（会话匹配检查已要求 bootstrap 存在，再分级是死代码）；reload 拆除失败只加特征化测试（拆除失败后槽位拒绝后续读）。
- T017：`session.stderr` 接生产者而不是删类型（理由见 roadmap），并额外加了每回合 50 行转发上限与一条封顶提示——常量 `STDERR_FORWARD_MAX_LINES_PER_TURN` 本来就写在脱敏模块里没人用，静默截断会让面板显示看似新鲜的旧摘录。五类没并进一份录制：thinking 与问答合成一轮，压缩、重试各一份，子代理沿用原 fixture 重录，主录制场景刻意不动以保证 diff 可归因。重试录制用真 3 秒 sleep。
- T013：块大小用几何倍增而非「固定放大到输出预算若干倍」——固定倍数只把常数变小，helper 每块从头重读的总量仍是 O(n²)；翻倍后块起点呈等比数列，总重读量约为已扫字节的 2 倍。没走「HostIo 暴露可复用句柄」：helper 是一次性子进程，句柄跨不了进程边界，真复用要改成常驻子进程协议。core-host-05 选「exec 请求加可选 `maxStderrBytes`」而非 HostIo 自收 stderr：预算算法本就在 exec 的 consume 里，分流是一处改动。bash 工具仍用合计预算（它要把 stderr 一起展示给模型）。契约文档 p1-0 第 4 节同步改写。
- T034：范围只到 JSON.parse 失败的行；能解析但结构校验失败的行 CLI 会保留，仍拒绝（删一条完整在盘的行会让两读者反向分歧）。加了决策 006 没写的 64 条上限：无上限时 header 能解析、正文完全不是 JSONL 的文件会被「逐行修好」，几百万条 preview 驻留内存且 seq 额度撑到校验失效。孤儿 parentId 仍拒绝但错误信息点名被跳行：pi 的 `_buildIndex` 不校验父子、`getTree` 把孤儿当根、`getBranch` 走到缺父就停，对齐要改写可读行的 parentId 或让 branchEntries 静默截断，且只自愈一次（重开时已无 skipped 记录会再报废）。GUI 提示按 run 挂在第一条 `session.status` 而非开会话时：先按任务书做了 bootstrap 期发事件，发现 `WorkerManager.handleWorkerEvent` 在槽位非 ready 时直接丢弃，bootstrap 期槽位是 creating，整体回退改走 projector（retry 同路）；代价是用户要发一条消息才看得到，reducer 粘住后一直在。
- T021：没改 `readUserProvidersForRuntime` 返回形状，加姊妹函数 `readUserProviderGroupForRuntime`（原函数「失败即空」对写盘侧是正确语义）。「无缓存时用哪份」选随包快照而非空目录：A3 离线首启已把快照写进 models.json，统一成空目录会让用户编辑一次服务就抹掉唯一可用目录；规则定为有缓存用缓存，无缓存托管用随包、本地用空目录。本地模式只挡随包基线不挡 wire 缓存（连缓存一起丢会在切模式瞬间静默重写 models.json，超出本次）。本地模式凭据 null 不触发回落（本地没有可继承凭据，null 是常态）。决策放进新文件 nativeCatalog.ts（index.ts 模块加载期抓 electron 四个模块）。
- T035：父目录止于文件系统根之下，不在 `~` 截断（官方口径；`~/.claude/CLAUDE.md` 走 user 层）；预算按渲染顺序装、耗尽即止，不给 cwd 预留（读取顺序与渲染顺序分家正是 context-prompt-13 那类截断 bug 的来源）；globals 加可选 `scope`（缺省 user）而非拆两个参数；symlink 守卫从「工作区根内」改「所在目录内」（父目录本就在工作区外）；local 策略 scope id 沿用 `'project'`（`PolicyScopeId` 是渲染层两张穷举表的键）；skills 不造 local 根；按需条目只进消息不进系统提示词（保缓存前缀），启动链整体标已装；注入走 pi steering 队列（唯一发 message_end、能进 agent.state.messages 与会话文件的路径）。**两处未按决策字面**：指令层此前无信任门控，补上后 managed 路线不再装仓库 CLAUDE.md（Q009）；「随包策略优先级最高」与 D-Q9 决策 4 冲突未改（Q010）。顺手修掉 prompt root 未 realpath 的坑。
- T024：轮转选「重命名 + 保留三代」而非删旧行或就地截断（只追加 JSONL 删行要重写整文件，截断会切半行）；文件大小每次从盘上 stat 而非进程累计（文件比进程活得久）；单条超上限照样整行写入；轮转失败不阻止写入但经 flush 以 `trace_rotate_failed` 上报。write 的 preview **没有**在构造侧截断：核实不落盘，截了只会让审批卡显示不全；限额改放真正落盘的 MCP 侧。图片 1 MiB/张、2 MiB/次 是按「base64 1 MiB ≈ 768 KiB PNG 够一张截图」推的，无真实样本。
- T020：`migrate.ts` 整体迁到 `src/shared/subagentMigration.ts`（根 tsconfig 排除 src/runtime，Main 引不到，不搬就得抄一份）；查表用 Map 而非 `Object.hasOwn`；`writeScalar` 不加 throw，改在 save() 做文档级往返断言（纯渲染函数抛错波及所有调用方，往返断言同时覆盖引号改写与换行注入）；`subagentHistoryUsage` 保留并有了生产调用方（重开会话补种子，否则总量含父历史不含子历史）；guidance 的 edit / write 块也拆了；决策 005 的独立字段是 `usage.updated.delegated`，会话总量经 `applyTurnUsage` 的 `source: 'tool'` 桶包含委派、占比现算不存第三份数字；委派结算时额外发一条 `usage.updated` 复述上一轮 turn 数字（渲染层取最后一条不累加）；MCP 标签放 `default` 分支末位不抢服务端 description（改写 1 条既有用例）；`ToolsPlugin.register` 重名抛错且浅拷贝，refresh 只能原地改写注册表那份；bootstrap 子代理目录加载提前到版本戳之前（-10 refute 指出两个 trace 键永远写不进）。
- T022：dispose 里多个清理失败用 AggregateError 聚合上报，单个失败原样抛；`killTree` 抽成 `createTreeKiller`，平台、spawner 与 POSIX 组信号（`killGroup`）都可注入，Windows 半边在 Linux 上用注入测；readDirectory 的路径校验与 disposed 判断提前到调用点，并把迭代计入 pending 让 shutdown 等它；反向验证是整文件撤修（写用例的会话丢失，逐条记录不可考）。

## 转移与待盯项

| 项 | 去向 |
|---|---|
| grep 单行灾难性回溯不可中断（需 worker 线程） | 记录，不做 |
| session-11（open 失败路径 unlink 盖掉原始错误；IO 已 dispose 的窄路径仍在） | 未列入任务，风险与改前持平 |
| legacy.ts `lines()` 非首行裸 SyntaxError | 未列入审计，首行已被拦下，留记录 |
| `SkillsPlugin.refresh()` 的触发端（Main 侧 getSlashCommands 或「重新加载技能」动作） | T020 / UI |
| skills 模板 `$1` 先展开后参与后续占位匹配；frontmatter 未闭合引号跨行 | 未列入任务，与 pi 上游行为一致，留记录 |
| MCP resources / prompts / sampling / roots / HTTP-SSE | P5-3 既有豁免 |
| 子代理压缩（checkpoint 链、new_context、跨 run 恢复） | 不做；P5-2 契约应显式记「delegate 只做预算守卫 + maxTurns 兜底」，归 T027 文档回写 |
| `shared/types/nativeSession.ts` 的 `NativeIndexedRunRequest.targetPath` 与 `NativeSessionIndexAdapter.ts` 透传（死字段） | T025 |
| `keepRecentTokens` 对本 runtime 输出已无可观测影响（常量保留，注释说明） | 记录 |
| `src/agent-host/worker.ts` 转交 modelCatalog 无直接用例 | 由 RPC 层用例 + typecheck 保证，记录 |
| 「打开即提示」：recovery 进 `WorkerBootstrapResult`，由 `WorkerManager.publishHistoryTriplet` 与创建路径挂到已有 `session.status idle` 上 | T020 / UI（未动 src/main） |
| 渲染层无组件读 `session.recovery` | T020 / UI |
| `readForkHistory` 与 `nativeImport` 的 open 点会重写但不上报 recovery | 只读用途无接收方，记录 |
| 「我方重写会话文件时 pi CLI 正持有旧 fd」的并发场景 | 未验证，记录 |
| 明文路径 readLines 仍固定 32 KiB；加密路径单块内仍从文件头重读（需 helper 定位读或常驻） | 记录，不做 |
| 64 MiB 扫描上限现在最多多读一块（≤2 MiB）才抛 io_limit | 语义无实质变化，记录 |
| 真实加密机 TSD 链路（真 helper + NODE_OPTIONS 告警） | 批次 E 上机 |
| Main → Worker 的 `settingSources` 透传：`WorkerBootstrapPayload` 加 `settingSources?` 并在校验函数接受；Main 填值；`nativeWorkerRuntime.ts` create 处透传一行 | 待有生产者（设置页或策略）时一并接，暂无任务 |
| 用户自建服务的 model 级 baseUrl（`UserProvider.models` 只有 id） | 需动设置页与 vault 结构，未列任务 |
| 本地模式是否应继承公司凭据（写盘与 native 交付都不看 credential mode 取 managedCredential） | 既有行为、两条路一致，记录 |
| 父目录链在深层路径上的 IO 开销（每目录 ≤4 次 realpath，每 run 一次 compose） | 未基准，记录 |
| Windows 下 `instructionDirectories` 终止条件（`dirname('C:\\')`） | 批次 E 上机 |
| `.claude/rules/*.md` | 决策 007 / 008 明确不做 |
| 用户附件（图片与文本）无任何体积上限，绕过所有闸门直接落会话文件（`agent-loop/attachments.ts`） | 对账表「待落地」，需派单 |
| `tool_execution_start` 把工具参数全文写进 runs.jsonl（write 的 8 MiB 整份一行） | 对账表「待落地」，建议沿用 `MAX_TRACE_PREVIEW_CHARS` |
| compaction 摘要正文无上限（`context/index.ts`） | 对账表「待落地」 |
| 会话文件缺「单行最大字节」兜底（`session/store.ts`） | 独立小任务评估 |
| 多 worker 并发轮转同一 runs.jsonl（最坏多轮转一次丢一代） | T031 |
| 项目级 `.pi/agents` 不纳入导入扫描；导入不覆盖同名 user 文档 | 契约规则 3 / 有意不做 |
| 低资源（2 核 / 3.3GB）10 路并发委派的界面表现 | ARD D10 另案 |
| 两个迁移 IPC handler 薄包装无单测；`refreshTaskTool` 在 plan 模式跳过改写无用例 | 记录 |
| T020 的「导入旧定义」面板与 Run 面板「其中委派」行 | 批次 E GUI 点验 |

## 逐波记录

- 2026-09-14 批次 B 第一波 **已提交（439ab922 / ce2a7af8）**：T012（11 条发现全部落地，tools.test 41→53 条，13 次反向验证判红）、T015（6 条全部成立并修复，含两进程同时抢陈旧锁的确定性复现，9 条新用例）。全量 398 文件 / 5739 测试、三套 tsc、11 个改动文件 Biome 通过。
- 2026-09-14 批次 B 第二波 **已提交（942ee464 / 39afacc8）**：T018（12 条全部成立，mcp.test 20→35 条，fixture 新增四个行为开关与 shot 工具，19 次反向验证判红）、T019（9 条 + 决策 004 全部落地，skills.test 31→48 条，7 条反向验证判红）。全量 398 文件 / 5774 测试、三套 tsc、11 个改动文件 Biome 通过。
- 2026-09-14 批次 B 第四波（上半）**已提交 T022（b5b0f3aa）**：上一个会话在机器「死机」后丢失：它其实已提交并推送了 `84ac35e0`（T017 + T022 两半合一），但本地分支被回退到 `3395ae7c`、改动留在工作区，只查本地历史与 reflog 会误判「该提交不存在」——排查时要一并看 `origin/`。本次把本地分支重新对齐到已发布的 `84ac35e0`，事故修复作为其上的增量提交。编排者单独验证：三套 tsc 通过；全量 399 文件 / 5828 测试中 5826 通过，2 条红是 guiEventContract 的 golden 录制（T017 改了消息 id，属预期，归 T017 重录）。**事故复盘**：三次「死机 / 锁屏 / 窗口全关」的真凶是 T022 代理写的用例用假 pid 1 调 `process.kill(-1, 'SIGKILL')`——Linux 上是向当前用户全部进程广播，journal 特征 `user@1000.service: Main process exited, code=killed, status=9/KILL`，systemd-oomd 与内核 OOM 均无记录；之前按内存不足处置（flock、限 worker）全部无效。修法：`createTreeKiller` 增加可注入 `killGroup`，用例改桩并加守卫用例；另一条宽限用例改为等子进程装好 SIGTERM handler（原先偶发判绿）。教训写入 docs/agent-project-engineering.md 附录 B（`e1e188d1`）。T017 的用例、fixture 重录与 session.stderr 生产者随后以 `83a9d0f8` 落地，第四波收口时全量 400 文件 / 5852 测试通过。
- 2026-09-14 批次 B 第三波 **已提交（2c0eb30c / a9ea7230）**：T014（15 条全部处理，新增 21 条用例、删 3 条，15 次反向验证判红）、T016（13 条全部落地，跨 runtime / agent-host / Main 三层，新增 18 条用例，10 次反向验证判红）。全量 398 文件 / 5809 测试、三套 tsc 通过，29 个改动文件 Biome 仅 1 条 HEAD 既有警告（workerEndToEnd.test 未使用 import）。
- 2026-09-14 批次 B 第五波 **已提交（19f9e888 / 1f1f40d0）**：T013（tools-10、core-host-05 全部落地，host.test 36→38、tools.test 53→54，3 次反向验证判红；同一份 1 MB 文件跳行读 30 次调用→5 次）、T034（session-02 中段形态，sessionCodec 14→22、session 20→23、sessionInterop 11→13、chatSessionsCore 60→62，12 处反向验证判红；wire 守卫 [W-1] 由编排者更新为五键并补 [W-1b]，这是全量测试唯一一条红，属预期）。全量 400 文件 / 5870 测试、三套 tsc、18 个改动文件 Biome 通过。
- 2026-09-15 批次 B 第六波 **已提交（d3949e84 / 5b305fdb）**：T021（4 条全部落地，PiModelConfigService.test 42→48，新建 nativeCatalog.test 6、readUserProviderGroup.test 6，7 组反向验证判红）、T035（决策 007 / 008 落地，净增 61 条用例，21 次反向验证判红；两处与决策字面不同记 Q009 / Q010）。全量 406 文件 / 5950 测试、三套 tsc、30 个改动文件 Biome 通过。
- 2026-09-15 批次 B 第七波 **已提交（8564ba41 / 60b250f3）**：T024（对账表 + runs.jsonl 轮转 + trace 内存封顶 + MCP 图片限额，新建 trace.test 11、mcp.test 35→38，4 组反向验证判红）、T020（20 条全部处理，新建 subagentHardening.test 22、runtimeToolVocabulary.test 23，其余 7 个文件净增，删 subagentProjection 17 条，19/19 反向验证判红）。全量 408 文件 / 6024 测试、三套 tsc、39 个改动文件 Biome 通过；唯一一条红是子代理 golden 录制，重录后差异全部可归因 T020（`delegated` 字段、结算多一条 usage.updated、活动 id 序号）。提交时 T020 代理预先 `git rm` / `git mv` 的三处被误带进 T024 提交，已 reset 重做。
