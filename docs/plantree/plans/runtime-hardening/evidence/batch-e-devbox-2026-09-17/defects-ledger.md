# T032 开发机组点验缺陷清单（草稿，审查完毕后总结）

| # | 来源项 | 缺陷 | 证据 | 建议归属 |
|---|---|---|---|---|
| D1 | DEV-33 | 会话总预算检查在写队列内部（store.ts:357），一次 session_size_limit 拒绝后 enqueue 的 tail 变成已 reject 的 promise，同一 store 此后连纯文本都写不进；重开 store 才恢复。与 attachments.ts:50-52 及容量对账表「会话本身照常可写」相反 | sessionAttachmentFill.test.ts 用例 1；/tmp/t032/sections/DEV33-runtime.md | 独立小修，不等 T058：T058 后总预算拒绝几乎不再触发，但「队列内 reject 使 store 永久只读」的逻辑仍在，任何路径触发一次就中毒。最小修法：照 capacity-04 给总预算加一道队列外预检 |
| D2 | DEV-33 | ATTACHMENT_TURN_STORED_BYTES 与 SESSION_MAX_ENTRY_BYTES 同为 8 MiB 但前者称 base64、后者称整行 JSON，正好顶到附件上限的发送被 session_entry_size_limit 拒且不点名附件 | sessionAttachmentFill.test.ts 用例 2 | 并入 T058（决策 015 附件旁路落地后附件字节移出 JSONL，这个接缝自然消失），不单修 |
| D3 | A1 方法学 | 模型目录两侧口径不一致：选择器读磁盘 models.json（PiModelConfigService.ts:444，零过滤），worker 拿的是 Main 用「凭据库用户服务 + 托管缓存」组的内存目录（:649），不读 models.json（bootstrap.ts:477-478）。任何只存在于磁盘的 provider 都「列得出、发不出」，且下次改设置会被 writeUserProviderRuntimeConfig 静默覆盖。本机 vllmproxy 是 09-11 手写进 models.json 的，从没进凭据库；T007 no_api_key / T021 回落 / ANTHROPIC_* 环境变量三个怀疑对象均排除 | /tmp/t032/sections/investigate-vllmproxy-catalog.md | 产品侧最小修法：readPiModelCatalog 也过一遍 parsePiCatalog（或选择器改读同一份内存目录）；诊断出口并入 T050；口径对齐建议新开条目。本机配置修法：设置页新增 vllmproxy 服务（slug 须为 vllmproxy、地址不带 /v1），别再改 models.json |
| D4 | DEV-7 | 索引修复成功后的 `console.warn('[chat] Session index was damaged …')` 在 main.log / 按天日志 / dev.log 都没有；仓库注释称 electron-log 吞掉低于 error 的级别。T038 说明的「记 main.log」没兑现 | dev-A1/dev-mainlog-notes.txt | 小修：改成 log.error 或 log.warn 经 electron-log；需先核 electron-log 接线 |
| D5 | DEV-9 | 删掉临时工作区后其下聊天从侧栏静默消失、无提示，索引行与 JSONL 仍在（判据描述的形态，按判据算复现） | dev-A1/dev-09-27-temp-workspace-remove.json | 产品决策：是提示用户还是把会话转成 unbound 显示；建议列入总结让用户定 |
| D6 | DEV-8（线索） | 未绑定会话在同一次运行里从侧栏消失、下次启动又全部回来，未定位 | dev-A1/dev-08-reclaim-on-next-boot.json | 线索，需复现定位后再定 |
| D7 | DEV-10 | Codex 会话目录下任一日期子目录不可读（mode 000）时，整个 Codex 源从导入面板消失，而不是跳过那一天；权限恢复后立刻回来。CodexSessionScanner.walk 的 readdir EACCES 没有按目录兜住，冒到 scanAllLegacySources 的 catch {} 被吞 | ⛔ 真机确认复现（已知 import-up-01，T052 批次 F 待修） | dev-C/dev-10-02-panel-with-eacces-scrolled.png、dev-C/dev-10-pane-with-eacces.txt；/tmp/t032/sections/C.md | 已有归属：T052 批次 F。修的时候连 D8 一起修 |
| D8 | DEV-10 | 上述失败对用户与对日志都完全无声：界面无任何提示，EACCES 在 dev.log / main.log / 按天日志三处 grep 计数均为 0 | ⚠️ 疑似缺陷（可观测性），与 D7 同源但容易只修「不整源消失」而漏掉「要说出来」 | dev-C/dev-10-mainlog-grep.txt | 并入 T052：至少在源被部分跳过时给一条诊断，界面给一句「有 N 个目录读不到」 |
| D9 | DEV-11 | 导入失败文案中英混排：原因句是英文原文（Claude session exceeds the 4000-entry import limit / …the 67108864-byte import limit），汇总句是中文；不给下一步，字节数用裸 67108864 而非 64 MiB | ⚠️ 文案质量，不影响功能（守卫与残留清理都正确） | dev-C/dev-11-pane-after-35mib.txt、dev-C/dev-11-pane-after-64mib.txt | 小修：在渲染层给已知的 ClaudeImportSourceError 做中文映射并补一句下一步 |
| D10 | DEV-11 / DEV-13 | 整条导入链路没有任何日志：42 次成功导入 + 2 次守卫失败，三处日志里连一行 legacy-import 都没有 | ⚠️ 疑似缺陷（可观测性） | dev-C/dev-11-13-mainlog-grep.txt | 小修：导入批次起止与每条失败各记一行（注意 electron-log 吞低级别，见 D4） |
| D11 | DEV-13 | 导入面板的会话列表不做虚拟化，320 行一次性全渲染；批量导入起手与收尾各有一次 1.2～1.6 秒的整表重渲染停顿（其余 50 多秒 CDP 往返中位 5 ms、滚动 221/221 正常） | ⚠️ 轻微，320 份量级可接受，份数再上一个量级需复核 | dev-C/dev-13-batch40-watch.json、dev-C/dev-13-timing-summary.txt | 记录即可，不建议现在改 |
| D12 | DEV-3 | 兼容根（~/.agents/subagents）里的子代理定义，在设置页编辑后再删除，第一次删除只删掉主目录里的影子副本，那一行带着编辑前的旧内容回来，要删第二次才真的消失；全程无提示说明「你编的是副本」。成因：编辑走 subagentCatalog.ts:236/386 的写路径，无条件 mkdir 主目录再写 <agentDir>/subagents/<name>.md（:408），兼容根原件不动；删除只删得到主目录那一份，合并规则「主目录 > 兼容根 > 内置」（catalog.ts:78-89）让兼容根那份重新浮上来 | ⛔ 真机确认复现，**DEV-3 判据直接判负** | dev-B/dev-03-t2-after-delete.txt、dev-B/dev-03-05-list-after-delete.png；/tmp/t032/sections/B.md | 需要产品决策再定修法：是「编辑兼容根定义时就地改兼容根文件」还是「删除时连兼容根一起删」还是「界面上标出这一行有两个来源」。建议列入总结让用户定 |
| D13 | DEV-2 | 设置里的「保存位置」（defaultTemporaryPath）Main 侧恒读不到：readSettings()（main/ipc/settings.ts:82-87）返回 settings.json 顶层，而该文件顶层只有 credentialMode 与 aiclient-settings 两个键，用户设置全在 aiclient-settings.state 里。ScratchWorkspaceService.ts:83 与 TempWorkspaceService.ts:36 都这么读 => settingsTemporaryPath() 恒为 ''，scratch 根永远回落 ~/JYWAI/temporary。渲染层自己从 store 读得到真值并显式传 basePath，结果是「临时工作区去了新目录、临时对话还在老目录」；ScratchWorkspaceService.ts:78-82 的 F2-a 注释所描述的行为在当前设置文件形状下拿不到 | ⚠️ 疑似缺陷（前任实测现象 + 本批读码定因，未写用例复核） | dev-B/dev-02-t3-after-gui-change.txt、dev-B/dev-02-t4-after-restart.txt；/tmp/t032/sections/B.md | 先补一条用例把「Main 读得到 defaultTemporaryPath」钉住，再决定修 readSettings 的解包还是改这两处读法；建议新开条目，别塞进 T037（T037 修的是换根后的清理，不是读不到设置） |
| D14 | DEV-1 | 归档正在跑回合的临时对话时，归档 / worker 退出 / scratch 目录删除三步全过程零日志（main.log、按天日志、dev.js 标准输出三处 mtime 都停在归档之前）。成功路径本就不记：ScratchWorkspaceService.ts:244 的 [scratch] failed to remove 只在删除失败时打，WorkerSlot.ts:478 的 Worker exited 只用于 crash 分支组装错误消息 | ⚠️ 疑似缺陷（可观测性）：「删对了」与「没删」在日志上无法区分 | dev-B/dev-01-run2-observations.txt | 小修，与 D4 / D8 / D10 同族（都是 electron-log 吞级别 + 成功路径不记）：建议一起处理，归档收尾至少记一行「session X: worker 退出 → 释放 <dir>」 |
| D15 | DEV-4 | worker bootstrap 失败（本轮由协议版本不匹配制造，超时 60 s）之后：① 用户刚发的那条消息被吞，会话区回到 "No messages yet…"、输入框也空，只能重打；② session-index.json 留下一行 title="" 的空壳会话（session-1789644851492-gidi2oz），重启后以「Session xxxxxx」出现在侧栏 | ⚠️ 疑似缺陷（两条都与 DEV-4 判据无关，是顺手撞到的；①对用户更疼） | dev-B/dev-04-protocol-mismatch.txt、dev-B/dev-04-01-bootstrap-timeout.png | ① createSession 失败时把用户输入还回 composer，属小修；② 与 DEV-28「索引 title 为空」同源，建议并到那条一起看 |
| D16 | DEV-18 | AICLIENT_NODE24_PATH 已是没人读的环境变量（唯一读它的 resolveNode24Runtime 无生产调用方，只有 agent-host/index.ts 的 re-export 与单测），Node 24 横幅分支 isNode24ResolutionFailure 同样只被单测引用、describeHostStatus 压根没调它；但 dev.env 还留着这一行并配着解释注释，HostStatusBanner.tsx:8-12 的文档注释也还在承诺这个分支 | ⚠️ 死代码 + 文档漂移，不影响功能，但会让后来的人按注释去验一个不存在的场景 | dev-B/dev-18-node24-banner.txt | 二选一：要么删掉 NodeRuntimeResolver / isNode24ResolutionFailure / dev.env 那一行与两处注释，要么把 Node24 解析重新接回去（若打包态 TUI 仍需要外部 Node，需先查清 PiTuiPty 走的是哪条）。同时检查单第 18 行要改（见 B.md「判据问题」一） |
| D17 | DEV-14（terminal-03） | TUI 模式下从聊天 A 切回一个此前被挂起的聊天 B，终端整屏空白（xterm 35 行全空），而 PTY 仍活着：piTui.status() 仍列着它、/proc 里进程在、敲键盘经 piTui.onData 有回显。成因是两头各自都对、接缝没人负责重绘：PiTuiPty.ts:307-308 的 replayBuffer 只累积「挂起期间」的输出，而挂起的 pi 空闲不产出，于是 :265-268 在 resume 时什么都不发；渲染层这边 terminalId 变化让 xterm 整个重建（useXterm.ts:861-897 清理 + 重新 initTerminal），新实例回滚缓冲区是空的。用 piTui.resize(id,70,20) 逼 pi 重绘一次即整屏恢复 | ⛔ 真机确认复现（A→B 与 B→A 两个方向各一次）。注意 terminal-03 判据本身是过的（画面确实换成了当前聊天、敲的字也确实落进当前聊天的 JSONL），这是判据之外顺手撞到的 | dev-D/dev-14-02-back-on-chatB.png、dev-D/dev-14-04-repro-blank-on-chatA.png、dev-D/dev-14-03-chatB-after-forced-repaint.png、dev-D/dev-14-tui-02-blank-on-chatB.txt；/tmp/t032/sections/D.md | 小修，归属待定：resume 时若 replayBuffer 为空就主动触发一次重绘（最直接的办法就是 resume 路径上补一次 resize 抖动，实测有效），或让渲染层在重建 xterm 后向 PTY 要一次全屏刷新 |
| D18 | DEV-16（两个窗口对同一会话开终端） | 两个窗口能同时对同一份会话 JSONL 起 pi --session，界面无任何提示、无拦截、无降级；两个 pi 各自在 open 时读一次文件，各自把自己那轮挂在同一个 parentId 上，把会话树静默分叉成两支（实测 12 号与 14 号条目 parentId 同为 7c1df411）。writer.lock 全程由 GUI worker 持有（pid 408197，utility 进程存活），对 pi 不设防——piTui.ts:121 注释「the pi CLI never takes the worker's writer lock」就是字面意思。根因：sessionGuard 是模块级单例但 transferTo 只转移不处置（piTuiSession.ts:79-85），而真正持有 PTY 的 PiTuiPtyController 是按 windowId 分的（piTui.ts:43），两个窗口互相看不见对方的 #live | ⛔ 真机确认复现。判据问的「是否能出现两个 pi --session 同一文件的进程」——能，两份逐字 argv 都抓到了 | dev-D/dev-16-argv-window1.txt、dev-D/dev-16-argv-window2.txt、dev-D/dev-16-shared-session-chain.txt、dev-D/dev-16-proc-two-windows.txt、dev-D/dev-16-writer-lock-during.json；/tmp/t032/sections/D.md | 需要产品决策再定修法：是「第二个窗口开同一会话时拒绝并提示」还是「转移所有权、把第一个窗口的终端 dispose 掉（transferTo 注释里描述的那个行为，跨窗口时并没有发生）」还是「允许但在 UI 上标出同一会话已在别处打开」。建议列入总结让用户定 |
| D19 | DEV-32（H/21 内嵌 TUI 模型缺失覆盖层） | H/21 P0 那套中文「本应用没有这个模型」覆盖层在当前会话路径上已经触发不到，用户看到的是裸英文 `Error: no model "<provider>/<id>" in the catalog (N available)`。isModelMissingError()（modelMissingError.ts:34-37）只认 WORKER_MODEL_NOT_FOUND 码或子串 `Pi model not found`；全仓 grep -a 确认：前者只在 src/runtime/worker/nativeUtility.ts:167,186 抛出（那是工具/目录查询的 utility 路径），后者在 src/ 里已经没有任何生产抛出点（只剩注释、匹配常量本身与单测）。会话真正跑起来时报错的是 src/runtime/plugins/model-adapter/index.ts:77，两个信号都不带。对照：historyError.ts 那套还活着（同一批点验里「会话历史已损坏」中文卡片正常出） | ⛔ 真机确认复现，**DEV-32 的取证方式「GUI 侧截覆盖层文案」截不到**（判据句只写了 TUI 侧，TUI 侧是过的） | dev-D/dev-32-03-gui-send-model-missing.png、dev-D/dev-32-04-tui-model-missing.png、dev-D/dev-32-tui-screen-model-missing.txt；/tmp/t032/sections/D.md | 小修：把 model-adapter 那条 `no model ... in the catalog` 也纳入 isModelMissingError 的识别（或让会话路径统一改抛 WORKER_MODEL_NOT_FOUND），并补一条用例把「渲染层能认出运行时实际抛的那条错」钉住。与 D3 同源不同症：D3 是两侧目录口径不一致，D19 是提示卡片的匹配条件和实际错误对不上 |
| D20 | DEV-25（F7c 问答卡） | 问答卡整张卡的固定文案没有走 i18n：`QUESTION_TITLE = 'Questions'`、`SKIPPED_TITLE = 'Questions skipped'`、`OTHER_LABEL = 'Other…'`、`SKIP_LABEL = 'Skip'`、`CONTINUE_LABEL = 'Continue'`（`questionCardModel.ts:31,33,126,206,207`）都是裸常量，`QuestionCard.tsx:413` 直接 `title={QUESTION_TITLE}`、`:509/:518` 直接渲染两个 LABEL，`:519` 还硬写了一个 `Ctrl + Enter`；同一个文件里的权限卡变体每一句都过了 `t()`（`:650`、`:658`、`:671`、`:682`、`:687`、`:695`、`:730`）。结果是中文界面里，权限卡全中文、紧挨着的问答卡整片英文 | ⛔ 真机确认（`--plan ask-question` 触发的真卡截图） | dev-E1/dev-25-03-question-card.png、dev-E1/dev-25-question-card-computed.json；/tmp/t032/sections/E1.md | 小修：把五个常量改成字典 key 并在渲染处过 `t()`，`Ctrl + Enter` 同理。与批次 D 记的「`Pi TUI closed` 等英文条」同族，建议合成一个 i18n 补漏包 |
| D21 | DEV-24（F4 GUI 观感） | 时间线上的重试横幅标题是英文模板字符串，`retryBanner.ts:107-108` 直接拼 `` `Upstream error ${errorStatus} — retrying${counts}, the turn is still running` ``（另一分支 `Network retry…`），`MessageTimeline.tsx:1838` 的展开摘要写死 `Details`，两处都没过 `t()`；而正下方 composer 的同一件事是中文「正在重试 · 1/3 · 3 秒后重试 · 0s」。一次重试用户同时看到中英两句在讲同一件事 | ⛔ 真机确认复现（两轮 503 各截到） | dev-E1/dev-24-02-retry-t02-attempt1.png、dev-E1/dev-24-retry-log.json；/tmp/t032/sections/E1.md | 小修，与 D20 同族同包：`deriveRetryBanner` 返回字典 key + 参数，渲染处再 `t()` |
| D22 | DEV-25（F7c 问答卡） | `QuestionItem.header`（`src/shared/types/runtimeEvents.ts:536-537`，注释自称「Short chip/tag label (~12 chars per SDK contract)」）声明了但**全链路无人渲染**：runtime 的 ask 工具照收照传（`src/runtime/plugins/tools/ask.ts:80-88` schema 有 `header`，`:130` 原样带进 `RuntimeQuestionItem`），渲染层 `QuestionCard.tsx` / `PendingQuestionDock.tsx` 全文件 grep `.header` 零命中。实测发一个带 `header:"点验顺序"` 的 ask 调用，卡上完全找不到这四个字 | ⚠️ 真机确认（模型给了 header，界面不显示，也不报错） | dev-E1/dev-25-03-question-card.png（卡上只有问题与选项）；/tmp/t032/fake-gateway.mjs 的 `ask-question` plan | 二选一：要么在问答卡上渲染这个 chip（上游 SDK 就是按 chip 设计的），要么把字段从 schema 与类型里删掉——留着不渲染，模型每次都要为它花 token 且用户永远看不到 |
| D23 | DEV-25（F7a/F7c 视觉口径） | 权限卡与问答卡的标题层级全部靠 `font-medium`(500)：权限卡的段头「权限」与卡标题「write — 写入工作区文件」实测 14px/500，问答卡的 `Questions` 段头、问题正文、选项标题同样 14px/500，选项**描述**还从按钮基类继承了 13px/500。`docs/design-system.md` §Font Weight 把「卡片 / 对话框 / 段落标题」判给 `font-semibold`(600)、把「描述 / 占位文本」判给 `font-normal`(400)，并明写硬约束「500 不得作为任何层级区分的唯一载体」，理由正是 **Win10 的 Segoe UI 静态族没有 500，CSS 字重匹配会降到 400**（CSS Fonts 4 §5.2）。本机 Linux 看不出来，这批点验的下一站 Windows 加密机上两张卡的标题会和正文逐像素相同 | ⚠️ 档位不符（几何、字号、间距、动画、阴影全部在档，偏差只有字重这一维） | dev-E1/dev-25-token-audit.md、dev-E1/dev-25-permission-card-computed.json、dev-E1/dev-25-question-card-computed.json | 小修：两张卡的标题改 `font-semibold`，选项描述补 `font-normal`。建议同时在 Windows 机上复拍一次对照图，把「500 在 Win10 上不存在」这条钉成可回归的证据 |
| D24 | DEV-33（真机 GUI） | 会话撞上 `session_size_limit` 之后，这条对话在本次运行里**变成一条静默的死会话**：清掉附件、打一句纯文本、点「发送消息」，输入框被清空（说明 `runSend` 真的跑了），但时间线不出现任何消息、不出现任何错误、假网关收不到请求、会话文件一个字节都不长；连试 3 次全一样（其中一次还先手工调过 `chat.createSession` 成功拿到 requestId，仍然无效）。退出应用重开后同一条会话立刻恢复正常（纯文本发出、网关计数 +1、文件 +933 B）。worker 侧其实每一次都真的抛了 `session_size_limit`（退出时的 stderr 回放里逐条可见，`store.ts:358`），只是渲染层第一次之后再也不显示。这是 D1（总预算检查在写队列内部、一次 reject 让 tail 永久 reject）在真机 GUI 上的样子，比 D1 记的更疼一层：**用户连那条 `session_size_limit` 都看不到第二次，只看到「点了没反应」** | ⛔ 真机确认复现（product path，3 次） | dev-E2/dev-33-06-plaintext-after-refusal-timeline.txt、dev-E2/dev-33-08-plaintext-attempt2-timeline.txt、dev-E2/dev-33-07-store-after-refusal.json、dev-E2/dev-33-09-diag-create-after-refusal.json、dev-E2/dev-33-34-mainlog-grep.txt、dev-E2/dev-33-bytes-table.md；/tmp/t032/sections/E2.md | 与 D1 同一个根（`store.ts:357` 的队列内 reject），但修 D1 的队列外预检只解决「后续写能不能成功」，不解决「失败的那次连话都不说」。建议一并补：send 链路上任何 worker 侧拒绝都必须在时间线留一条 error 消息 |
| D25 | DEV-33（真机 GUI） | 附件的**文件名在会话重开后丢失**：发送时时间线上的附件片是 `big-5mib.png` / `small-1mib.png`，退出重开同一会话后同样位置变成 `image/png` / `image/png`。成因是会话 JSONL 里的 image block 只有 `{type, data, mimeType}` 三个键（实测逐行核对），`MessageAttachmentMeta.name` 只活在实时 runtime 事件里、从不落盘，历史回放便只剩 media type | ⚠️ 真机确认复现 | dev-E2/dev-33-11-reopened-timeline.txt（重开后）对照 dev-E2/dev-33-05-refusal-timeline.txt（发送时）、dev-E2/dev-33-34-session-rows.md | 小修：把附件 meta 随消息落盘（或在 image block 上带 name）。注意决策 015 的附件旁路落地后附件字节要移出 JSONL，届时 name 该放哪要一起定，建议挂在 T058 上一起想 |
| D26 | DEV-33 / DEV-34（真机 GUI） | Composer 的**附件文案整片没走 i18n**：`attachmentLimits.ts` 里六句用户可见的话全是裸英文模板 —— `largeAttachmentHint`（`:163` `Attachments total 6.0 MB — sending may take longer.`，本批中文界面上实拍到）、`admitAttachment` 的 empty/too-many/too-large/total-exceeded 四句（`:76,:81,:91,:100`）、`planImageAttachment` 的 unsupported-type / oversized-pixels 两句（`:132,:142`）。同一条 composer 上，「执行 · 每次询问」「发送消息」都是中文，附件提示是英文。另外 worker 侧拒绝也是裸英文原文直出：`Error: session exceeds the configured size budget`、`Error: attachment "huge-7mib.png" is 7340034 bytes; the limit is 5242880 bytes per attachment` | ⚠️ 真机确认（两句 runtime 错误 + 一句 hint 都实拍到） | dev-E2/dev-33-05-refusal-error-dom.json、dev-E2/dev-34-03-timeline-oversize-single.txt、dev-E2/dev-33-01-fifth-send-refused.png | 小修，与 D20 / D21 同族同包：附件文案改字典 key，runtime 错误码（`session_size_limit` / `attachment_size_limit`）在渲染层做一次中文映射并补一句下一步 |

（D7～D11 由批次 C 代理直接追加，见 C.md；D12～D16 由批次 B 代理追加，见 B.md「疑似缺陷清单」，表内字母 J/K/L/M/N 对应这里的 D12/D13/D14/D15/D16）
（D17～D19 由批次 D 代理追加，见 /tmp/t032/sections/D.md「疑似缺陷清单」。批次 D 另记三条更轻的、不单独编号：① pi TUI 的 spawn / 退出 / 退出码在 main.log、按天日志、dev.js 输出三处全无日志，与 D4/D8/D10/D14 同族；② `Pi TUI closed / Returned to the GUI session.` 与会话损坏时的错误条是英文，中文界面里没走 i18n；③ 关闭第二个窗口弹的是「确认退出／确定要退出应用吗？」，关一个窗口问的却是退不退整个应用。）

（D20～D23 由批次 E1 代理追加，见 /tmp/t032/sections/E1.md「疑似缺陷清单」。批次 E1 另记两条更轻的、不单独编号：① `provider_retry` 在 main.log / 按天日志 / dev.js 标准输出三处 grep 计数均为 0，重试只在需要手动 export `AICLIENT_RUNTIME_TRACE_DIR` 的 trace 里可见，与 D4/D8/D10/D14 同族；② 重试预算耗尽后用户看到的只有一个裸 mono 错误块（网关原文完整、可读、不截断），没有标题、没有下一步、没有重试按钮，与第 17 项那三张有标题有引导的历史错误卡不是一个规格。）

（D24～D26 由批次 E2 代理追加，见 /tmp/t032/sections/E2.md「疑似缺陷清单」。批次 E2 另记三条更轻的、不单独编号：① 拒绝发生时三处日志当场 grep 计数全 0，`main.log` 更是全程 0 行；那几条 `session_size_limit` / `attachment_size_limit` 只在**杀掉应用时**由 Main 的 `crashed: Worker exited … last N stderr line(s)` 把 worker 的 stderr 回放出来，而且被 N 截断（第 1 次运行 4 次失败只留下 3 次）——应用还在跑时运维手段一条都看不到，与 D4/D8/D10/D14 同族；② 移除一个文件面板正开着的仓库时，`file:list` 把原工作区的**绝对路径**当相对路径拼到 ai-client 仓库根上（`scandir '/home/ai/code/ai-client//tmp/t032/ws-paths'`，注意双斜杠），逐级三条 ENOENT，界面无可见症状；③ 「一条消息最多 5 个附件」这条上限只有渲染层有，Main 与 runtime 两侧都没有，绕开 Composer 发 6 张图会一路通到模型并写进会话文件 —— 实际风险被每条 8 MiB 的落盘上限兜住，记录备查。）

---

## 处置（2026-09-17 回写）

批次 D4 九项修补（T060～T068）已落地并经两轮只读审阅与两轮真机复验，**未提交**。
逐项依据见[批次 D4 证据 README](../batch-d4-fixes-2026-09-17/README.md)（含每任务一节、审阅结论与复验结论）。

| # | 处置 |
|---|---|
| D1 | 已修（T061，复验 ✅）：总预算改队列外预检 + `enqueue` 拒绝不再毒化队列 |
| D2 | 归 T058（批次 F）：[决策 015](../../decisions/015-attachment-sidecar-for-session-budget.md) 附件旁路落地后这个接缝自然消失；T061 用例 2 继续把它钉在原处 |
| D3 | 已修一半（T062，复验 ✅）：选择器改吃 worker 拿的同一份目录文档；「过滤规则两侧不一致」的另一半排在 T050 之后 |
| D4 | 已修（T066，复验 ✅）：根因是本仓把 transport 钉成 `error` 而非 electron-log 吞级别；索引修复行现落按天日志 |
| D5 | 转 [Q016](../../open-questions.md)，待拍板（静默消失 / 删除前提示 / 转 unbound 三选一） |
| D6 | 已修（T060，复验 ✅）：`rebindSessionsToTree` 补未绑定分支。同类的 `!preferred` 早退未修，见证据 README「审阅未修」第 10 条 |
| D7 | 归 T052（批次 F，import-up-01） |
| D8 | 归 T052（与 D7 同源，修时一并给诊断出口） |
| D9 | 已修（T067，复验 ✅）：Main 发 code + 上限值，渲染层出中文并把 `67108864` 说成 `64 MiB` |
| D10 | 已修（T066，复验 ✅）：导入批次起止各一行 + 每条失败一行 |
| D11 | 只记录（320 份量级可接受，份数再上一个量级需复核） |
| D12 | 已修（T063，复验 ✅）：编辑写回来源文件、删除按名字清两个根；方案见[决策 017](../../decisions/017-subagent-definitions-edit-at-source-root.md) |
| D13 | 已修（T060，复验 ✅）：Main 侧统一解包入口；衍生 [Q018](../../open-questions.md)（临时工作区认领判据） |
| D14 | 已修（T066，复验 ✅）：归档 / worker 退出 / scratch 释放三步各一行，且「删了」与「保留」分开记 |
| D15 | 已修（T064，复验 ✅ 正反两条）：一审判 blocking（输入框非空时载荷会彻底丢），回炉加回落后关闭；索引空壳行失败即删 |
| D16 | 已修（T068，复验 ✅）：Node 24 解析器与横幅分支整体删除，注释与 `dev.env` 同步；`checklist-e.md` 的 DEV-18 行退役 |
| D17 | 已修（T065，复验 ⚠️ 约 4% 偶发）：首版抖动真机未生效，回炉改为「留在预置尺寸 + 由前端 attach 后补真实尺寸」；27 次切换里 26 次整屏重绘、1 次仍空白 |
| D18 | 已修（T065，复验 ✅）：按会话文件键记归属窗口并拦跨窗口；一审判 blocking（回滚过度释放）回炉后关闭 |
| D19 | 已修（T062，复验 ✅）：错误码沿既有回程通道上路 + 新建中文覆盖层卡；round-1 半生效，回炉后 round-2 完全成立 |
| D20 | 已修（T067，复验 ✅）：问答卡固定文案全部走词典 |
| D21 | 已修（T067，复验 ✅）：重试横幅拆四个键 + `Details` 走词典 |
| D22 | 已修（T068，复验 ✅）：`header` 渲染成 chip，位置在问题正文正上方 |
| D23 | 已修（T068，复验 ✅ Linux 口径）：两张卡标题改 `font-semibold`、选项描述补 `font-normal`；Win10 对拍留 T033 |
| D24 | 已修（T061，复验 ✅）：D1 修好后第二句纯文本不再失败，静默死会话随之消失；被拒那次的红块仍在 |
| D25 | 已修（T061，复验 ✅）：image block 上挂 `aiclientName` 并在回放侧读回；只对修复后新写入的消息有效 |
| D26 | 已修（T067，复验 ✅）：附件六句连同同一条提示里的邻居句一起走词典；「运行时错误要不要升级成卡片」属 [Q017](../../open-questions.md) |

**各批「不单独编号的轻项」**：pi TUI spawn / 退出码、`provider_retry`、预算拒绝三处零日志 → 已修（T066，复验 ✅）；
`Pi TUI closed` 等英文条与「关第二个窗口弹整应用退出」→ 已修（T065，复验 ✅）；
重试预算耗尽只有裸 mono 错误块 → 转 [Q017](../../open-questions.md)；
`file:list` 绝对路径当相对路径拼接产生双斜杠 ENOENT、「一条消息最多 5 个附件」两侧无校验 → 只记录。
