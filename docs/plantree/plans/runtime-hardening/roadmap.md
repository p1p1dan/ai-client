# Roadmap — Runtime 加固与收口

Role: roadmap。本文件是任务身份、状态、顺序的唯一权威。建立：2026-09-14。发现编号（如 `permissions-01`）指向[审计证据](../runtime-evolution/evidence/runtime-audit-2026-09-14/README.md)；批次 D 编号（如 `main-aux-01`、`d-cross-01`）指向[批次 D 补审证据](evidence/batch-d-audit-2026-09-15/README.md)。

状态：⬜ 未开始 · 🔧 进行中 · ✅ 已落地（挂提交与证据）· ⏸ 推迟（写明原因）。

## 顺序与依赖

批次 A（安全与数据完整性）先于 B（功能正确性）先于 C（清扫与文档）；D（补审）与 A/B 并行，只读不冲突；D2（补审修补，上机前）在 E 之前；D3（收口后追加修补）与 D4（开发机点验修补，源自 T032）同样在 E 之前，D4 排在 T032 之后、T033 之前；E（现场）最后一次上机，合并 A～D 的现场项；F（补审修补，上机后）在 E 之后。批次内按编号顺序，编号即建议顺序。

## Done

- ✅ **T001** bash 静态分析补全 — `c4e2b2e4`（2026-09-14）。permissions-01/02/03/04/05/06/19 已修；shellPolicy.test 26→60 条，每类反向验证记录在提交说明。PERM-1 探针需真机与真实模型，转入 T032 检查单。
- ✅ **T009** 打包门禁 native-only — `89c73e5b`（2026-09-14）。cutover-01 已修；CI 三平台绿灯待推送后的打包作业确认。cutover-16 拆解结论记入 T025，门禁新增断言建议记入 T028。
- ✅ **T003** H/20 交叉写入 — `0332214c`（2026-09-14）。session-01/02/03/13、cutover-04 已修；Q005 结案（可达，已修「CLI 打开未再写」形态），中段坏行记 Q008。真机一圈归 T032。
- ✅ **T004 + T005** 子代理错误路径与内部报告投影 — `4e80f9ff`（2026-09-14）。subagent-core-01/02/03/04/05、rpc-projector-01/04、loop-model-06 已修；新增 golden 录制 `nativeGuiSubagentEventStream.json`。权限行的委派归属展示留 UI 批次。
- ✅ **T002** 权限 surface 映射 — `10581139`（2026-09-14）。permissions-09、skills-mcp-11/12、cross-05 已修。取舍：`trustedPath` 让已信任技能目录默认 allow，任何层级写的 `skill: ask` 都不会弹卡，只有显式 deny 能拦（贴合 K1「不弹卡」与 headless 约束）；随包 `skill: {'*': 'ask'}` 保留作可覆盖占位。skill 顺带进 plan 模式白名单。
- ✅ **T006** 子代理继承父回合 — `d71eb2ec`（2026-09-14）。cross-01/02 已修；`SubagentConfig.thinkingLevel` 保留为未经 bindRun 调用方的兜底。
- ✅ **T007** 模型绑定与目录诊断 — `780dd9c1`（2026-09-14）。loop-model-01/02/04/07/08、cross-06 已修。取舍：`no_api_key` 只对需要 key 的 7 种协议且无 Authorization 类头时生效，bedrock / vertex / pi-messages 不受影响。**待盯**：凭据库未解锁时 Main 下发空 key，该 provider 现在会从目录消失（之前是 43 秒后报错），只剩 catalog_empty 消息里的 `id:no_api_key` 可诊断——归 T021 钥匙串 locked 场景与 T032 上机检查单。auth.json 损坏诊断只覆盖读盘路径。
- ✅ **T010** 搜索遍历容错 — `1262e3b0`（2026-09-14）。tools-01/07 已修；错误码集合含 EPERM/EISDIR/ELOOP，与 skills / mcp / prompt 三处一致。
- ✅ **T011** trace 脱敏与体积 — `b714a925`（2026-09-14）。core-host-03、permissions-12 已修；tools-11 复核后不做（驻留点在渲染层，卡片设计即显示完整内容）。
- ✅ **T008** 导入清单与路径 — `23ac5df5`（2026-09-14）。import-catalog-01/07/10/11 已修；「四个 RPC」实际只有三个带 targetPiSessionId，discard 传完整路径。import-catalog-09 残项 `36389d1d`：旁车清理、空暂存目录删除、单条失败不阻断；`RuntimeHostIoService` 新增 `rmdir`（host.test 未单独覆盖，归 T022）。
- ✅ **T027（两件机械活）** signoff SA09/SA12/SA15/SA19 实况标注、P5-2-5 标注 — `95e63960`（2026-09-14）。T027 其余文档回写仍在批次 C。
- ✅ **T012** 工具输出形状 — `439ab922`（2026-09-14）。tools-02/03/04/05/06/08/09/12/13/14/17 已修；tools.test 41→53 条，13 次反向验证。取舍：read 可见预算固定减 128 字节留给续读尾巴；grep 超时给截断说明而非抛错；JS 正则单行灾难性回溯仍不可中断（需独立线程，记录不做）。
- ✅ **T015** 会话 store 与写锁 — `ce2a7af8`（2026-09-14）。session-04/05/06/09/10/12 已修（04/05 原判待定，用确定性用例证实成立）；新增 9 条用例。取舍：写失败经 `writeFailure` 取值器观测而不进 contracts；抢占归属用原始字节比对而非 token；输掉竞争把误取的锁写回；无效 label 行丢弃而非报错。
- ✅ **T018** MCP — `942ee464`（2026-09-14）。skills-mcp-01/02/03/04/05/06/07/14/15/16/21/24 已修；mcp.test 20→35 条，19 次反向验证。取舍：04 用字节缓冲而非 StringDecoder（一处同修 04 与 14）；注册失败记 `connection.toolErrors` 而非 `error`，保住 mcp_failed 计数与 close() 语义；工具名点号替换而非拒绝，config 服务器名校验不收窄；图片最多 8 张且文本块永不为空。resources / prompts / sampling / roots / HTTP-SSE 仍在 P5-3 既有豁免内。
- ✅ **T019** skills 与模板 — `39afacc8`（2026-09-14）。skills-mcp-08/09/10/17/18/19/20/22/25 与决策 004 已修；skills.test 31→48 条。取舍：悬空 symlink 留诊断比 pi 上游严格；块标量报诊断不解析（模块不引 YAML）；`refresh()` 已实现但 Main 侧无调用方（触发端归 T020 / UI）；`$1` 先展开后参与后续匹配、未闭合引号跨行两条姊妹问题未列入，未动。
- ✅ **T014** 压缩与提示词 — `2c0eb30c`（2026-09-14）。context-prompt-01/02/03/04/05/06/08/10/11/12/13/14/15/16/17 已处理（05 / 12 复核后成立）；`run.targetPath` 与 root→leaf 走法已删（决策 007），提示块措辞改为「冲突以更具体文件自述为准」；新增 21 条用例、删 3 条，15 次反向验证。取舍：hard limit 下先丢保留尾重装 checkpoint、仍不够报 `context_too_large`（不是纯 skipped）；tool-guidance 不随 mode 变（保静态前缀）；`staticPrefixBytes` 改描述不改口径；子代理只做预算守卫 + 硬上限 truncated，不做压缩（理由在 run.ts 模块注释）。`subagent/migrate.ts` 与 `shared/types/nativeSession.ts` 的同名 `targetPath` 是迁移预览目标路径，未删，后者归 T025。
- ✅ **T016** worker 生命周期与超时 — `a9ea7230`（2026-09-14）。worker-runtime-01/02/05/06/07/09/12、rpc-projector-05/06/08/10、import-catalog-04、context-prompt-07 已修；新增 18 条用例，10 次反向验证。取舍：compact 预算用 `WORKER_COMPACT_BUDGET_MS`（worker 45 秒）< `WORKER_COMPACT_REQUEST_TIMEOUT_MS`（Main 60 秒）两个共享常量而非新增 RPC 字段；`handle.dispose()` 失败吞掉并记日志（上抛会顶掉 worker 退出路径）；reload 拆除失败只加特征化测试（worker-runtime-03 已被驳回）。`src/agent-host/worker.ts` 转交 modelCatalog 那一行无直接用例（只有 30 秒级真进程测试覆盖），由 RPC 层用例 + typecheck 保证。
- ✅ **T017** 事件投影与 golden 录制 — `84ac35e0`（代码）+ `83a9d0f8`（用例、录制、生产者）（2026-09-14）。**批次 D 复核（chat-event-04）**：tool.updated 在 native 下没有任何生产者，「带 input」这半边实际未生效，归 T053。rpc-projector-02/03/07/13/17/18、permissions-08 已修。golden 录制扩到五类：主录制与子代理录制重录，新增问答（含 thinking）、压缩、重试三份 fixture；主录制场景未动，其 diff 可全部归因于修复（幻影空消息消失、工具行改挂发起它的消息、每条 assistant 带 model）。`session.stderr` 选择**接生产者**而非删类型：类型、脱敏模块与三个渲染消费者自 T-35 起俱在，缺的只是 emit，原生产者随 Claude CLI 后端退役消失；WorkerManager 逐行脱敏后经既有 dispatch 发出，每回合上限 50 行并发封顶提示。rpc-projector-13 复核：推断成立且更重（游标停在废弃文本上，使后续 delta 与 message_end 最终 flush 全哑），可达性来自 pi-ai Responses 适配器在 output_item.done 时整块覆写，未用真 provider 实跑。取舍：重试录制用真 3 秒 sleep（agent-loop 无 sleep 注入点）；「timed_out 不算用户决定」的断言放渲染层（`USER_RESOLUTIONS` 是渲染层的表，runtime 不能 import 它）。
- ✅ **T022** host exec 与 bootstrap — `84ac35e0` + `117d97a9`（2026-09-14）。core-host-04/06/07/08/09/10/11/14/20 与 T008 遗留的 rmdir 单测已修；host.test 新增 16 条、bootstrap.test 1 条、新建 eventsPlugin.test 2 条；反向验证为整文件撤修（对 HEAD 源码跑新用例 16 条判红），非逐条。取舍：dispose 多个失败用 AggregateError 聚合、单个原样抛；Windows 半边（taskkill 重入、跟踪、失败上报）靠注入平台与 spawner 在 Linux 上测；core-host-05（stderr 预算）归 T013。**事故**：代理写的 POSIX 组 kill 用例用假 pid 1 走真 `process.kill(-1)`，三次把桌面会话整个杀掉并被误判为内存不足；已改为可注入 `killGroup` + 守卫用例，另一条宽限用例改为等 SIGTERM handler 就绪。教训记入 docs/agent-project-engineering.md 附录 B（`e1e188d1`）。
- ✅ **T034** 会话文件中段坏行自愈 — `1f1f40d0`（2026-09-14）。session-02 中段形态、决策 006 已落地：中段 JSON.parse 失败的行跳过并在持写锁时原子重写（与 `pi --session` 的 `parseSessionEntryLine` 读法一致），跳过行记 trace（`session_recovered` note）与 `session.status` 的 `recovery` rider，渲染层 reducer 粘住不清；新增 15 条用例（含真实 pi SessionManager 两读者一致、「撕裂尾片 + CLI 续写」组合）、12 处反向验证。取舍：范围只到 JSON 不可解析（能解析但结构无效的行 CLI 会保留，仍拒绝）；超过 64 条坏行拒绝打开且不动文件；孤儿 parentId 仍拒绝但错误信息点名被跳行（pi 容忍孤儿，对齐要改写可读行或静默截断分支）；提示按 run 发而非开会话时（bootstrap 期 Main 槽位在 creating 会丢事件），「打开即提示」需 WorkerBootstrapResult 带 recovery，未动 src/main；无 UI 组件。wire 守卫 [W-1] 更新为五键并补 [W-1b]。
- ✅ **T013** TSD 读取效率 — `19f9e888`（2026-09-14）。tools-10、core-host-05 已修：readLines 在 node-fallback 下扫描块几何倍增（首块 32 KiB，下限对齐输出预算，封顶 2 MiB），总重读量 O(n²)→约线性、进程创建上百次→个位数；`RuntimeExecRequest` 新增可选 `maxStderrBytes`，HostIo 的 TSD 回落改为 `maxBytes + 1` 加 4 KiB 独立 stderr 预算；p1-0 契约第 4 节同步。host.test 36→38、tools.test 53→54，3 次反向验证。取舍：块大小几何倍增而非固定放大（固定倍数仍是 O(n²)）；不做可复用句柄（helper 是一次性子进程，句柄跨不了进程边界）；stderr 预算放 exec 而非 HostIo 自收；明文路径保持 32 KiB；helper 顺序读语义未动。真实加密机链路仍未跑。
- ✅ **T021** 模型目录 Main 侧 — `d3949e84`（2026-09-15）。import-catalog-02/03/06/12 已修：托管模式凭据不可读或用户组读不出 → `resolveNativeModelCatalog` 返回 undefined 走读盘回落（locked 场景断言的是回落，不是 dropped，Blocked By 那条随之关闭）；`managedHalf()` 统一写盘与内存交付的托管半边选取（有缓存用缓存；无缓存托管用随包快照、本地用空目录）；model 级 baseUrl 经 validateModel / toPiModelsJson 可达；决策抽成零 electron 依赖的 `resolveNativeModelCatalogWith(deps)` 并补 6 条单测。PiModelConfigService.test 42→48，新建两个测试文件各 6 条，7 组反向验证。取舍：加姊妹函数 `readUserProviderGroupForRuntime` 而非改原函数返回形状（写盘侧「失败即空」是对的）；本地模式只挡随包基线不挡 wire 缓存；本地模式凭据 null 不触发回落；用户自建服务的模型无处放 baseUrl，未做。`src/shared/piModelConfig.ts` 加一个可选字段。
- ✅ **T035** 指令分级加载 + settingSources + local 层 — `5b305fdb`（2026-09-15）。决策 007 / 008 落地：父目录链止于文件系统根之下（`/` 不读、不在 `~` 截断）；预算按渲染顺序装、耗尽即止（不给 cwd 预留，远处大文件可挤掉近处，有意接受）；按需加载经 InstructionTracker + pi steering 队列以 `'project-instructions'` 内部标记消息注入，每份一次；globals 条目加可选 `scope`（缺省 user，托管 AGENTS.md 标 managed 永远装）；symlink 守卫改「所在目录内」；local 策略文件 scope id 沿用 `'project'`；skills 无 local 根；prompt root 改 realpath（修掉 /var→/private/var 会让整个工作区判成外部的坑）。净增 61 条用例、21 次反向验证。**两处未按字面**：① 指令文件此前无信任门控，现补上后 managed 路线（一直发 projectTrusted=false）不再装仓库 CLAUDE.md / AGENTS.md → Q009；② 「随包策略优先级最高」与 D-Q9 决策 4「用户永远赢」冲突，未改，随包仍是最底层 base scope → Q010。未做：Main → Worker 的 settingSources 透传（三处形状见批次 B 落地记录转移表）；rules 目录。
- ✅ **T024** 容量对账 — `8564ba41`（2026-09-15）。对账表 [evidence/capacity-reconciliation-2026-09-15.md](evidence/capacity-reconciliation-2026-09-15.md)：必须先限的是 MCP 工具响应（唯一体积由第三方决定的落盘来源，改前一次约 8 MiB、4 次顶满且顶满后打不开），已落图片 1 MiB/张、2 MiB/次，文本按字节计；其次子代理转录（T020 同波落）。runs.jsonl 按字节轮转（8 MiB × 3 代，重命名不截断，大小从盘上 stat，轮转失败经 flush 上报 `trace_rotate_failed`）；TracePlugin._runs 封顶 100 条 / 4 MiB（permissions-12 后半）。tools-11 复核不成立（write 审批 preview 不落盘，只到审批卡 + trace 截 4000）。新建 trace.test 11、mcp.test 35→38，4 组反向验证。待落地（禁区文件，记在对账表）：用户附件无体积上限、`tool_execution_start` 把工具参数全文写进 trace、compaction 摘要正文无上限、会话文件缺单行最大字节兜底；多 worker 并发轮转归 T031。
- ✅ **T020** 子代理数据与展示 — `60b250f3`（2026-09-15）。subagent-data-01～17、subagent-core-06/12/13 全部处理，决策 005 落地：花费经 `usage.updated.delegated` 到 Main 与渲染层并入会话总量，Run 面板显示委派占比，重开会话经 `historyUsage()` 补种子；定义每个顶层 run 经 `refresh()` 重读并原地改写 Task 描述与枚举；事件上限 200 条后发一次 `capped`；转录每字符串 8 000 字符 + 会话级 2 MiB 预算，超出只写 started / settled；TaskWait details 报告 4 000、delegationIds 去重 + maxItems 50；provider 上限在 resolveModel 执行、目录诊断进日志与失败文案、目录加载提前到版本戳之前；活动 id 加序号、turn_start 计数、截断统一走代理对回退、guidance 分块 + regex；迁移器整体迁到 `src/shared/subagentMigration.ts`（根 tsconfig 排除 src/runtime）并修原型链键 / 布尔规范化 / BOM；`unquote` 还原 `''`、save() 写后往返断言；迁移入口 = Main 预览 / 写入 + 两个 IPC + 设置页「导入旧定义」面板（只扫 `<agentDir>/agents`，不覆盖同名，原文件保留）；工具动词表补 glob / browser_preview / ask / skill / new_context / Task* / mcp__*；删除孤儿 `subagentProjection.ts`（T025 清单划掉）；golden 子代理录制重录，差异全部可归因。净增 61 条用例、删 17，19/19 反向验证。未做：项目级 `.pi/agents` 不纳入导入；低资源并发按 ARD D10 另案；两个 IPC handler 薄包装无单测；未起 Electron。signoff SA15 / SA19 已改口。
- ✅ **T023** i18n — `a28b3f93`（2026-09-15）。批评者 i18n 缺口与 cutover-17 已处理：权限卡 runtime 改发 `PermissionRequestAction` 标识，渲染层 `derivePermissionPrompt` 查表再 `t()`；导入提示改英文成句 + `HistoryNotice{key, params}`（从未落盘，无需兼容）；codexItemMapper 5 处内部备注改英文，其余命中全是引用中文原文的注释、保留；守卫 ROOTS 加 runtime / agent-host、后缀加 .mjs / .mts、分根下限与后缀覆盖断言、块注释塌缩保行号，ALLOWED 零新增。主 golden 重录 diff 恰一行。9 组反向验证含守卫两根探针与缩回 ROOTS。取舍：保留 `createRuntimeApprovalBridge`（bridge 半边在生产里是活的），只把 approve 改英文键 + 下标映射；Extension UI 弹窗不加翻译（第三方任意字符串、生产不可达）。未做：legacy 后端插件自己的英文 ui.select 弹窗（镜像方向，建议单独立项）。
- ✅ **T025** 死代码与死字段清扫 — `45a7a347`（2026-09-15）。cutover-05/06/07/20、permissions-11/15、subagent-data-13、cross-07、rpc-projector-09/15 已修：随包 `@juicesharp/rpiv-ask-user-question` 与 `@gotgenes/pi-subagents` 从依赖 / 打包门禁 / lockfile 摘除（332→238 包）并加复制过滤 + 产物校验双重守卫，`@gotgenes/pi-permission-system` 因 Main 权限策略面板仍读它的 config.json 保留；五个孤儿模块（bundledFeaturePlugins / extensionInventory / commandInventory / sessionTierAuthorizer / permissionActivity）连同测试删除，permissionPlugin.ts 486→245 行；PiWorkerRuntime 12 个可选方法收紧为必填，9 处 `*_UNAVAILABLE` 死分支删除；sessionKeysMatch、leafCheckpoint（Main 侧 piLeaf 镜像保留）、`NativeIndexedRunRequest.targetPath` 一并删除。34 个相关测试文件 564 条全绿，三套 tsc 通过，9 组反向验证判红。取舍：extensionUiBridge 整链（cutover-06）只改注释保留、Sub-agents 开关拆表保留、permissions-15（approve 桥超时改可配置）保留，详见 [evidence/batch-c-2026-09-15.md](evidence/batch-c-2026-09-15.md)；extensionUi 整链退役提案转 [Q012](open-questions.md)。
- ✅ **T027**（其余文档回写） — `1b7c55fd`（2026-09-15）。cutover-08/12/18、core-host-16/17/18、subagent-core-16、审计第五节 P1-8 已修：删已退役的 `AICLIENT_RUNTIME_BACKEND` 开关说法、统一派生明文文件「不删」结论、piSessionPreflight 保留理由改写为「只剩 samePiSessionPath」、evidence/p1 补 Windows 五态现场证据日期、signoff SA09/SA12 改写为实况、P5-2-5 补「已修（T020）」、四个跨区域冲突节点裁决回指、8 处已推翻发现追加标注（未删原句，编排者已认可）、P5-2 契约补「delegate 只做预算守卫」、P1-8 行补现场证据陈旧说明（150 个提交，21 个动过 host/tools/bootstrap）。首轮被审阅代理判 blocking（遗漏 core-host-18），回炉一轮后 pass-with-notes；10 文件 189 条相对链接 0 失败。core-host-15（flags.ts 孤立 JSDoc）超出「只改 docs」范围转 T028。两件机械活的 Done 行（SA09/SA12/SA15/SA19 实况标注、P5-2-5 标注）已在 `95e63960` 记录，见上一条。
- ✅ **T028** 守卫与基线 — `a11ccbe0`（2026-09-15）。cutover-11/13、core-host-01/02、批评者 P2-5/P2-6 缺口已修：打包门禁判定摘成纯函数并补 `extensions` 缺失 / `exitCode` / `workerExecutable`-transport 匹配三类断言；口径 A 守卫改认子路径 import 并加阳性对照 fixture；workerEntryWiring 补两条真实子进程用例（legacy / 拼写错误）；`DEFERRED_SERVICES` 清空、门禁改读 runtime 源码正反双向校验；`RUNTIME_CONFIG_VERSION` 从 `runtime_p3_complete_v1` 解冻到 `runtime_p6_hardening_v1` 并写分代规则（bootstrap.ts / contracts.ts / README 三处）；runtime-baseline 脚本链新增 `--dry-run`，`compare.mjs` 改收 `--baseline`（任意后端参考）或 `--legacy`，不再强制要求 legacy 归档。另完成 5 项转移：workerStripOnlyCompat 动态 import 盲区修复（覆盖 16→99 文件）、`patch-pi-permission-system.mjs` 删第五组信封豁免补丁（其余四组因 permissionPolicyIntegration.test.ts 依赖保留）、flags.ts 孤立 JSDoc 整理、两个探针脚本死分支清理、piSessionPreflight.ts 就地收窄到 44 行（只留 samePiSessionPath）。11 个相关测试文件 58 条 + `node --test` 的 metrics.test.mjs 5 条全绿，三套 tsc 与 biome 干净，13 组反向验证判红。dry-run 输出与详情见 [evidence/batch-c-2026-09-15.md](evidence/batch-c-2026-09-15.md)。 Q006 随本任务结案（参考归档收任意后端 + config_version 分代）。
- ✅ **T026** 用户可见的退役残留 — `2cfed556`（2026-09-15）。cutover-02/03/10 已修：插件页权限归属文案改为「本应用自带权限系统审批每个对话，你装的 pi 权限扩展只影响内嵌终端」（`PermissionSystemOwner` 的 `bundled` 改为 `none`）；侧栏「插件」面板改名「能力」面板，数据从 native runtime 自己的 MCP / 技能 / 子代理服务投影（新 wire 字段 `capabilities`，「未报告」与「生产者报零」两态可区分，不再解析 `ui.setStatus` 状态行）；opt-in 空转链（Main → 环境变量 → worker.ts → piWorkerRpcServer 的 `optInExtensions`）整条删除；Sub-agents 开关改直接读 `nativeSubagentSettings`（未表态=开，与 native 默认一致）。18 个相关测试文件 323 条全绿，三套 tsc 通过，8 组反向验证判红。取舍：extensionUi 整链仍未删（转 [Q012](open-questions.md)）、`piOptInFeatures` 持久化键名保留、`resolveManagedPiPtyEnv` 改 pass-through。**验收口径**：开发机三处界面点验归批次 E（与批次 A / B 惯例一致），本任务做到的是行为在 store / 组件静态 / Main / runtime 用例中可证；点验清单见 [evidence/batch-c-2026-09-15.md](evidence/batch-c-2026-09-15.md)。
- ✅ **T036** extensionUi 整链退役（批次 C 追加，决策 012） — `ce7f3b3a`（2026-09-15）。cutover-06 已删：worker / runtime / Main / 渲染层九个专属模块与全部测试整删，runtimeEvents / workerRpc / ipc / preload / piWorkerRpcServer 的 respondExtensionUi 通道与 WorkerManager 阻塞请求簿摘除；`createRuntimeApprovalBridge` 与 `ExtensionUiRequest` 一并删，bootstrap 传了 tools 而无 `permissions.approve` 即抛 `runtime_approval_missing`（19 个 runtime 测试文件改用共享 fixture `__tests__/fixtures/approval.ts` 显式声明审批面）；LeftNav 待审批徽标改接 `chatSessions.pendingPermissions`（编排者认可的行为改动，否则恒为 0）；reload 改 `permissions.drain('aborted')` 结算残留 gate；旧事件流回放用例（nativeStreamReplay）钉住含 `extensionUi.request` 的片段不抛不渲染；新增长期守卫 `retiredSurfaceAbsence.test`（扫 src/ + scripts/ 词汇零命中，latin1 读绕 NUL）。审阅回炉一次（误删一条无关用例，已恢复）。三套 tsc 全绿，6 组反向验证判红，净减约 5100 行。未做：`verify-question-layout.mjs` 需 Electron 未实跑；冻结基线归档 manifest 里的旧文件名不改。

- ✅ **T029** Main 侧宿主与渲染层补审 — 只读，无提交（2026-09-15）。9 个区域（main-host、main-host-aux、utility-chain、session-index、import-upstream、terminal-tui、agent-host-lib、chat-tool-vocab、chat-event-vocab）、35 个代理；85 条发现，确认 80（2 high：main-aux-01 scratch 越界删根外目录、session-index-01 索引读坏后覆盖成空）。批评者缺口 1/2/3/4/5/20 covered、8 partial、19 closed-by-fix。证据：[evidence/batch-d-audit-2026-09-15/README.md](evidence/batch-d-audit-2026-09-15/README.md)；发现进本 roadmap 的 T037～T055。
- ✅ **T030** 未认领节点补审 — 只读，无提交（2026-09-15）。5 个区域（smoke-p0-6、cordis-spike-d1、field-nodes、baseline-comparability、h-nodes）、13 个代理；14 条发现，确认 11，无代码级 medium。首次判定：P1-7 incomplete（日常提交与 PR 不跑任何自动化测试）、F3 incomplete（根因未拍板且看板口径自相矛盾）、P2-0 / P5-2-0 complete，其余 complete-with-gaps。缺口 6/7 covered、9 partial（P5-2 父节点仍无人认领）、10 closed-by-fix。证据同上。
- ✅ **T031** 并发、容量与 Windows / TSD 静态面补审 — 只读，无提交（2026-09-15）。4 个区域（concurrency、windows-static、tsd-utility、capacity-leftovers）、15 个代理；30 条发现全部确认（1 high：windows-01 Git Bash 盘符写法绕过不可覆盖 deny，静态推断）。缺口 15 covered、12/13/14 partial（静态面补齐，现场证据仍为零）、16 closed-by-fix。产出批次 E 检查单草案 [evidence/batch-d-audit-2026-09-15/checklist-e.md](evidence/batch-d-audit-2026-09-15/checklist-e.md)（34 条上机必做 + 150 项全量）。**与字面不同**：「TSD helper 契约用替身固化」未在只读批次里落代码，17 条替身用例清单转为 T047 的验收项。
- ✅ **T037** scratch / 临时工作区：越界删除、归属判定与归档顺序 — `99db822c`（2026-09-15）。main-aux-01/02/03/07 已修：越界守卫改用 resolve + relative 判包含（新增 `workspaceContainment.ts` 供 scratch 与临时工作区共用，含 Windows 折大小写）；归档先等 worker 退出再删其 cwd；临时工作区归属判定两侧规范化，设置值带尾部分隔符或 `..` 段时自愈重新生效。16 条用例，4 组反向验证判红后复原；pnpm typecheck 与 biome 干净。取舍：main-aux-03 只记「本次运行已知根集合」，不做跨重启持久化——换根后重启的 unbound 会话从「带信任残留运行」变为「打不开」，方向更安全但更硬；main-aux-02「release 拒绝仍有活 worker 的会话」的硬断言未写进服务层，改为唯一调用点顺序 + 用例钉住。转移：main-aux-03 跨重启残留并入 T040 追加项 4（resume / ensure 对 unbound 会话在当前根下重新分配 scratch 目录）。证据：[evidence/batch-d2-2026-09-15.md](evidence/batch-d2-2026-09-15.md)。
- ✅ **T038** 会话索引读写安全网 — `e007bf71`（2026-09-15）。session-index-01/05/11 已修：读坏按「不存在 / 内容坏 / 读不动」三条分支处理，内容坏先备份为 `session-index.json.corrupt-<时间戳>` 再用可解析的行重建，读不动保留上一版并拒绝写、loaded 保持 false 等文件解锁后自动重读；setArchived / recordCreated / 三条 runtime 事件分支改走 mutateAndFlush，写失败回滚内存；索引行加 2000 条上限与回合合并写（10 秒窗口）；writeJsonAtomically 补 fsync。9 条用例，3 组反向验证判红后复原；pnpm typecheck 与 biome 干净。取舍：读坏按失败种类分三条分支而非二选一（Windows 杀软短暂占用文件不应被当内容损坏走备份重建）；2000 行上限与 10 秒合并窗口是无实测依据的拍值，上机后按容量对账表回调；行为变化：索引「读不动」时新建聊天当场报错，不再静默覆盖历史。转移：索引降级状态的界面提示需新 IPC（`getHealth()` 已就位）与「删除会话真正入口」两项去批次 F；另两处无 fsync 的 writeJsonAtomically（RemoteConnectionManager.ts、LegacyImportManifest.ts）留后续批次。证据：[evidence/batch-d2-2026-09-15.md](evidence/batch-d2-2026-09-15.md)。
- ✅ **T039** Windows 行为面 — `3498cec6`（2026-09-15）。windows-01/02/03/04/05/07/09 已修：权限 deny 比对前统一路径形态（Git Bash / Cygwin / 扩展长度前缀折成原生写法）；已跑完的命令不再由 taskkill 决定成败，清理失败记 `cleanupError`（同规则也放宽到 POSIX 进程组信号）；stdio 型 MCP 在 exec 层按 PATHEXT 解析 .cmd 并包 cmd.exe；无 shell 不登记 bash；edit 认 CRLF；reg query 与 bash 输出按控制台代码页解码（新增 `src/shared/windowsCodePage.ts`）。22 条用例，7 组反向验证判红后复原；三套 tsc 与 biome 干净。取舍：windows-03 一律用 `path.win32` 而非宿主 path 语义（接手代理修正，否则 Linux 上测不出 Windows 分支是否生效）；windows-02「清理不决定成败」的规则行为面扩大到 POSIX，有独立用例钉住。转移：Main 侧无 Git Bash 的启动预检与中文横幅、MCP 配置校验诊断、file 工具 MSYS 路径解析、edit/write 混行尾边角均转批次 F 或 T032；七条现场结论（W3/W4/W5/W7/W8/W10/W11）归 P4-6 上机日，节点不因本任务转绿。证据：[evidence/batch-d2-2026-09-15.md](evidence/batch-d2-2026-09-15.md)。
- ✅ **T040** fork 生命周期闭环：绑定、提交与暂存文件回收 — `d4a361b5`（2026-09-15）。session-index-02/04/09 + T037 转来的追加项 4 已修：fork 未绑定会话时索引行补写 unbound（绑定会话不写），渲染层 materializeIndexedPiChatSession 接受「有 unbound 标记、没有工作区」的行，按 mergeSessionIndex 同一形状建会话，fork 不再当场报「未能材料化」，重启后不再被当孤儿丢掉；新增 worker.fork.accept RPC（协议类型 + 守卫 + PiWorkerRpcServer 分发 + PiWorkerRuntime 接口 + NativeWorkerRuntime 实现）补齐提交那一半，Main 在索引行落库后向源 worker 发一次 accept（尽力而为，失败只记日志），已采纳的 fork 再被 discard 会被拒；worker 造 fork 文件前先写同名 sidecar 标记 `<uuid>.jsonl.staged`（含 parentSessionId），Main 在 ensureReady 里做一次回收扫描（只删「带标记且无索引行认领」的转录，删不掉就保留标记留给下次启动），fork 窗口里崩溃不再在会话目录留下无主 JSONL；追加项 4：resume 分支对 row.unbound 且 cwd 已不在当前 scratch 根下的会话，在当前根下重新分配 scratch 目录并保持 unbound，不再按项目目录受信任恢复。新增 13 条用例（WorkerManager 85→94、chatPiWorkerRouting +2、workerEndToEnd +1、chatSessionActions +1），4 组反向验证判红后按 sha256 复原；三套 tsc 与 biome 干净。取舍（编排者已认可）：acceptFork 选「接上」而非删除（全仓唯一旧调用点在死模块 NativeSessionIndexAdapter，删除会让暂存标记永不清除，且 contracts.ts 早留了 acceptFork 这个能力名）；暂存形态用 sidecar 而非改名（fork worker 已按路径打开转录，改名要同步改 slot key / 索引 runtimeIdentity / worker file 三处）；回收扫描落在 Main 的 WorkerManager.ensureReady 而非 runtime（认领与否只有索引答得上来，Main 不 import runtime 子包；生产触发点只有 chat:ensureHost，若要无条件启动扫描可提到 main/index.ts）。审阅结论 `pass`。转移：SessionTreeDialog.handleFork 失败文案与补救入口 → 批次 F / E 点验；session-index-10、session-index-03 归各自原分组。证据：[evidence/batch-d2-2026-09-15.md](evidence/batch-d2-2026-09-15.md)。
- ✅ **T045** 并发多会话：写锁陈旧判定、抢占窗口与 trace 轮转 — `e5a2d5b9`（2026-09-15）。concurrency-01/02/03/06 + 并入的 windows-06、capacity-05 已修：抢占陈旧写锁不再先删后建，`<lock>.takeover` 哨兵（createOnly）串行化抢占，哨兵内重读锁确认仍是判过的那份，再把写好的新锁 rename 原子盖上，锁名任何时刻都有文件；陈旧判定纳入 acquiredAt 与本进程 startedAt（acquiredAt 早于本次开机、或 pid 是我们而进程不是即判陈旧），PID 复用不再把会话永久锁死；补「强制接管」的 runtime 半边（acquireWriterLock 支持 force，SessionConfig.forceTakeover 接通，拒绝文案带持有者、已持有时长与补救提示）；trace 轮转加跨进程互斥（落盘前 createOnly 抢 `<traceDir>/runs.rotate.lock`，锁覆盖 stat + 三次 rename + appendFile），多 worker 共用 traceDir 不再双重轮转丢代、追加不交织；子代理定义写盘改原子写（临时文件 + fsync + rename）。新增 15 条并发用例（sessionWriterLock 11→21、trace 11→15、subagentCatalog 27→28），含三进程抢同一把陈旧锁与两进程共用 traceDir 两条真实进程用例，6 组反向验证判红后按 sha256 复原；两套 tsc 与 biome 干净。取舍（编排者已认可）：未做「24 小时自动判陈旧」（会误杀开了几天的活写者），达成「PID 复用不永久锁死」靠重启自愈 + 用户强制接管自愈；trace 轮转用目录锁而非按进程分文件（不破坏已落地的 T024 轮转契约与现场取证脚本读法）；`src/runtime/plugins/session/store.ts` 改了 11 行范围外代码（SessionConfig.forceTakeover 接线），不接的话 force 参数在仓库里没有任何调用方。审阅结论 `pass-with-notes`（PID 复用且「没重启过机器」这一支仍要靠界面半边落地才算真正解除）。转移：「强制接管」界面半边（Main → worker → 渲染层 session_locked 错误码与按钮）→ **新开 T045b**（排在波 3 T043 落定之后）；subagent refresh() 读侧宽容兜底 → 批次 F；store.ts:588 close() 丢弃锁释放失败信号的可观测出口 → 后续任务。取证提醒：设了 AICLIENT_RUNTIME_TRACE_DIR 后 trace 目录会短暂出现 runs.rotate.lock，按 runs* 通配收集证据的脚本要过滤（已核实 scripts/ 下当前没有这类通配）。证据：[evidence/batch-d2-2026-09-15.md](evidence/batch-d2-2026-09-15.md)。
- ✅ **T041** Main 宿主：跨工作区斜杠命令回退、关机拆除顺序与 dispose 事件闸 — `19b64a77`（2026-09-15）。main-host-01/02/03 已修：getSlashCommands 回退改按工作区匹配（指名会话有 ready worker 用它；没有但池里有同 cwd 的 ready worker 就用那个；池里没有本工作区的 worker 或会话尚未启动就返回空列表；未指名 sessionId 的开始屏保留最近 worker 回退），重写「managed 模式不看工作目录、项目作用域被扣下」的失效依据注释（决策 009 后 NATIVE_PROJECT_TRUSTED 恒真）；disposeAll 里导入槽拆除套 try/catch 并记日志，失败不再连带跳过会话池的优雅拆除与 state='stopped'，也不再让外层 Promise.all 毫秒级失败清掉 7 秒兜底强杀定时器（强杀失败时刻意保留导入槽所有权供 forceKillAllNow 复用）；新增 ManagedSlot.drainingEvents 排空窗口，从 retireEntry 开到 slot.dispose() 落定（finally）关，期间只放行 permission.resolved / question.resolved，槽位身份 / generation / 会话归属三道闸原样保留。新增 7 条用例（WorkerManager 94→101），4 组反向验证判红后按 sha256 复原；两套 tsc 与 biome 干净。取舍（编排者已认可）：main-host-03 用发现原文给的「更小改法」——有界排空窗口，而非字面改 retireAndDispose 顺序（字面改法会让正在拆的 entry 最长 6 秒仍可被 send / compact / 容量淘汰命中）。审阅结论 `pass-with-notes`（「逐个 try/catch」措辞比实现略强，排空窗口是字面之外的做法，均已认可）。转移：cleanupAllResources 的 7 秒 deadline 无条件 finally 兜底（会改变正常关机语义，需单独拍板）、invalidateAll 补 session.status disconnected（需新增 reason 并渲染层接住）、开始屏未指名 sessionId 时的跨仓库回退残留（需透传 workspacePath，跨 preload / IPC / 渲染层三层）→ 批次 F。证据：[evidence/batch-d2-2026-09-15.md](evidence/batch-d2-2026-09-15.md)。
- ✅ **T046** 容量与进程对账表的余项：字节预算与进程回收 — `0d273b17`（2026-09-15）。capacity-01/02/03/04、concurrency-04/05/09 + T045 转来的 close() warn 已修：用户附件加服务端双重上限（单件 5 MiB 原始字节镜像 Main 的 MAX_ATTACHMENT_READ_BYTES，整条消息 8 MiB 落盘字节计进 32 MiB 会话预算，超限在拷贝 payload 之前抛 attachment_size_limit）；trace 的 tool_execution_start 参数走 traceSafeToolArgs（复用 MAX_TRACE_PREVIEW_CHARS 逐字符串叶子截断，结构原样保留，深度封顶 6 层）；压缩摘要加 MAX_COMPACTION_SUMMARY_BYTES = 256 KiB，超限走既有 giveUp('compaction_over_budget')，run 不失败；会话文件加单行安全网 SESSION_MAX_ENTRY_BYTES = 8 MiB（写队列外预检 + 队列内精确检查两道）；MCP 服务器改为按内存分档的跨会话全局进程预算（每会话 4/6/12、全机 12/36/120，分档镜像 WorkerManager 的 3/6/10）；exec.ts 新增 createOrphanReaper，长驻子进程收到可捕获信号或 exit 时按进程组回收（SIGKILL 仍由运行器 disconnect 看门狗覆盖）；导入 worker 在 fork 处立账（MAX_IMPORT_WORKERS = 1、importWorkerCount() 可读）。**首轮审阅判 `blocking`**：concurrency-09 新加的信号量在 fork 同步抛错（工作区缺失、打包版缺 node runtime）时不会释放座位，Main 进程余生所有导入都会被拒——比改前更差，审阅代理用「先加用例不改代码」的方式复现了判红。回炉把三个入口（inspect/reconcile/create）共用的启动段（占座 → fork → 注册 onExit → 建 slot → onSlotCreated）收进 startImportWorker 辅助函数整段 try/catch：fork 或建 slot 抛错时先 kill 已创建的进程再还座再 rethrow，补 2 条用例；二审判 `pass`，并指出上一轮报告用 grep 数 it( 的用例计数因 it.each 存在偏差，改用 vitest 对 HEAD（e5a2d5b9）与当前分支实跑核对，核实的真实净增为 agentLoop 12→15、contextCompaction 29→30、session 23→25、mcp 38→41、host 44→49；另新建 attachmentBudget.test.ts 6 条、PiImportProcess.test.ts 5 条（含回炉 2 条）。两套 tsc 与 biome 干净。取舍（编排者已认可）：**新开决策 014**——MCP 每会话上限从固定 16 改为按内存分档 4/6/12（全机 12/36/120）；capacity-04 双道检查（预检放队列外，避免一次拒绝把整个会话变只读）；concurrency-05 只做可捕获信号半边（Main 按进程组强杀 worker 那一半转批次 F）；concurrency-04 分档镜像写在 mcp/config.ts（WorkerManager 当时被 T041 占用）。**新开未决问题 Q014**——32 MiB 会话预算 vs 渲染层 5 MiB 单图上限的算术冲突，待用户拍板，不阻塞 D2。`src/runtime/__tests__/host.test.ts` 的 orphan reaper 5 条用例因该文件同时装着 T047 在飞的 TSD 分帧改动，未随 T046 提交，将随 T047 提交进入。转移：导入 worker 计进 WorkerManager 容量账与分档注释互指、Main 强杀 worker 按进程组杀（WorkerSlot）、CHAT_SEND 第一道附件字节闸 → 批次 F；审阅指出的「延迟 payload 只有队列内兜底」已在 appendEntry 附近注释点名 → 待盯。证据：[evidence/batch-d2-2026-09-15.md](evidence/batch-d2-2026-09-15.md)、[decisions/014-mcp-per-session-budget-by-memory-tier.md](decisions/014-mcp-per-session-budget-by-memory-tier.md)。
- ✅ **T042** 凭据脱敏统一与 stderr 限额 — `2ffdd152`（2026-09-16）。凭据规则统一到 `stderrRedaction.ts`，worker stderr 的 IPC / main.log / 崩溃回放与 provider 错误的 trace / 会话文件出口全部覆盖；无换行巨量 stderr 同时按内存和行数收口。定向 5 文件 / 184 条全绿，三套 tsc 与改动文件 Biome 全绿；恢复会话后的独立审查未发现阻塞项。
- ✅ **T043** 渲染层 native 词汇表 — `f3c5db03`（2026-09-15）。chat-tool-01～07/09、ah-lib-03、chat-event-07 已修，含 runtime 工具注册表到渲染层查表点的对账守卫。
- ✅ **T044** 问答卡与等待态状态机 — `38173c4b`（2026-09-16）。问答槽改为按 questionId 键控的队列，支持同会话多卡与乱序回答；permission / question 最后一个 gate 结算后从 waiting_* 回到 running，已结束回合不复活。定向 4 文件 / 108 条全绿，三套 tsc 与改动文件 Biome 全绿；恢复会话后的独立审查未发现阻塞项。
- ✅ **T047** TSD 回落链路与两种载体 — `d3ebd9e6`（2026-09-15）。tsd-01～07 与 main-host-04 已修；真实加密机与 utility 载体仍按验收要求留 T032/T033 重采版本绑定证据。
- ✅ **T048** 终端 / TUI 行为面 — `8dd1b65d`（2026-09-16）。terminal-01/03/04/08/09/10 与 concurrency-07 已修：退出确认、会话级 terminal id、复活绑定、shell 精确识别与 Windows 回退、控制器重建及 Main 接管闸均有用例。定向 5 文件 / 51 条全绿，三套 tsc 与改动文件 Biome 全绿；Windows / 加密机真实行为留 T032/T033 现场。
- ✅ **T055** 文档与现场口径回写 — `3092f33b`（2026-09-15）。field-01～05、windows-08、smoke-01、import-up-10、spike-01、terminal-07 已按现场事实和当前代码更正。
- ✅ **T056** 假平台依赖测试债专项 — `8a3ce4a6`（2026-09-16）。八个测试文件的路径分隔符字面量债转绿（skills / subagentDefinitions 的 fakeSource 构键归一、agentWireStatic / nativeWorkerDependencyBoundary / piCliIsBundledToolOnly 比较端 `split(sep).join('/')`、PiWorkerProcess 期望值改 `join()` 值等价、piWorkerEnv 补 `join`、panelVisibilityStatic `split('/')`→`basename()` 修 ALLOWED 白名单在 Windows 恒失效）：只改测试、零生产代码改动、断言强度不降（独立只读审阅逐 expect 核对 pass）；8 文件合跑 147/147 绿。归因勘误：agentWireStatic 实为 5 条分隔符债；「探测文件夹具 3 条」实属 permissionPolicyIntegration（6 条 bundled→builtin 退化，另行排查——重跑不带 `--ignore-scripts` 的安装可验证补丁假设）。
- ✅ **T057** Windows 路径语义缺陷波（含 shellPolicy 越界安全洞） — `2c1e4ba3`（2026-09-16）。深查四缺陷经**两轮对抗性安全审阅**落地：① canonicalPath junction/symlink + `..` 越界（accept-edits 零审批完整逃逸链、内置 deny 失效）改逐段解析——含 `..` 整段时先 realpath 前缀再施加 `..`，缺失段用深度计数、`..` 归零时对当前前缀重新 realpath（fail-closed），保证返回值第一个不存在组件前不残留未解析链接段；不含 `..` 的热路径逐字节不变；顺带修扩展 UNC 根（`\\?\UNC\srv\share`）解析。② workerSessionKey POSIX 分支改 `path.posix.normalize` + 入口绝对路径断言（该断言使 staged-fork 清扫的宿主 join 撞错，连带新增 `joinWorkerPath()`、WorkerManager 改用之，范围内必需）。③ LocalFileAccess 比较用小写、返回原始 realpath。④ promptService 两条过严断言收窄为精确断言（截断 `toBe('中')`、越界 symlink 对 root 下零 readFile，被测性质仍钉死；另生出 Q015）。shellPolicy 67→70（+5 junction/symlink/dangling/缺失段回归用例）、promptService 16、workerSessionKey 4、WorkerManager 106、localFileReadGuard 4、permissions 9 全绿，三套 tsc 过。**首版计数分支曾引入 ENOENT 粘性回归**（`missing` 布尔不复位 + `..` 弹回真实目录 → 链接段漏 realpath，同输入在旧实现下被拦），经第一轮对抗审阅在真实 runtime 逮出（read/write/bash 三通道零审批读出工作区外文件），以深度计数复位修正；二轮 35 个对抗输入经三通道复攻逃逸数 0、不变量零违规。证据：[defect-deep-dive-2026-09-16.md](evidence/defect-deep-dive-2026-09-16.md)。
- ✅ **T059** 家目录改为独立 user 层（[决策 016](decisions/016-home-tier-instruction-gating.md)，Q015 结案） — `b32fa9da`（2026-09-17）。家目录从 project 链结构性移除（命中家目录后 `slice` 掉它及其以上），改作独立的 user 层 global：对所有项目生效（含工作区根本不在家目录下的）、只取一份、顺序 `~/.pilab/AGENTS.md` → `~/.claude/CLAUDE.md` → `~/.codex/AGENTS.md` 找到即停、都没有则为空；全局层不读 `CLAUDE.local.md`；`/home`、`C:\Users` 这类多用户共享目录不读；globals 与 project 链按 realpath 做文件级去重作兜底；`home` 经 bootstrap 注入（纯模块不调 `os.homedir()`）；`scripts/runtime-baseline/run-native.mjs` 补 `settingSources: []`（旧的机器无关性是靠 `projectTrusted:false` 顺带挡住家目录换来的，新语义下须显式关）。收口复核：决策 016 的七条决定与 roadmap 十一项验收逐条在代码与用例里找到对应（projectInstructions.test.ts 新建 290 行 / 46 条，promptService +2 条）。提交侧记录：三套 tsc 与改动文件 Biome 干净、定向五套 103/103、反向验证 10 组、独立审阅 `pass-with-notes`、14 项对抗输入 0 攻破，并按审阅的变异测试结论补了「家目录移除与 user 开关无关」那条用例。**收口全量补跑逮出一条提交时漏掉的回归**（2026-09-17）：`join('.pilab', 'AGENTS.md')` 这个字面量触发 `src/shared/__tests__/defaultPaths.test.ts` 的改名守卫（该守卫要求 `APP_STATE_DIR` 除定义处外不出现在任何源文件；T059 的定向五套不含它，只有全量才撞得上）。按该文件 POLICY_PATTERN 的既有先例修：白名单新增 runtime 子包条目（runtime 是独立 npm 包，对 `src/shared` 只有会被擦除的 type-only import，值 import 会成为第一条跨包运行时依赖），forcing function 立在 `projectInstructions.test.ts`——首项改由 `join(APP_STATE_DIR, 'AGENTS.md')` 拼出，改名漏改会在那里判红。2 组反向验证判红后复原（改生产码里的目录名 → runtime 用例 3 条判红、仓库扫描反而转绿，正是需要 forcing function 的那一幕；去掉白名单 → 仓库扫描判红）。**该修复尚未提交**。

## In Progress

- ✅ **T032** 上机检查单与开发机组 — 只读点验，证据随 `db8956a8` 提交（2026-09-17）。**正式检查单已产出** → [checklist-e.md](checklist-e.md)（计划根下，与本文件并列；批次 D 的草案作为存证保留原样）：四个来源合并去重完毕，新增 20 项（旧树 P5-2 六行 / P5-4-P5-5 四行 / H-20 两行 / P6-3 第 4 条 / P6-4 回退互读 / 四条审计静态推断项 / 三条旧树未覆盖项），并做了三条裁决（cordis 判据写反已改正、并发轮转已由 `trace.test.ts:350` 覆盖、CI 不触发测试已结案）。**开发机组 37 项（35 + DEV-36/37）当日全部处置完毕**，分七批执行：✅ 通过 28 项、⚠️ 部分成立 3 项（DEV-11 导入文案、DEV-32 GUI 侧覆盖层、DEV-36 插件页有条件句）、⛔ 判负或复现已知缺陷 3 项（DEV-3 兼容根子代理定义删除语义、DEV-10 Codex 子目录不可读整源静默消失、DEV-16 两个窗口对同一会话开 `pi --session` 静默分叉）、⛔ 当前不可执行 2 项（DEV-12 本机无真实旧格式 Codex 样本、DEV-18 Node 24 解析属死代码）、🚫 裁决不需人工 2 项（DEV-35 已由 `trace.test.ts:350` 覆盖、baseline-01 CI 已结案）。检查单同日更正 16 行：5.1 判据修正新增 15 行（DEV-22、DEV-33 ×2、DEV-11、DEV-5、DEV-13、DEV-1、DEV-2、DEV-15、DEV-32、DEV-17、DEV-24、DEV-25、DEV-29、DEV-36），5.2 新增 DEV-18（死代码不可执行）；另第 2.3 节 MODEL-48 放宽为「CDP 真鼠标序列可驱动」。点验产出 26 条疑似缺陷 D1～D26，收敛成新开的**批次 D4**（T060～T068）与两条待拍板问题 **Q016 / Q017**。方法与环境：模型除第二批 dev-A1 用了 `maxapi/grok-4.6` 真实回合外全部走本地假网关（vault 临时注册、每批还原），Electron 累计起 20 次，产品代码零改动，唯一新增仓库文件是测试用例 `src/runtime/__tests__/sessionAttachmentFill.test.ts`（2 条）。收口全量 Vitest（Linux 开发机、单 worker）**413 文件 / 6271 条全部通过，退出码 0，237.55 s**，存档 [closeout-vitest-full.txt](evidence/batch-e-devbox-2026-09-17/closeout-vitest-full.txt)；对比 T059 收口的 411 / 6268（2 failed 为开机时长相关的 `sessionWriterLock` 两条），本次 +2 文件（`pluginGraphIncomplete.test.ts` 已提交、`sessionAttachmentFill.test.ts` 新增）。证据：[evidence/batch-e-devbox-2026-09-17/README.md](evidence/batch-e-devbox-2026-09-17/README.md)。**本轮全部未提交**（提交需用户批准）。
- 批次 D2 已收口（2026-09-16），批次 D3（T056 `8a3ce4a6` / T057 `2c1e4ba3` / T059 `b32fa9da`）已全部落地并收口（2026-09-17）。
- **批次 D4（T060～T068）九项已全部落地并经两轮审阅、两轮真机复验（2026-09-17），代码 `d2bbbf13`、文档 `db8956a8`**——逐项结论见下方批次 D4 表，证据 [evidence/batch-d4-fixes-2026-09-17/README.md](evidence/batch-d4-fixes-2026-09-17/README.md)。本段当前在飞：提交批次 D4 与 T032 的证据，然后进 **T033**（最后一次上机，先补一次打包构建）。
- **批次 G（T072～T076）五项已全部落地并提交（2026-09-18）**，`17eb4581` / `0144320e` / `cd734d61`，未推送——来源是用户当日直接提出的五条界面诉求，不走审计/点验通道；逐项结论见下方批次 G 表，证据 [evidence/batch-g-chat-ux-2026-09-18/README.md](evidence/batch-g-chat-ux-2026-09-18/README.md)。**全程只有静态验证，未启动 Electron**：三处观感/对比度判定（暗色第二跳 1.33×、浮层遮挡、折叠后清爽度）并入 T033 上机日。
- **批次 H（T077～T087 + 提级的 T053）新开并十项落地，已分 13 个本地提交（2026-09-18，未推送）**，来源是当天用户在 Windows 测试机上使用打包 Build #59（源码 `13e6cdb7`，早于批次 G，不含 `b24d5c81` / `17eb4581` / `0144320e` / `cd734d61` / `d119da25`）产生的十条现场反馈，均经只读排查在代码里定位到具体行号；逐项反馈与根因见 [topics/t033-field-day/07-findings.md](topics/t033-field-day/07-findings.md)。四条新增决策：[023](decisions/023-bypass-permissions-tier.md)（第四档权限模式「完全放行」）、[024](decisions/024-prompt-cache-ttl-split.md)（prompt cache TTL 主对话 1h / 子代理 5m 分离）、[025](decisions/025-search-tool-native-over-pi-fff.md)（搜索工具优先自建，`@ff-labs/pi-fff` 仅作设计参考）、[026](decisions/026-permission-grant-granularity.md)（授权记忆的粒度：文件精确匹配、bash 记前缀、按会话持久化不跨会话）。**T077 / T078 / T079 / T080 / T081 / T053 / T082 / T083 / T084 / T086 十项当日实现并分 13 个本地提交落地**（`8f39e3d3`、`c18d92dc`、`0a97e6d3`、`6875fa47`、`4721cede`、`97c08b58`、`8ba25048`、`04984979`、`3d2c3cfd`、`b930b5b9`、`6913c264`、`0a4f6f61`、`f3b658d4`），分批提交前全量 vitest 429 文件 / 6641 条全绿、每个提交单独过三套 typecheck、后三个修正提交（`6913c264` / `0a4f6f61` / `f3b658d4`）各自相关测试绿（352 / 267 项），**全部未推送**；T085 阻塞于 [Q019](open-questions.md) 未开始；T087 推迟另行跟进。批次内新增的 Q022～Q026 已全部结案（Q022/Q024 见决策 023/024 追加注记；Q023 见决策 026；Q025/Q026 记入下方 T080/T086 落地注记，未另立决策），待拍板列表只剩 [Q019](open-questions.md)。逐项结论见下方批次 H 表，落地明细、本地点验状态见[批次 H 证据](evidence/batch-h-field-fixes-2026-09-18/README.md)（本地 CDP 点验进行中）。顺带修复了两个因当日改动挂死的渲染层测试（`subagentPanelMount`、`SettingsContent`，见批次 H 证据「顺带修复」节）。下一步：本地点验收口 → 推送 → T033 第二轮上机验证。

## Next

### 批次 A：安全与数据完整性

| ID | 任务 | 节点 | 覆盖的发现 | 验收 |
|---|---|---|---|---|
| T001 | bash 静态分析补全：遍历 command 节点的 redirect 子节点（前置重定向、here-string）；命令名 basename 归一并把 timeout/nice/nohup/time/stdbuf/command 一类包装词按剥壳或 unresolved 处理；`bash/sh/zsh/dash script` 无 -c 时置 unresolvedPaths；命令名位置的命令替换走 visitSubstitutions；贴合选项与 `key=value` 操作数登记为路径；auto 档改到 unresolvedPaths 判定之后；addPath/expand 统一路径分隔符 | P1-5 | permissions-01/02/03/04/05/06/19 | shellPolicy.test 新增每类形态的正反用例；PERM-1 探针复跑；混合分隔符用例不需真 Windows |
| T002 | 权限 surface 映射：`mcp__server__tool` 授权时以 `mcp` 为 surface、`server:tool` 为匹配值；skill 工具与 `/skill:name` 展开过 authorize（surface `skill`，目录已信任的路径判 allow 不弹卡）；删随包策略里无生产者的 `ls` 规则；allow-session 保持会话级不收敛（决策 003），审批卡文案注明对本会话所有代理生效 | P1-5 / P5-1 / P5-3 | permissions-09、skills-mcp-11/12、cross-05 | 策略里写 `mcp: deny` / `skill: deny` 在 auto 档真的拦住；permission.activity 出现 skill 行 |
| T003 | H/20 交叉写入：解码端不把 CLI 行计入 seq 空间或对陈旧 seq 按位置容忍；TUI 退出（含 pi 自行退出）无条件 reload；CHAT_COMPACT / CHAT_REWIND 前做 TUI 释放；`assertHostPromptAllowed` 接到发送路径；compactionAnchor 改为从 retainedTail 反查条目 id，查不到退化为 undefined；先补「CLI 追加后我方以陈旧 seq 再写」「CLI 补换行后再打开」两条用例把 session-02 从待定推到结论 | H/20 / P3-1 | session-01/02/03/13、cutover-04 | sessionInterop.test 新增交替写入用例；真机 GUI→TUI→GUI 一圈留到批次 E |
| T004 | 子代理编排错误路径：admit 之后到 SubagentRun 接手之间整段 try/catch，失败即 settle 为 failed；drain() 加兜底超时；TaskStop 只对真正被停掉的记录 markDelivered，已完成目标把报告放进返回文本；prune 不淘汰未交付项；run() 区分 stopped/timed_out 并带 lastReportText | P5-2-2 | subagent-core-01/03/04/05 | 新增 projectInstructions 拒绝、TaskStop 传已完成 id、registry 满 100 条淘汰三条用例 |
| T005 | 内部报告与审计投影：交回 prompt 打内部标记，projector 不投影为用户气泡、不盖 attemptId/附件（落盘保留但标记）；权限活动过滤条件放宽为「本 run 父调用或已登记的委派调用」，payload 带 delegationId/agentName | P3-4 / P5-2-4 | rpc-projector-01/04、subagent-core-02、loop-model-06 | 一条带子代理的 run 端到端事件流用例；guiEventContract 录制扩到子代理 |
| T006 | 子代理继承父回合：bindRun 传父回合解析出的 model ref 与 thinkingLevel，resolveModel 第三档用它；委派面板 resolvedModel 与父一致 | P5-2-1 / P5-2-2 | cross-01/02 | 用例：父回合切模型后委派，子代理 ref 等于父 |
| T007 | 模型绑定与目录诊断：展开后的 headers 放进 auth resolve 返回值或每个 Model；per-model api 用 pi-ai 的 map 形态；缺 API key 归入不可重试并进 dropped（`no_api_key`）；空 baseUrl 进 dropped（`no_base_url`）；auth.json 损坏给诊断；dropped 在 catalog_empty 抛错前也留一条出口 | P0-4 / P5-5 | loop-model-01/02/04/07/08、cross-06 | 用例断言请求真的带 User-Agent；catalog_empty 时 dropped 可读 |
| T008 | 导入清单与路径：清单校验同时接受 `<id>.jsonl` 与 `*_<id>.jsonl`；parseRecord 失败打 warn；四个导入 RPC 对 targetPiSessionId 用 isLegacyImportPathSegment；reconcile 清理写锁旁车与暂存目录，且单条失败不连坐后续记录；测试替身改 native 命名并补「落盘 → 新实例重载 → already-imported」用例 | P5-4 | import-catalog-01/07/09/10/11 | 替身改名后现有用例仍绿；reconcile 多记录用例 |
| T009 | 打包门禁 native-only：verify-packaged-app 只跑 native lane，packaged-worker-smoke 删 --backend 与 legacy 分支；顺带只读审查打包链（agent-host-build-lib、packaging-budget、afterPack）给出 native-only 后的门禁形状 | P6-5 / P6-3 | cutover-01、cutover-16 | `pnpm verify:packaged` 在 CI 三平台绿 |
| T010 | 搜索遍历容错：walk() 先看 entry.kind，symlink 直接跳过，realpath/readDirectory 包 try/catch 按跳过处理；checkShellPaths.expand 的 readDirectory 对 ENOENT/ENOTDIR/EACCES 按空展开 | P1-4 / P1-3 | tools-01/07 | 悬空 symlink、EACCES 子目录、不存在的通配父目录三条用例 |
| T011 | trace 脱敏与体积：provider 错误正文过 redactSensitiveErrorText 并截 600 字再进 trace 与 run 结果；审批 preview 进 trace 前截断；write preview 不常驻内存 | P0-6 / P1-6 | core-host-03、permissions-12、tools-11 | 含 `Authorization: Bearer` 的假错误在 runs.jsonl 里是 [REDACTED] |

### 批次 B：功能正确性

| ID | 任务 | 节点 | 覆盖的发现 | 验收 |
|---|---|---|---|---|
| T012 | 工具输出形状：状态尾巴先留预算再拼接；单行超限时 nextOffset 前进；空结果给占位文本；bash details 只放标量，read details 去重复 text；include/pattern 不含 `/` 时按任意深度；正则循环加取消点与单文件时间预算；edit/readBeforeChange 保 BOM；非 UTF-8 报带 code 的错误；CRLF 去 `\r`；limit 恰满不误报 | P1-2/3/4 | tools-02/03/04/05/06/08/09/12/13/14/17 | 每条一个用例；tools.test 顶满 50 KiB 的 read 与 bash |
| T013 | TSD 读取效率：readLines 在 node-fallback 下把块大小提到输出预算量级或让 HostIo 暴露可复用句柄；helper 的 stderr 与 stdout 预算分开 | P1-2 / P1-0 | tools-10、core-host-05 | fake HostIo 计数断言 readFile 调用次数 |
| T014 | 压缩与提示词：回合边界压缩成功后写回 agent.state.messages；shapeForCheckpoint 重算 fileOps；hard limit 下 compaction_empty_range 与摘要超窗两条死路给降级；instructionSource 吞 invalid_host_request；entriesFor 切片口径与 snapshot 一致；提醒档位按会话；run 前 prepareTurn 记 trace；isWithinRoot 处理文件系统根；指令文件编码识别；tool-guidance 工具名大小写；`run.targetPath` 连同 root→leaf 分支与测试删除（决策 007） | P2-2/3/4/8 | context-prompt-01/02/03/04/05/06/08/10/11/13/14/15/16/17 | 「压缩后同一 run 再 prompt」用例；checkpoint details 覆盖被摘要消息 |
| T015 | 会话 store 与写锁：close() 用 allSettled 等队列、锁释放后正常 resolve；释放锁校验 token；clearStale 抢占加归属校验；旧 label 行校验 targetId；首行解析包 code；32 MiB 常量集中；补两个进程同时抢陈旧锁的并发用例 | P3-1 / P3-3 | session-04/05/06/09/10/12 | 写失败后 dispose 正常完成的用例 |
| T016 | worker 生命周期与超时：discardFork 在 dispose 前 unlink；reload/compact 传与 bootstrap 同量级超时且 compact 传 signal；显式 effort=off 不回落配置；handleDispose try/finally，disposed 置位放在拆除之后；setPermissionTier 不谎报；utility.start 交付 modelCatalog | P4-1/3/4 / P5-5 | worker-runtime-01/02/05/06/07/09/12、rpc-projector-05/06/08/10、import-catalog-04、context-prompt-07 | 假 handle dispose reject 的用例；utility 路径不读 models.json |
| T017 | 事件投影与 golden 录制：assistant 消息惰性开启、无内容不发 completed；providerRetry 的 onRetry 发带 retry 的 session.status；tool.updated 带 input；超时产出 timed_out；session.stderr 接生产者或删类型；重录 nativeGuiEventStream 并扩到 thinking / 子代理 / 重试 / 问答 / 压缩 | P3-4 / P4-5 | rpc-projector-02/03/07/13/17/18、permissions-08 | 重录 fixture 后逐条读 diff；renderer 回放测试同步 |
| T018 | MCP：tools/list 留在连接预算内且 connectMcpServers 有小于 60 秒的总预算；listTools 去重、register 包 try/catch 记进 connection.error；工具名白名单收窄到 `[A-Za-z0-9_-]`；StringDecoder 跨 chunk；image part 透传；request 接 AbortSignal 并发 notifications/cancelled；项目 disabled 覆盖用户同名；帧长按字节计且超限杀进程；spawn 失败不伪造 client；未实现方法回 method not found；MAX_SERVERS 截断按声明顺序 | P5-3 | skills-mcp-01/02/03/04/05/06/07/14/15/16/21/24 | mcp.test 新增分帧、重名、慢 tools/list、取消四类 |
| T019 | skills 与模板：symlink 条目 stat 后再判；解析 disable-model-invocation 并在提示词过滤；YAML 块标量报诊断；$ARGUMENTS 用函数替换值；描述回退截 60 字；目录扫描可刷新或文档改口；项目 `.agents/skills` 从 cwd 及祖先目录一路找到仓库根，近者优先（决策 004） | P5-1 | skills-mcp-08/09/10/17/18/19/20/22/25 | skills.test 新增 symlink、disable、块标量用例 |
| T020 | 子代理数据与展示：迁移预览接 IPC + 设置页入口或改签收；定义每个顶层 run 重读；subagentUsage 并入 usage.updated 独立字段送到 Main 并入会话总量（决策 005）；事件条数上限与 capped；转录写入限额；渲染层动词/参数表补 glob、browser_preview、ask、skill、new_context、Task*、mcp__*；定义写回转义对称；迁移器原型链键与带引号布尔；诊断出口；usage 求和复用 addUsage；turn 计数转发；截断保代理对；BOM 文档两读者一致 | P5-2-1/4/5/6 | subagent-data-01～17、subagent-core-06/12/13 | 定义编辑后下一 run 生效的用例；面板与主时间线工具行渲染的跨层用例 |
| T021 | 模型目录 Main 侧：T007 之后凭据库未解锁会让 provider 从目录消失而非 43 秒后报错，locked 场景用例要断言这一行为或改为延迟绑定；resolveNativeModelCatalog 在凭据不可读或用户组读失败时返回 undefined 走读盘回落；托管半边选取抽成共用函数并按凭据模式判定；model 级 baseUrl 在 Main 组装路径可达；补 resolveNativeModelCatalog 单测 | P5-5 | import-catalog-02/03/06/12 | 钥匙串 locked 场景用例 |
| T022 | host exec 与 bootstrap：`HostIoPlugin.rmdir` 补单测（T008 残项新增，非空目录 ENOTEMPTY）；dispose 里 trace flush 失败不吞 exec 清理失败；长驻子进程 kill 宽限用尽报 exec_cleanup_failed；Windows killTree 加重入保护并跟踪 taskkill；spawn/shutdown 并发回收；abort 监听器摘除；readDirectory 校验前置；runner 助手路径缓存；EventsPlugin 订阅者隔离 | P1-0 / P0-3 | core-host-04/06/07/08/09/10/11/14/20 | host.test 新增运行中取消与清理失败路径 |
| T023 | i18n：runtime 产出的用户可见文案改为结构化标识、渲染层查词典；noHardcodedChinese 扫描根加 runtime 与 agent-host | P1-6 | 批评者 i18n 缺口、cutover-17 | 守卫反向验证 |
| T034 | 会话文件中段坏行自愈：解码遇到无法解析的中段行跳过并重写文件（与 pi CLI 读法一致，决策 006）；被跳过的行记进 trace 与会话状态提示；补「撕裂尾片 + CLI 续写」组合用例 | P3-1 / H/20 | session-02 中段形态（Q008） | 用例：坏行在中段时文件可打开、两读者条目一致、提示可见 |
| T035 | 项目指令按官方分级加载（决策 007，只认 CLAUDE.md / AGENTS.md）：会话开始装 cwd 及所有父目录的指令文件；工具插件在 read / edit / write / grep 命中 cwd 子树文件后按需补装该路径到 cwd 之间未装过的指令文件，以内部标记消息注入、每份一次；提示块声明各级累加无硬优先级；沿用 32 KiB 预算、symlink 守卫、未信任项目不装；`settingSources` 三值开关管全部分层来源（指令、权限策略、MCP、skills / prompts），省略即全开；新定义 local 层文件（`.pi/agent/pi-permissions.local.jsonc`、`.pi/mcp.local.json`、`CLAUDE.local.md`），合并优先级 随包 > 编程选项 > local > project > user（决策 008）。不做 rules | P2-2 / P1-5 / P5-1 / P5-3 | 决策 007 / 008（原 context-prompt-03 的产品缺口） | 用例：父目录文件开局在提示词中；读子目录文件后下一轮出现该目录指令且只注入一次；未信任项目不装；关掉 user 后用户 mcp.json 与全局策略不读；local 策略覆盖 project 同键 |
| T024 | 容量对账：按来源列出写进会话文件与 trace 的最大字节（工具结果、审批 preview、子代理转录、MCP 响应），与 32 MiB 预算对账并给 runs.jsonl 轮转 | P3-1 | 批评者容量缺口、subagent-data-05、tools-06 | 一份对账表 + 至少一处限额落地 |

### 批次 C：退役清扫与文档回写（已全部完成）

T025～T028 已全部落地，任务描述与验收见上方 Done 段对应行；逐波记录、提交与覆盖、与字面不同的做法、转移与待盯项见 [evidence/batch-c-2026-09-15.md](evidence/batch-c-2026-09-15.md)。

### 批次 C 追加（决策 012，2026-09-15）

T036 已落地（见 Done 段与 [evidence/batch-c-2026-09-15.md](evidence/batch-c-2026-09-15.md)「追加：T036」节）。

### 批次 D：审计覆盖补全（只读，已全部完成）

T029～T031 已落地，任务描述与验收见上方 Done 段；证据集 [evidence/batch-d-audit-2026-09-15/README.md](evidence/batch-d-audit-2026-09-15/README.md)，落地记录 [evidence/batch-d-2026-09-15.md](evidence/batch-d-2026-09-15.md)。

### 批次 D2：补审修补（上机前，2026-09-15 新开）

来源：批次 D 接缝审查员的 20 个修补组（[cross-and-critic.md](evidence/batch-d-audit-2026-09-15/cross-and-critic.md)「修补分组」），归属 fix-before-e 的 12 组 + docs-only 1 组。发现编号指向[批次 D 证据](evidence/batch-d-audit-2026-09-15/README.md)。3 条 high 全在本批。[决策 013](decisions/013-batch-d2-fix-all-before-field.md)：全做、不压缩；建议顺序 T037 / T038 / T039 → T040 / T041 / T042 / T045 / T046 → T043 / T044 / T047 / T048，T055 可与任一波并行。执行方式沿用批次 C（每波两个实现代理 + 只读审阅，波间三套 tsc，全量 vitest 只在收口跑一次，每条修补至少一处反向验证）。

T037～T045、T045b、T046～T048、T055 已全部落地（14 项 14 提交，批次 D2 收口于 2026-09-16）；落地记录见 [evidence/batch-d2-2026-09-15.md](evidence/batch-d2-2026-09-15.md)。

| ID | 任务 | 严重级 | 覆盖的发现 | 验收 |
|---|---|---|---|---|
| T042 | 凭据脱敏统一到一份规则、装在所有出口上：src/agent-host/stderrRedaction.ts 与 src/runtime/plugins/agent-loop/providerErrors.ts 两份实现的合并，以及三个出口：WorkerManager.ts:2062 进 main.log 的两路、provider 错误正文落会话文件那一路；另含 hostStderr.ts 的条数限制。（已并入 ah-lib-01、ah-lib-02） | medium | main-aux-06, main-aux-08 | 仓库里只剩一份脱敏规则，且规则强度不低于既有那份（能认出裸密钥形状与 JSON 形态的密钥）；worker stderr 无论走 IPC、main.log 还是崩溃回放都经过它；provider 错误正文写进会话文件与 trace 之前也经过它；无换行的 stderr 流同时受内存与条数限制，一次巨量输出不会把 50 行崩溃回放挤空。配一组含真实密钥形状样本的用例。 |
| T043 | 渲染层词汇表：工具名、审批卡参数、恢复错误码与文案：src/renderer/components/chat/ 下的 toolCard.ts、questionCardModel.ts、piToolNames.ts、subagentActivityModel.ts、turnTiming.ts、permissionActivityRow.ts、historyError.ts，以及 src/runtime/plugins/permissions/activity.ts 与 skills/index.ts 的标签——全部是「渲染层还在按旧引擎的词汇查表」这一个根因。 | medium | chat-tool-01, chat-tool-02, chat-tool-03, chat-tool-04, chat-tool-05, chat-tool-06, chat-tool-07, chat-tool-09, ah-lib-03, chat-event-07 | native 注册的每个工具名（小写 grep / glob / ask / skill / browser_preview / new_context、TaskWait/TaskList/TaskStop、mcp__*）在主时间线、子代理面板、Run 面板都能查到正确的动词与参数，不再出现「Ran …」或原始工具名；只带路径的审批卡显示被批准的那个路径；mcp__ 拆分在服务器名含 __ 或结尾为 _ 时给出正确标签；恢复失败的分类表认得 native 的错误码；权限活动行的 resolution / autoReason 与 T020 新写的 arg 文案、技能审批卡的 Skill 标签全部过 t()。补一条用例拿 runtime 的工具注册表去对账渲染层的每个查表点（这条测试目前完全不存在，见 chat-tool-08，归入事件协议组）。 |
| T044 | 问答卡与等待态状态机：src/renderer/stores/chatSessions.ts 的问答槽位与 waiting_* 状态回程——两条都会让界面卡在一个假状态上。 | medium | chat-event-01, chat-event-02 | 同一会话能同时挂多个问答卡（按 questionId 键控），第二个问答不再挤掉第一个、也不会让那一回合永久挂起；waiting_permission / waiting_question 在审批或回答落地后回到 running / idle，Run 面板不再从用户点「允许」起一直显示「等待审批」。两条各一条用例，含「先答第二个再答第一个」的乱序场景。 |
| T047 | TSD 回落链路与两种载体的行为差：src/runtime/host/worker.ts 的回落开关、host/io.ts 的 helper 协议、plugins/tools/index.ts 的 grep、plugins/tools/read-lines.ts、src/main/services/agent-host/WorkerTransport.ts 的 utility 支路、src/runtime/smoke/p1-utility-worker.ts——加密机是主要现场，这条链路目前只有阅读没有可复现证据。（已并入 main-host-04） | medium | tsd-01, tsd-02, tsd-03, tsd-04, tsd-05, tsd-06, tsd-07 | utility 载体的 worker stdout 像 node 载体一样被排空（D11 第 5 条落实到位），并有一条用例覆盖；TSD 回落在真实平台上有出厂触发路径，而不是只在不可能命中的平台上启用；grep 的逐文件读取对单个读不了的文件容错，不再让整次搜索失败；helper 的 stdout 帧协议不再被子进程的启动噪声污染（加帧或改用独立 fd）；首 16 字节恰为 TSD 魔数的明文文件有可读出口；块大小封顶后的重读量与注释所称的「约线性」一致，或改注释说实话；utility 探针改为走产品路径推导宿主配置，carrier 断言不再同义反复。做完后上机重跑一次并把结果绑当前版本戳（这是 P1-8 升 complete 的前置）。 |
| T048 | 终端 / TUI 行为面：进程退出确认、会话绑定与接管把关：src/main/services/terminal/PiTuiPty.ts、PtyManager.ts、ShellDetector.ts、src/main/ipc/piTui.ts、src/renderer/components/chat/usePresentationSwitch.ts、src/renderer/hooks/useXterm.ts——H/20 互通的另一半。 | medium | terminal-01, terminal-03, terminal-04, terminal-08, terminal-09, terminal-10, concurrency-07 | kill 之后等待并校验进程真的退出，失败要上报而不是吞掉；终端 id 按会话而不是按应用，TUI 模式下切会话不会仍绑在上一个会话的 JSONL 上；复活用的 open 带 sessionFile，PTY 已不在时不会静默起一个全新的 pi 会话；自定义 shell 推参不再用子串匹配（/bin/sh 不能被判成 PowerShell）；Windows 默认 shell 与 spawn 失败回退有 Windows 分支；dispose() 带 terminalId，不会让窗口的控制器永久报废；「TUI 接管前不能有正在跑的回合」在 Main 的 IPC 入口也复核一次，不只靠渲染层。 |
| T055 | 文档与现场口径回写：docs/plantree/plans/runtime-evolution/README.md 的看板、docs/plans/2026-09-09-bash-carrier-decision.md、Windows-P4-6-evidence/ 下的启动脚本与清单、evidence/p4-6/perm1/README.md、evidence/p0 的冒烟存档、external-agent-migration/README.md、docs/plans/2026-09-08-runtime-evolution-ard.md，以及 tuiHandoverWiring.test.ts 里那条不成立的注释——不改行为，只把文档改成实话。 | low | field-01, field-02, field-03, field-04, field-05, windows-08, smoke-01, import-up-10, spike-01, terminal-07 | 看板不再把现场明确拒签的 R2/R3 写成载体对照结论、也不再据此宣告放行规则结案；D1 决策文档的现场结果栏如实填写（含 R4 方法已被证明无效）；F7a/F7c 行与 F5 行的自相矛盾消除；已删除的后端开关在启动脚本与清单里的四处残留清掉（脚本不再回显假确认）；PERM-1 证据的复现命令更正，并明确 T032 复跑不得就地覆盖被标 legacy 的历史证据；P1-8 行删掉已被 T028 推翻的 RUNTIME_CONFIG_VERSION 冻结说法；P0-6 与 H/21 的证据陈旧程度如实注记（分别落后 227~228 与若干提交，且 H/21 记的还是 pi 时代落盘路径）；ARD 里 D1 三项 Cordis 能力零落地写成落地注记；「suspend 之后另一个写者不再追加」这条注释改掉。 |
| T045b | 强制接管接线：Main → worker → 界面（T045 验收的界面半边）：src/shared/types/workerRpc.ts 的 bootstrap 载荷、src/runtime/worker/nativeWorkerRuntime.ts、src/main/ipc/chat.ts resume 分支、WorkerManager 的 resume / bootstrap 载荷、src/shared/types/sessionHistory.ts 与 src/renderer/components/chat/historyError.ts 的 session_locked 一档、现有历史错误面上的「强制接管」按钮 | medium | concurrency-02（界面半边） | 验收：会话被陈旧锁挡住时界面显示「被进程 <pid> 持有 <时长>」而不是 read_failed 并出现「强制接管」按钮；点击后带 forceTakeover 的 resume 走通、锁被接管、会话打开；非锁类错误不显示该按钮；worker / Main / 渲染层各一条用例；顺带修 T045 审阅两处 cosmetic（SessionLockedError 子类未覆盖 this.name；takeOver() 哨兵占用分支的拒绝文案丢失持有者信息）。 |

### 批次 D3：收口后追加修补（2026-09-16 新开）

来源：批次 D2 收口全量归因（[vitest-attribution-2026-09-16.md](evidence/vitest-attribution-2026-09-16.md)）与缺陷深查存证（[defect-deep-dive-2026-09-16.md](evidence/defect-deep-dive-2026-09-16.md)）。用户拍板：测试债一次专项全清（2-A）；缺陷按深查建议 1-A 执行（shellPolicy 主修 + 三条顺手修）；Q014 以[决策 015](decisions/015-attachment-sidecar-for-session-budget.md)结案转批次 F 的 T058。

T056（`8a3ce4a6`）、T057（`2c1e4ba3`）、T059（`b32fa9da`）已全部落地（3 项 3 提交，批次 D3 收口于 2026-09-17）。T056 只改测试零生产改动、独立只读审阅 pass；T057 经两轮对抗性安全审阅（首版 ENOENT 粘性回归被逮、回炉改缺失深度计数、二轮 35 对抗输入逃逸 0）。收口全量 Vitest：46 failed | 6203 passed | 1 skipped（19 文件失败），对比 D2 收口基线 90 failed / 28 文件降 44 条、9 个文件；18 个可捕获失败文件经逐一核对为基线失败文件的严格子集，无 T056/T057 引入的新回归（shellPolicy、agentWireStatic 隔离复跑 91/91 全绿，全量残留各自只剩基线既有的载荷超时 / 夹具生命周期失败）。证据见 [defect-deep-dive-2026-09-16.md](evidence/defect-deep-dive-2026-09-16.md)。

T059 的收口全量另在 **Linux 开发机**上跑（2026-09-17，单 worker，411 文件 / 6268 条，**2 failed | 6266 passed**，221 s）。两台机器的数字不可相减：上面那份 46 failed 是 Windows 开发机的，剩余债几乎全是 Windows 专属（缺 `rg.exe`、EBUSY 清理竞态、MSYS bash 假设），在 Linux 上本就不复现。首跑 3 failed 的处置：1 条是 T059 自己引入的改名守卫回归（已修并复跑转绿，见 Done 区 T059 行），另 2 条与 T059 无关——`sessionWriterLock` 的两条强制接管用例构造「2 小时前取得」的锁，而 `writerLock.ts:242` 把早于本次开机的锁判为陈旧，本机开机仅 29 分钟，锁被判陈旧、接管成功、预期的 `session_locked` 没抛出；受控实验把构造锁龄改为 10 分钟（晚于开机）后拒绝路径立即正常（消息给出 `held for 10m`），证明产品逻辑无缺陷、是用例吃了真实开机时间，建议单独排期（修法：让用例控制 `os.uptime()`）。

| ID | 任务 | 严重级 | 来源 | 验收 |
|---|---|---|---|---|
| T056 | 假平台依赖测试债专项：skills / subagentDefinitions / agentWireStatic / piWorkerEnv / PiWorkerProcess / panelVisibilityStatic / nativeWorkerDependencyBoundary / piCliIsBundledToolOnly 八个测试文件的路径分隔符字面量债（shellPolicy 的一条并入 T057） | low | 归因 A 类 | 八个文件 Windows 全绿；只改测试不改生产代码（PiWorkerProcess 按消费方核实后定）；断言强度不降（无删断言、无放松 matcher、无 skipIf） |
| T057 | Windows 路径语义缺陷波：`canonicalPath` 逐段解析主修（junction/symlink + `..` 越界——含内置 deny 失效与 accept-edits 零审批逃逸链）；workerSessionKey POSIX 分支误用原生 resolve；localFileReadGuard 返回值大小写污染；promptService 两条过严断言；shellPolicy 缺引号用例 | **high**（首条；余三条 low） | 深查 #1～#4 | shellPolicy 的 symlink 回归用例转绿并新增 junction 场景用例（含指向工作区自身的 junction + `..` 上爬）；不含 `..` 的快路径行为逐字节不变；反向验证判红复原；三套 tsc 与相关测试全绿 |
| T059 | 家目录改为独立 user 层（[决策 016](decisions/016-home-tier-instruction-gating.md)，Q015 结案）：家目录从 project 链移除、改作 user 层 global，**对所有项目生效**（含工作区不在家目录下的）；全局层只取一份，顺序 `~/.pilab/AGENTS.md` → `~/.claude/CLAUDE.md` → `~/.codex/AGENTS.md`，找到即停、都没有则为空；全局层不读 `CLAUDE.local.md`；家目录之上的多用户共享目录（`C:\Users` / `/home`）全部跳过；globals 与 project 链做文件级 canonical 去重作兜底；baseline 脚本传 `settingSources: []` 保住机器无关性 | low（语义地雷，当前不可触发） | Q015 | 工作区不在家目录下时也读到全局文件；`settingSources` 含/不含 user 时读/不读各一条；三项全缺时全局层为空；家目录之上的层不读；家目录不在链上时链行为不变；global 与链命中同一文件只出现一次；未信任项目仍读 user 层、project 链全不读（既有语义，非本次引入）；层内 first-one-wins；预算 user 层先于 project 消费；反向验证判红复原；三套 tsc 与相关测试全绿 |

### 批次 D4（上机前，开发机点验修补，2026-09-17 立项）

来源：T032 开发机组 37 项点验产出的 26 条疑似缺陷 D1～D26（逐条台账见 [defects-ledger.md](evidence/batch-e-devbox-2026-09-17/defects-ledger.md)，现场原文见[批次 E 开发机证据](evidence/batch-e-devbox-2026-09-17/README.md)的汇总节与各批「疑似缺陷清单」小节）。用户拍板「审查完毕后总结并派人修」，本批在 T033 上机之前做完，修完逐项回开发机真机复验。建议顺序：两条 high（T060 / T061）先做，其余 medium / low 按文件范围不冲突即可并行。

**2026-09-17：九项全部落地，代码 `d2bbbf13`、文档 `db8956a8`。** 经三组只读一审（T064 / T065 判 blocking）→ 回炉 R-A / R-B / R-C → 二审（rework-2 另判一条 blocking 并已修）→ 两轮开发机真机复验。逐项结论见下表验收列，完整证据 [evidence/batch-d4-fixes-2026-09-17/README.md](evidence/batch-d4-fixes-2026-09-17/README.md)。

| ID | 任务 | 严重级 | 覆盖的发现 | 验收 |
|---|---|---|---|---|
| ✅ T060 | 临时根「保存位置」设置 Main 侧恒读不到（`readSettings()` 读 settings.json 顶层，用户设置在 `aiclient-settings.state`），scratch 根永远回落默认目录；顺带复现定位 D6（未绑定会话运行中从侧栏消失、重启回来） | **high** | D13、D6 | Main 经统一设置读取入口拿到 defaultTemporaryPath；改设置后新建临时对话的 scratch 目录落在新根；用例钉住读取路径；DEV-2 补格真机复验 ✅；衍生 [Q018](open-questions.md)。**已落地（2026-09-17，未提交）**：一审 pass-with-notes、无回炉，真机复验 ✅（新根落点 / 重启后索引 / D6 侧栏不掉）→ [证据](evidence/batch-d4-fixes-2026-09-17/README.md) |
| ✅ T061 | 会话总预算拒绝后 store 中毒成静默死会话（总预算检查在写队列内、tail 变已 reject 的 promise），附件名不落盘重开后丢失 | **high** | D1、D24、D25 | 总预算改队列外预检；一次 `session_size_limit` 拒绝后同一会话纯文本可发或给出可见错误，静默丢弃判负（`sessionAttachmentFill.test.ts` 改断言可写）；附件 name 落盘并回放；DEV-33 真机复验。**已落地（2026-09-17，未提交）**：一审 pass-with-notes（证伪「未知键到不了 provider」）→ 回炉 R-C 在 runtime 出网口剥键 → 二审 pass，两轮真机复验 ✅（含 `pi-messages` 请求体取证）→ [证据](evidence/batch-d4-fixes-2026-09-17/README.md) |
| ✅ T062 | 模型目录两侧口径不一致（选择器读磁盘 models.json、worker 用凭据库内存目录），只存在于磁盘的 provider「列得出、发不出」；H/21 模型缺失中文覆盖层已触发不到（`isModelMissingError` 不认 `no model "…" in the catalog`） | medium | D3、D19 | 两侧用同一份目录；覆盖层认现行错误信号；DEV-32 GUI 侧复验。**已落地（2026-09-17，未提交）**：一审 pass-with-notes → 回炉 R-A 补中文覆盖层卡 → 二审 pass-with-notes，复验第一轮半生效、第二轮 ✅ 完全成立 → [证据](evidence/batch-d4-fixes-2026-09-17/README.md) |
| ✅ T063 | 兼容根 `~/.agents/subagents` 定义编辑后写成主目录影子、删除要删两次且回旧内容 | medium | D12 | 编辑写回原文件或明确提示已复制；删除一次即消失；DEV-3 复验 ✅；方案见[决策 017](decisions/017-subagent-definitions-edit-at-source-root.md)。**已落地（2026-09-17，未提交）**：一审 pass-with-notes、无回炉，真机复验 ✅（四时点：原地改、删一次即消失、重启不回来）→ [证据](evidence/batch-d4-fixes-2026-09-17/README.md) |
| ✅ T064 | worker bootstrap 失败后用户刚发的消息被吞、索引留 title 为空的空壳行 | medium | D15 | 失败时输入还回 composer 并给可见错误；不留空壳索引行；DEV-4 复验。**已落地（2026-09-17，未提交）**：一审 **blocking**（输入框非空时载荷会丢）→ 回炉 R-A 加 ↺ 回落 → 二审 pass-with-notes（blocking 关闭），真机复验 ✅ 正反两条都做 → [证据](evidence/batch-d4-fixes-2026-09-17/README.md) |
| ✅ T065 | 两个窗口对同一会话各起 `pi --session` 把会话树静默分叉；TUI 切回被挂起的聊天整屏空白 | medium | D18、D17（+ 轻项：TUI 关闭 / 会话损坏英文条、关第二窗口弹整应用退出） | 第二窗口被拦或降级并提示；切回自动重绘；DEV-14/16 复验。**已落地（2026-09-17，未提交）**：一审 **blocking**（open 失败回滚过度释放）→ 回炉 R-B 定向释放并重做 D17 触发点 → 二审 pass-with-notes（blocking 关闭），复验 D18 与关窗文案 ✅、D17 第二轮基本成立但留约 4% 偶发空白 → [证据](evidence/batch-d4-fixes-2026-09-17/README.md) |
| ✅ T066 | 可观测性：electron-log 吞 warn 致索引修复日志缺失；导入链路、归档 / worker 退出 / scratch 删除、pi TUI spawn 与退出码、provider_retry、预算拒绝各自零日志 | low | D4、D10、D14（+ 各批轻项） | 上述路径各一行落 main.log；T038 说明的「记 main.log」兑现。**已落地（2026-09-17，未提交）**：一审 pass-with-notes（门槛下降把既有未脱敏日志提拔到落盘）→ 回炉 R-C 修三处脱敏 + 预算拒绝补码 → 二审判 **blocking**（`clearInternal` 同一明文回显）已修，两轮真机复验 ✅（落点是按天日志，非 `main.log`）→ [证据](evidence/batch-d4-fixes-2026-09-17/README.md) |
| ✅ T067 | i18n 补漏：问答卡固定文案、重试横幅与 Details、导入失败原因句（字节数用 MiB）、Composer 附件六句、TUI 英文条 | low | D9、D20、D21、D26 | 中文界面无英文硬编码，DEV-11/24/25/33 复拍。**已落地（2026-09-17，未提交）**：一审 pass-with-notes（跨模块 `instanceof` 接线无用例）→ 回炉 R-C 只补用例不改生产码 → 二审 pass，真机复拍 ✅ 四处 + 原「遗留」的附件总量提示均为中文 → [证据](evidence/batch-d4-fixes-2026-09-17/README.md) |
| ✅ T068 | 卡片标题字重 `font-medium`（Win10 无 500 档）、`QuestionItem.header` 声明未渲染、Node 24 解析与横幅死代码及 dev.env / 注释漂移 | low | D23、D22、D16 | 标题改 semibold 并 Win10 复拍；header 渲染或删契约；死代码删除或接回、注释同步。**已落地（2026-09-17，未提交）**：一审 **pass**（本批唯一无 notes）、无回炉，真机复验 ✅（字重 computed 600/400、chip 就位、无 Node 24 残留）；Win10 对拍留 T033 → [证据](evidence/batch-d4-fixes-2026-09-17/README.md) |

其余缺陷的归属：D2（两个 8 MiB 上限量的不是同一个东西）归批次 F 的 T058（[决策 015](decisions/015-attachment-sidecar-for-session-budget.md) 附件旁路落地后这个接缝自然消失）、D7 / D8（Codex 子目录不可读整源静默消失且三处日志零行）归批次 F 的 T052、D11（导入面板 320 行不虚拟化）只记录不改；D5（删除临时工作区后其下聊天的去向）与「运行时错误块是否升级成卡片」转为 [Q016 / Q017](open-questions.md)。D1～D26 的逐条处置已回写进 [defects-ledger.md](evidence/batch-e-devbox-2026-09-17/defects-ledger.md) 末尾的处置表。

### 批次 E：现场

| ID | 任务 | 范围 | 验收 |
|---|---|---|---|
| T032 | 上机检查单：合并旧树待现场项（P5-2 六行、P5-4/P5-5 五行、P6-3 第 4/6 条、H/20 I5、F3 根因）、T001 后的 PERM-1 探针复跑（`scripts/run-perm1-probe.mjs`，需真实模型回合）与审计静态推断项（permissions-19、core-host-05/07、tools-10、cutover-03、P1-8 证据重采、P6-4 旧版产物互读） ；**批次 D 追加**：并入 [checklist-e.md](evidence/batch-d-audit-2026-09-15/checklist-e.md)（批评者 34 条上机必做 + 18 区域 150 项，按 windows / encrypted / utility / real-model 分组，其中标 dev-box 的项在上机前先做完）与 field-06 点名的现场项（F1～F7、GUI A/2、A/4、A/10、B/5、C/8、F/13、F/15、F3 根因）；P4-6 经接缝裁决为 incomplete，R2/R3/R4 三行从未执行，上机日必须给出结论 | 旧树第 11 批 + 审计 + 批次 D | 检查单每项有判据与取证方式。**2026-09-17：正式检查单已产出** → [checklist-e.md](checklist-e.md)（计划根下；四来源合并去重，原 150 项判据逐字搬入、新增 20 项、三条裁决、回填出口表、取证规矩）。**2026-09-17 开发机组全部完成** → [证据](evidence/batch-e-devbox-2026-09-17/README.md)：37 项（35 + DEV-36/37）分七批处置完毕，28 ✅ / 3 ⚠️ 部分成立 / 3 ⛔ 判负 / 2 ⛔ 不可执行 / 2 🚫 裁决不需人工；检查单同日更正 16 行；26 条疑似缺陷转批次 D4 与 Q016 / Q017；收口全量 413 文件 / 6271 条全绿。未提交 |
| T033 | 最后一次上机：加密 Windows 一次性全量验收。**2026-09-17 上机日执行单已备** → [topics/t033-field-day-runbook.md](topics/t033-field-day-runbook.md)（装机与旧包获取、上机前 15 项自检、按检查单 §1 的执行顺序把 134 项排成十余轮、34 条必做的编号映射、三刀砍单顺序、远程协作与证据命名；逐项分片六份）；上机前的打包构建已完成（run [35295618831](https://github.com/p1p1dan/ai-client/actions/runs/35295618831)，源码 `13e6cdb7`，七个 job 全 success，三份 worker-smoke 已归档 → [证据](evidence/batch-e-build-2026-09-18/README.md)，**WIN-19 据此可标 ✅**）；可提前造的样本已备 → [field-samples](evidence/batch-e-devbox-2026-09-17/tools/field-samples/README.md) | T032 | 逐项取证进 evidence（目录约定 `evidence/batch-e-field-<date>/`） |

### 批次 F：补审修补（上机后）

归属 fix-after-e 的 6 组（T053 因 2026-09-18 T033 现场反馈 #5 提级至批次 H，任务 ID 不变，见下方批次 H 表）；上机日不依赖它们，现场之后修。

| ID | 任务 | 严重级 | 覆盖的发现 | 验收 |
|---|---|---|---|---|
| T049 | utility 通道的超时预算与 AI 三功能的产品侧表现：src/main/services/agent-host/PiUtilityService.ts、src/main/services/ai/{code-review,branch-name,commit-message}.ts、src/renderer/components/settings/AISettings.tsx、source-control/{CodeReviewModal,CommitBox}.tsx——一次性补全通道从设置到结果渲染的一整条。（已并入 utility-07） | medium | utility-01, utility-02, utility-03, utility-04, utility-05, utility-06, utility-08, utility-09, utility-10 | 有一张成文的超时预算表：冷启动与热请求分开、Main 侧与 worker 侧保持严格不等式（照 /compact 的 60s>45s 做法），worker 侧那份真的能轮到；设置页的超时有上界且越界在输入处就拒绝，不再到 RPC 载荷守卫那里报成「缺 id / cwd / prompt」；模型下拉查不到已存值时回写而不是只显示「自动」；分支名过 stripCodeFence；评审弹窗在已有流式内容时错误不再顶掉正文；三个功能的错误文案过 t() 而不是直出引擎英文串；评审弹窗标题默认「自动」时不显示空括号；AI 设置页不再硬编码宿主状态为 ready、目录不可用时有说明。src/main/services/ai/* 与 stores/codeReview.ts 补上第一批用例。 |
| T050 | 能力目录的诊断没有出口：MCP / 技能 / 子代理：src/runtime/worker/nativeWorkerRuntime.ts 的 capabilities()、src/shared/types/workerRpc.ts 的 WorkerCapabilityInventory、src/renderer/components/workspace-shell/LeftDock.tsx 的 Capabilities 对话框、src/main/services/agent-host/subagentCatalog.ts 的 read() 与写侧、src/runtime/plugins/mcp/config.ts 的上限切片——一个根因：限额与加载失败在 runtime 侧执行，在用户看得到的地方没有任何位置。 | medium | d-cross-01, d-cross-02, d-cross-03, concurrency-08, main-aux-09 | 能力清单带诊断通道（source / code / path / message，按条数封顶），Capabilities 对话框在对应分组下列出「为什么它不在列表里」；子代理管理界面不再自己重算目录（复用 loadSubagentCatalog + mergeSubagentDefinitions），或至少套上同一个 16 条上限并把越界行标成「不会被加载」；超过 16 台的 MCP 服务器以 ok:false + 说明出现在面板里而不是消失；SkillsPlugin.refresh() 接上生产调用方（或明确删除并改注释），多会话的技能菜单不再长期分叉；兼容根 ~/.agents/subagents 的删除与改名语义修正（不再只删影子、不再直接删掉兼容根下的原文件）并补测试。诊断 message 目前是英文裸串，这一组要顺带决定是结构化交给渲染层查词典还是接受英文。 |
| T051 | 重开会话的回放覆盖，以及权限 / 问答从未落盘这件事：src/runtime/plugins/permissions/ 与 plugins/tools/ask.ts 的落盘（目前零 session 引用）、src/agent-host/piSessionTimeline.ts 的投影、src/renderer/stores/chatSessions.ts 的 mapHistoryBlock——必须三侧一起改，只改渲染层修不好。 | medium | chat-event-12, d-cross-04 | 审批决定与问答回答以结构化 custom 条目写进会话文件（工具、路径/命令、决定、来源、时间戳、questionId 与所选项），投影层映射成 HistoryBlock，回放后权限卡行、审批审计行与已答问答卡都能还原；新条目计入会话字节预算并过统一脱敏；补一条「批准三次 + 回答一次 → 重开会话 → 四项都在」的端到端用例。若决定不做持久化，则必须改掉 mapHistoryBlock 上方那条「用和实时 runtime 分支相同的字段用法」的注释，并在界面上说明这些行不保留。 |
| T052 | 会话导入上游：扫描容错、Codex 转换与限额对账：src/main/services/legacyImport/ 下的 CodexSessionScanner.ts、CodexRollout.ts、ClaudeSourceAdapter.ts、legacyImportSanitization.ts 与 src/shared/types/legacyImport.ts——导入内容正确性的一整片。 | medium | import-up-01, import-up-02, import-up-03, import-up-04, import-up-05, import-up-06, import-up-07, import-up-08 | 一个读不动的子目录或超过 1 万个文件不再让整份 Codex 历史从导入列表里静默消失（部分失败要报出来）；以「<」开头的真实用户消息不再被当合成注入丢弃；导入一条会话不再重扫重解析整个 ~/.codex/sessions；上游的 64 MiB / 4000 条 / 64 KiB 口径与 32 MiB 会话预算对账，越界在入口就拒而不是走完整条链路在最后一步失败；display 条目 title 的生产侧约束与 worker 校验（非空且 ≤ 256）对齐；进模型上下文的正文也过脱敏且认得 JSON 形态的密钥；Codex 思考块的 summary / content 口径与同仓 codexItemMapper 一致、丢弃要记诊断；补一条把真实转写结果喂给 native 写入器的用例。 |
| T054 | 退役残留清扫、宿主边界守卫与 CI 门禁：src/main/services/agent-host（tier 通道、WorkerSlotDiagnostic、replaceCrashedTransport、index.ts 桶文件）、src/renderer/components/chat/hostStatus.ts、src/agent-host/userResourcePaths.ts、src/runtime/__tests__/hostBoundary.test.ts、SessionIndexService 周边的次要接线、.github/workflows/build.yml 与 vitest.config.ts——一批「删掉或接上，并把替它背书的注释一起改掉」。 | low | main-host-05, main-host-06, main-host-07, main-host-08, chat-event-08, ah-lib-04, ah-lib-05, ah-lib-06, terminal-05, terminal-06, session-index-03, session-index-06, session-index-07, session-index-08, session-index-10, baseline-01, baseline-02, spike-02 | tier RPC 通道、五种 WorkerSlotDiagnostic、replaceCrashedTransport 状态机、agent-host 目录桶文件、host.ready / host.error 与 Node 24 指引分支、userResourcePaths.ts：逐个给出「删除」或「接上生产者」的结论并落地，注释同步；宿主边界守卫扩到覆盖 src/runtime → src/agent-host 这条边并把规则写成文；NativeSessionIndexAdapter 与它的集成测试给出去留结论；chat:createSession 不再传索引不存在的 effort 字段；重命名要么接上 worker 协议让会话文档的 name 真的被写入、要么在界面上说明只改索引；fork 走和其他会话变更一样的 TUI 交接；mergeSessionIndex 的 orphaned 接上消费者，会话因目录消失而蒸发时有提示；TUI 守卫用例的断言改成与现状一致（TUI 现在靠 --session 续接）；TUI 活动指示灯不再拿 terminalId 当会话 id 查表；日常提交与 PR 触发测试；D9 缓存命中率的单测重新纳入 vitest / CI；bootstrap 的 plugin_graph_incomplete 兜底路径补一条用例。 |

| T058 | 附件旁路文件 + 引用（[决策 015](decisions/015-attachment-sidecar-for-session-budget.md)，Q014 结案）：附件字节不进会话 JSONL、另存旁路目录、JSONL 只存引用；写入 / 回放 / 导出 / 脱敏 / 字节预算 / 渲染层一体改造 | medium | Q014（capacity-01 第八节的算术张力） | 会话 JSONL 不再内联附件字节，极端构造不再能以个位数消息打满 32 MiB 预算；旁路文件计入统一脱敏（T042）与磁盘预算；导入 / 导出 / 回放对引用的解析各一条用例；T058 落地前维持 32 MiB / 5 MiB / 8 MiB 现状数值 |

### 批次 F 追加（2026-09-17 用户拍板，决策 018 / 019 / 020）

T071 严重级 medium（可能误伤用户已有项目数据），在本批次内建议优先于 T069 / T070 执行。

| ID | 任务 | 严重级 | 覆盖的发现 | 验收 |
|---|---|---|---|---|
| T069 | 删除临时工作区前确认并把其下聊天转回未绑定分组（[决策 018](decisions/018-temp-workspace-removal-keeps-chats-visible.md)，结 Q016） | low | D5、Q016 | 删除前确认框列出该目录下的对话条数；删除后这些聊天转为未绑定会话，在未绑定分组下可见、可打开；补一条用例 + 一次真机复验 |
| T070 | 运行时错误统一改成带标题与下一步的引导卡片（[决策 019](decisions/019-runtime-errors-as-guided-cards.md)，结 Q017） | low | Q017、DEV-24、DEV-33 判据问题 | 重试预算耗尽与会话超预算两种错误均改为带标题的卡片，原始报错文字折叠进「详情」，按钮可用；复用 T062 的 `ModelMissingNotice` 骨架；中文界面下卡片文案无英文硬编码；补一条用例 + 一次真机复拍 |
| T071 | 临时工作区根固定加一层应用专属子目录（[决策 020](decisions/020-temp-workspace-root-app-owned-subdir.md)，结 Q018） | medium | Q018、T060 审阅备注（`evidence/batch-d4-fixes-2026-09-17/reviews/group1.md` T060 note 1） | 保存位置指向任意目录时，只在其专属子目录内创建与认领临时工作区；把保存位置设成真实项目的父目录后不再对该项目执行 `git init`（用例先复现原风险判红，修后转绿）；默认路径（`~/JYWAI/temporary`）下行为不变；已存在的临时工作区仍可正常打开；补一次真机复验 |

### 批次 G：聊天区信息层级与授权交互（2026-09-18 用户直接提出，已全部落地）

来源不是审计也不是点验，是用户当日提出的五条界面诉求（附参照截图 `sharePic/20260918/`，未纳入版本库）。第 4 条含一处分叉，经三候选拍板结为[决策 022](decisions/022-permission-gate-serialized-single-docked-card.md)；时间线一项推翻了 2026-08-29 与 2026-09-10 两条旧裁定，结为[决策 021](decisions/021-turn-work-group-restores-elapsed-and-fold.md)。三个提交 `17eb4581` / `0144320e` / `cd734d61`，**未推送**。落地记录与遗留缺口见[批次 G 证据](evidence/batch-g-chat-ux-2026-09-18/README.md)。

| ID | 任务 | 严重级 | 来源 | 验收 |
|---|---|---|---|---|
| ✅ T072 | 回合过程收进「工作组」：最后一个 `answer` 段为最终输出留在组外，其余进组；运行中 header 显示「工作中」并展开，结束后变「已工作 xx 秒」并自动折叠；三档对比度（[决策 021](decisions/021-turn-work-group-restores-elapsed-and-fold.md)） | — | 用户诉求 5 | 交替形态只有最后一段散文在组外；以 notice 收尾时最终输出仍可见（FB4 回归）；纯文字回合不渲染工作组；用户手动展开后不被自动折叠覆盖；时长未知回落步骤数且不渲染任何秒数。**已落地（`cd734d61`）**：新增 `turnProcessFold.test.ts` 21 条 + `turnTiming` 7 条 + `chatTimelineLayout` 5 条，6 组反向验证 |
| ✅ T073 | 移除「授权详情」折叠条 | — | 用户诉求 3 | 静默自动放行的授权不再有界面出口（数据与日志仍在，`includeAllowed` 为回头路并有测试守着）；拒绝/报错的 `PermissionActivityRows` 保留。**已落地（`cd734d61`）** |
| ✅ T074 | 授权闸串行化 + 浮层单卡（[决策 022](decisions/022-permission-gate-serialized-single-docked-card.md)） | — | 用户诉求 4 | 并发多调用只发一张卡；排队中不发事件不起计时（假计时器钉死）；FIFO；自动放行不进队列；`permission.requested` 带 `queuePosition` / `queueDepth`；浮层在 Composer 上方非模态，`queueDepth > 1` 才显示进度；内联退化为冻结副本；红线用例断言「恰好一张可答卡且在浮层里」。**已落地（`17eb4581` + `cd734d61`）**：runtime 10 条用例 6 组反向验证，渲染层 12 条用例 |
| ✅ T075 | 新建会话幂等：四个入口先判空再决定停留 / 原地改目标 / 新建 | — | 用户诉求 1 | 目标一致则停留、跨项目则 retarget 复用、非空则照旧新建；不给 `ChatSession` 加字段；不产生 `session-index.json` 写入。**已落地（`0144320e`）**：28 条用例，3 组反向验证。编排者补正两处——判空谓词改用 `computeEverHostBound`（原判据会把磁盘恢复、转录未回放的真实会话误判为全新并改写其绑定）、右键菜单入口补接线 + 三条静态测试改断言 |
| ✅ T076 | 「Branches」按钮从滚动消息区移进顶栏，文案改用既有词条 | — | 用户诉求 2 | 按钮出现在 `SessionBar` 不在 `MessageTimeline`；文案走 `t('Session branches')`（「会话分支」）；显示条件 `runtimeIdentity != null`、禁用条件严格 `idle`（刻意不复用顶栏 `busy`，它只认 `running`/`starting`），且只此一份定义。**已落地（`cd734d61`）**：4 条用例 |

### 批次 H：T033 现场反馈修复（2026-09-18 用户测试机现场反馈与根因分析）

来源：2026-09-18 用户在 Windows 测试机上使用打包 Build #59（源码 `13e6cdb7`，早于批次 G，不含 `b24d5c81` / `17eb4581` / `0144320e` / `cd734d61` / `d119da25`）产生的十条反馈，均已由只读排查在代码里定位根因，详见 [topics/t033-field-day/07-findings.md](topics/t033-field-day/07-findings.md)。其中两处结为新决策：[决策 023](decisions/023-bypass-permissions-tier.md)（第四档权限模式「完全放行」）与[决策 024](decisions/024-prompt-cache-ttl-split.md)（prompt cache TTL 主/子分离）；工具耗时数据另结为[决策 025](decisions/025-search-tool-native-over-pi-fff.md)（搜索工具优先自建，`@ff-labs/pi-fff` 仅作设计参考）；授权记忆的放行粒度结为[决策 026](decisions/026-permission-grant-granularity.md)。T053（原属批次 F）因反馈 #5 提级并入本批，任务 ID 不变。ID 前缀状态符沿用本文件顶部图例（⬜ 未开始 · 🔧 进行中 · ✅ 已落地 · ⏸ 推迟）。**2026-09-18 同日代码落地并分 13 个本地提交**：T077 / T078 / T079 / T080 / T081 / T053 / T082 / T083 / T084 / T086 十项已完成实现并通过全量测试，**已提交（本地未推送）**，效果留给 T033 第二轮上机日现场确认；T085 仍未开始（阻塞于 [Q019](open-questions.md)）；T087 维持推迟。批次内新增的 Q022～Q026 均已结案，待拍板列表只剩 [Q019](open-questions.md)。落地明细、验证结果、本地点验状态见[批次 H 证据](evidence/batch-h-field-fixes-2026-09-18/README.md)。**2026-09-18 下半场**：本地点验（两轮）与真机联调期间另发现两类不属于上述十条反馈的问题，当日修复并新开 T088 / T089（见下方「批次 H 追加」表）；T080 副判据 FAIL 已修复（`03e0b6d8`，见上方 T080 行）；[Q021](open-questions.md) 已完全结案，且批次立项时「输出慢＝5 分钟缓存 TTL」的判断被同日实测推翻，详见 [perf-2026-09-18.md](evidence/batch-h-field-fixes-2026-09-18/perf-2026-09-18.md)。批次 H 本地提交合计 17 个，均未推送。

| ID | 任务 | 优先级 | 来源 | 验收 |
|---|---|---|---|---|
| ✅ T077 | 缓存 TTL 主/子分离 + 设置项：prompt cache TTL 从当前统一档位拆分为主对话 1 小时、子代理 5 分钟两档，并暴露为设置项，对齐 Claude Code 客户端已有的 `promptCacheTtl` / `subagentPromptCacheTtl`（[决策 024](decisions/024-prompt-cache-ttl-split.md)） | P0 | 现场反馈 #8 | 主对话与子代理的 TTL 可独立设置且默认值符合决策；落地后用真实请求的 cacheWrite 触发情况分桶验证中转是否透传 `ttl: '1h'`（结果记入 [Q021](open-questions.md)）。**已落地（`8f39e3d3`，本地未推送，2026-09-18）**：新 `src/shared/types/promptCacheTtl.ts`，通路贯穿 renderer 设置 → main → bootstrap → agent-loop，SDK 实跑验证 long 档确实产出 `cache_control.ttl:"1h"`；开发机 CDP 点验抓到真实请求体，主对话三处缓存标记带 `ttl:"1h"`、子代理不带（PASS）。**~~`cacheWrite1h` 恒为 0，中转是否透传待验~~ 已于 2026-09-18 推翻**：那批观测采集于 09-17，早于本任务落地（09-18 08:09）一整天，当时主对话本就是 5m 档；中转一侧经用户在平台核实为如实透传。剩余只是端到端确认，见 [Q021](open-questions.md)。**2026-09-18 下半场两处更正**：① [Q021](open-questions.md) 已完全结案——真实请求端到端确认中转如实透传 `ttl:'1h'` 且上游按 1 小时处理（`cacheWrite1h=10742` 后间隔 13.5 分钟仍 `cacheRead` 命中）。② **本任务当初立项时『解决输出慢』的因果判断被推翻**：缓存命中相对冷缓存只省 2.3 秒（28.2s→25.9s），量级远小于「首字 6.8s→29~51s」的现场落差；真正的大头是思考 token 量（关闭思考后总时长降 64%）。[决策 024](decisions/024-prompt-cache-ttl-split.md) 主档 1 小时依然成立（省成本、省这 2.3 秒），但不再是「输出慢」的解药，该决策已追加注记更正。完整数据见 [perf-2026-09-18.md](evidence/batch-h-field-fixes-2026-09-18/perf-2026-09-18.md)。详见[批次 H 证据](evidence/batch-h-field-fixes-2026-09-18/README.md)。 |
| ✅ T078 | 完全放行档：在手动/建议/全自动三档之上新增第四档「完全放行」（bypass permissions），bash 静态分析解析不出的路径（`unresolvedPaths`）也直接放行不弹卡；进入该档需显式二次确认，激活期间界面持续可见提示（[决策 023](decisions/023-bypass-permissions-tier.md)） | P0 | 现场反馈 #2 | 四档权限模式可选且互斥；「完全放行」档下 `unresolvedPaths` 非空的命令不再弹授权卡；进入前有二次确认交互；激活期间界面有持续可见的档位提示。**已落地（`0a97e6d3`，本地未推送，2026-09-18）**：`PermissionGear` 新增 `bypass`，仍保留白名单/用户 deny/内置路径黑名单/plan 裁剪/deny scope 五类硬拒绝；更正一处前提——默认策略无内置 `rm` 拒绝，bypass 下 `rm -rf` 直接执行（已由[决策 023](decisions/023-bypass-permissions-tier.md)追加注记结案 Q022：不加破坏性命令黑名单）。详见[批次 H 证据](evidence/batch-h-field-fixes-2026-09-18/README.md)。 |
| ✅ T079 | SelectPopup zIndex 修复：`ProviderSetupDialog.tsx:193` 与 `:237` 两处 `SelectPopup` 漏传 `zIndex`，默认落到 `DROPDOWN`（40）被 `MODAL_CONTENT`（51）盖住，导致设置页「添加 AI 服务」下拉不可见；补齐这两处，并核查全仓另外 4 处同类漏传（`select.tsx:65`、`z-index.ts:14,18,19` 附近的调用点） | P0 | 现场反馈 #1 | 设置页「添加 AI 服务」下拉在弹窗内可见可点；全仓 `SelectPopup` 调用点补一条 zIndex 传参的静态守卫用例，防止同类漏传再次出现。**已落地（`c18d92dc`，本地未推送，2026-09-18）**：11 个设置页文件 29 处补齐、`ProviderSetupDialog` 补 `zIndexLevel="nested"`、三个 `AlertDialog` 补 nested；Menu 类弹层核实目前均不出现在 Dialog 内，暂不构成同类问题。详见[批次 H 证据](evidence/batch-h-field-fixes-2026-09-18/README.md)。 |
| ✅ T080 | 运行中切权限档：先拆掉 `configure()` 的副作用（清空 grants 并递增 epoch 会让排队中的授权请求被拒），再放开挡住「运行中切权限档」的三道锁：`ComposerPermissionTrigger.tsx:126`（sending 时禁用）、`WorkerManager.ts:2085`（activeRequestId）、`nativeWorkerRuntime.ts:889`（assertIdle） | P1 | 现场反馈 #3 | 回合运行中可以切换权限档；切换不会让排队中的授权请求被误拒；三道锁分别有对应的用例覆盖新行为。**已落地（`6875fa47` + `0a4f6f61`，本地未推送，2026-09-18）**：只放开档位（gear）、模式（mode）仍锁死；新 RPC `worker.setPermissionGear`；变宽时排队中请求按新档重判并放行。**Q025 已结案**：不区分「因放宽档位而放行」与用户主动点允许，新增的 wire 枚举值 `autoReason:'gear_widened'` 已删除，改按普通 `allow` 结算（用户原话「不要区分」），修正提交 `0a4f6f61`。**副判据闭环（2026-09-18 下半场，提交 `03e0b6d8`）**：本地点验第一轮发现「卡挂着时 mode 单选项应灰掉」未生效（六项全可点），根因是 `ComposerPermissionTrigger` 的锁跟随 `ChatComposer` 的本地发送闩 `sending`，只覆盖 IPC 在飞的一小段，等授权卡出现时早已回落成 false；改为跟随 `busy` 与 `sending` 的并集，prop 同步更名 `turnActive`。第二轮复验 PASS：卡挂着时两个模式项 `aria-disabled="true"`、四个档位项可点，菜单文案换成「本轮对话进行中只能修改权限档位。」。详见[批次 H 证据](evidence/batch-h-field-fixes-2026-09-18/README.md)。 |
| ✅ T081 | 授权记忆放宽匹配：当前只有 allow-session 写入 grants，且 `grantKey` 是 `[tool, path, command, paths]` 的精确 JSON 串（纯内存，重启即丢），稍有出入就要重新申请；放宽匹配规则以减少同类操作的重复弹卡 | P1 | 现场反馈 #4 | 同一会话内对同类命令/路径的重复调用不再反复弹授权卡；放宽后的匹配规则有对应用例，且不放宽安全边界之外的范围。**已落地（`4721cede` + `6913c264`，本地未推送，2026-09-18）**：新 `permissions/grants.ts`，bash 记 `[前缀, 工作区根]`（多子命令工具取前两词）；新增会话 JSONL 持久化（custom 条目 `aiclient.permissionGrants`）。**Q023 已由[决策 026](decisions/026-permission-grant-granularity.md)结案**：文件类记忆改为只记批准的那一个文件（不再放大到父目录/工作区），对齐 Claude Code；bash 前缀粒度维持不变；存活期维持随会话持久化、不跨会话，修正提交 `6913c264`。详见[批次 H 证据](evidence/batch-h-field-fixes-2026-09-18/README.md)。 |
| 🔧 T053 | 事件协议的无生产者字段与「丢事件不可检测」：src/shared/types/runtimeEvents.ts 的字段清理、src/runtime/plugins/tools/index.ts 的 tool.updated、src/runtime/events/projector.ts 的 usage.updated、src/renderer/components/chat/ 下为这些字段保留的分支，以及 runtimeToolVocabulary 的对账测试。 | P1（原批次 F 严重级 low） | 批次 D 发现 chat-event-03/04/06/09/10/11、chat-tool-08；T033 现场反馈 #5（`projector.ts:520` `buildPiUsagePayload` 第二参硬写 undefined、`contextSurfaceModel.ts:576-585` 整体替换，导致委托时上下文占用归零）提级并入本批 | 每个没有生产者的字段二选一：接上生产者，或连同渲染层的分支一起删掉，不留「注释说它还在用」的状态；子代理花费那条 usage.updated 带上 context，上下文占用徽标不再消失；seq 要么接上消费者并在丢事件时可检测（Main 丢弃发生在编号之前），要么在协议注释里明说它不承担这个职责；补一条测试拿 runtime 注册表对账渲染层的所有查表点；golden 录制补上目前缺的六类事件（tool.updated / session.failed / session.stopped / session.stderr / custom.* / preview.requested）。**已落地（`97c08b58`，本地未推送，2026-09-18）**：**只处理了现场反馈 #5 提级的那一半**——`projector.ts` 缓存 `lastContextUsage` 供 `delegated()` 复用，两份 golden fixture 重录；批次 D 原发现的 chat-event-03/04/06/09/10/11、chat-tool-08（其余无生产者字段、seq 可检测性、六类事件录制）**未在本轮处理**。详见[批次 H 证据](evidence/batch-h-field-fixes-2026-09-18/README.md)。 |
| ✅ T082 | 保管箱变更触发 auth.json 写盘：`PiModelConfigService.writeRuntimeConfig()`（`:645-661`）目前只在改自建服务、App 启动、账号迁移、手动同步四个时机重写 `auth.json`，保管箱（vault）本身的变更不触发；TUI 是独立进程只读磁盘上的 `auth.json`（`PiTuiPty.ts:163-184`），因此保管箱改了 TUI 也请求不到新增的 provider。补上「保管箱变更」这个触发时机 | P1 | 现场反馈 #6 | 保管箱内容变化后，无需应用重启或手动同步，TUI 端下一次请求即可读到更新后的 `auth.json`。**已落地（`04984979`，本地未推送，2026-09-18）**：**根因经复核更正**——五个写入点其实都已挂重写钩子，缺口是异步钩子不等待/不捕获失败造成的时序竞态窗口，非完全无触发时机（见 [07-findings.md](topics/t033-field-day/07-findings.md) 现象 #6 更正）；修法是 `CredentialVault` 新增 `onChange`，`main/index.ts` 接 `wireVaultAuthJsonResync` 合并重写。详见[批次 H 证据](evidence/batch-h-field-fixes-2026-09-18/README.md)。 |
| ✅ T083 | 工具提示词补强 + glob 性能：提示词引导模型用 grep/glob 工具而不是 bash 硬跑搜索命令（现场实测模型用 bash 跑 `grep -ran` 耗时 183 秒、`find .` 耗时 120 秒）；优化 glob 工具在大目录树（Windows 实测两次调用平均 50.8 秒）下的性能（[决策 025](decisions/025-search-tool-native-over-pi-fff.md)） | P1 | 现场反馈 #8「工具耗时」小节 | 提示词改动后同类任务不再优先选 bash 硬跑 `grep -ran` / `find .`；glob 大目录树基准测试有可比数字。**已落地（`8ba25048`，本地未推送，2026-09-18）**：`walk()` 改进目录一次 + seen 集合防环、正则/路径策略按需缓存、新增目录级 `.gitignore` 识别；Linux 基准（18,848 文件）glob 9,877ms→1,241ms、grep 16,551ms→4,667ms；**Windows 未实测**，留 T033。详见[批次 H 证据](evidence/batch-h-field-fixes-2026-09-18/README.md)。 |
| ✅ T084 | 上下文浮层改点击：当前上下文占用的展示/触发方式改为点击交互（具体形态待实现时细化） | P2 | 用户诉求 | 上下文浮层从当前交互方式改为点击触发；不影响该浮层原有信息。**已落地（`3d2c3cfd`，本地未推送，2026-09-18）**：`ComposerUsageChip` 由悬停 Tooltip 改为受控 Popover；`popover.tsx` 新增 `zIndex` 属性并把硬编码 `z-50` 改成 `Z_INDEX.DROPDOWN`，连带影响 `UserFooterPill` 账号卡层级，**未点验是否有副作用**。详见[批次 H 证据](evidence/batch-h-field-fixes-2026-09-18/README.md)。 |
| ⬜ T085 | 子 agent 独立展示位：为子代理的执行过程新增独立展示位（当前只能在工具行下展开，见 `ToolRows.tsx:164-165` 挂 `SubagentActivity`），具体形态（侧边抽屉 / 独立浮窗 / 可钉住面板）待拍板 | P2 | 现场反馈 #9 | 阻塞于 [Q019](open-questions.md)；拍板后据此细化验收标准 |
| ✅ T086 | TUI `new` 解绑至少加提示：pi 自己的 `SessionManager` 切到新路径时，GUI 侧的 `session-index.json` 无目录扫描、无 `fs.watch`（`SessionIndexService.ts:94-96`），导致静默解绑；至少给出一个可见提示 | P2 | 现场反馈 #7 | 执行 `pi` 侧 `new` 命令后 GUI 至少有可见提示，不再是纯静默；不要求本任务解决底层的目录扫描/`fs.watch` 缺失。**已落地（`b930b5b9` + `f3b658d4`，本地未推送，2026-09-18）**：终端停止时扫描目录快照 diff，发现未登记的新 `.jsonl` 先用系统通知报路径（`b930b5b9`）。**Q026 已结案**：用户期望是新会话出现在侧栏；核实 pi `/new` 写出的会话文件与本应用格式只差首行头，runtime 打开时已有自动转换（`legacy.ts` 的 `prepareSessionConfig` 转成 `.native-v4.jsonl` 并 resume），此前「登记会变成点开报错」的判断有误，已更正——现在终端关闭时读文件头登记进索引并广播刷新侧栏，登记失败才退回弹通知；spawn TUI 时显式传 `--session-dir dirname(sessionFile)`，关掉工作区 `.pi/settings.json` 可改落点的漏洞，修正提交 `f3b658d4`。详见[批次 H 证据](evidence/batch-h-field-fixes-2026-09-18/README.md)。 |
| ⏸ T087 | 网关对 claude 模型 503/超时：现场两段短测试里 claude provider 一次未成功、142 秒零输出，是独立问题，与现象 #8 的「慢」无关，本批（批次 H）不实现，另行跟进 | P3 | 现场反馈 #10 | — |

### 批次 H 追加（2026-09-18 下半场，本地点验与真机联调期间当日新发现、当日修复）

来源不是 Build #59 的十条现场反馈，是当天本地点验（[pointcheck](evidence/batch-h-field-fixes-2026-09-18/pointcheck/README.md)）与真机联调过程中新发现的两类问题，发现后当日直接修复。三个新提交均**未推送**：`a72e0337`（登录入口空操作）、`23abdd48`（登录/退出登录前确认）、`99ab7d57`（A 轮期间关闭本机配置入口）。T080 副判据 FAIL 的闭环提交 `03e0b6d8` 已并入上方批次 H 表 T080 行，不单列任务号。批次 H 本地提交合计从 13 个增至 **17 个**。

| ID | 任务 | 优先级 | 来源 | 验收 |
|---|---|---|---|---|
| ✅ T088 | 应用内五处「去登录」入口修复为真正可用，且去登录 / 退出登录前先确认并列出这次会失去什么：原先账号卡片登录按钮、退出登录、消息流两处失败卡片、终端一处，只派发一个没有真正处理者的事件，门禁路由看的是主进程 `hasEnteredApp()` 闩锁，点击后界面毫无反应；退出登录还误导用户「需要重新注册」，实际只是原地变成未登录 | P0 | 现场发现 → 当日修复 | 五处入口点击后真的能路由回登录页；退出登录前先弹确认框列出具体影响（正在跑的回合、终端、未发送草稿等按「最违背直觉的排最前」排列），无可丢失内容时不弹框直接走；宿主未挂载时 fail-closed 拒绝。**已落地（`a72e0337` + `23abdd48`，本地未推送，2026-09-18）**：新增 `auth:requestSignIn` 通道（`src/main/ipc/auth.ts:150` 起），按序 `setCredentialMode('managed')` → `refresh()` → `clearAppEntry()`（顺序不能换：local 模式下先 `refresh()` 会把仍然有效的账号误判为未登录）；五处入口统一走新 hook `useSignInRequest`。确认清单经代码核实两处推翻先前假设——运行中回合期间去登录会导致渲染层事件订阅摘除、输出永久卡住且授权卡画不出来（此前以为「主进程里的会话不受影响」只对了一半）；编辑器未保存标签实测不会丢（`closeAllFiles` 生产零调用），文案改写为「尚未写入磁盘」而非「会丢失」。新增测试 `authRequestSignIn.test.ts`（178 行）、`userProfileSignIn.test.ts`（+155）、`signInConfirmFlow.test.ts`（328 行）、`signInLossModel.test.ts`（162 行）。 |
| ✅ T089 | A 轮测试期间关闭「使用本机已有配置」欢迎页入口（阶段性措施） | P2 | 现场发现 → 当日修复（阶段性措施） | 按钮置灰不可点击、不可聚焦，避免测试期间出现两条凭据路线并存；不删除任何 local 模式相关代码。**已落地（`99ab7d57`，本地未推送，2026-09-18）**：开关是 `WelcomeView.tsx` 的 `LOCAL_SETUP_ENTRY_DISABLED` 常量，当前置 `true`。**撤销方式**：A 轮结束后把该常量改回 `false` 即可放开，按钮、处理函数与 `enterApp('local')` 通路均原样保留，未删除任何代码。与提交信息里提到的「决策 D68」（本 worktree 未保留该文档，仅存在于历史提交记录）不冲突——D68 禁止的是依据机器探测结果灰掉按钮（两个方向都会判错），本次禁用不向机器询问任何事，在配置完好的机器上长相完全一致，是产品阶段性关闭路线而非机器探测。新增测试 `welcomeLocalEntry.test.ts`（96 行）。 |
| ✅ T090 | MODEL 组 09-19 点验缺陷四项：F1 命中跳行只在编辑器已开其他文件时不生效、F2 命中列表弹层锚在视口左上角、F5 Stop 收尾把上下文徽标清成 0%、F6 时间线审批行读不出子代理名字 | P1 | [evidence/batch-h-devbox-pointcheck-2026-09-19/findings.md](evidence/batch-h-devbox-pointcheck-2026-09-19/findings.md) | **已修，四个本地提交（均未推送，2026-09-19）**：`171d1369` fix(editor) F1 命中跳行在编辑器已开其他文件时也生效（`EditorArea.tsx` 守卫改比较 Monaco 模型 URI，新增 `files/__tests__/editorPendingCursor.test.ts` 3 条）；`539cd754` fix(chat) F2 弹层锚定到搜索行（`HitListPopover.tsx` 去掉 `display: contents` 包裹，`children` 收紧为单个 `ReactElement`，新增 `hitListPopoverTrigger.test.ts` 2 条）；`f059a8ae` fix(runtime) F5 aborted / error 收尾不再清零上下文（`projector.ts` 的 `turn_end` 对占位消息不覆盖 `lastTurnUsage` / `lastContextUsage`，`runtimeEvents.test.ts` +3 条）；`6ea3fb2d` fix(chat) F6 审批行归因（`permissionActivityRow.ts` 改认 `delegationId` / `agentName`，`runtimeEvents.ts` 类型注释更新，测试 +2 条）。收口验证（Linux 开发机）：三套 tsc（根 / `src/runtime` / `src/agent-host`）全部退出 0；全量 Vitest 单 worker **450 文件 / 6909 条全部通过，277 s**（对比 09-18 收口的 447 / 6843）。GUI 实测另行进行中，结果待补。F3 / F7（审批行审计留痕）已由[决策 027](decisions/027-permission-rows-no-audit-trail.md)结案为不做；F8（pi 会话名打通）见 [Q028](open-questions.md) 推迟。 |

### 批次 I：2026-09-19 用户现场反馈（九项，当日调查、当日立项）

来源是用户 2026-09-19 在 Windows 测试机与开发机上使用 1.0.0-test.14 的九条口头反馈。每条先派只读调查（六个代理静态追踪 + 一个真机复现，假网关、`src/` 零改动），根因查清后再立项；第 1/2 项另有真机复现证据（`/tmp/t091-repro/`，收口时归档到 evidence）。没有根因或需用户拍板的项不派修，登记为未决问题（Q029～Q032）。

**落地提交（2026-09-19，本地未推送，9 个代码提交）**：`09344a6d` T091 · `9b62dd2d` T092 · `4f987ee4` T094 + T095 · `ef692304` T096 + T097 + T098 · `ea7ea4be` T099 · `5f5433fa` T093 runtime 半段 · `e00d7715` T093 渲染半段 · `c1559038` T101 · `848f5981` T102 · 文档 `d6182b62` · 追加 `ffd0b841` T103 · `fb120545` T100。共享文件按 hunk 拆分到各自任务（`MessageTimeline.tsx` / `i18n.ts` / `toolCard.ts` / `bootstrap.ts` / `runtimeEvents.ts` / `LeftNav.tsx`），只有 `runtimeEvents.test.ts` 的 T093 断言与 T101 用例同在一个 hunk、随 T101 提交。中间提交未逐个跑 tsc，最终树三套 tsc 退出 0、全量 463 / 7059 绿。

| ID | 任务 | 优先级 | 来源 | 验收 |
|---|---|---|---|---|
| ✅ T091 | 新建对话在回合处理中失效：① 点发送后 1～3.5 秒握手窗口内点「新建」，`isFreshEmptySession` 把正在发送的会话判成空白会话而走复用分支，什么都没发生；② 过了窗口新会话建出来了，但 `ChatComposer` 的 `sending` 是组件级一份不分会话，新会话继承 Stop 按钮 / 发送锁 / 模型与权限与目录选择器锁；③ 在新会话点 Stop 走 `stopActiveSession` 停的是空转的新会话，原会话继续跑；④ 侧栏搜索框有关键字时新建的 `New chat` 被标题过滤掉；⑤ 本次运行内新建的临时对话不带 `unbound` 标记，侧栏三处只认标记，首发前不可见 | P0 | 用户反馈 #1 / #2 + 真机复现 | 防重逻辑认「正在发送」；发送态改记在飞会话 id，面向用户的读端按会话判断，再入闩与队列释放保持全局；Stop 停在飞会话；活动会话不被标题搜索过滤；侧栏用「有标记或 workspaceId 为空」判定临时对话。`stopping` 不在 `isRunningStatus` 的既有缺口本次不动 |
| ✅ T092 | 「结束对话」后历史当场消失：`endSessionRuntime.ts:60` 删 `messages[id]` 只是为了让下次点行触发 resume（内存无消息＝要起 worker），而 `activeSessionId` 不动，用户还停在该会话上就看到「暂无消息」；容量回收路径对同一件事反而保留时间线 | P1 | 用户反馈 #3 | 结束只摘 worker 绑定、置 `disconnected`，不删时间线；弹窗文案改为「历史仍可见，下次发送自动重新启动」；侧栏行悬停 ✕ 的英文硬编码标题改走 i18n 并说明「从列表移除」。`endSessionRuntime.test.ts` 那条「清掉会让重开跳过 resume 的状态」断言翻转为「保留时间线」，作为正式验收点。重启后看历史仍需起 worker，见 Q029 |
| ✅ T093 | 渠道故障时一条消息可拖到几十分钟：退避梯子跑满只有 43 秒（3 / 10 / 30 秒，限流另一份预算最多 86 秒），主聊天链路不设自己的请求超时，落到 OpenAI SDK 默认 10 分钟墙钟；无首字节 / 空闲超时、无整条消息总时长上限；未知错误兜底按可重试；子代理重试完全静默；横幅倒计时静态；无「立即放弃」 | P1 | 用户反馈 #5（grok 渠道七八分钟） | [决策 029](decisions/029-provider-timeout-and-retry-policy.md)（2026-09-19 拍板）：undici 全局 dispatcher 设首字节 + 流中空闲超时默认 120 秒（设置项可调可关）并显式传 SDK `timeoutMs`；横幅活倒计时 + 「立即放弃」；父循环补流中断恢复共用 3 次预算；未知错误兜底收紧（有 4xx 状态码不重试、无状态码才重试）；次数与 3 / 10 / 30 秒梯子不变；不加总时长上限；子代理重试可见、每次尝试记日志、清 loop-model-03 / -11。T087 随之复验。本机无该事件日志（当天只跑过 claude 会话），需用户提供测试机 `logs/aiclient-2026-09-19.log` 中 `provider retry` / `turn failed` 行 |
| ✅ T094 | 文件面板删除 / 新建 / 重命名后树不刷新：四个 CRUD 只 `invalidateQueries` 一个无订阅者的子目录键，等于什么都没做；子目录 `loadChildren` 无条件吃缓存，折叠再展开也不读盘；切走面板即卸载并停 watcher；dev 下 StrictMode 双调用使 `watchStart / watchStop / watchStart` 撞成竞态，watcher 一个都没起 | P1 | 用户反馈 #7 | CRUD 成功后主动重读父目录并剪掉已删路径的展开记忆；`loadChildren` 不再无条件吃缓存；主进程 watcher 守卫认识 `stopping` 态；新增 `useFileTree.test.ts` 与 `fileWatcherLifecycle.test.ts`（此前覆盖为零）。`mountPolicy` 是否改 keep-alive 另议；Git 面板提交历史 / 分支列表不自动刷新见 T100。真机点验：新建 282ms 出现 ✅；删除路径被既有的 `window.confirm` 挡住未取证（T103） |
| ✅ T095 | 文件重命名把裸文件名交给 `file:rename`，主进程按进程 cwd `path.resolve`：工作区≠cwd 时静默失败，工作区＝cwd 时文件被挪到仓库根目录 | P1 | 调查 #7 时顺带发现 | 渲染层拼完整目标路径；主进程对非绝对路径目标直接拒绝 |
| ✅ T096 | 长思考块读到末尾要拖回顶部才能收起 | P2 | 用户反馈 #4 | [决策 028](decisions/028-thought-block-sticky-fold-header.md)：折叠头吸顶，定高不截断（避开 F10 振荡）、背景完全不透明覆盖全宽（含左侧竖线）、收起时滚回折叠头；两条 sticky 测试红线放宽为「定高吸顶头允许」并改写护栏说明；`design-system.md` 补条目。与 T098 串行，T098 先落地 |
| ✅ T097 | 回合状态行视觉上贴着用户气泡、离后续输出远；字号 13px 偏小 | P2 | 用户反馈 #6 | 气泡→状态行 `gap-3`（12px 宽松档），状态行→内容 `gap-2`（8px 标准档），字号 `text-meta` → `text-ui`（14px）；同步更新 `chatTimelineLayout.ts` 的间距算术注释与 F-B9 断言 |
| ✅ T098 | 思考进行中无法折叠：流式期间 `toolCard.ts:864` 的 `showBody = !streaming && hasText` 让行走 `expandable: false` 分支，渲染成没有任何控件的纯 `<div>`；是当年修「思考 12～20 秒像死机」时刻意去掉的，三份测试钉着「无 chevron」 | P2 | 用户反馈 #8 | 流式思考行也走 Collapsible、默认展开；Collapsible 分支抽成子组件并按 `running` 换 key，使「没表过态的思考块结算后自动收起」（决策 021）原样保住，用户点过的选择跨结算保留；折叠头不加实时秒数（工作组头部已有） |
| ✅ T099 | Windows 首启无迁移提示、设置里迁移面板三项勾着却全冻结：三处入口都被 T089 的 A 轮开关 `LOCAL_SETUP_ENTRY_DISABLED` 关着（欢迎页 / 首启弹窗 / 设置面板），预勾选逻辑不看开关造成「勾着却动不了」的矛盾态；Claude Code / Codex 导入不碰凭据故未关 | P2 | 用户反馈 #9 | Q032 已拍板（2026-09-19，用户：A 轮未结束，仍在 Windows 上测试）：入口保持关闭；面板在开关为真时预勾选清空、复选框 `aria-checked=false`，说明文案加「测试期间暂未开放」；回写 T089：常量已搬到 `src/renderer/lib/aRoundTesting.ts`，且覆盖三处入口。放开前须实测「替换同名项」那个 `<label>` 包 Switch 的写法 |
| ✅ T101 | 工具调用执行中不显示：read / write 等工具在准备与执行期间时间线上什么都不出现，只在完成后出现「已编辑」「已预览」结算行；用户截图里 GLM 5.2 输出「下面开始编写文件：」后画面停住数分钟直到回合结束（工作组头部「Working 6m 30s」仍在转） | P1 | 用户反馈 #10 | 根因已坐实（2026-09-19 只读调查）：pi SDK 在参数流式期一直推 `message_update`（`toolcall_start / delta / end` 包在 `assistantMessageEvent` 里，partial 的 `toolCall` block 已带 id / name，`arguments` 边解析边填、`path` 头几个 chunk 就有），但 `src/runtime/events/projector.ts` 的 `deltas()` 只抽 text / thinking 两种 block，`tool.started` 唯一发出点是 `tool_execution_start`，此时参数早已完整；那几分钟连 liveness 事件都没有，所以「工作中」在转、下面什么都不动。渲染层机制齐全（`pairToolBlocks` 未配对即 running，running 行独立成行、write 可展开 diff 预览）。**pi-app 与 PI-Desktop 都不做**（pi-app 曾把 toolcall_delta 混进气泡当 bug 修掉），无上游参考，形态自定。修法：`deltas()` 扫 `toolCall` block，新 id 发 `tool.started`（name + 部分参数），参数变了发 `tool.updated`（复用 T053 判定的无生产者死字段 `input`）；`tool_execution_start` 改幂等只补最终参数；`closeAssistant / finish` 给未结算工具行兜底（Stop / 截断留下永远转的行是新引入风险）。**风险**：`toolcall_delta` 按 chunk 触发、全链路无节流，流式期只发路径与体量摘要、`content` 全量留到 `toolcall_end`；store 用 `===` 比对 `toolInput` 会每次重算全文 diff，需节流；参数顺序不保证，行要能先只画工具名。顺带修 `i18n.ts:1293` `Editing` 键被设置页「编辑」抢占（运行中行会显示「编辑 x.html」）。金样本 `nativeGui*EventStream.json` 收口时重录（faux provider 的 `arguments` 到 toolcall_end 才填，「提前显示路径」只能靠手搓 projector 单测） |
| ✅ T102 | 查看历史会话不再起 worker：[决策 030](decisions/030-read-only-history-replay.md) 档位 A——主进程只读回放通路（`readFile` → 内存内 v3 转换 → `decodeSession` → `branchEntries` → ISO 适配 → `projectPiSessionHistory` → `paginatePiSessionHistory`），点开会话先显示历史，发送时才 resume；有活 worker 的会话仍走老路 | P1 | 用户反馈 #3 → Q029 | 无 worker 的会话点开即见历史且不占 worker 名额；翻页无 worker 也可用；同一份会话文件只读回放与 worker 回放投影结果对照测试相等；`@earendil-works/pi-agent-core` 显式进根 package.json；不经 `JsonlSessionStore.open`。与 T092 串行 |
| ✅ T103 | 文件面板「删除」用 `window.confirm()`（`FilesSurfaceView.tsx:96`）：同步阻塞的原生框、文案硬编码英文、与全应用自绘 `AlertDialog` 不一致，且让 CDP 点验无法覆盖删除路径（T094 删除判据因此未取证） | P2 | 批次 I 点验 D-1（非本批引入，最后改动 `deba6cd7`） | 换成同族 `AlertDialog` 并接 i18n；改完补跑 T094 删除判据。**已落地 `ffd0b841`（2026-09-19，未推送）**：受控 AlertDialog，标题按文件 / 文件夹区分，正文「将从磁盘删除「名字」，此操作不可撤销」（撤销栈只管移动与复制）；`FileTree.onDelete` 补传 `isDirectory`；新增 `filesDeleteConfirm.test` 4 条。`DiffViewer` 另一处 `window.confirm` 未动 |
| ✅ T100 | Git 面板提交历史与分支列表不自动刷新（只在应用内 commit / checkout 后失效，外部命令行操作不出现）；左栏三套失效体系（runtimeEventBus / git query key / fs-watcher）互不相通 | P3 | 调查 #7 横扫 | **已落地 `fb120545`（2026-09-19，未推送）**：主进程新增 `git:head-signature`（`rev-parse HEAD --symbolic-full-name HEAD` + `for-each-ref refs/heads refs/remotes` 摘要，只读 refs；空仓库返回空签名，远端返回 null）；渲染层 `useGitExternalRefresh` 面板激活且窗口非空闲时每 5 秒取签名，变化即失效 log / log-infinite / branches，窗口重聚焦显式失效一次（全局 staleTime 60 秒会让默认 refetch-on-focus 跳过）；未搭 status 轮询便车（被「自动 fetch」设置门控，语义不相干）；刷新按钮同时失效三项。新增 `gitHeadSignatureRefresh.test` 4 条 + 真实临时仓库 `gitHeadSignature.test` 5 条。统一「资源变脏」入口与 `.git/` watcher 仍另议 |

### 批次 J：2026-09-19 聊天区排版与过程折叠（用户当日直接提出三条界面诉求，不走审计 / 点验通道）

来源是用户 2026-09-19 在提完批次 I 那十条反馈之后，又直接提出的三条界面诉求（聊天区字体字号不可设置、正文与过程信息区分不显著、过程条目太碎）。施工清单 [topics/chat-typography-and-process-fold.md](topics/chat-typography-and-process-fold.md) 当日建立，含用户已拍板的 D1～D7、五条红线、逐文件改动点与测试影响面。**本批不登记在批次 I 表内**：来源不同（不是现场反馈调查），且其中两条推翻已拍板的决策。

**两条线**：线 A（排版，任务 1～4，即 T104）、线 B（聚合与折叠，任务 5 / 6，即 T105 / T106）。施工清单明写**两条线不能并行** —— 它们同时改 `MessageTimeline.tsx`、`ToolRows.tsx`、`src/shared/i18n.ts`，其中 `MessageTimeline.tsx` 有 2600+ 行且只靠源码 AST 扫描锁字面量，两边同时改必然互相踩。建议先 A 后 B。

| ID | 任务 | 优先级 | 来源 | 验收 |
|---|---|---|---|---|
| ✅ T104 | 聊天区字体与两档字号可在设置中调整：正文与过程信息（工具行、思考正文）当时**都是** `text-markdown` 14px，只靠颜色分层，区分不显著；设置里有终端字体、编辑器字体，唯独聊天区没有 | P1 | 用户诉求 #1 / #2（[决策 032](decisions/032-chat-typography-configurable-two-tiers.md)） | 新增两个**运行时可变**语义字号 token `--text-chat-body` 16px / `--text-chat-process` 13px 并由聊天区根节点 `<section>` 的 inline CSS 变量覆盖（**绝不写 `documentElement`**）；生产代码 13 处 font-size class 切到新 token，含 markdown h1–h6；token 注册进 `tailwind-merge` 的 `font-size` 组；设置里三个控件（字体族 / 正文字号 12~24 / 过程字号 12~20，过程不得大于正文的钳制落在 store setter 里）；出厂默认下观感与改前一致（输入框派生行高在 16px 档恰好等于原 24px，卡片高度 74px 降级为 floor）。**已落地 `0bc99095`（2026-09-20，未推送）**：新增 `src/shared/types/chatTypography.ts`（范围常量 + 两个交叉钳制解析器，非有限值回落默认值）、`ChatTypographySettings.tsx`（挂进现有 `AppearanceSettings`，**未新增设置分组**，含三行实时预览）、8 条中文词条；新增 3 个测试文件（`chatFontOverrideStatic` 钉住注入点与「不写 documentElement」、`chatTypographyTokens`、`chatFontSection`），更新 5 个既有测试文件的字面量断言。**构建期不内联已用真实构建验证**（Tailwind 4.1.18 + vite 7.3.1 产物是 `.text-chat-body{font-size:var(--text-chat-body)}`，变量引用保留，任务 0 的技术前提成立）。**回合状态行的 `text-ui`（14px）不参与本次改动**（D2，T097 结论不变） |
| ✅ T105 | 过程条目聚合：一个回合内**连续出现**的工具调用**不分类型**合并成一条（D3）；夹在中间的思考打断聚合、前后各成一条（D4）；聚合行文案 `N 个工具调用 · 正在运行 npm test` / `N 个工具调用 · 最后编辑 App.tsx`（D5） | P1 | 用户诉求 #3（[决策 031](decisions/031-tool-calls-aggregate-and-work-group-always-folded.md)） | 两层做：**item 层**新增 `mergeAdjacentToolGroups` 缝合同一串跨助手消息的 toolGroup（中间夹 text / question / 未应答 permission / notice 则不合并；合并产物带 `messageIds`，否则流式思考的 block id 会静默取不到）；**row 层**重写 `deriveToolGroupRows` 的切段规则（分隔符 = thinking 条目 | 带 permission 记录的 run，段内 ≥2 才聚合，单条回落单行）与 `deriveAggregateRow`（计数改用段长、删掉按类型去重与 `AGGREGATE_VERB`、动词取 `toolVerb` 的 `running` / `refused` 槽）。**两条保留例外**：带 permission 的 run 作分隔符独立成行（否则叠加「永远折叠」后用户要点两次才看到一次授权）、`turnWorkGroupAwaitsUser` 一个字不改。**运行中也显示聚合行**，删除「遇到 running 就停止聚合」的前缀限制（偏离 A07「running 行不给 chevron」，按 T098 对思考行的同样方式登记）。**已落地 `765729da`（2026-09-20，未推送）**：`deriveToolGroupRows` 的切段只认 thinking 与 `run.permission != null`；`deriveAggregateRow` 改为「段长 + 最后一个动作」，`AGGREGATE_VERB` 与按 `file_path` 去重连同 `{{count}} file(s)`/`{{count}} search(es)` 段一并删除无空壳，新增 `toolActionNoun()` 语义别名与 `ToolRowView.verbText`；把流式 block id 的解析抽成独立纯模块 `streamingBlockId.ts`（`MessageTimeline.tsx` 在 node 环境无法挂载，放组件里只能靠源码扫描，无法验证「解析到了正确的消息」），带 6 例单测。**取值口径修正一处**：段内任一 run 在跑时取最后一个 **running** 的 run 而非最后一个 entry —— 并行调用下「后面的已完成、前面那个还在跑」是真实形状，按条目取会打印出已经结束的那条 |
| ✅ T106 | 工作组始终折叠：运行中也不自动展开（D6） | P1 | 用户诉求 #3（[决策 031](decisions/031-tool-calls-aggregate-and-work-group-always-folded.md)） | `turnWorkGroupOpen` 的入参**删掉** `settled`（不是留着不用），只要 `forcedOpen` 或用户点过的选择为真才展开，否则一律 `false`；**仍然纯派生、零 `useEffect`**；红线 `turnWorkGroupAwaitsUser`（未应答授权强制展开）一个字不改 —— 工作组永远折叠之后它是唯一还能自动展开工作组的规则；折叠头**保持英文**（D7，`messageTimelineWiring.test.ts` 与 `turnProgress.test.ts` 的两条 `HEAD-EN` 测试不动），运行中显示当前动作（新增纯函数 `deriveTurnCurrentAction`：倒序扫到第一个 running 的 run，**无 running 时回落到最后一个已完成的动作** —— 否则工具间隙只剩一个只有秒数在跳的裸头，与卡死无异）、结束后显示步数。**已落地 `765729da`（2026-09-20，未推送）**：`deriveTurnCurrentAction` 返回 `{ run, verb, state }`，只要有任一 run 存在就不会返回 null（`state` 取 `running` 或 `refused` 槽的动词原形），`verb` 解析成 catalog key 由渲染点一次 `t()` 落地；头部合成改为 `joinTurnProgressLine(headText, [actionClause, stepsClause, ...])`，当前动作 clause 只在 `!settled` 时加、spinner 保留；新增词条 `Last {{action}}` 与 `{{count}} step(s)`（聚合行前导复用已有 `{{count}} tool calls`，运行中动作复用 `TOOL_VERBS[x].running`，不新写分隔逻辑） |

**本批新增决策两条**：[031](decisions/031-tool-calls-aggregate-and-work-group-always-folded.md)（聚合 + 始终折叠，**推翻决策 021 的「运行中自动展开」那一半**，理由记为「用户当下诉求」，021 的另一半 —— 结束后折叠、头部报时长、纯派生无 effect、未应答授权强制展开 —— 全部保留）、[032](decisions/032-chat-typography-configurable-two-tiers.md)（两档可配置，对 `design-system.md`「字号只能用固定 token」的一次**有据偏离**，红线「绝不写 documentElement」仍然成立）。

**证据目录**：[evidence/batch-j-chat-typography-2026-09-19](evidence/batch-j-chat-typography-2026-09-19/README.md)（已建目录，GUI 点验四张图 + 阅读栏字数实测 + 反向验证记录**待线 B 收口后补**）。

### 批次 K：2026-09-21 正文可见性与审阅 diff 落点（用户当日直接提出三条界面诉求，不走审计 / 点验通道）

来源是用户 2026-09-21 提出的三条界面诉求（正文被折进工作组、工具行尾部过长、审阅条目要点开就看到 diff 并能跳文件）。施工清单与本批的红线、逐文件改动点、测试影响面一并记录在 [topics/answer-visibility-and-diff-open.md](topics/answer-visibility-and-diff-open.md)；**施工移交单**（开工前置、逐任务交付物与证据要求、验收判据、已知的坑、任务记录栏）见 [topics/answer-visibility-and-diff-open-handoff.md](topics/answer-visibility-and-diff-open-handoff.md)。

**本批不推翻既有决策。** 诉求 2（工具行去后缀）是一次**显示内容的收窄**：用户在看到第一版分析把它写成「推翻决策 031」后当场更正 ——「不是推翻，是现在显示的内容太长了」。决策 031 关于「头部是唯一进度证据、工具间隙不可返回 null」的全部结论继续成立，只是头部不再打印参数与命令行。

**本批与批次 J 的线 B 改同一批文件**（`MessageTimeline.tsx` / `ToolRows.tsx` / `i18n.ts`），必须在批次 J 线 B 收口后开工；本批内部 T107 → T108 串行、T109 → T110 串行（理由同上，见 topic 文件）。

| ID | 任务 | 优先级 | 来源 | 验收 |
|---|---|---|---|---|
| 🟡 T107 | 正文被折进工作组：最后一个 answer 段之前的一切（含正文）都进折叠区，读者要展开去找模型说过的话 | P1 | 用户诉求 #1（[topic](topics/answer-visibility-and-diff-open.md) K1） | `splitTurnWorkGroup` 的判据改为「answer 段一律在外、process 段进组」，返回形状从 `{leading, grouped, finalAnswer, trailing}` 改为有序的分段列表（一个回合可有多个折叠组）；`MessageTimeline.tsx` 按分段顺序渲染，每个 `processGroup` 各包一个 `TurnProgressHead`；**必须保留**：无 answer 的回合 notice 留在组外（否则塌成一行藏掉错误）、无 process 则无组头、`turnWorkGroupAwaitsUser` 一字不改；**已知代价**：正文与计数行交替出现（正文 → 计数行 → 正文），这是用户在 [三栏对比页](/tmp/aiclient-preview-answer-visible-3ways.html) 上看过 A/B/C 后选的 B 形态；**验收判据**：真实长回合（工具调用 ≥ 20 次）里屏幕上过程行数 ≈ 正文段数，而不是 ≈ 工具调用次数 —— 这一条不能只靠静态页判断 |
| 🟡 T108 | 工具调用行尾部过长：`3 次工具调用 · 最后运行 cd "/e/Projects/chipTestMachine/…" && "c:/Program Files/…"` 影响观感与注意力 | P1 | 用户诉求 #2（[topic](topics/answer-visibility-and-diff-open.md) K3） | 两处来源都改：`toolCard.ts` 的 `aggregateActionText` / `deriveAggregateRow` 只留计数（`verbText` 一并退役，回到 `verb` 的字面量路径，还掉 i18n 覆盖率扫描的债）；`MessageTimeline.tsx` 工作头的 `actionClause` 不再经 `formatToolArg` 取参数，`deriveTurnCurrentAction` 本身保留（决策 031 的「间隙不可为 null」仍活）。**连带清理**：`ToolRowView.verbText`、`aggregateActionText`、`toolActionNoun()` 若失去生产者则删（同决策 031 §5 的判据，不留无人消费的导出）。**用户已确认**：单条（未聚合）工具行的主参数（路径 / 命令）保留；本次只去摘要后缀。**验收**：含长命令行（≥ 100 字符）的真实回合里，工作头与聚合行都不出现命令行文本 |
| ⬜ T109 | 审阅条目默认展开噪声：`defaultOpen={index === entries.length - 1}` 让最后一条自动展开，长会话里累加成一片 | P1 | 用户诉求 #3（[topic](topics/answer-visibility-and-diff-open.md) K4 / A0） | `SessionReviewPanel.tsx` 的 `defaultOpen` 一律 `false`，只认用户点击；展开时渲染 patch hunk 头里的行号栏（新增行右列、删除行左列，**取不到时留空而不是编造**）；条目头补 hover 与键盘可达。测试影响：`sessionReviewInteraction.test.ts` 的现有默认展开断言需改写 |
| ⬜ T110 | 审阅条目无法直接跳到文件的 diff 页 | P1 | 用户诉求 #3（[topic](topics/answer-visibility-and-diff-open.md) K4 / A1） | 条目右侧「打开文件」按钮改调 `useEditorStore.getState().openDiffTab({kind:'workdir', path, status})`（复用 `stores/diffTabTarget.ts` 的现成形状与 `diffCenterTabsStatic.test.ts` 的既有链路），**不再**调 `onShowFiles`；`WorkspaceShell.tsx` 的 `onShowFiles` / `filesOpen` 若失去最后一个消费者则删掉传递链。**交付说明必须写明**：这会打开中列的 `git-diff://` 页签，`diffTabActive` 令中列独占、聊天区让位 —— `centerLayoutModel.ts` 的既有行为，不是本任务引入。**不做 A2**（按 unified patch 渲染的独立视图）：仓库无此类组件，成本最高收益最小 |
| ⬜ T111 | 中断 / 报错后该回合先前的流式正文消失，展开工作组也看不到 | P2 | 用户诉求 #4，用户当日决定**先放一放** | **必须排在 T107 之后取证**：T107 改了折叠规则，「消失」很可能自然消失或变形，先修可能是在修一个已不存在的现象。已排除的路径（2026-09-21 只读分析）：`session.failed` / `session.stopped` 两个 reducer 只改 status，流式 append 路径不清理 blocks。待取证嫌疑：`historyReplayMerge.ts` 的文本折叠匹配在截断文本上的行为、`chatSessions.ts:707` 的 `incomplete && blocks.length === 0` 占位分支。取证方式：起一次 dev、复现一次中断、dump 该消息的 blocks（类型 + 长度 + 首 40 字符）与是否走 history 合并，不装依赖、不跑全量 |

**施工进度（2026-09-21）**：T107（`678d79b7`）、T108 实现与定向验证完成，真实长回合证据待取；详见 [批次 K 执行记录](evidence/batch-k-answer-visibility-2026-09-21/README.md)。用户确认过程组默认折叠、单条工具行主参数保留。

**本批不新增决策**（三条诉求都不改变既有决策的结论，见上）。演示页两页在 `/tmp`（`aiclient-preview-answer-fold-and-diff-open.html`、`aiclient-preview-answer-visible-3ways.html`），用户已看过并拍板；长期留存需在收口时移入 `evidence/`。


## Deferred

| 项 | 原因 |
|---|---|
| 前缀稳定性模块（P2-7）保留或删除 | ARD D9 明写为可选加强项；见 Q002 |
| ~~父循环流中失败恢复~~ | 2026-09-19 由[决策 029](decisions/029-provider-timeout-and-retry-policy.md)推翻，随 T093 落地 |
| 「本次会话允许」按委派归属收敛 | 已决不做：保持会话级，见[决策 003](decisions/003-allow-session-stays-session-scoped.md) |
