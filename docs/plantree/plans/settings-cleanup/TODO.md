# Settings Cleanup 执行清单

> 本文件提供执行进度视图；任务身份、状态与依赖以 [roadmap.md](./roadmap.md) 为准，
> 当前阶段、阻塞与验证结果以 [implementation-status.md](./implementation-status.md) 为准。
> 完成实现与验收后才勾选对应任务，不将准备完成视为任务完成。

## 开工准备

- [x] 确认计划目录，读取 README、roadmap 与 implementation-status。
- [x] 验证命令工具可用；2026-09-07 检查时工作区干净，当前分支领先远端 2 个提交。
- [x] 初步定位 S04 的类型、默认值、setter、迁移、设置 UI 与旧壳消费方。
- [x] 获得明确的代码修改授权；S04 与 S01/S02/S03 已实现。

## 任务进度

- [ ] **S04：删除纯死设置。** 清理 Agent 通知三项、fileTreeAutoReveal 和增强输入设置；同步调整旧壳消费方，验证旧 profile 水合不丢其他设置，补充删除不变量测试。
- [ ] **S01：全局搜索移植进新壳。** 补充入口和两个搜索快捷键，验证搜索结果跳转编辑器。
- [ ] **S02：仓库设置入口补进新壳。** 保留自动初始化与初始化脚本配置，移除隐藏仓库项及旧管理对话框。
- [ ] **S03：分支切换补进 GitSurfaceView。** 支持本地/远程分支与新建分支，验证切换后的改动和历史刷新。
- [ ] **S05：删除旧壳。** 已按本轮授权，以 `deba6cd7` 保存补齐线后继续；逐个核实旧壳专属文件，保留新壳共享依赖。
- [ ] **S06：删除随旧壳失效的设置。** 保留 S01 已接入新壳的搜索快捷键，验证旧 profile 兼容。
- [ ] **S07：重排为九类并统一设置行原语。** 在 S04、S06 完成后执行，更新分类测试与国际化字符串。

## 每项验收

当前 S04 明细（[实现证据](./evidence/s04-removed-settings.md)）：

- [x] 删除通知 UI、闲置翻译及五项设置的类型、默认值和 setter。
- [x] 调整旧壳消费方，移除增强输入容器与底栏测高链路。
- [x] 水合及磁盘清理移除旧键，保留其他配置。
- [x] 编写删除不变量、旧 profile 水合与磁盘清理回归测试。
- [x] 相关 3 个测试文件、5 个测试通过；改动文件 Biome 和全量类型检查通过。
- [x] 全仓 lint：修复 1 处既有单行格式错误后通过。
- [x] 全仓测试已分 25 批执行，首轮结果与补跑记录已落盘。
- [ ] 完整门禁：剩余 4 个环境相关失败文件；整套类型检查触及堆上限，拆分检查通过。

补齐线实现进度：

- [x] S01：新壳搜索入口与常驻快捷键、工作区切换保护、异步搜索取消修复；相关 11 项测试通过。
- [x] S02：仓库行操作菜单、精简设置对话框、删除旧管理对话框；2 项测试通过，会话菜单 4 项回归通过。
- [x] S03：分支选择与创建接入、完整缓存刷新；4 项操作测试与 31 项缓存键测试通过。
- [ ] 系统编译依赖安装确认、剩余测试门禁与 GUI 点验。
- [x] 按用户本轮授权，先将 S01–S04 保存为本地提交 `deba6cd7`，再继续 S05；未合入主分支。

删除线实现进度：

- [x] S05：移除旧壳分支、开关、浮窗和专属文件；普通设置对话框复用 SettingsContent，保留共享终端、文件、搜索和 Git 组件。
- [x] S06：移除随旧壳失效的字段与 setter，清理旧 profile，保留搜索快捷键；同步去掉旧 Action Panel 菜单与添加仓库中的分组入口。
- [x] S07：九类设置、共享页面/分区/行原语、旧分类书签迁移、Pi 模型及权限国际化；12 个有效快捷键保留。
- [x] 旧壳不变量、profile 迁移、分类迁移、真实 React 分类切换及快捷键录制测试通过。
- [x] 扩展回归、最终 lint 与拆分类型检查证据整理，见 [本轮验证记录](./evidence/2026-09-07-s05-s07-validation.md)。
- [ ] 完整门禁、GUI 点验、主分支合入；实现完成不等于完整验收完成。

通用要求：

- 更新 roadmap 状态，并在 implementation-status 记录验证结果与未完成项。
- 删除任务提供静态不变量测试；迁移调整验证旧 profile 的其他设置保持完整。
- 重任务前检查 RAM、Swap、磁盘与遗留构建进程；检查和测试串行执行。
- Vitest 按小批次执行，使用 `--maxWorkers=1 --no-file-parallelism`；不直接运行全量测试或整套生产构建。
- GUI 验证按计划并入 UI 对齐累计点验；未完成的验证如实记录。
