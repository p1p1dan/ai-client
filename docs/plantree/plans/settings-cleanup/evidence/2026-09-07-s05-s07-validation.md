# S05–S07 验证记录

日期：2026-09-07。基线：`deba6cd7`，分支：`feat/model-catalog-admin`。
实现范围和顺序调整见 [实现证据](./s05-s07-implementation.md)。

## 执行条件

- 主机实测 RAM 约 1.9 GiB，根分区剩余约 7.8 GiB。
- Vitest 串行，每批至多 8 文件，固定 `--maxWorkers=1 --no-file-parallelism`，Node 堆上限 768 MiB。
- 每批检查内存；176 个 renderer 测试文件共 22 批，批次结束的可用内存约 832–948 MiB。
- 当前 shell 无 pnpm 命令，使用仓库已有 `node_modules/.bin/vitest`、`tsc`、`biome`，未更换依赖版本。
- 未运行整套生产构建或 Agent Host 打包，没有启动 Electron GUI。

## 测试结果

| 验证 | 结果 |
|---|---|
| renderer 扫描 176 文件、22 批 | 首轮 3048 项通过，1 项失败 |
| 首轮唯一失败文件 messageTimelineWiring | 删除 EnhancedInput 后字体固定计数需从 7 调整为 6；复跑原文件 48 项全部通过 |
| 新增 worktreeInitialization | 1 文件、5 项通过，覆盖最新脚本、路径、禁用/空脚本/失败、普通终端入口仍关闭 |
| 初始化变更后的状态机与共享终端回归 | 4 文件、118 项通过 |
| 最后一批快捷键、初始化、surfaceRegistry、挂载回归 | 4 文件、42 项通过 |

按文件与用例去重，本轮最终覆盖 renderer **177 文件、3054 项**，均已取得通过结果。
这是分批执行加失败复跑和新增测试的汇总，不是单次整套 Vitest 命令的输出，也不代表全仓测试三绿。

专门覆盖：

- S05 删除不变量：旧壳入口、开关、专属组件不复活；共享终端、文件、分支、Diff 组件仍存在。
- S06：旧 profile 的移除字段、幂等迁移、磁盘清理、其他 namespace 和有效配置保持完整，搜索自定义绑定保留。
- S07：九类顺序和旧分类书签迁移；真实 React 分类切换、受控恢复、权限仓库路径；12 个快捷键控件及实际录制保存。
- S01–S03 回归：搜索竞态与工作区切换、两个搜索快捷键、真实仓库设置表单保存、分支切换与缓存刷新。

实现过程中另修正了 PiResources 静态测试的旧分类 ID，以及设置行重排时遗漏的快捷键控件。
初始化脚本的新测试揭示普通终端打开动作按产品决策被禁止，因此新增仅限初始化的状态机动作；原有 rail 和普通快捷键隐藏规则继续通过回归。

## 类型与静态检查

- 最终生产代码拆分类型检查通过：继承根 tsconfig，保持 Agent Host 排除范围，额外排除测试，Node 堆上限 896 MiB。
- 新增及修改测试单独类型检查通过：包括设置、删除不变量、仓库表单、初始化、字体扫描相关测试；纳入 `src/preload/types.ts` 与 `src/renderer/vite-env.d.ts` 保持桥接和 Vite 声明完整，堆上限同为 896 MiB。
- 全仓 Biome 通过：982 个文件，0 error；既有 27 warning、17 info 未当作错误。
- `git diff --check` 通过。
- General、Appearance、Keybindings 拆分前后核对 store 绑定集合，缺失项仅为计划删除的旧壳设置及其 setter。

## 尚未验证

- 真实 Electron GUI：仓库选择、会话、搜索结果跳转、分支切换、设置布局与窄窗口。
- 新建 worktree 后初始化脚本的真实 PTY 执行、输出与生命周期；现有证据覆盖创建成功调度、失败不调度、终端入口以及匹配 cwd 后消费的已有组件逻辑。
- 上轮遗留的 permissionPatchScript、permissionPolicyIntegration、SessionManager、PiTuiPty 四个环境相关失败文件，本轮未重跑；需补齐独立 Agent Host 依赖或 node-pty 原生模块。
- 整套类型检查和整套生产构建未重跑；不能把拆分检查记成整套检查通过。
- 未推送、未合入主分支；S01–S07 仍保留 In Progress，完整验收后才可标为 Done。

## 原始证据

- `/tmp/settings-cleanup-renderer-results.json` 与 `/tmp/settings-cleanup-renderer-{1..22}.log`：renderer 分批文件清单、状态、原始输出。
- `/tmp/settings-cleanup-font-recheck.log`：字体计数修正后的复跑。
- `/tmp/settings-cleanup-init-final.log` 与 `/tmp/settings-cleanup-final-regression.log`：初始化和最终相关回归。
- `/tmp/settings-cleanup-typecheck-final.log`、`/tmp/settings-cleanup-tests-typecheck.log`：最终类型检查，均退出 0、无诊断。
- `/tmp/settings-cleanup-lint.log`：全仓静态检查。

结束时可用 RAM 约 1.0 GiB、Swap 已用约 917 MiB，根分区剩余约 7.8 GiB；无遗留测试或构建进程。
