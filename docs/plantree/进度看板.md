# 进度看板

Role: implementation-status。更新日期：2026-09-24（`feat/ctrl-enter-interject` 收口：审查修复与子代理死循环防护已分 6 个提交落地，待开发机点验与 Windows 实测后快进 main）。

## Current Phase

- **批次 M：Ctrl+Enter 插话分支收口（2026-09-24，T118～T136）**：代码审查确认的 15 条问题全部修完；另修 v1.0.2 现场的子代理工具死循环（glm-5.2 在一条回复里写出 5709 个调用、一个都没执行、流了约 19 分钟）。分 6 个提交 `4a964c7d` / `568233a4` / `6e85aff2` / `645c65a0` / `4be46769` / `46789042`，未推送。用户当日拍板三条决策：[041 插话不等后台子代理](plans/runtime-hardening/decisions/041-interject-does-not-wait-for-background-delegates.md)、[042 子代理工具防空转](plans/runtime-hardening/decisions/042-delegation-tool-loop-guard.md)、[043 思考默认折叠与打断回合展开](plans/runtime-hardening/decisions/043-thought-folded-and-interrupted-turns-open.md)。验证：三套 tsc 通过，全量 493 文件 / 7461 例仅 1 例既有失败（T124 / Q033）。证据 [review-and-loop-incident-2026-09-24](plans/runtime-hardening/evidence/review-and-loop-incident-2026-09-24.md)；开发机点验进行中（[interject-branch-devbox-2026-09-24](plans/runtime-hardening/evidence/interject-branch-devbox-2026-09-24/)）。分支流程：`feat/runtime-evolution` 已于 09-24 退役并删除（本地与远端，无独有提交），本分支验证后快进 main，此后新工作从 main 按主题开分支。

- **2026-09-22～09-23 未逐条登记**：这两天的展示与发版改动（决策 033～040、1.0.1 / 1.0.2 发布、升级后补迁 `8b3404b3` / `c7a3f89a` 等）没有回写本看板，以 [decisions](plans/runtime-hardening/decisions/) 与 [process-preview 证据](plans/runtime-hardening/evidence/process-preview-2026-09-23/README.md) 为准。批次 L 的 T116 / T117 是否已被 `8b3404b3` 覆盖，待核对后再改 roadmap。

- **批次 L 立项（2026-09-21，T115 已修 / T116 · T117 待修，交由 Windows 端）**：测试者从 `1.0.0-test.16` 升级后侧栏全空 —— 项目列表与对话列表都没了，会话 jsonl 却完好。现场实测出三个位置：`~/.pilab/<名>/`（迁移搬，但名单把目录名写成了从未存在过的 `AiClient`，实际是 `jyw-ai-client`，已修 `d4b9445e`）、`<appData>/<名>/Local Storage/`（项目列表，**不搬**）、`<appData>/<名>/session-index.json`（**对话列表**，不搬且内含 jsonl 绝对路径，搬了还得改写）。后两项是 Windows 专有路径，本机无法实跑，**用户决定交由 Windows 端执行方改并在真机验收**，本机只留诊断与代码备注。细节见 [topics/app-state-migration-gaps.md](plans/runtime-hardening/topics/app-state-migration-gaps.md)。顺带记录三条附带发现：`sessionStorage` 桥渲染层零调用（死链路）、`app:setLanguage` 无主进程 handler（每次启动抛一次）、`sessionWriterLock.test.ts` 在 CI 上会抖（`35604442339` 因它变红，同代码的 `35597486859` 是绿的）。

- **批次 K 续立项（2026-09-21，T112～T114）**：批次 K 的现场验收当天做了 —— 起真实 dev 实例跑长回合，用户看到 `N 个步骤 → answer → N 个步骤 → answer → 已工作 47 秒 → answer`，当场指出最后一个本该报步骤数的位置却报了整回合时长。根因是**作用域混用**：T107 用 `workedMs={lastGroup ? workedMs : null}` 躲开时长重复报，代价是让最后一个折叠头承载回合级信息，与只报组级步骤数的头长得一样。用户拍板拆开：折叠头一律只报步骤数、单条过程段不套折叠壳、回合级信息独立一行钉在回合末尾（运行中转圈，结束后 `✻ 已工作 54 秒 · 完成于 17:05 · 8 次工具调用 · 思考 12 秒`，不挂 token 用量）。**本批会把 T12-b（2026-08-29）删掉的回合头结束态与墙上时间装回来**，相关注释必须改写。同时更正一处过期注释：`turnProcessFold.ts` 说「输入框上方那条状态行仍在各走各的秒」是错的，该行 T-31 §3 已搬进回合。演示页 [工作区钉末尾](plans/runtime-hardening/evidence/batch-k-answer-visibility-2026-09-21/aiclient-preview-workzone-at-end.html)，移交单 [workzone-at-turn-end-handoff](plans/runtime-hardening/topics/workzone-at-turn-end-handoff.md)。

- **批次 K 续执行（2026-09-21）**：T112（`f47a69b7`）/ T113（`97af2183`）/ T114（与本段同一提交）串行落地，每条单独提交、未推送、未触发 CI。**这一轮拿到了批次 K 缺的真实 GUI 证据**：登录后在 dev 实例里跑真实回合，截到单条过程段直出 + 多条折叠、折叠头只报步骤数、末尾行运行中（转圈 `✻ 工作中 N 秒 · 当前动作`）与结束后（`✻ 已工作 25 秒 · 完成于 18:08 · 8 次工具调用 · 思考 2 秒`）、悬停条只剩复制按钮。`deriveTurnWorkGroupLabel` 随「三选一」职责退役，新增 `deriveTurnWorkZone` / `countTurnToolCalls` / `TurnWorkZoneRow` / `turnWorkZoneClass()`；五处 T12-b 旧注释按新规则改写。本机只跑定向单文件 Vitest 与三套 `tsc --noEmit`，全量门禁未跑。逐条记录（含三处偏离与五条不确定项）见[移交单 §6](plans/runtime-hardening/topics/workzone-at-turn-end-handoff.md)。

- **批次 K 执行（2026-09-21）**：T107（`678d79b7`）/ T108（`3a0c13fa`）/ T109（`5c7c7596`）/ T110 实现及定向验证完成，每任务单独提交、未推送。真实长回合 GUI 验收与 T111 中断取证缺少可用模型凭据及会话，不标 Done。用户确认过程组默认折叠、单条工具行参数保留。详细 TODO、变更与证据见 [批次 K 执行记录](plans/runtime-hardening/evidence/batch-k-answer-visibility-2026-09-21/README.md)。

- **批次 J 三项全部落地（T104 `0bc99095` · T105 / T106 `765729da`，2026-09-20，未推送），证据待取（2026-09-19 建立）**：来源是用户在提完批次 I 那十条反馈之后当日直接提的三条界面诉求（聊天区字体字号不可设置、正文与过程信息区分不显著、过程条目太碎），**不走审计 / 点验通道**，因此单独立为批次 J 而不是登记进批次 I 表内；其中两条推翻已拍板的决策，故新开两条决策。**线 A** 新增两个**运行时可变**语义字号 token `--text-chat-body` 16px / `--text-chat-process` 13px，运行时覆盖**只挂在聊天区根 `<section>`**（**绝不写 `documentElement`**，静态守卫 `chatFontOverrideStatic.test.ts` 钉住），生产代码 13 处 font-size class 切到新 token（含 markdown h1–h6），token 注册进 `tailwind-merge` 的 `font-size` 组（否则颜色类被静默吞掉），设置里三个控件（字体族 / 正文字号 12~24 / 过程字号 12~20，过程不得大于正文的钳制落在 store setter 里，非有限值回落默认值而非下限）；出厂默认下观感与改前一致（输入框派生行高在 16px 档恰好等于原 24px，卡片高度 74px 降级为 floor）。**构建期不内联已用真实构建验证**（Tailwind 4.1.18 + vite 7.3.1 产物是 `.text-chat-body{font-size:var(--text-chat-body)}`，变量引用保留）。**回合状态行的 `text-ui`（14px）不参与本次改动**（D2，T097 结论不变）。新增 3 个测试文件、8 条中文词条（未新增设置分组），5 个既有测试文件的字面量断言同步更新。**线 B** 是过程条目聚合（T105：连续工具调用不分类型合并成一条，思考打断聚合，带 permission 的 run 作分隔符独立成行）+ 工作组始终折叠（T106：`turnWorkGroupOpen` 删掉 `settled` 入参，仍纯派生零 `useEffect`，`turnWorkGroupAwaitsUser` 一个字节不改）——**两条线与线 A 不能并行**（同时改 `MessageTimeline.tsx` / `ToolRows.tsx` / `i18n.ts`），已按「先 A 后 B」串行落地。决策 [031](plans/runtime-hardening/decisions/031-tool-calls-aggregate-and-work-group-always-folded.md)（推翻决策 021 的「运行中自动展开」那一半，021 另一半全部保留）、[032](plans/runtime-hardening/decisions/032-chat-typography-configurable-two-tiers.md)（对 `design-system.md`「字号只能用固定 token」的有据偏离）；`design-system.md` 已补偏离条目。证据 [batch-j-chat-typography-2026-09-19](plans/runtime-hardening/evidence/batch-j-chat-typography-2026-09-19/README.md)（目录已建、**内容仍空**：点验四张图 + 阅读栏字数实测 + 反向验证记录待补）。**下一步：GUI 点验取证（静态测试证明不了观感）→ 归档 → 才谈推送。**
- **批次 I：用户当日十条现场反馈，十一项修复全部落地、未提交（2026-09-19 晚）**：用户在 Windows 测试机与开发机上用 1.0.0-test.14 陆续提出十条反馈（处理中新建对话失效、侧栏不刷新、结束对话历史消失、长思考块难收起、grok 渠道七八分钟、状态行贴气泡、文件树删除不刷新、思考中不能折叠、Windows 迁移面板冻结、工具调用执行中不显示）。按「先只读调查、根因坐实再立项」执行：七个只读调查代理 + 一次真机复现（假网关、`src/` 零改动），发现问题 1/2 最常撞上的其实是「点发送后 1～3.5 秒握手窗口内新建被防重逻辑吃掉、什么都没发生」，问题 2 真因是搜索框过滤而非渲染延迟，问题 9 是 T089 的 A 轮开关扩到了未记录的两处入口，问题 10 是投影层丢掉了 pi SDK 一直在推的工具参数流式事件（pi-app / PI-Desktop 均不做，无上游可抄）。四条待拍板当日全部结案：Q031 → [决策 028](plans/runtime-hardening/decisions/028-thought-block-sticky-fold-header.md)（看过三方案演示页后选吸顶折叠头，要求不透明不穿插）、Q030 → [决策 029](plans/runtime-hardening/decisions/029-provider-timeout-and-retry-policy.md)（对齐 pi 官方 CLI 的首字节 / 空闲超时默认 120 秒可调可关、父循环补流中断恢复、活倒计时 + 立即放弃、次数与梯子不变）、Q029 → [决策 030](plans/runtime-hardening/decisions/030-read-only-history-replay.md)（档位 A：主进程只读回放，发送时才起 worker）、Q032 → A 轮未结束，迁移入口保持关闭只修矛盾态。落地 T091～T099、T101、T102 共十一项，随后用户拍板追加 T103（删除确认框）与 T100（Git 面板历史 / 分支自动刷新）当日落地；收口验证：三套 tsc 退出 0，金样本 5 份重录，全量 Vitest 单 worker **463 文件 / 7059 条全部通过，313 s**（对比 T090 收口的 450 / 6909）。锁文件只保留新增 `@earendil-works/pi-agent-core` 一条，`pnpm install` 顺带刷新的两处传递依赖解析已回退。用户当日另两条工作方式要求已记入记忆：同时在跑的子代理最多 3～4 个；「每个 chunk 都触发」一类风险要用外行能懂的话解释。证据 [batch-i-user-feedback-2026-09-19](plans/runtime-hardening/evidence/batch-i-user-feedback-2026-09-19/README.md)。GUI 点验：8 项 7 ✅、T094 删除路径因既有 `window.confirm` 未取证 → 立项 T103 修掉。分组提交 13 个（9 代码 + 2 文档 + T103 + T100）。**2026-09-20 已推送**（`058fc70f` → `156b038e`，含版本号提交 `1.0.0-test.15`）并手动触发打包 [35478806840](https://github.com/p1p1dan/ai-client/actions/runs/35478806840)：Windows / Linux 全 success、worker 冒烟 ok；macOS 首次在 DMG 制作步 `hdiutil: Device not configured`（runner 偶发）后重跑，结果见 [batch-i-build-2026-09-20](plans/runtime-hardening/evidence/batch-i-build-2026-09-20/README.md)（2026-09-24 补登）。

- 更早的阶段条目（批次 I 之前）原文见 [旧阶段归档](plans/runtime-hardening/history/2026-09-24-进度看板-旧阶段归档.md)。

## Next Target

1. ~~T128 点验缺陷修复~~：已落地 `403b7575`，GUI 复验 8 项全 ✅（[recheck-t128](plans/runtime-hardening/evidence/interject-branch-devbox-2026-09-24/recheck-t128/README.md)）。
2. 版本号升到 `1.0.3-test.1`（单独 chore 提交），推送本分支，`workflow_dispatch` 手动打包——只产出安装包，不发 Release、不触发自动更新。
3. 用户在 Windows / 加密机上按[交接单的 Windows 实测清单](../plans/2026-09-24-ctrl-enter-interject-handoff.md)实测，包括用 glm-5.2 长上下文复现死循环。
4. 通过后 `main` 快进到本分支。

## Last Landed

- 2026-09-24 批次 M 六个提交（**未推送**）：`4a964c7d` T118 插话审查修复；`568233a4` T119 分支栏；`6e85aff2` T120 时间线计时与展开规则；`645c65a0` T123 注释英文化；`4be46769` T121 字号文档同步；`46789042` T122 子代理工具死循环防护（含开关 `AICLIENT_RUNTIME_LOOP_GUARD`）。
- 2026-09-24 `feat/runtime-evolution` 本地与远端删除（无独有提交，内容全部在 main）。
- 2026-09-20 批次 J 线 A：T104 `0bc99095` feat(chat) 聊天区字体与两档字号可在设置中调整（**未推送，本轮不单独提交**）。新增两个运行时可变语义字号 token `--text-chat-body` 16px / `--text-chat-process` 13px（仓库里唯一一对运行时可变字号 token，px 而非 rem —— 运行时 inline 覆盖写的是 px）与 `src/shared/types/chatTypography.ts`（范围常量 + 两个交叉钳制解析器，非有限值回落默认值）、`ChatTypographySettings.tsx`（挂进现有 `AppearanceSettings`，未新增设置分组，含三行实时预览）、8 条中文词条；13 处生产 font-size class 切换（含 markdown h1–h6），token 注册进 `tailwind-merge` 的 `font-size` 组；运行时覆盖挂在聊天区根 `<section>`（**绝不写 `documentElement`**）；新增 3 个测试文件（`chatFontOverrideStatic` / `chatTypographyTokens` / `chatFontSection`），5 个既有测试文件的字面量断言同步更新。同日前一笔文档提交 `b9539eb1` docs(plan) 登记施工清单。**线 B（T105 / T106）尚未落地**，见 Active TODO 第 15 条。
- 2026-09-19 晚 批次 I 十三项修复，12 个本地提交（**未推送**）`09344a6d` `9b62dd2d` `4f987ee4` `ef692304` `ea7ea4be` `5f5433fa` `e00d7715` `c1559038` `848f5981` `d6182b62` `ffd0b841` `fb120545`（见 [roadmap](plans/runtime-hardening/roadmap.md) 批次 I 表）：T091 新建对话防重认在飞发送 + 发送态按会话 + Stop 停在飞会话 + 侧栏活动会话不被搜索过滤 + 临时对话判定；T092 结束对话保留时间线；T093 首字节 / 空闲超时、流中断恢复、未知 4xx 快失败、子代理重试可见、每次尝试记日志、横幅活倒计时 + 立即放弃、设置项；T094 文件树 CRUD 后重读 + watcher 竞态；T095 重命名拼完整路径；T096 思考块折叠头吸顶；T097 状态行间距与字号；T098 思考中可折叠；T099 迁移面板矛盾态；T101 工具行在参数流式期即出现（摘要输入 + 100ms 合并）；T102 主进程只读回放历史；T103 文件删除改自绘确认框；T100 Git 面板历史 / 分支按 head 签名自动刷新。新增决策 028 / 029 / 030。
- 2026-09-19 推送与打包：分支 46 个本地提交全部推送 origin；`d2c63372` 版本更新至 `1.0.0-test.14`；`e451aca8` 修点验探针的 Biome 门禁错误并让 lint-staged 覆盖 `.mjs` / `.cjs`（首次打包 [35441375128](https://github.com/p1p1dan/ai-client/actions/runs/35441375128) 因此在 Gate 4/7 失败）；重新触发的 [35442564448](https://github.com/p1p1dan/ai-client/actions/runs/35442564448) 全 success，产物与三平台 worker 冒烟归档在 [batch-h-build-2026-09-19](plans/runtime-hardening/evidence/batch-h-build-2026-09-19/README.md)。
- 2026-09-19 MODEL 组 09-19 点验缺陷四项修复（T090，四个本地提交，均未推送）：`171d1369` fix(editor) F1 命中跳行在编辑器已开其他文件时也生效；`539cd754` fix(chat) F2 命中列表弹层锚定到搜索行；`f059a8ae` fix(runtime) F5 aborted / error 收尾不再把上下文占用清成 0%；`6ea3fb2d` fix(chat) F6 时间线审批行显示发起请求的子代理。F3 / F7 由[决策 027](plans/runtime-hardening/decisions/027-permission-rows-no-audit-trail.md)结案为不做（Q027），F8 对应的 Q028 推迟。详见[批次 H 09-19 点验证据](plans/runtime-hardening/evidence/batch-h-devbox-pointcheck-2026-09-19/README.md)「修复与收口验证」节。
- 2026-09-19 MODEL 组剩余 10 项点验证据与判据回写（文档提交，无代码）：证据目录 [batch-h-devbox-pointcheck-2026-09-19](plans/runtime-hardening/evidence/batch-h-devbox-pointcheck-2026-09-19/README.md)（README / prep-notes / findings + 10 个 pointcheck 子目录 + 归档探针）；04-model.md 六处判据措辞、checklist-e §5.1 七行、open-questions Q027 / Q028。
- 2026-09-18 及更早见[旧阶段归档](plans/runtime-hardening/history/2026-09-24-进度看板-旧阶段归档.md)。

## Active TODO

1. T134～T136：T128 复验记下的显示细节、「继续」重发语义、Stop 后 trace 误记（均不阻塞本批）。
2. `1.0.3-test.1` 手动打包 + Windows 实测（T118～T122 的真机验证），通过后 main 快进。
3. T124 / [Q033](plans/runtime-hardening/open-questions.md)：`updaterChannel.test.ts` 在 electron-updater 6.7.3 下失败，复核 `486855f9` 的根因推断。
4. T126：会话树隐藏 `aiclient.permissions` / `aiclient.subagent` 条目（疑似「Windows 会话分支全是 custom」的来源）。
5. 批次 L 的 T116 / T117 已核对为 `8b3404b3` + `c7a3f89a` 落地（roadmap 已回写），Windows 现场复验待补。

## Blocked By

- Windows 实测要等用户在 Windows / 加密机上安装手动打包的 `1.0.3-test.1`。
- 旧阻塞项（均已结案或移交批次 E）见[旧阶段归档](plans/runtime-hardening/history/2026-09-24-进度看板-旧阶段归档.md)。

## Last Verified

- 2026-09-24 T128 GUI 复验（**Linux 开发机**，T128 修复未提交时的工作树，与提交 `403b7575` 同内容；本地假网关、隔离 profile）：8 项全 ✅——失败卡显示且掐断后可直接续发、失败后队列放行、点开折叠块 6 秒内位置不动、运行中 `/compact` 有提示且回合后可执行、插话回合重开显示「已工作 20 秒」、已拒绝 / 未执行显示（实时与重开一致）、切分支报错显示 git 原话、冒烟回归通过。全量 Vitest 500 文件 / 7520 例仅 T124 既有 1 例失败；三套 tsc 退出 0。
- 2026-09-24 批次 M 开发机 GUI 点验（**Linux 开发机**，`46789042`，本地假网关、隔离 profile，零真实 provider 调用）：21 项 18 ✅ / 2 ❌ / 1 取证。插话（A1～A9）、分支栏（B1～B5）、运行中计时与思考默认折叠（C1、C2）、死循环防护形态 A / 关闭开关 / 取证（D2～D4）通过；D1 掐断本身正确（第 47 个调用处断开、零执行、不再请求、续发不带退化回复）但中文失败卡不出现；C3 点开思考仍被滚走。转 T128。证据 [interject-branch-devbox-2026-09-24](plans/runtime-hardening/evidence/interject-branch-devbox-2026-09-24/README.md)。
- 2026-09-24 批次 M 收口验证（**Linux 开发机**，最终代码 `46789042`）：三套 tsc 退出 0；改动涉及的 80 个文件 Biome 干净；全量 Vitest（单 worker）**493 文件 / 7461 例，仅 `updaterChannel.test.ts` 1 例既有失败**（与本分支无关，T124）；另一轮曾多出 1 例偶发失败、日志截断未能定位（T127），6 个时序敏感的新测试文件连跑 3 遍 88 例全过。金样本 `nativeGuiSubagentEventStream.json` 已按 Task 启动回复的新文案重录。
- 2026-09-19 T090 收口验证（**Linux 开发机**）：三套 tsc（根 / `src/runtime` / `src/agent-host`）全部退出 0；全量 Vitest 单 worker **450 文件 / 6909 条全部通过，277 s**（对比 09-18 收口的 447 / 6843）；GUI 实测（dev 模式 + CDP，HEAD `6ea3fb2d`，5 次真实回合）四条全部通过——F1 第二个文件跳到 57 行、F2 弹层贴行下 3.7 px、F5 Stop 前后徽标均 1%、F6 审批行「代子 Agent explorer 请求」；同轮确认平台 off 档（`updatedAt 10:55:45Z`）端到端可用，菜单文案仍英文 `Off`（F4 未修）。证据 [fix-verify/](plans/runtime-hardening/evidence/batch-h-devbox-pointcheck-2026-09-19/pointcheck/fix-verify/)。详见[批次 H 09-19 点验证据](plans/runtime-hardening/evidence/batch-h-devbox-pointcheck-2026-09-19/README.md)「修复与收口验证」节。
- 2026-09-19 MODEL 组开发机点验（**Linux 开发机**，dev 模式 + CDP 9222，托管 `claude` 凭据经公司中转，网关探活 200）：10 项处置结果见上；应用起停 5 次均按 `/proc` 逐 pid 核 `exe` 后 SIGTERM；用户配置只在 MODEL-49 新建过策略文件并已删除校验；**未跑全量 Vitest、未跑 tsc、未打包**（`src/` 零改动，基线沿用 09-18 的 447 / 6843）。
- 2026-09-18 及更早见[旧阶段归档](plans/runtime-hardening/history/2026-09-24-进度看板-旧阶段归档.md)。

## 节点导航

| 计划 | 入口 | 内容 |
|---|---|---|
| Runtime 加固与收口（活跃） | [roadmap](plans/runtime-hardening/roadmap.md) | 批次 A 安全与数据完整性 T001～T011（已落地）；B 功能正确性 T012～T024 + T034 / T035（已落地）；C 清扫与文档 T025～T028 + T036（已落地）；D 补审 T029～T031（已落地）；D2 补审修补（上机前）T037～T048 + T055（已落地）；D3 收口后追加修补 T056 / T057 / T059（已落地）；D4 开发机点验修补（上机前）T060～T068；E 现场 T032（已完成）～T033；F 补审修补（上机后）T049～T054 + T058；G 用户界面诉求 T072～T076（已落地）；H T033 现场反馈 T077～T089 + T053（已落地）；I 用户现场反馈 T091～T103（已落地）；J 聊天区排版与过程折叠 T104 / T105 / T106（均已落地）；K 正文可见性与审阅 diff 落点 T107～T111（执行中，现场验收待补） |
| Runtime 自主化演进（已收口） | [核心任务树](plans/runtime-evolution/README.md) | P0～P6、GUI A～H、现场缺陷 F1～F7；节点状态为历史，审计异议见[审计证据第五节](plans/runtime-evolution/evidence/runtime-audit-2026-09-14/README.md#五裁决后的逐节点判定) |

功能与架构：[ARD](../plans/2026-09-08-runtime-evolution-ard.md)。计划目录：[Plantree 注册入口](README.md)。
