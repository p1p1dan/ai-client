# 进度看板

Role: implementation-status。更新日期：2026-09-12。
这是给用户查看和续接工作的短看板；实际节点完成状态与证据统一维护在[核心任务树](plans/runtime-evolution/README.md)。

## Current Phase

- **H / 19 统一 agent 目录：U1～U6 已提交（`4284c893`），验证案例 4、5 已于 2026-09-11 真机点验通过**，[实施计划](plans/runtime-evolution/topics/unified-agent-directory.md) · [验证](plans/runtime-evolution/evidence/unified-agent-directory/README.md#验证案例-45-的真机点验2026-09-11)。两种模式一律用本应用目录、五项资源一次性复制迁移、插件复用 pi 的 install/remove、借用机制整条删除。H / 17 遗留的模型配置与会话失联缺陷随之修掉。本轮点验第一次回答了「装完在会话里真的可用」——判据取自 pi 自己的扩展清单，不是磁盘；另外三件踩坑（开发机上切不动凭据模式、CDP 里删弹层 DOM 会把应用点废、仓库根 pi SDK 在纯 Node 下 import 不起来）都记在验证文档里。案例 7 依赖 H / 20；未打包。
- **H / 18 左侧 Chat 栏对齐：S1～S5 已完成并现场点验通过**，[实施计划](plans/runtime-evolution/topics/sidebar-pi-desktop-alignment.md) · [验证](plans/runtime-evolution/evidence/sidebar-pi-desktop-alignment/README.md)。两个分区各有右键菜单、仓库行右键与「更多」同源、未读完成/失败徽标、收起按钮移入 rail 并改为 toggle。点验另修掉一处 Esc 缺陷（菜单开着按 Esc 会把整个面板收走）。未打包。
- H / 17 已实现（`3f6a61fd`）；[重排顺序](plans/runtime-evolution/README.md#执行顺序)共 11 批。
- **H / 21 外部 Agent 迁移：四摊全部落地并现场点验通过**。P0 把「模型缺失」的报错接到迁移入口上；P1 在用户撞上失败之前先问一次（只对有东西可搬的老用户弹一次、五项默认全勾、从不覆盖）；点验查出并修掉的四条里，迁移改掉 provider 原键那条会让整个迁移白做。最后一摊 **Claude / Codex 对话导入 C1～C6 已完成** `2715b9a6`（2026-09-11）：设置 · Pi 里能扫出本机的 Claude / Codex 项目、勾选导入，导入的对话进侧栏、打开就是完整历史、能接着聊。**一条重要订正**：读取与转写这条链路 T34 时其实就写完了，只是**全仓没有一处界面挂载它**——本轮真正补的是入口、以及「工作目录不是本应用的项目」时落为临时对话（否则导进来的会话在侧栏里根本不显示）。「可续聊」已闭环：换成用户 2026-09-11 指定的点验凭据（vllmproxy + claude-sonnet-5）后，绑定仓库与未绑定的两条导入会话都给出了基于导入历史的真实回复。[策略](plans/runtime-evolution/topics/external-agent-migration.md) · [施工计划](plans/runtime-evolution/topics/conversation-import.md) · [点验](plans/runtime-evolution/evidence/external-agent-migration/README.md#c1c6-对话导入2026-09-11)
- **第 4 批「本地缺陷」已完成（2026-09-11，`4f7dedf7`）**。这一批两项的结局不一样：**重载后重复公告与多开空会话**查证后定性为**非缺陷**，不改代码；**中文界面英文残留**里「硬编码的那批」已修并真机点验。它和 9 月 11 日早些时候补的 57 条不是一回事——那批是「写了 `t()` 但没词条」，这批是**根本没经过 `t()`**，所以原来的扫描器看不见它们。改法是把纯函数吐出来的英文重新定义成「词典的键」：行首动词一路当键传到渲染那一层统一翻一次，要拼数字和路径的参数段则在构造时就翻好。[验证与点验](plans/runtime-evolution/evidence/chinese-ui-residue/README.md)
- **第 5 批（P4-6 收口）已开工，F3 的开发机那一半做完了**。用户 2026-09-11 追加规矩：先把开发机做到完全正常，必须在加密机才能测的留到最后一次上机。按这条把 F3 拆成两半——开发机这半已取证：纯 Node、Electron 主进程走应用自己的 IPC、Git 面板界面，**三层全部正常**，所以 Main→Git 这条链本身没缺陷，现场那个「stdout 空、stderr 正常」确认是加密机专属。[记录](plans/runtime-evolution/evidence/f3-dev-probe/README.md)
- **TUI-1 已反转**：用户推翻方案 C，会话 GUI/TUI 互通改为必须做，可行性已实测通过（[验证](plans/runtime-evolution/evidence/gui-tui-session-interop/README.md)），落地节点 [H / 20](plans/runtime-evolution/topics/gui-tui-session-interop.md)，排在最后但为 P6-1 前置。
- 测试策略变更（用户 2026-09-10）：Node 与 Git 已确认在企业白名单内，后续验证一律在开发机进行，全部做完后再上加密 Windows 实测一次，不再分轮上机。
- **第 6 批（P2-5/P2-6 真实缓存命中率）已完成（2026-09-12）**。自有 runtime 在真实网关上跑完 P2-0 的同一套六场景，**99.97%**，高于 95.01% 门槛。关键做法是**把旧后端也在同一网关上重采一遍**：命中率主要是网关的性质，而原基线用的 `maxapi` 今天已不供 `claude-sonnet-5`，直接对着 95.01% 比会把「换网关」算成「换后端」。同网关下 legacy 也是 99.97%，两者差 **-0.0073 个百分点**——没有可观察的退步。另外两点要记住：这个网关几乎把整段前缀直接写进缓存（整套 input 只有 56 个 token），所以 D9 那条比率**在这里会饱和、分辨力很弱**，真正有信息量的是「一次会话内系统提示词逐字节不变」和「首轮之后每轮只再写几十个 token」，两项都已取证；失败样本 `run-...native-01` 原样留档（`grep: command not found` 是采集脚本自己传了空环境，不是 runtime 缺陷）。[记录](plans/runtime-evolution/evidence/p2-5/README.md) · [对比报告](plans/runtime-evolution/evidence/p2-5/comparison.md)
- 剩余量（2026-09-12 订正）：**18 个节点未开始**（[P5](plans/runtime-evolution/README.md#p5) 十三个、[P6](plans/runtime-evolution/README.md#p6) 五个）是后半程主体。进行中的部分按实际待办去重后是 **9 摊**，其中六摊是“代码已完成、等验证”，真正还要写代码的只有 F3 与 F5。
- [P1](plans/runtime-evolution/README.md#p1) 实现齐备，P1-0/P1-3/P1-8 核心 Windows 验证通过；P1-5/P1-6 留策略与新权限链回归。

## Last Landed

- P2-5/P2-6 采集工具与结果（2026-09-12）：新增 `run-native.mjs`（自有 runtime 跑同一套 `p2-0-v3`）、`verify-native.mjs`（离线重算 v4 会话的命中率与断言）、`compare.mjs`（出新旧对比报告，缺少「无法对齐的设置」声明时直接拒绝出报告）、`preflight.mjs`（探网关供不供目标模型）。`src/runtime/index.ts` 多导出一个 `resolveWorkerShell`，让采集脚本用生产同一个 shell 解析而不越过包边界。[记录](plans/runtime-evolution/evidence/p2-5/README.md)
- 硬编码中文改走词典（2026-09-11）。用户决定：**界面上出现英文可以接受，只要意思清楚**。所以不是把中文抠掉，是把中文从代码挪进词典——英文当键、中文当值，切语言时才会跟着变。15 个文件、82 条字面量（还有一批 JSX 裸文本，第一版扫描器漏了），108 条新词条。影响最重的是输入框旁那个权限档控件，整块都是中文字面量。第 4 批清的是反方向，它的扫描器看不见这一半。新增守卫 `noHardcodedChinese.test.ts`（已反向验证）。**发给模型的提示词故意没动**。[记录](plans/runtime-evolution/evidence/chinese-ui-residue/hardcoded-chinese.md)
- 重试退避按用户决定改成 **3s → 10s → 30s**（此前网关故障 1/2/4/8s、限流 2s 起），三次重试、四次尝试，持续故障总等 43 秒。两条预算仍各记各的次数，服务端 `Retry-After` 一律优先。[记录](plans/runtime-evolution/evidence/p4-6/f4-retry/README.md)
- F2-b 三时点取证完成（2026-09-11）。最要紧的一条：**侧栏行的「关闭」什么都不删**——索引行与目录在未重启时原样不动，只是行离开了侧栏，所以「界面里消失了」绝不能读成「文件被删了」。「归档」才会当场删目录；而**重启会把整个临时根目录连锅端**，与用哪个入口无关，于是重启后必然出现「索引行在、目录已不存在」的状态（U05-a 的设计）。[记录](plans/runtime-evolution/evidence/p4-6/f2b/README.md)
- PERM-1 与权限链复验通过（2026-09-11，legacy + native 各一趟）。查出的最要紧的一点是**两个后端的审批界面根本不是同一张**：native 是结构化中文权限卡（带高风险徽标与倒计时），legacy 是 pi 插件自己的英文 `ui.select` 弹窗。探针第一版只认中文那张，在 legacy 下把插件弹窗读成了「压根没弹审批」。[记录](plans/runtime-evolution/evidence/p4-6/perm1/README.md)
- F4 重试在开发机上首次被真的触发（2026-09-11）：本地 HTTP 假网关 + 真 `createRuntime`，四条路全过。此前任务树一直挂「代码在、现场未触发」。现成的单测不算数——它让 `fauxProvider` 抛一个异常，走的是按文本分类那条路，而现场那条失败发生在真实 HTTP 上，读的是状态码和 `Retry-After`。[记录](plans/runtime-evolution/evidence/p4-6/f4-retry/README.md)
- H / 19 验证案例 4、5 真机点验通过（2026-09-11）：三个探针分别回答装/卸全流程、项目级插件的机制、托管模式下的界面。点验顺带修掉一个显示缺陷——插件面板把随包权限系统显示成 `src`（入口是 `.../pi-permission-system/src/index.ts`，取上一级目录名就取到了构建目录），已改成跳过 `src`/`dist`/`lib` 这类目录再上取一级，只上一级。[记录](plans/runtime-evolution/evidence/unified-agent-directory/README.md#验证案例-45-的真机点验2026-09-11)
- 四个源文件里的裸控制字节改成转义，并加了一条守卫测试，`a189e126`。起因是排查 F/13 时 `grep` 查不到 `useFolderDiffStats.ts`——那个文件含字面 NUL，被 `file(1)` 判成 `data`，**grep/ripgrep 默认静默跳过二进制文件**，于是 hook 读起来像死代码，我据此写了一条「F/13 从未接线」的错误结论。实际接线一直是好的。NUL 当分隔符没问题，写成字面字节才是问题；换成 `'\0'` 后运行期字节不变、文件恢复可搜索。[记录](plans/runtime-evolution/evidence/f3-dev-probe/README.md)
- 第 4 批中文界面英文残留（硬编码的那批）`4f7dedf7`：工具动词表、思考行、权限卡、审批记录行、子 Agent 面板、输入框占位、回合状态行、排队条、侧栏 New、会话分支对话框、模型按钮 aria-label 全部接上词典；另修 15 条词典自身的中英混排值（「搜索 Session」→「搜索会话」）。新增两个守卫测试，其中渲染那条已反向验证过不是空测试。[验证与点验](plans/runtime-evolution/evidence/chinese-ui-residue/README.md)
- H / 21 对话导入 C1～C6 `2715b9a6`：设置 · Pi 新增「从 Claude Code / Codex 导入历史对话」，未匹配到仓库的导入落为临时对话，Codex 旧格式可读，标题不再取斜杠命令回显。[点验](plans/runtime-evolution/evidence/external-agent-migration/README.md#c1c6-对话导入2026-09-11)。真机验到：导入 → 侧栏 → 打开看历史 → 接着聊拿到回复 → 重复导入报「已存在」。
- H / 21 现场点验后的四项修复：迁移保留 provider 原键、计数改按会话、空目录不再让提示复活、composer 状态条接入映射，`b7dacfb0`。[点验与复验记录](plans/runtime-evolution/evidence/external-agent-migration/README.md)。**四项都已真机复验通过**。
- H / 21 P1：老用户首启一键确认迁移，只对有东西可搬的人弹一次、五项默认全勾、从不覆盖，`f147059b`（三出口修正 `0a0df6d1`）。[记录](plans/runtime-evolution/topics/external-agent-migration.md#p1-落地记录2026-09-10f147059b)。已真机点验。
- H / 21 P0：模型缺失的报错指向迁移入口，五个界面共用一份判定与文案，`22da278c` + `b7dacfb0`。[记录](plans/runtime-evolution/topics/external-agent-migration.md#p0-落地记录2026-09-1022da278c)。已真机点验。
- H / 18 S1～S5：侧栏分区与仓库行右键菜单、未读结果徽标、收起按钮移入 rail，`70b9a31d`。[验证](plans/runtime-evolution/evidence/sidebar-pi-desktop-alignment/README.md)。未打包、未现场签收。
- H / 19 U1～U6：统一 agent 目录、迁移能力、插件界面、删除借用机制，`4284c893`。[验证](plans/runtime-evolution/evidence/unified-agent-directory/README.md)。未打包、未现场签收。
- 本轮[审阅栏与更新提醒](plans/runtime-evolution/topics/session-review-and-updates.md)已提交：R1/R2 `6be1d70a`、U1 `b919b1aa`（含提交前评审修复）。尚未打包或现场签收。
- 会话写入锁残留已修：陈旧锁可接管、活写者与他机锁仍拒绝，`280f49fc`；[验证](plans/runtime-evolution/evidence/session-writer-lock/README.md)。未打包、未现场回归。
- 本地真实应用实测后补修：差异上限每侧 4000 行 `1f45531f`、权限卡倒计时 `baeff487`、过程提前折叠 `ca6aac0f`、F7b 单份状态与折叠去耗时 `225c325e`。
- 上一轮基线 `80b7e3ab`，包含 `7e455b59` 的已完成过程折叠和启动公告对齐。
- test.13 不包含包后提交及本轮修改；当前安装包证据不能替代本轮验收。

## Active TODO

当前只推第 1、2 项；第 3 项起的排期与理由见[执行顺序](plans/runtime-evolution/README.md#执行顺序)。

1. **H / 21 已完成**（P0/P1/点验修复/对话导入 C1～C6 全部落地并真机点验，含可续聊硬验收）。
2. **H / 19 已完成**（验证案例 4、5 于 2026-09-11 真机点验通过；案例 2、3 随 H/21 一并通过；案例 7 依赖 H / 20）。
3. **第 4 批已完成**（中文界面英文残留已修并真机点验；重复公告与多开空会话定性为非缺陷）。默认字体已定为维持系统字体栈，不做字体选择器。
4. **第 5 批（P4-6 收口）开发机侧四项全部完成**：F3（不复现，加密机那半推迟）、F4（真实 HTTP 四条路，[记录](plans/runtime-evolution/evidence/p4-6/f4-retry/README.md)）、PERM-1 与权限链（legacy / native 两趟，[记录](plans/runtime-evolution/evidence/p4-6/perm1/README.md)）、F2-b 三时点取证（[记录](plans/runtime-evolution/evidence/p4-6/f2b/README.md)）。真实更新源下载/安装仍按用户 2026-09-10 决定暂缓。
5. **第 6 批（P2-5/P2-6）已完成**（2026-09-12）：native 99.97%、同网关 legacy 99.97%，差 -0.0073 个百分点，均高于 95.01% 门槛。[记录](plans/runtime-evolution/evidence/p2-5/README.md)
6. 下一批是第 7 批 **P5-1、P5-3**（skills / 模板含 F5 提问能力、MCP bridge）。

## Blocked By

- F3：开发机上**不复现**（三层已取证），确认加密机专属，按用户 2026-09-11 决定推迟到最后一次上机。用户提出的根因方向是「加密软件认调用方」——若成立，换哪个 git 都没用，得在 Electron 和 git 之间插一个白名单进程。该方向只能上机验证。
- ~~F2-b 缺删除前/后/重启后的索引与目录证据。~~ 已于 2026-09-11 取证完成，[记录](plans/runtime-evolution/evidence/p4-6/f2b/README.md)。

## Last Verified

- 2026-09-12 P2-5/P2-6 真实缓存：两个后端各 28 次真实模型调用，六场景全绿；两份离线复核脚本都从原始 usage、会话 JSONL 与 trace 重算通过（不访问网关）。已量化两处可比性风险——网关不做响应缓存（同一请求两次返回不同 message id），以及唯一一处「温」数据 B02 的影响是 0.018 个百分点。[记录](plans/runtime-evolution/evidence/p2-5/README.md)
- 2026-09-11 硬编码中文与重试节奏：全量 **378 文件 / 5320 测试通过**，根目录与 runtime 两套 tsc 通过，Biome 回到既有基线；改完后用 PERM-1 探针真机复验，权限档弹层的中文一字不差、全部判据仍过。
- 2026-09-11 F2-b：两条临时会话分别走「关闭」与「归档」，索引与目录两侧分别取证，跨一次真实重启。探针自己踩的两个坑也记了——归档是要确认的（第一版点完就走，读出过「点了归档但索引没变」的假缺陷）、侧栏可见性不能用整屏 `innerText` 判断。[记录](plans/runtime-evolution/evidence/p4-6/f2b/README.md)
- 2026-09-11 PERM-1：档位弹层选完即关（`data-open` 100ms 内消失）、「全自动」保持打开换确认面板、取消不应用；native 下真实回合弹出中文权限卡并成功放行，命令真的执行。[记录](plans/runtime-evolution/evidence/p4-6/perm1/README.md)
- 2026-09-11 F4 重试：四条路通过，且按用户当日决定把退避改成 **3s → 10s → 30s**（此前 1/2/4/8s，「不要太频繁」）。实测 503×2 后成功（3 次请求）、一直 503 时 4 次请求耗尽、429 按服务端 `Retry-After` 只等 1s 且走独立预算、退避中取消 401ms 结束只发 1 次请求。判据同时核对 trace 备注与假网关数到的真实请求次数。[记录](plans/runtime-evolution/evidence/p4-6/f4-retry/README.md)
- 2026-09-11 H / 19 案例 4、5：全量 **377 文件 / 5316 测试通过**，根目录与 runtime 两套 tsc 通过，改动文件 Biome 干净；开发机 Electron + CDP 走真实控件装/卸 `npm:pi-jingle`，磁盘与界面两侧分别取证，另有 pi `SettingsManager` 的 `projectTrusted` 真差分。[记录](plans/runtime-evolution/evidence/unified-agent-directory/README.md#验证案例-45-的真机点验2026-09-11)。未打包。
- 2026-09-11 F3 开发机取证 + 控制字节清理：全量 **377 文件 / 5314 测试通过**，根目录与 runtime 两套 tsc 通过；开发机 Electron + CDP 实测 Git 面板与 git IPC 均正常。未打包。
- 2026-09-11 第 4 批：全量 **376 文件 / 5312 测试通过**，根目录、runtime、agent-host 三套 tsc 通过；开发机 Electron + CDP 真机点验，合成 transcript 一屏覆盖工具行 / 审批记录 / 权限卡 / 侧栏 / 输入框。[记录与截图](plans/runtime-evolution/evidence/chinese-ui-residue/README.md)。未打包。
- `1.0.0-test.13` / `c0ae2a34`：[CI 34424337205](https://github.com/p1p1dan/ai-client/actions/runs/34424337205) 全部构建与 gate 成功；[固定证据](plans/runtime-evolution/evidence/p4-6/ci-34424337205/README.md)。尚无 test.13 现场复验记录。
- test.12 加密 Windows：[两载体六工具、安装包 worker、v3 resume、GUI/agent 加密文本读写与缺陷结果](../../Windows-P4-6-evidence/test12-reverify.md)。
- 2026-09-10 本轮：24 文件 / 364 测试通过，根目录、runtime、agent-host 三套 tsc 通过；隔离 Electron 交互证据见上述目录。提交前评审修复 5 项后复跑 12 文件 / 169 测试及根目录、runtime tsc 通过。未执行完整生产构建或新 CI 打包。
- 2026-09-10 本地真实应用（native + 真实模型）：审阅栏全流程、权限卡与倒计时、F7f 通过，F7b 复现；[记录](plans/runtime-evolution/evidence/session-review-and-updates/README.md#本地真实应用验证)。
- 2026-09-10 会话写入锁：runtime 全量 28 文件 / 344 测试通过，根目录与 runtime 两套 tsc 通过；含 5 项接管用例的反向对照。未在真实应用重现一次崩溃后重开。
- 2026-09-10 H / 17：全量 362 文件 / 5161 测试通过（首次覆盖渲染层——此前根 `node_modules` 缺 `happy-dom`，DOM 测试根本跑不起来）。未打包、未现场回归。
- 2026-09-10 GUI/TUI 互通可行性：双向读写四轮交替通过，用双方各自的真实读写器；定位五处格式差异，其中三处只有真实闭环才暴露。[记录](plans/runtime-evolution/evidence/gui-tui-session-interop/README.md)。仅验证，未改生产代码。
- 2026-09-10 pi 插件机制：实测 `install` / `remove` / `list` 可用，落点即 agent 目录（`<agentDir>/npm/node_modules` + `settings.json` 的 `packages`），`remove` 为真卸载。
- 2026-09-10 H / 18 **真实应用点验通过**：用 CDP 的真实鼠标/键盘事件走完验证案例 1/2/3/5——两个分区各弹各的菜单、行菜单压过分区菜单、仓库行与「更多」同组动作、未读徽标出现并在点开后归零、rail 收起按钮与 Ctrl+B 双向生效；[记录与截图](plans/runtime-evolution/evidence/sidebar-pi-desktop-alignment/README.md#真实应用点验2026-09-10开发机)。自动化侧全量 368 文件 / 5201 测试通过，三套 tsc 与 Biome 通过。
- 2026-09-10 CDP「连不上」定因：**是内存压力，不是通道坏**。本机 3.3 GB / 2 核，人为占住 950 MB 后 CDP 从 10 秒退化到 37 秒、窗口从 1 秒退化到 42 秒；代理、Vite 冷缓存、「要等窗口」三种猜测均已逐一排除。
- 2026-09-10 H / 19：全量 363 文件 / 5175 测试通过，根目录、agent-host、runtime 三套 tsc 通过；另对真实 pi CLI 跑过一次装/关/开/卸的闭环冒烟，并由此发现并修掉「禁用后插件从列表消失」（`pi list` 会加 ` (filtered)` 后缀）。[记录](plans/runtime-evolution/evidence/unified-agent-directory/README.md)。

## 节点导航

| 计划 | 主节点入口 | 子节点 |
|---|---|---|
| Runtime | [P0 骨架](plans/runtime-evolution/README.md#p0) | P0-1～6 |
| Runtime | [P1 工具与权限](plans/runtime-evolution/README.md#p1) | P1-0～9 |
| Runtime | [P2 上下文与提示词](plans/runtime-evolution/README.md#p2) | P2-0～8 |
| Runtime | [P3 会话与事件](plans/runtime-evolution/README.md#p3) | P3-1～6 |
| Runtime | [P4 集成](plans/runtime-evolution/README.md#p4) | P4-0～6；现场验收矩阵 |
| Runtime | [P5 扩展](plans/runtime-evolution/README.md#p5) | P5-1～5；P5-2-0～7 |
| Runtime | [P6 切换](plans/runtime-evolution/README.md#p6) | P6-1～5 |
| GUI | [A～F 批次](plans/runtime-evolution/README.md#gui-任务树) | 任务 1～16 |
| 现场问题 | [修复列表](plans/runtime-evolution/README.md#现场缺陷与修复) | F1～F7、EFFORT-1、PERM-1、TUI-1、v3 resume |

功能与架构：[ARD](../plans/2026-09-08-runtime-evolution-ard.md)。计划目录：[Plantree 注册入口](README.md)。
