{
  "_comment": [
    "Mid-session settings contract (D48 S4), generated from the codex binary itself:",
    "  codex app-server generate-json-schema --experimental --out <dir>",
    "Same provenance as codex-method-contract.json (METHOD names) and",
    "codex-turn-schema.json (turn PARAM shapes); this file pins the shapes the",
    "zero-turn permission channel sends and reads back.",
    "It exists because thread/settings/update accepts UNKNOWN FIELDS SILENTLY",
    "[实测 06-probes P1 严格性观察: unknown key -> {} , no notification, no effect],",
    "so a misspelled field is invisible at runtime AND in any test that only checks",
    "'the request was accepted'. The parameter builder is whitelist-only and this",
    "snapshot is what the whitelist is pinned against.",
    "SandboxPolicy is the one shape that must never be copied from thread/start:",
    "thread/start takes `sandbox` as a kebab-case STRING (read-only /",
    "workspace-write / danger-full-access) while turn/start and",
    "thread/settings/update take `sandboxPolicy` as a camelCase discriminated",
    "OBJECT. Three call sites, two spellings [实测 06-probes §0.2]."
  ],
  "codexVersion": "codex-cli 0.145.0",
  "capturedAt": "2026-08-17",
  "ThreadSettingsUpdateParams": {
    "required": ["threadId"],
    "propertyNames": [
      "approvalPolicy",
      "approvalsReviewer",
      "collaborationMode",
      "cwd",
      "effort",
      "model",
      "multiAgentMode",
      "permissions",
      "personality",
      "sandboxPolicy",
      "serviceTier",
      "summary",
      "threadId"
    ],
    "permissionsDescription": "Select a named permissions profile id for subsequent turns. Cannot be combined with `sandboxPolicy`."
  },
  "SandboxPolicy": {
    "variants": [
      {
        "type": "dangerFullAccess",
        "propertyNames": ["type"]
      },
      {
        "type": "externalSandbox",
        "propertyNames": ["networkAccess", "type"]
      },
      {
        "type": "readOnly",
        "propertyNames": ["networkAccess", "type"]
      },
      {
        "type": "workspaceWrite",
        "propertyNames": [
          "excludeSlashTmp",
          "excludeTmpdirEnvVar",
          "networkAccess",
          "type",
          "writableRoots"
        ]
      }
    ]
  },
  "AskForApproval": {
    "stringVariants": ["untrusted", "on-request", "never"]
  },
  "ThreadSettings": {
    "required": [
      "approvalPolicy",
      "approvalsReviewer",
      "collaborationMode",
      "cwd",
      "model",
      "modelProvider",
      "sandboxPolicy"
    ],
    "propertyNames": [
      "activePermissionProfile",
      "approvalPolicy",
      "approvalsReviewer",
      "collaborationMode",
      "cwd",
      "effort",
      "model",
      "modelProvider",
      "multiAgentMode",
      "personality",
      "sandboxPolicy",
      "serviceTier",
      "summary"
    ]
  },
  "ThreadSettingsUpdatedNotification": {
    "required": ["threadId", "threadSettings"],
    "propertyNames": ["threadId", "threadSettings"]
  }
}
