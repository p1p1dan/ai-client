# 待办任务执行计划

本文档整理当前待办第 6 至第 11 项的产品口径、实施任务、批次顺序和验收边界，供后续执行者按批次领取和实施。

## 已确认产品口径

- 流式回复中的 `↓` 显示 assistant 已接收的**字符数**，不是 token 数，也不是金额。
- 顶部原“...”按钮及其中的 Reload、Developer Tools、GitHub、Exit 全部隐藏，不迁移到其他入口。
- 公告通知在**每次启动 App 时自动弹出**。
- 周限额的单位是**金额**，不是调用次数或 token。

## 批次一：聊天交互与模型选择

### 任务 6：发送状态增加流式回复字符数

#### 目标

- 发送状态继续显示用户发送内容的 `↑` 字符数。
- assistant 开始返回文本后，实时显示 `↓` 字符数。
- `↓` 统计 assistant 已接收到的文本字符数。
- `↓` 随流式内容持续增长。
- 没有 assistant 文本时不显示 `↓`。
- thinking、tool call 和其他状态事件不计入 `↓`。
- 新一轮发送开始时清零上一轮的 `↓` 计数。

#### 实施范围

- Pi 流式文本事件到 Renderer 的数据链路。
- 发送状态行及其状态模型。
- 字符数计算和流式状态清理。
- 相关单元测试和回归测试。

#### 验收

发送过程中应能出现类似以下状态：

```text
Percolating… · ↑ 2 chars · ↓ 128 chars · 6s
```

其中 `↓` 必须对应当前已接收到的 assistant 文本字符数，不能使用 token 数或仅在 turn 结束后才出现的 usage 数据代替。

### 任务 7：修复启动阶段模型错误显示 `unverified`

#### 目标

- App 刚启动、模型目录尚未完成加载时，不显示错误的 `· unverified`。
- 模型目录加载中，保留当前模型名称，但暂不判定其是否未验证。
- 模型目录加载完成后，再根据真实目录决定是否显示 `unverified`。
- 区分目录请求失败与目录中确实不存在该模型。
- session 或 agent 切换时，不能复用上一个对象的验证状态。

#### 实施范围

- 模型目录加载状态。
- `catalogLoaded`、loading、error 状态。
- 模型选择 reconcile 逻辑。
- `ComposerModelTrigger` 和模型选择纯函数。
- 启动竞态、目录延迟和请求失败测试。

#### 验收

- 启动瞬间不再错误显示 `grok/grok-4.6 · unverified`。
- 目录加载成功且包含该模型时，显示为已验证。
- 目录加载完成且不包含该模型时，才显示 `unverified`。
- 目录请求失败时，不把网络失败直接等同于模型未验证。

### 任务 11：修复 `@` 文件引用弹层超出画幅

#### 目标

- `@` 文件引用弹层始终位于当前可视区域内。
- 弹层不能向下延伸到画幅外。
- 上方空间不足时可以自动切换到下方。
- 下方空间不足时可以自动切换到上方。
- 结果过多时在弹层内部滚动，不推动页面或编辑区整体变高。
- 长文件名、长路径和窄窗口不能撑破弹层。

#### 实施范围

- `ChatComposer` 的 mention popup。
- mention popup 定位逻辑。
- viewport 可用空间计算。
- 弹层最大高度和内部滚动。
- slash popup 与 mention popup 的定位一致性。
- 窄窗口和低分辨率测试。

#### 验收

- 底部输入框打开 `@` 弹层时，弹层完整可见。
- 弹层高度不会超过当前 viewport 可用空间。
- 结果较多时只在弹层内部滚动。
- 窗口 resize、输入法弹出和输入框位置变化后，弹层仍然正确定位。

## 批次二：Pi 模型配置与请求头

### 任务 8：为 Pi 请求增加自定义 User-Agent

#### 目标

- Pi Worker 启动时生成：

```text
AICLIENT_PI_USER_AGENT=claude-cli-pilab/<app.getVersion()>
```

- Pi provider 配置增加：

```json
{
  "headers": {
    "User-Agent": "$AICLIENT_PI_USER_AGENT"
  }
}
```

- 该字段最终写入 Pi 使用的 `models.json`。
- managed remote config、stale cache、local config 等配置路径都不能丢失该字段。
- 自定义 `headers.User-Agent` 覆盖 Pi 默认 User-Agent。
- 最终请求头必须是：

```text
User-Agent: claude-cli-pilab/<版本号>
```

不能是：

```text
User-Agent: pi (win32 10.0.26100; x64)
```

#### 实施范围

- `PiWorkerProcess` 的 Worker 环境变量。
- `piModelConfig` 常量和环境变量。
- shared provider 类型。
- 配置校验。
- `PiModelConfigService` 写入 `models.json`。
- Pi Worker 或 SDK 请求头测试。

#### 验收

- 生成的 `models.json` 中存在 provider headers。
- 测试可以捕获最终 HTTP 请求头。
- 版本号来自 `app.getVersion()`。
- 不通过类型逃逸绕过配置校验。

## 批次三：启动公告通知与顶部铃铛

### 任务 9：增加每次启动自动弹出的公告通知

#### 目标

- App 每次启动时联网拉取公告。
- 有符合条件的公告时，启动阶段自动弹出通知弹窗。
- 顶部原“...”按钮替换为铃铛按钮。
- 铃铛按钮放在原“...”按钮的位置。
- 原“...”按钮及其菜单功能全部隐藏，不新增替代入口。
- 通知内容接口暂时沿用模型管理页面的服务地址。
- API 具体路径先按客户端约定实现，后续由 onboard 项目补齐。
- 网络失败、接口超时不能阻塞 App 启动。

#### 实施范围

- Main 进程公告获取服务。
- 公告缓存与已读状态。
- Preload IPC。
- Renderer 铃铛按钮。
- 启动自动弹窗。
- 公告列表或详情展示。
- 未读状态和重复公告处理。
- 空公告、接口失败、长内容、窄窗口处理。

#### 启动规则

- 每次启动执行公告检查。
- 有公告时自动弹窗。
- 没有公告时不弹窗。
- 公告内容应支持多条，或至少保留扩展空间。
- 公告弹窗不能阻塞主界面加载。

#### 验收

- 启动 App 后触发公告拉取。
- 有公告时自动出现弹窗。
- 顶部显示铃铛，不再显示“...”。
- 点击铃铛可以查看当前公告。
- 原“...”菜单不再出现。
- 网络失败时 App 正常进入主界面。

## 批次四：用户卡片周限额

### 任务 10：将本月调用次数改为周限额金额

#### 目标

- 左下角用户卡片中的“本月调用次数”改为“周限额”。
- 周限额单位为金额。
- 显示已使用金额与额度上限。
- 能表达剩余额度或使用进度。
- 保留现有 loading、错误、未登录和刷新行为。
- 超过额度时提供清晰的超限状态。

#### 建议显示语义

```text
周限额
已使用金额 / 周限额金额
```

例如：

```text
周限额
$12.40 / $50.00
```

#### 实施范围

- Usage shared 类型。
- Main `UsageService`。
- onboard usage 接口适配。
- `useUsageStats`。
- `UserProfileCard`。
- 紧凑版 `UserFooterPill` 的同步显示检查。
- 周限额金额、剩余额度、周期结束时间和超限状态。

#### 验收

- 正常返回额度时显示金额和进度。
- 金额格式统一。
- 未配置额度时显示“暂不可用”，不显示 `NaN` 或错误百分比。
- 网络失败不影响用户信息和退出登录功能。
- 超额状态可识别。
- 登出后清理旧额度数据。
- 周期更新后能刷新最新额度。

## 执行顺序

### 第一批：前端即时问题

1. 任务 7：启动模型 `unverified` 问题。
2. 任务 11：`@` 文件引用弹层布局问题。
3. 任务 6：流式 `↓` 字符数。

### 第二批：Pi 配置链路

4. 任务 8：Pi User-Agent 和 `models.json` headers。

### 第三批：应用级通知

5. 任务 9：启动公告、铃铛按钮、隐藏原“...”菜单。

### 第四批：账户额度

6. 任务 10：周限额金额显示及 Usage 接口扩展。

任务 9 和任务 10 都需要 onboard 接口配合。客户端可以先按约定的数据契约完成本地实现，再接入真实接口。
